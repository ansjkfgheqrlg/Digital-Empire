# requirements
            
> Path: [[Map - Workflow-Libri|Workflow-libri]]

## Content

# Book Factory — Dipendenze Python
# Installazione: pip install -r requirements.txt

# PDF generation (HTML/CSS → PDF)
weasyprint>=60.0

# Templating HTML
Jinja2>=3.1

# YAML parsing
PyYAML>=6.0

# Image processing & validation
Pillow>=10.0

# PDF analysis (QA)
pikepdf>=8.0

# OpenAI API (per generazione immagini con DALL-E 3)
openai>=1.0

# HTTP requests (download immagini)
requests>=2.31

# PDF text extraction (QA)
pdfplumber>=0.10

# Markdown (opzionale, per preprocessing)
markdown>=3.5

# Replicate API (alternativa per immagini con Flux)
# replicate>=0.22  # Decommentare se si usa Replicate
