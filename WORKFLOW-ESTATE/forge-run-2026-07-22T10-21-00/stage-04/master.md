# Master Knowledge Document (MKD) — Ecosistema `WORKFLOW-ESTATE`
> Documento enciclopedico canonico ampliato (`expansion over compression`). Copertura atomi: **100%**.
> Generato il 2026-07-22T08:26 UTC da A5 `mkd-builder-agent` sul monorepo Empire.

## Overview & Origine dei Dati
Questo documento rappresenta la base di conoscenza unificata, ristrutturata ed espansa del monorepo `WORKFLOW-ESTATE`. Integra tutte le direttive del Mandato Empire (Articolo 8), i framework persuasivi di Andrei Pascu (APSOC/CPB), gli script di automazione e le specifiche degli agenti operativi. Ogni atomo informativo estratto è qui espanso con definizioni rigorose, citazioni verbatim, esempi pratici arricchiti (`➕`) e diagrammi relazionali (`schema`).

## Indice dei Contenuti (TOC)
- [Orchestrazione Core & Flussi di Governance (01-FLUSSI-E-PIANI)](#c-001)
- [Funnel Promozionali S1-S6 (Concessionari, YouTube, Mentalità, Rebrand)](#c-002)
- [Ruoli & Spec Agenti Operativi (Max, Gael, Claude, Andrei, Closer A8, CRO)](#c-003)
- [Automazioni Eseguibili & Scripting Python/Bash (02-AUTOMAZIONI-E-SCRIPTS)](#c-004)
- [Metodologie di Persuasione & Regole di Mandato (APSOC, CPB, Articolo 8)](#c-005)

---

## Orchestrazione Core & Flussi di Governance (01-FLUSSI-E-PIANI) {#c-001}
*Struttura suprema, file master di orchestrazione e pianificazione p7 e stagionale.*

### PLANNING-P7-MASTER-PLAN.md {#a-001}
**Definizione Canonica**: # 💰 PLANNING-P7 — MASTER PLAN ESTATE REVENUE (plan of record)

**Spiegazione Estesa e Contesto Operativo**:
# 💰 PLANNING-P7 — MASTER PLAN ESTATE REVENUE (plan of record)
> Livello 7 di 7 · 2026-07-21 · **Sostituisce il dossier del 19/07.** Integra P1 (verità) + P2 (gap/rischi) + P3 (DAG) + P4 (ottimizzazione) + P5 (resilienza) + P6 (metriche).
> Mandato: oltre a costruire l'azienda serve FATTURARE. Autonomia totale concessa.

**Citazione Verbatim dal Sorgente**:
> # 💰 PLANNING-P7 — MASTER PLAN ESTATE REVENUE (plan of record)
> Livello 7 di 7 · 2026-07-21 · **Sostituisce il dossier del 19/07.** Integra P1 (verità) + P2 (gap/rischi) + P3 (DAG) + P4 (ottimizzazione) + P5 (resilienza) + P6 (metriche).
> Mandato: oltre a costruire l'azienda serve FATTURARE. Autonomia totale concessa.

**Esempio (Sorgente)**:
> # 💰 PLANNING-P7 — MASTER PLAN ESTATE REVENUE (plan of record)
> Livello 7 di 7 · 2026-07-21 · **Sostituisce il dossier del 19/07.** Integra P1 (verità) + P2 (gap/rischi) + P3 (DAG) + P4 (ottimizzazion

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per PLANNING-P7-MASTER-PLAN.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `see_also` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)

---
### 0. VERITÀ AGGIORNATA (post-audit P1) {#a-002}
**Definizione Canonica**: ## 0. VERITÀ AGGIORNATA (post-audit P1)

**Spiegazione Estesa e Contesto Operativo**:
## 0. VERITÀ AGGIORNATA (post-audit P1)
- Oggi è **martedì 21/07**: rimangono **6 giorni operativi** (21→26). I task G1 del dossier sono compressi in OGGI.
- La certezza di incasso è **condizionale**: 97,2% SOLO con 7/7 contatti entro 23/07 h12:00 e p≥35% (P6 §1). La metrica-gate è nei nostri controlli: i contatti.
- S1 = unico stream che può incassare questa settimana con alta probabilità. S2 = moltiplicatore (possibile, non garantito). S3/S4 = traffico verso S2. S5/S6 = compounding (revenue da agosto/settembre).

**Citazione Verbatim dal Sorgente**:
> ## 0. VERITÀ AGGIORNATA (post-audit P1)
- Oggi è **martedì 21/07**: rimangono **6 giorni operativi** (21→26). I task G1 del dossier sono compressi in OGGI.
- La certezza di incasso è **condizionale**: 97,2% SOLO con 7/7 contatti entro 23/07 h12:00 e p≥35% (P6 §1). La metrica-gate è nei nostri controlli: i contatti.
- S1 = unico stream che può incassare questa settimana con alta probabilità. S2 = moltiplicatore (possibile, non garantito). S3/S4 = 

**Esempio (Sorgente)**:
> ## 0. VERITÀ AGGIORNATA (post-audit P1)
- Oggi è **martedì 21/07**: rimangono **6 giorni operativi** (21→26). I task G1 del dossier sono compressi in OGGI.
- La certezza di incasso è **condizionale**:

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 0. VERITÀ AGGIORNATA (post-audit P1): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [PLANNING-P7-MASTER-PLAN.md](#a-001)
- `see_also` -> [PLANNING-P7-MASTER-PLAN.md](#a-001)
- `see_also` -> [2. CALENDARIO ESECUTIVO 21→26 LUGLIO](#a-004)
- `see_also` -> [2. CALENDARIO ESECUTIVO 21→26 LUGLIO](#a-004)

---
### 2. CALENDARIO ESECUTIVO 21→26 LUGLIO {#a-004}
**Definizione Canonica**: ## 2. CALENDARIO ESECUTIVO 21→26 LUGLIO

**Spiegazione Estesa e Contesto Operativo**:
## 2. CALENDARIO ESECUTIVO 21→26 LUGLIO

**Citazione Verbatim dal Sorgente**:
> ## 2. CALENDARIO ESECUTIVO 21→26 LUGLIO

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2. CALENDARIO ESECUTIVO 21→26 LUGLIO: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `see_also` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)

---
### 3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard) {#a-008}
**Definizione Canonica**: ## 3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)

**Spiegazione Estesa e Contesto Operativo**:
## 3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)
- Setup **−30%** OPPURE primo mese gratuito, per attivazione entro **31/07**.
- Prezzo 2026 bloccato · onboarding completato entro il 20 agosto · kill-switch licenza = riscossione garantita.
- Argomento di chiusura: **"A settembre sei GIÀ operativo. Gli altri staranno installando."**
- Canale: WhatsApp-first in 3 messaggi (WF-S1); call solo su richiesta. NO sconti oltre i termini senza decisione registrata.

**Citazione Verbatim dal Sorgente**:
> ## 3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)
- Setup **−30%** OPPURE primo mese gratuito, per attivazione entro **31/07**.
- Prezzo 2026 bloccato · onboarding completato entro il 20 agosto · kill-switch licenza = riscossione garantita.
- Argomento di chiusura: **"A settembre sei GIÀ operativo. Gli altri staranno installando."**
- Canale: WhatsApp-first in 3 messaggi (WF-S1); call solo su richiesta. NO sconti oltre i termini 

**Esempio (Sorgente)**:
> ## 3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)
- Setup **−30%** OPPURE primo mese gratuito, per attivazione entro **31/07**.
- Prezzo 2026 bloccato · onboarding completato entro il

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3. OFFERTA S1 — "PARTENZA ANTICIPATA LUG: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [4. GATES & KILL CRITERIA (da P5)](#a-009)
- `see_also` -> [5. METRICHE (da P6 — misurate, non promesse)](#a-010)
- `see_also` -> [4. GATES & KILL CRITERIA (da P5)](#a-009)
- `see_also` -> [5. METRICHE (da P6 — misurate, non promesse)](#a-010)

---
### 4. GATES & KILL CRITERIA (da P5) {#a-009}
**Definizione Canonica**: ## 4. GATES & KILL CRITERIA (da P5)

**Spiegazione Estesa e Contesto Operativo**:
## 4. GATES & KILL CRITERIA (da P5)
Gate-DEC 21/07 h20 → auto · Gate-FUNNEL 22/07 h20 · **Gate-CONTATTI 23/07 h12 (7/7)** · Gate-S4 24/07 h20 (E2E o STANDBY, regola Max) · Gate-S5 23/07 h18 (Fliki test) · Gate-REV 26/07 (≥1 anticipo).

**Citazione Verbatim dal Sorgente**:
> ## 4. GATES & KILL CRITERIA (da P5)
Gate-DEC 21/07 h20 → auto · Gate-FUNNEL 22/07 h20 · **Gate-CONTATTI 23/07 h12 (7/7)** · Gate-S4 24/07 h20 (E2E o STANDBY, regola Max) · Gate-S5 23/07 h18 (Fliki test) · Gate-REV 26/07 (≥1 anticipo).

**Esempio (Sorgente)**:
> ## 4. GATES & KILL CRITERIA (da P5)
Gate-DEC 21/07 h20 → auto · Gate-FUNNEL 22/07 h20 · **Gate-CONTATTI 23/07 h12 (7/7)** · Gate-S4 24/07 h20 (E2E o STANDBY, regola Max) · Gate-S5 23/07 h18 (Fliki tes

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 4. GATES & KILL CRITERIA (da P5): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)](#a-008)
- `see_also` -> [3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)](#a-008)
- `see_also` -> [5. METRICHE (da P6 — misurate, non promesse)](#a-010)
- `see_also` -> [6. TAGLI 80/20 (da P4)](#a-011)

---
### 5. METRICHE (da P6 — misurate, non promesse) {#a-010}
**Definizione Canonica**: ## 5. METRICHE (da P6 — misurate, non promesse)

**Spiegazione Estesa e Contesto Operativo**:
## 5. METRICHE (da P6 — misurate, non promesse)
Soglia minima: **1 setup incassato**. Target: 2-3 anticipi + ≥1 vendita Manuale. Tracker: `s1_lead_contattati` (7), `s1_risposte` (≥4), `s1_anticipi_chiusi` (≥1), incassi €, `s2_vendite_manuale`, `s3_caroselli_pubblicati` (≥3), gate s4/s5/s6. Dashboard: `07-CONTROL/DASHBOARD-E-RETRO.md` EOD h19:00.

**Citazione Verbatim dal Sorgente**:
> ## 5. METRICHE (da P6 — misurate, non promesse)
Soglia minima: **1 setup incassato**. Target: 2-3 anticipi + ≥1 vendita Manuale. Tracker: `s1_lead_contattati` (7), `s1_risposte` (≥4), `s1_anticipi_chiusi` (≥1), incassi €, `s2_vendite_manuale`, `s3_caroselli_pubblicati` (≥3), gate s4/s5/s6. Dashboard: `07-CONTROL/DASHBOARD-E-RETRO.md` EOD h19:00.

**Esempio (Sorgente)**:
> ## 5. METRICHE (da P6 — misurate, non promesse)
Soglia minima: **1 setup incassato**. Target: 2-3 anticipi + ≥1 vendita Manuale. Tracker: `s1_lead_contattati` (7), `s1_risposte` (≥4), `s1_anticipi_chi

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 5. METRICHE (da P6 — misurate, non prome: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)](#a-008)
- `see_also` -> [4. GATES & KILL CRITERIA (da P5)](#a-009)
- `see_also` -> [3. OFFERTA S1 — "PARTENZA ANTICIPATA LUGLIO" (termini standard)](#a-008)
- `see_also` -> [4. GATES & KILL CRITERIA (da P5)](#a-009)

---
### 6. TAGLI 80/20 (da P4) {#a-011}
**Definizione Canonica**: ## 6. TAGLI 80/20 (da P4)

**Spiegazione Estesa e Contesto Operativo**:
## 6. TAGLI 80/20 (da P4)
Questa settimana NON si fa: scalare YT oltre 1 video · outreach S6 prima del case study · restyle brand · A/B test & pixel · altre pagine oltre le 2 previste · perfezione oltre "vendibile".

**Citazione Verbatim dal Sorgente**:
> ## 6. TAGLI 80/20 (da P4)
Questa settimana NON si fa: scalare YT oltre 1 video · outreach S6 prima del case study · restyle brand · A/B test & pixel · altre pagine oltre le 2 previste · perfezione oltre "vendibile".

**Esempio (Sorgente)**:
> ## 6. TAGLI 80/20 (da P4)
Questa settimana NON si fa: scalare YT oltre 1 video · outreach S6 prima del case study · restyle brand · A/B test & pixel · altre pagine oltre le 2 previste · perfezione olt

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 6. TAGLI 80/20 (da P4): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [4. GATES & KILL CRITERIA (da P5)](#a-009)
- `see_also` -> [5. METRICHE (da P6 — misurate, non promesse)](#a-010)
- `see_also` -> [4. GATES & KILL CRITERIA (da P5)](#a-009)
- `see_also` -> [5. METRICHE (da P6 — misurate, non promesse)](#a-010)

---
### 8. ATTIVAZIONE WORKSHOP {#a-013}
**Definizione Canonica**: ## 8. ATTIVAZIONE WORKSHOP

**Spiegazione Estesa e Contesto Operativo**:
## 8. ATTIVAZIONE WORKSHOP
Il piano è eseguito dal workshop: orchestrazione in `03-WORKFLOWS/workflows.yaml`, reparti e agenti in `04-AGENTS/AGENTS-REGISTRY.md`, sistema nervoso in `06-NERVOUS-SYSTEM/`, memoria in `00-MEMORY/` (viva dal bootstrap, CP-001). Architettura completa: `02-ARCHITECTURE/ARCHITETTURA-ESTATE.md`.
---
⛓️ Trace P12: `PLANNING-P7#estate-2026` · sintesi di P1..P6 · memory: CP-001, DEC-EST-001..004

**Citazione Verbatim dal Sorgente**:
> ## 8. ATTIVAZIONE WORKSHOP
Il piano è eseguito dal workshop: orchestrazione in `03-WORKFLOWS/workflows.yaml`, reparti e agenti in `04-AGENTS/AGENTS-REGISTRY.md`, sistema nervoso in `06-NERVOUS-SYSTEM/`, memoria in `00-MEMORY/` (viva dal bootstrap, CP-001). Architettura completa: `02-ARCHITECTURE/ARCHITETTURA-ESTATE.md`.
---
⛓️ Trace P12: `PLANNING-P7#estate-2026` · sintesi di P1..P6 · memory: CP-001, DEC-EST-001..004

**Esempio (Sorgente)**:
> ## 8. ATTIVAZIONE WORKSHOP
Il piano è eseguito dal workshop: orchestrazione in `03-WORKFLOWS/workflows.yaml`, reparti e agenti in `04-AGENTS/AGENTS-REGISTRY.md`, sistema nervoso in `06-NERVOUS-SYSTEM/

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 8. ATTIVAZIONE WORKSHOP: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [6. TAGLI 80/20 (da P4)](#a-011)
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `see_also` -> [6. TAGLI 80/20 (da P4)](#a-011)
- `see_also` -> [WF-MASTER.md](#a-014)

---
### WF-MASTER.md {#a-014}
**Definizione Canonica**: # WF-MASTER — Orchestratore della settimana (21→26/07/2026)

**Spiegazione Estesa e Contesto Operativo**:
# WF-MASTER — Orchestratore della settimana (21→26/07/2026)
> Owner: chief-forge · Ricorre: continuo · Config macchina: `workflows.yaml`
> Unico compito: far eseguire il MASTER PLAN (P7) rispettando gates, coda swarm, memoria.

**Citazione Verbatim dal Sorgente**:
> # WF-MASTER — Orchestratore della settimana (21→26/07/2026)
> Owner: chief-forge · Ricorre: continuo · Config macchina: `workflows.yaml`
> Unico compito: far eseguire il MASTER PLAN (P7) rispettando gates, coda swarm, memoria.

**Esempio (Sorgente)**:
> # WF-MASTER — Orchestratore della settimana (21→26/07/2026)
> Owner: chief-forge · Ricorre: continuo · Config macchina: `workflows.yaml`
> Unico compito: far eseguire il MASTER PLAN (P7) rispettando g

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-MASTER.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `see_also` -> [8. ATTIVAZIONE WORKSHOP](#a-013)
- `see_also` -> [8. ATTIVAZIONE WORKSHOP](#a-013)
- `see_also` -> [Loop giornaliero](#a-015)

---
### Loop giornaliero {#a-015}
**Definizione Canonica**: ## Loop giornaliero

**Spiegazione Estesa e Contesto Operativo**:
## Loop giornaliero
```
09:00  leggi dashboard (07-CONTROL) → gate di oggi? → brief mentale 5'
09:30  corsia Max (finestra contatti S1) · corsia Gael (build in sequenza P7)
19:00  WF-MEM-EOD: metriche → dashboard → checkpoint EOD → segnala slittamenti
```

**Citazione Verbatim dal Sorgente**:
> ## Loop giornaliero
```
09:00  leggi dashboard (07-CONTROL) → gate di oggi? → brief mentale 5'
09:30  corsia Max (finestra contatti S1) · corsia Gael (build in sequenza P7)
19:00  WF-MEM-EOD: metriche → dashboard → checkpoint EOD → segnala slittamenti
```

**Esempio (Sorgente)**:
> ## Loop giornaliero
```
09:00  leggi dashboard (07-CONTROL) → gate di oggi? → brief mentale 5'
09:30  corsia Max (finestra contatti S1) · corsia Gael (build in sequenza P7)
19:00  WF-MEM-EOD: metriche

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Loop giornaliero: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `see_also` -> [8. ATTIVAZIONE WORKSHOP](#a-013)
- `see_also` -> [WF-MASTER.md](#a-014)
- `see_also` -> [8. ATTIVAZIONE WORKSHOP](#a-013)

---
### Mappa gate (specchietto rapido) {#a-017}
**Definizione Canonica**: ## Mappa gate (specchietto rapido)

**Spiegazione Estesa e Contesto Operativo**:
## Mappa gate (specchietto rapido)
| Gate | Deadline | 🟢 se | 🔴 → azione |
|---|---|---|---|
| DEC | 21/07 20:00 | DEC-001 ATTIVA | impossibile (auto-default) |
| FUNNEL | 22/07 20:00 | test €1 OK | fallback checkout (≤2h) |
| CONTATTI | 23/07 12:00 | 7/7 contattati | follow-up delegato + push S2 compensa |
| S4 | 24/07 20:00 | E2E auto OK | mentalita.brutale STANDBY |
| S5 | 23/07 18:00 | Fliki OK | ladder video → altrimenti slitta |
| REV | 26/07 | ≥1 anticipo incassato | RETRO causa radice → pattern correttivi |

**Citazione Verbatim dal Sorgente**:
> ## Mappa gate (specchietto rapido)
| Gate | Deadline | 🟢 se | 🔴 → azione |
|---|---|---|---|
| DEC | 21/07 20:00 | DEC-001 ATTIVA | impossibile (auto-default) |
| FUNNEL | 22/07 20:00 | test €1 OK | fallback checkout (≤2h) |
| CONTATTI | 23/07 12:00 | 7/7 contattati | follow-up delegato + push S2 compensa |
| S4 | 24/07 20:00 | E2E auto OK | mentalita.brutale STANDBY |
| S5 | 23/07 18:00 | Fliki OK | ladder video → altrimenti slitta |
| REV | 26/

**Esempio (Sorgente)**:
> ## Mappa gate (specchietto rapido)
| Gate | Deadline | 🟢 se | 🔴 → azione |
|---|---|---|---|
| DEC | 21/07 20:00 | DEC-001 ATTIVA | impossibile (auto-default) |
| FUNNEL | 22/07 20:00 | test €1 OK | f

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Mappa gate (specchietto rapido): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `see_also` -> [Loop giornaliero](#a-015)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [Loop giornaliero](#a-015)

---
### Dipendenze (da P3 DAG) {#a-018}
**Definizione Canonica**: ## Dipendenze (da P3 DAG)

**Spiegazione Estesa e Contesto Operativo**:
## Dipendenze (da P3 DAG)
DEC-001→(landing, email) · audit→(bio S3, config S4) · DEC-002→kit S6 · case-study→outreach S6 · Fliki-test→WF-S5 · DEC-004→WF-S5.

**Citazione Verbatim dal Sorgente**:
> ## Dipendenze (da P3 DAG)
DEC-001→(landing, email) · audit→(bio S3, config S4) · DEC-002→kit S6 · case-study→outreach S6 · Fliki-test→WF-S5 · DEC-004→WF-S5.

**Esempio (Sorgente)**:
> ## Dipendenze (da P3 DAG)
DEC-001→(landing, email) · audit→(bio S3, config S4) · DEC-002→kit S6 · case-study→outreach S6 · Fliki-test→WF-S5 · DEC-004→WF-S5.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Dipendenze (da P3 DAG): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [Mappa gate (specchietto rapido)](#a-017)
- `see_also` -> [Mappa gate (specchietto rapido)](#a-017)

---
### Handoff memoria {#a-019}
**Definizione Canonica**: ## Handoff memoria

**Spiegazione Estesa e Contesto Operativo**:
## Handoff memoria
Ogni venera... no: **ogni sera h19:00** — `checkpoint --task EOD-<data> --note "gate: ... slittamenti: ..."`. Domenica 26/07 → WF-MEM-RETRO.
---
⛓️ P12: `WF-MASTER#estate-2026` · orchestra: WF-S1..S6, WF-MEM-* · fonti: PLANNING-P7, ARCHITETTURA

**Citazione Verbatim dal Sorgente**:
> ## Handoff memoria
Ogni venera... no: **ogni sera h19:00** — `checkpoint --task EOD-<data> --note "gate: ... slittamenti: ..."`. Domenica 26/07 → WF-MEM-RETRO.
---
⛓️ P12: `WF-MASTER#estate-2026` · orchestra: WF-S1..S6, WF-MEM-* · fonti: PLANNING-P7, ARCHITETTURA

**Esempio (Sorgente)**:
> ## Handoff memoria
Ogni venera... no: **ogni sera h19:00** — `checkpoint --task EOD-<data> --note "gate: ... slittamenti: ..."`. Domenica 26/07 → WF-MEM-RETRO.
---
⛓️ P12: `WF-MASTER#estate-2026` · or

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Handoff memoria: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [Mappa gate (specchietto rapido)](#a-017)
- `see_also` -> [Dipendenze (da P3 DAG)](#a-018)

---
### WF-PERF-LOOP.md {#a-020}
**Definizione Canonica**: # ⚡ WF-PERF-LOOP — il micro-ecosistema di performance (ciclo confermato)

**Spiegazione Estesa e Contesto Operativo**:
# ⚡ WF-PERF-LOOP — il micro-ecosistema di performance (ciclo confermato)
> Richiesta Max (21/07): **ogni azione del workflow, ogni singola volta**, viene analizzata (debug + soluzione + struttura + modifiche), salvata in uno spazio dedicato della memoria, studiata da agenti specifici che emettono **micro-input** verso gli altri agenti — e il miglioramento deve essere **confermato**, non auspicato.
> Posizione: gira DOPO ogni azione, trasversale a tutti i WF (S1..S6, MEM-*, MASTER). Sponsor: TRUTH-CMD · Esecuzione: Performance Cell (`04-AGENTS/PERFORMANCE-CELL.md`).

**Citazione Verbatim dal Sorgente**:
> # ⚡ WF-PERF-LOOP — il micro-ecosistema di performance (ciclo confermato)
> Richiesta Max (21/07): **ogni azione del workflow, ogni singola volta**, viene analizzata (debug + soluzione + struttura + modifiche), salvata in uno spazio dedicato della memoria, studiata da agenti specifici che emettono **micro-input** verso gli altri agenti — e il miglioramento deve essere **confermato**, non auspicato.
> Posizione: gira DOPO ogni azione, trasversale a

**Esempio (Sorgente)**:
> # ⚡ WF-PERF-LOOP — il micro-ecosistema di performance (ciclo confermato)
> Richiesta Max (21/07): **ogni azione del workflow, ogni singola volta**, viene analizzata (debug + soluzione + struttura + mo

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-PERF-LOOP.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [Dipendenze (da P3 DAG)](#a-018)
- `see_also` -> [Handoff memoria](#a-019)

---
### 1. IL CICLO (T0 → T5, chiuso e confermato) {#a-021}
**Definizione Canonica**: ## 1. IL CICLO (T0 → T5, chiuso e confermato)

**Spiegazione Estesa e Contesto Operativo**:
## 1. IL CICLO (T0 → T5, chiuso e confermato)
```
T0 AZIONE CHIUSA ──(post-task hook del router)──►
T1 CAPTURE    perf-collector scrive PERF record in 00-MEMORY/performances/
⚠️ scrittura diretta su file: NON consuma la quota 30 msg/giorno
│
T2  ANALYZE   perf-analyst legge il record ENTRO L'EOD e compila la scorecard 5D:
① correctness/debug (errori, retry, escalation)  ② qualità soluzione
③ struttura risposta/artefatto  ④ scope-fit (DoD rispettata?)
⑤ efficiency (TTD vs benchmark di ruolo)  + gate: traceability (CP presente?)
│
T3  SYNTHESIZE segnale nuovo → pattern candidato (ReasoningBank DRAFT)
segnale già visto → +1 ricorrenza (contatore sul pattern)
│
T4  DISPATCH   feedback-dispatcher emette micro-output MIRATI (via board, FB-* in memoria):
· TIP → all'agente coinvolto (micro-input: "la prossima volta fai X")
· RULE-NOTE → al regolatore (se una regola intralcia il lavoro reale)
· MUTATION-PROP → al comandante di casta (se lo stesso problema ricorre ≥3x)
regola anti-nagging: stesso TIP, stesso agente → non ripetere entro 3 task
│
T5  CONFIRM   alla PROSSIMA performance della stessa famiglia-task dell'agente:
✅ problema NON ricorre → FB → status: confirmed → pattern → ReasoningBank UFFICIALE
❌ problema ricorre    → FB → status: recurred  → escalation automatica:
mutation obbligatoria del prompt (v4) o pairing repair
```
**Il punto chiave ("ciclico confermato")**: un miglioramento ESISTE solo quando T5 lo conferma. Un TIP senza conferma alla performance successiva non è un miglioramento: è un suggerimento. Il loop tiene i conti.

**Citazione Verbatim dal Sorgente**:
> ## 1. IL CICLO (T0 → T5, chiuso e confermato)
```
T0 AZIONE CHIUSA ──(post-task hook del router)──►
T1 CAPTURE    perf-collector scrive PERF record in 00-MEMORY/performances/
⚠️ scrittura diretta su file: NON consuma la quota 30 msg/giorno
│
T2  ANALYZE   perf-analyst legge il record ENTRO L'EOD e compila la scorecard 5D:
① correctness/debug (errori, retry, escalation)  ② qualità soluzione
③ struttura risposta/artefatto  ④ scope-fit (DoD rispetta

**Esempio (Sorgente)**:
> ## 1. IL CICLO (T0 → T5, chiuso e confermato)
```
T0 AZIONE CHIUSA ──(post-task hook del router)──►
T1 CAPTURE    perf-collector scrive PERF record in 00-MEMORY/performances/
⚠️ scrittura diretta su f

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 1. IL CICLO (T0 → T5, chiuso e confermat: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [Handoff memoria](#a-019)
- `see_also` -> [WF-PERF-LOOP.md](#a-020)

---
### 2. LO SCHEMA — Performance Record (`00-MEMORY/performances/PERF-NNN-*.md`) {#a-022}
**Definizione Canonica**: ## 2. LO SCHEMA — Performance Record (`00-MEMORY/performances/PERF-NNN-*.md`)

**Spiegazione Estesa e Contesto Operativo**:
## 2. LO SCHEMA — Performance Record (`00-MEMORY/performances/PERF-NNN-*.md`)
```yaml
agente / task / wf / esito (success|partial|failed)
timestamps → TTD calcolato
debug: {errori, retry, escalation, fix applicati}
output_ref: path artefatto prodotto
verification: {verificatore, first_pass: bool, note}
scorecard 5D: correctness·solution·structure·scope-fit·efficiency (1-5) + gate traceability
feedback_collegati: [FB-ids]   # chiusura loop T5: confirmed | recurred
```
Comando: `python3 00-MEMORY/memory_manager.py perf --agent <id> --task <id> --wf <WF> --result success --ttd 2.5 --firstpass 1 --verifier funnel-verifier --debug "..." --note "..."`

**Citazione Verbatim dal Sorgente**:
> ## 2. LO SCHEMA — Performance Record (`00-MEMORY/performances/PERF-NNN-*.md`)
```yaml
agente / task / wf / esito (success|partial|failed)
timestamps → TTD calcolato
debug: {errori, retry, escalation, fix applicati}
output_ref: path artefatto prodotto
verification: {verificatore, first_pass: bool, note}
scorecard 5D: correctness·solution·structure·scope-fit·efficiency (1-5) + gate traceability
feedback_collegati: [FB-ids]   # chiusura loop T5: con

**Esempio (Sorgente)**:
> ## 2. LO SCHEMA — Performance Record (`00-MEMORY/performances/PERF-NNN-*.md`)
```yaml
agente / task / wf / esito (success|partial|failed)
timestamps → TTD calcolato
debug: {errori, retry, escalation, 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2. LO SCHEMA — Performance Record (`00-M: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [WF-PERF-LOOP.md](#a-020)
- `see_also` -> [1. IL CICLO (T0 → T5, chiuso e confermato)](#a-021)

---
### 3. LO SCHEMA — Feedback Record (`00-MEMORY/feedback/FB-NNN-*.md`) {#a-023}
**Definizione Canonica**: ## 3. LO SCHEMA — Feedback Record (`00-MEMORY/feedback/FB-NNN-*.md`)

**Spiegazione Estesa e Contesto Operativo**:
## 3. LO SCHEMA — Feedback Record (`00-MEMORY/feedback/FB-NNN-*.md`)
```yaml
tipo: TIP | RULE-NOTE | MUTATION-PROP
da: feedback-dispatcher (su analisi di perf-analyst)
a: agente | regolatore | comandante
micro-input: "..."            # piccolo, azionabile, da ricordare
su_performance: PERF-id
status: open → acked (obbligatorio) → confirmed | recurred
```
Comando: `python3 00-MEMORY/memory_manager.py feedback --agent <dest> --ftype TIP --note "..." --perf PERF-NNN`

**Citazione Verbatim dal Sorgente**:
> ## 3. LO SCHEMA — Feedback Record (`00-MEMORY/feedback/FB-NNN-*.md`)
```yaml
tipo: TIP | RULE-NOTE | MUTATION-PROP
da: feedback-dispatcher (su analisi di perf-analyst)
a: agente | regolatore | comandante
micro-input: "..."            # piccolo, azionabile, da ricordare
su_performance: PERF-id
status: open → acked (obbligatorio) → confirmed | recurred
```
Comando: `python3 00-MEMORY/memory_manager.py feedback --agent <dest> --ftype TIP --note "...

**Esempio (Sorgente)**:
> ## 3. LO SCHEMA — Feedback Record (`00-MEMORY/feedback/FB-NNN-*.md`)
```yaml
tipo: TIP | RULE-NOTE | MUTATION-PROP
da: feedback-dispatcher (su analisi di perf-analyst)
a: agente | regolatore | comanda

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3. LO SCHEMA — Feedback Record (`00-MEMO: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [1. IL CICLO (T0 → T5, chiuso e confermato)](#a-021)
- `see_also` -> [2. LO SCHEMA — Performance Record (`00-MEMORY/performances/PERF-NNN-*.md`)](#a-022)

---
### 5. ESEMPI CONCRETI (dal workshop reale) {#a-025}
**Definizione Canonica**: ## 5. ESEMPI CONCRETI (dal workshop reale)

**Spiegazione Estesa e Contesto Operativo**:
## 5. ESEMPI CONCRETI (dal workshop reale)
- forge-builder chiude la landing S2 → PERF: TTD 3h, first-pass NO (checkout rotto al test €1), debug: fallback Gumroad usato. → perf-analyst: structure OK, correctness −1 setup KYC, scope-fit OK. → dispatcher: TIP a funnel-engineer "test €1 SEMPRE prima di dichiarare live" + pattern DRAFT "Stripe KYC lento → aprire Gumroad in parallelo il giorno prima". Alla prossima prima-pass ✅ su WF-S6 landing → FB confirmed → pattern ufficiale.
- closer-ops invia follow-up → PERF: esito success ma 2h dopo la finestra (cadence). → TIP: "contatti S1 solo nelle finestre 9:30/18:00". Ricorre ancora → 3ª ricorrenza → MUTATION-PROP a REVENUE-CMD: inserire le finestre nel system-prompt di closer-ops.

**Citazione Verbatim dal Sorgente**:
> ## 5. ESEMPI CONCRETI (dal workshop reale)
- forge-builder chiude la landing S2 → PERF: TTD 3h, first-pass NO (checkout rotto al test €1), debug: fallback Gumroad usato. → perf-analyst: structure OK, correctness −1 setup KYC, scope-fit OK. → dispatcher: TIP a funnel-engineer "test €1 SEMPRE prima di dichiarare live" + pattern DRAFT "Stripe KYC lento → aprire Gumroad in parallelo il giorno prima". Alla prossima prima-pass ✅ su WF-S6 landing → FB c

**Esempio (Sorgente)**:
> ## 5. ESEMPI CONCRETI (dal workshop reale)
- forge-builder chiude la landing S2 → PERF: TTD 3h, first-pass NO (checkout rotto al test €1), debug: fallback Gumroad usato. → perf-analyst: structure OK, 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 5. ESEMPI CONCRETI (dal workshop reale): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `see_also` -> [3. LO SCHEMA — Feedback Record (`00-MEMORY/feedback/FB-NNN-*.md`)](#a-023)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 6. ATTIVAZIONE {#a-026}
**Definizione Canonica**: ## 6. ATTIVAZIONE

**Spiegazione Estesa e Contesto Operativo**:
## 6. ATTIVAZIONE
- **Ora (file-based)**: ogni reparto emette `perf` a chiusura task (l'hook è nel post_task di workflows.yaml); la Performance Cell analizza in EOD (T2–T4) e conferma alla next run (T5).
- **F5/F6 (runtime)**: il router invoca T1 automaticamente su `post_task`; T5 schedulato su ogni nuovo PERF della stessa famiglia.
- Validazione: ogni domenica al COUNCIL — tabella "TIP confermati vs ricorsi" = il vero termometro del miglioramento ciclico.
---
⛓️ Trace P12: `WF-PERF-LOOP#estate-2026` · input: direttiva Max 21/07 · aggancia: v4-MASTER §7 (punto 8) · agenti: 04-AGENTS/PERFORMANCE-CELL.md · storage: 00-MEMORY/performances + feedback

**Citazione Verbatim dal Sorgente**:
> ## 6. ATTIVAZIONE
- **Ora (file-based)**: ogni reparto emette `perf` a chiusura task (l'hook è nel post_task di workflows.yaml); la Performance Cell analizza in EOD (T2–T4) e conferma alla next run (T5).
- **F5/F6 (runtime)**: il router invoca T1 automaticamente su `post_task`; T5 schedulato su ogni nuovo PERF della stessa famiglia.
- Validazione: ogni domenica al COUNCIL — tabella "TIP confermati vs ricorsi" = il vero termometro del migliorament

**Esempio (Sorgente)**:
> ## 6. ATTIVAZIONE
- **Ora (file-based)**: ogni reparto emette `perf` a chiusura task (l'hook è nel post_task di workflows.yaml); la Performance Cell analizza in EOD (T2–T4) e conferma alla next run (T

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 6. ATTIVAZIONE: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `see_also` -> [5. ESEMPI CONCRETI (dal workshop reale)](#a-025)

---
### 1. Setup (21/07) {#a-028}
**Definizione Canonica**: ## 1. Setup (21/07)

**Spiegazione Estesa e Contesto Operativo**:
## 1. Setup (21/07)
- **Max**: lista 7 lead → tabella `07-CONTROL/LISTA-7-LEAD.md`: nome · stato relazione · canale preferito · ultimo contatto · esito.
- **Claude**: questo script (già pronto sotto) + argomentario obiezioni.

**Citazione Verbatim dal Sorgente**:
> ## 1. Setup (21/07)
- **Max**: lista 7 lead → tabella `07-CONTROL/LISTA-7-LEAD.md`: nome · stato relazione · canale preferito · ultimo contatto · esito.
- **Claude**: questo script (già pronto sotto) + argomentario obiezioni.

**Esempio (Sorgente)**:
> ## 1. Setup (21/07)
- **Max**: lista 7 lead → tabella `07-CONTROL/LISTA-7-LEAD.md`: nome · stato relazione · canale preferito · ultimo contatto · esito.
- **Claude**: questo script (già pronto sotto) 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 1. Setup (21/07): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `see_also` -> [6. ATTIVAZIONE](#a-026)

---
### 4. Cadence & Tracking (Controllo di Qualità ≥92%) {#a-034}
**Definizione Canonica**: ## 4. Cadence & Tracking (Controllo di Qualità ≥92%)

**Spiegazione Estesa e Contesto Operativo**:
## 4. Cadence & Tracking (Controllo di Qualità ≥92%)
- Prima di ogni invio/chiamata: verifica che il messaggio passi i criteri di `checklist_APSOC.md` (`SKILL & Agenti/Empire Studio Suite/andrei-pascu-system/checklist_APSOC.md`).
- Finestre operative Max: **h9:30** e **h18:00** (21→26/07). Follow-up dopo 48h di silenzio.
- EOD h19:00: `metric --name s1_lead_contattati --value <n>` · `s1_risposte` · su chiusura `s1_anticipi_chiusi` + `s1_incasso_eur` + `checkpoint --task WF-S1`.

**Citazione Verbatim dal Sorgente**:
> ## 4. Cadence & Tracking (Controllo di Qualità ≥92%)
- Prima di ogni invio/chiamata: verifica che il messaggio passi i criteri di `checklist_APSOC.md` (`SKILL & Agenti/Empire Studio Suite/andrei-pascu-system/checklist_APSOC.md`).
- Finestre operative Max: **h9:30** e **h18:00** (21→26/07). Follow-up dopo 48h di silenzio.
- EOD h19:00: `metric --name s1_lead_contattati --value <n>` · `s1_risposte` · su chiusura `s1_anticipi_chiusi` + `s1_incasso_e

**Esempio (Sorgente)**:
> ## 4. Cadence & Tracking (Controllo di Qualità ≥92%)
- Prima di ogni invio/chiamata: verifica che il messaggio passi i criteri di `checklist_APSOC.md` (`SKILL & Agenti/Empire Studio Suite/andrei-pascu

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 4. Cadence & Tracking (Controllo di Qual: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 5. Escalation & Sicurezza {#a-035}
**Definizione Canonica**: ## 5. Escalation & Sicurezza

**Spiegazione Estesa e Contesto Operativo**:
## 5. Escalation & Sicurezza
- 0 risposte sui primi 3 entro 22/07 h18:00 → checkpoint di allerta + revisione hook con Claude.
- Veto extra-sconto → `decision --title "sconto eccezione lead X"` (mai sconti fuori termini non registrati).
---
⛓️ P12: `WF-S1#estate-2026` · owner esecuzione: Max (90'/g max) · supporto: claude, chief-forge

**Citazione Verbatim dal Sorgente**:
> ## 5. Escalation & Sicurezza
- 0 risposte sui primi 3 entro 22/07 h18:00 → checkpoint di allerta + revisione hook con Claude.
- Veto extra-sconto → `decision --title "sconto eccezione lead X"` (mai sconti fuori termini non registrati).
---
⛓️ P12: `WF-S1#estate-2026` · owner esecuzione: Max (90'/g max) · supporto: claude, chief-forge

**Esempio (Sorgente)**:
> ## 5. Escalation & Sicurezza
- 0 risposte sui primi 3 entro 22/07 h18:00 → checkpoint di allerta + revisione hook con Claude.
- Veto extra-sconto → `decision --title "sconto eccezione lead X"` (mai sc

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 5. Escalation & Sicurezza: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 1. Pricing attivo (DEC-EST-001) {#a-037}
**Definizione Canonica**: ## 1. Pricing attivo (DEC-EST-001)

**Spiegazione Estesa e Contesto Operativo**:
## 1. Pricing attivo (DEC-EST-001)
- Manuale completo (203pp): **€67 lancio fino 31/07**, poi **€97**.
- Lead-magnet: **Parte 1 (cap. 1–3) gratuita** → raccolta email.
- Order-bump in checkout: **pack template +€27**.

**Citazione Verbatim dal Sorgente**:
> ## 1. Pricing attivo (DEC-EST-001)
- Manuale completo (203pp): **€67 lancio fino 31/07**, poi **€97**.
- Lead-magnet: **Parte 1 (cap. 1–3) gratuita** → raccolta email.
- Order-bump in checkout: **pack template +€27**.

**Esempio (Sorgente)**:
> ## 1. Pricing attivo (DEC-EST-001)
- Manuale completo (203pp): **€67 lancio fino 31/07**, poi **€97**.
- Lead-magnet: **Parte 1 (cap. 1–3) gratuita** → raccolta email.
- Order-bump in checkout: **pack

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 1. Pricing attivo (DEC-EST-001): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 2. Funnel minimo vendibile {#a-038}
**Definizione Canonica**: ## 2. Funnel minimo vendibile

**Spiegazione Estesa e Contesto Operativo**:
## 2. Funnel minimo vendibile
```
traffico (bio S3/S4 · push Max · lista outreach)
→ landing (empire-premium-style, motore site-*)
→ checkout (Stripe link → Gumroad → PayPal.me → bonifico)
→ email 1 consegna · email 2 caso d'uso · email 3 scadenza lancio
```

**Citazione Verbatim dal Sorgente**:
> ## 2. Funnel minimo vendibile
```
traffico (bio S3/S4 · push Max · lista outreach)
→ landing (empire-premium-style, motore site-*)
→ checkout (Stripe link → Gumroad → PayPal.me → bonifico)
→ email 1 consegna · email 2 caso d'uso · email 3 scadenza lancio
```

**Esempio (Sorgente)**:
> ## 2. Funnel minimo vendibile
```
traffico (bio S3/S4 · push Max · lista outreach)
→ landing (empire-premium-style, motore site-*)
→ checkout (Stripe link → Gumroad → PayPal.me → bonifico)
→ email 1 c

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2. Funnel minimo vendibile: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 3 email (caricate nel checkout/ESP il 22/07) {#a-040}
**Definizione Canonica**: ### 3 email (caricate nel checkout/ESP il 22/07)

**Spiegazione Estesa e Contesto Operativo**:
### 3 email (caricate nel checkout/ESP il 22/07)
- **E1 (istante)**: consegna link download + "inizia dal cap. 4" + bump.
- **E2 (+1g)**: caso d'uso concreto (da trascrizione a skill in 1 sessione) + CTA chi non ha comprato.
- **E3 (+3g)**: "il 31/07 il prezzo passa a €97" + FAQ (formato, aggiornamenti, rimborso 14gg).

**Citazione Verbatim dal Sorgente**:
> ### 3 email (caricate nel checkout/ESP il 22/07)
- **E1 (istante)**: consegna link download + "inizia dal cap. 4" + bump.
- **E2 (+1g)**: caso d'uso concreto (da trascrizione a skill in 1 sessione) + CTA chi non ha comprato.
- **E3 (+3g)**: "il 31/07 il prezzo passa a €97" + FAQ (formato, aggiornamenti, rimborso 14gg).

**Esempio (Sorgente)**:
> ### 3 email (caricate nel checkout/ESP il 22/07)
- **E1 (istante)**: consegna link download + "inizia dal cap. 4" + bump.
- **E2 (+1g)**: caso d'uso concreto (da trascrizione a skill in 1 sessione) + 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3 email (caricate nel checkout/ESP il 22: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 4. Traffico questa settimana (ordine P4) {#a-042}
**Definizione Canonica**: ## 4. Traffico questa settimana (ordine P4)

**Spiegazione Estesa e Contesto Operativo**:
## 4. Traffico questa settimana (ordine P4)
1. Lista outreach esistente (lead già raccolti) — firma email + P.S. dedicato.
2. Push Max su canali caldi (23→26/07): post + storie + 5 contatti diretti/giorno.
3. Bio/link delle pagine S3/S4 appena riattivate.

**Citazione Verbatim dal Sorgente**:
> ## 4. Traffico questa settimana (ordine P4)
1. Lista outreach esistente (lead già raccolti) — firma email + P.S. dedicato.
2. Push Max su canali caldi (23→26/07): post + storie + 5 contatti diretti/giorno.
3. Bio/link delle pagine S3/S4 appena riattivate.

**Esempio (Sorgente)**:
> ## 4. Traffico questa settimana (ordine P4)
1. Lista outreach esistente (lead già raccolti) — firma email + P.S. dedicato.
2. Push Max su canali caldi (23→26/07): post + storie + 5 contatti diretti/gi

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 4. Traffico questa settimana (ordine P4): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 5. Metriche {#a-043}
**Definizione Canonica**: ## 5. Metriche

**Spiegazione Estesa e Contesto Operativo**:
## 5. Metriche
`s2_funnel_live` (0/1 il 22/07) · `s2_vendite_manuale` · `s2_incasso_eur` · download lead-magnet (diagnostica). Anti-vanity: le visite non contano come risultato.
---
⛓️ P12: `WF-S2#estate-2026` · trace: DEC-EST-001 · fallback: checkout ladder (P5) · kill: R-09 scope creep

**Citazione Verbatim dal Sorgente**:
> ## 5. Metriche
`s2_funnel_live` (0/1 il 22/07) · `s2_vendite_manuale` · `s2_incasso_eur` · download lead-magnet (diagnostica). Anti-vanity: le visite non contano come risultato.
---
⛓️ P12: `WF-S2#estate-2026` · trace: DEC-EST-001 · fallback: checkout ladder (P5) · kill: R-09 scope creep

**Esempio (Sorgente)**:
> ## 5. Metriche
`s2_funnel_live` (0/1 il 22/07) · `s2_vendite_manuale` · `s2_incasso_eur` · download lead-magnet (diagnostica). Anti-vanity: le visite non contano come risultato.
---
⛓️ P12: `WF-S2#est

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 5. Metriche: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### PARTE A — S3 crea.illtuo_impero (22→26/07) {#a-045}
**Definizione Canonica**: ## PARTE A — S3 crea.illtuo_impero (22→26/07)

**Spiegazione Estesa e Contesto Operativo**:
## PARTE A — S3 crea.illtuo_impero (22→26/07)

**Citazione Verbatim dal Sorgente**:
> ## PARTE A — S3 crea.illtuo_impero (22→26/07)

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per PARTE A — S3 crea.illtuo_impero (22→26/0: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### A1. Audit (21/07 — chiude G-05) {#a-046}
**Definizione Canonica**: ### A1. Audit (21/07 — chiude G-05)

**Spiegazione Estesa e Contesto Operativo**:
### A1. Audit (21/07 — chiude G-05)
Output: `07-CONTROL/AUDIT-PAGINE-20260721.md` per OGNI pagina: handle · follower · ultimo post · accessi (email/password/2FA ok?) · bio attuale · link attuale · stato (attiva/dormiente).

**Citazione Verbatim dal Sorgente**:
> ### A1. Audit (21/07 — chiude G-05)
Output: `07-CONTROL/AUDIT-PAGINE-20260721.md` per OGNI pagina: handle · follower · ultimo post · accessi (email/password/2FA ok?) · bio attuale · link attuale · stato (attiva/dormiente).

**Esempio (Sorgente)**:
> ### A1. Audit (21/07 — chiude G-05)
Output: `07-CONTROL/AUDIT-PAGINE-20260721.md` per OGNI pagina: handle · follower · ultimo post · accessi (email/password/2FA ok?) · bio attuale · link attuale · sta

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per A1. Audit (21/07 — chiude G-05): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### A2. Attivazione (22/07) {#a-047}
**Definizione Canonica**: ### A2. Attivazione (22/07)

**Spiegazione Estesa e Contesto Operativo**:
### A2. Attivazione (22/07)
1. Bio → promessa + link funnel S2 (`link in bio → landing Manuale`).
2. Batch unico 23/07: **7 caroselli** via carousel-factory (1 run, config esistente).
3. Pubblicazione 1/giorno: Meta Graph API → Buffer → manuale (ultima spiaggia).
4. **Rampa anti-ban**: mai più di 1 post/giorno/pagina, zero automation su follow/like/DM.

**Citazione Verbatim dal Sorgente**:
> ### A2. Attivazione (22/07)
1. Bio → promessa + link funnel S2 (`link in bio → landing Manuale`).
2. Batch unico 23/07: **7 caroselli** via carousel-factory (1 run, config esistente).
3. Pubblicazione 1/giorno: Meta Graph API → Buffer → manuale (ultima spiaggia).
4. **Rampa anti-ban**: mai più di 1 post/giorno/pagina, zero automation su follow/like/DM.

**Esempio (Sorgente)**:
> ### A2. Attivazione (22/07)
1. Bio → promessa + link funnel S2 (`link in bio → landing Manuale`).
2. Batch unico 23/07: **7 caroselli** via carousel-factory (1 run, config esistente).
3. Pubblicazione

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per A2. Attivazione (22/07): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### A3. Angoli caroselli (batch 1) {#a-048}
**Definizione Canonica**: ### A3. Angoli caroselli (batch 1)

**Spiegazione Estesa e Contesto Operativo**:
### A3. Angoli caroselli (batch 1)
1. "Come ho automatizzato X con Claude Code" (caso reale) → CTA: Parte 1 gratis.
2. "5 errori che fanno tutti con Claude Code" → CTA: link in bio.
3. "Da 0 a skill in 1 sessione" (dal Manuale cap. 4) → CTA: Manuale.
4-7. Variazioni hook su 1-3 (riuso pattern vincenti → ReasoningBank in RETRO).

**Citazione Verbatim dal Sorgente**:
> ### A3. Angoli caroselli (batch 1)
1. "Come ho automatizzato X con Claude Code" (caso reale) → CTA: Parte 1 gratis.
2. "5 errori che fanno tutti con Claude Code" → CTA: link in bio.
3. "Da 0 a skill in 1 sessione" (dal Manuale cap. 4) → CTA: Manuale.
4-7. Variazioni hook su 1-3 (riuso pattern vincenti → ReasoningBank in RETRO).

**Esempio (Sorgente)**:
> ### A3. Angoli caroselli (batch 1)
1. "Come ho automatizzato X con Claude Code" (caso reale) → CTA: Parte 1 gratis.
2. "5 errori che fanno tutti con Claude Code" → CTA: link in bio.
3. "Da 0 a skill i

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per A3. Angoli caroselli (batch 1): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### B1. Pipeline target (tutti anelli, nessun buco) {#a-050}
**Definizione Canonica**: ### B1. Pipeline target (tutti anelli, nessun buco)

**Spiegazione Estesa e Contesto Operativo**:
### B1. Pipeline target (tutti anelli, nessun buco)
```
batch settimanale caroselli (carousel-factory, brand mentalita-brutale)
→ gate QA automatico (regole copy esistenti: hook, no-claim, CTA)
→ scheduler pubblicazione (Meta Graph API / Buffer)
→ report engagement automatico (settimanale)
→ loop: hook/CTA vincenti → ReasoningBank (WF-*-IMPROVE)
```

**Citazione Verbatim dal Sorgente**:
> ### B1. Pipeline target (tutti anelli, nessun buco)
```
batch settimanale caroselli (carousel-factory, brand mentalita-brutale)
→ gate QA automatico (regole copy esistenti: hook, no-claim, CTA)
→ scheduler pubblicazione (Meta Graph API / Buffer)
→ report engagement automatico (settimanale)
→ loop: hook/CTA vincenti → ReasoningBank (WF-*-IMPROVE)
```

**Esempio (Sorgente)**:
> ### B1. Pipeline target (tutti anelli, nessun buco)
```
batch settimanale caroselli (carousel-factory, brand mentalita-brutale)
→ gate QA automatico (regole copy esistenti: hook, no-claim, CTA)
→ sche

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per B1. Pipeline target (tutti anelli, nessu: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### B2. Gate E2E (24/07) {#a-051}
**Definizione Canonica**: ### B2. Gate E2E (24/07)

**Spiegazione Estesa e Contesto Operativo**:
### B2. Gate E2E (24/07)
Test dimostrabile: 1 carosello prodotto → QA pass/fail visibile → schedulato → report generato. Esito → `checkpoint --task WF-S4 --note "E2E ok/ko"`.

**Citazione Verbatim dal Sorgente**:
> ### B2. Gate E2E (24/07)
Test dimostrabile: 1 carosello prodotto → QA pass/fail visibile → schedulato → report generato. Esito → `checkpoint --task WF-S4 --note "E2E ok/ko"`.

**Esempio (Sorgente)**:
> ### B2. Gate E2E (24/07)
Test dimostrabile: 1 carosello prodotto → QA pass/fail visibile → schedulato → report generato. Esito → `checkpoint --task WF-S4 --note "E2E ok/ko"`.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per B2. Gate E2E (24/07): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### B3. Monetizzazione breve (se e solo se ON) {#a-052}
**Definizione Canonica**: ### B3. Monetizzazione breve (se e solo se ON)

**Spiegazione Estesa e Contesto Operativo**:
### B3. Monetizzazione breve (se e solo se ON)
link in bio → lead-magnet → S2 · shoutout a pagamento quando torna trazione · affiliate. Niente piani long-term-only.

**Citazione Verbatim dal Sorgente**:
> ### B3. Monetizzazione breve (se e solo se ON)
link in bio → lead-magnet → S2 · shoutout a pagamento quando torna trazione · affiliate. Niente piani long-term-only.

**Esempio (Sorgente)**:
> ### B3. Monetizzazione breve (se e solo se ON)
link in bio → lead-magnet → S2 · shoutout a pagamento quando torna trazione · affiliate. Niente piani long-term-only.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per B3. Monetizzazione breve (se e solo se O: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### Metriche S3/S4 {#a-053}
**Definizione Canonica**: ## Metriche S3/S4

**Spiegazione Estesa e Contesto Operativo**:
## Metriche S3/S4
`s3_caroselli_pubblicati` (≥3) · click bio→landing (diagnostica) · `s4_pipeline_e2e` (0/1) · vendite attribuite via S2 (lagging unico che conta).
---
⛓️ P12: `WF-S3-S4#estate-2026` · trace: F-04, F-07, R-06 · gate: Gate-S4 24/07 h20

**Citazione Verbatim dal Sorgente**:
> ## Metriche S3/S4
`s3_caroselli_pubblicati` (≥3) · click bio→landing (diagnostica) · `s4_pipeline_e2e` (0/1) · vendite attribuite via S2 (lagging unico che conta).
---
⛓️ P12: `WF-S3-S4#estate-2026` · trace: F-04, F-07, R-06 · gate: Gate-S4 24/07 h20

**Esempio (Sorgente)**:
> ## Metriche S3/S4
`s3_caroselli_pubblicati` (≥3) · click bio→landing (diagnostica) · `s4_pipeline_e2e` (0/1) · vendite attribuite via S2 (lagging unico che conta).
---
⛓️ P12: `WF-S3-S4#estate-2026` ·

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Metriche S3/S4: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 1. Pipeline 9-stage ( Empire Studio ) {#a-055}
**Definizione Canonica**: ## 1. Pipeline 9-stage ( Empire Studio )

**Spiegazione Estesa e Contesto Operativo**:
## 1. Pipeline 9-stage ( Empire Studio )
| Stage | Nome | Agente | Output |
|---|---|---|---|
| 0 | Memory bootstrap + Strategy Manifest | strategy-dept + memory-architect | nicchia (DEC-004) + focus salvati |
| 1 | Ingestion | yt-channel-ingester / video-single-ingester | 1 video competitor scelto (prova: view/outlier) |
| 2 | Frame extraction | script ffmpeg | frame-NNN.png campionati |
| 3 | Visione reale | video-watcher | struttura narrativa del video |
| 4 | Knowledge atoms | knowledge-extractor | atomi con trace `video-id#ts + frame-NNN` |
| 5 | Verifica | visual-verifier + compliance-auditor | **gate anti-copia**: riformulato, non tradotto letteralmente |
| 6 | Forge + Render | content-forge-invoker → **yt-fliki-renderer** | script IT + video renderizzato (o ladder fallback) |
| 7 | Wiki + Publish | wiki-writer → **yt-seo-publisher** | video live: titolo/desc/tag SEO + **link Manuale in descrizione** |
| 8 | Improve | update-proposer | pattern → ReasoningBank (`youtube/`) |
| 9 | Memory close | checkpoint-manager + memory-architect | `memory/youtube-runs/<run-id>/` + CP |

**Citazione Verbatim dal Sorgente**:
> ## 1. Pipeline 9-stage ( Empire Studio )
| Stage | Nome | Agente | Output |
|---|---|---|---|
| 0 | Memory bootstrap + Strategy Manifest | strategy-dept + memory-architect | nicchia (DEC-004) + focus salvati |
| 1 | Ingestion | yt-channel-ingester / video-single-ingester | 1 video competitor scelto (prova: view/outlier) |
| 2 | Frame extraction | script ffmpeg | frame-NNN.png campionati |
| 3 | Visione reale | video-watcher | struttura narrativa 

**Esempio (Sorgente)**:
> ## 1. Pipeline 9-stage ( Empire Studio )
| Stage | Nome | Agente | Output |
|---|---|---|---|
| 0 | Memory bootstrap + Strategy Manifest | strategy-dept + memory-architect | nicchia (DEC-004) + focus 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 1. Pipeline 9-stage ( Empire Studio ): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 2. Render ladder (se Gate-S5 🔴) {#a-056}
**Definizione Canonica**: ## 2. Render ladder (se Gate-S5 🔴)

**Spiegazione Estesa e Contesto Operativo**:
## 2. Render ladder (se Gate-S5 🔴)
1. **Fliki API** (chiave in `.env` — regola 4 dossier).
2. **Fallback**: script + stock footage (Pexels) + TTS + montaggio ffmpeg.
3. **Defer**: S5 slitta alla settimana prox con `error --wf WF-YT-RENDER`. **Mai** sottrarre ore a S1/S2.

**Citazione Verbatim dal Sorgente**:
> ## 2. Render ladder (se Gate-S5 🔴)
1. **Fliki API** (chiave in `.env` — regola 4 dossier).
2. **Fallback**: script + stock footage (Pexels) + TTS + montaggio ffmpeg.
3. **Defer**: S5 slitta alla settimana prox con `error --wf WF-YT-RENDER`. **Mai** sottrarre ore a S1/S2.

**Esempio (Sorgente)**:
> ## 2. Render ladder (se Gate-S5 🔴)
1. **Fliki API** (chiave in `.env` — regola 4 dossier).
2. **Fallback**: script + stock footage (Pexels) + TTS + montaggio ffmpeg.
3. **Defer**: S5 slitta alla setti

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2. Render ladder (se Gate-S5 🔴): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 3. Struttura directory di run (memory-first) {#a-057}
**Definizione Canonica**: ## 3. Struttura directory di run (memory-first)

**Spiegazione Estesa e Contesto Operativo**:
## 3. Struttura directory di run (memory-first)
```
00-MEMORY/  (operativa)
memory/youtube-runs/<run-id>/    checkpoint P10 + log API
memory/knowledge/<video-id>/     atomi + trace P12
memory/workflow-state/youtube/   stato canali + ReasoningBank YT
memory/errors/youtube/           video saltati, errori Fliki, gate anti-copia
```
(questa sottostruttura si crea nel vault second-brain alla prima run; l'operativa resta in 00-MEMORY con `checkpoint --task WF-YT-*`)

**Citazione Verbatim dal Sorgente**:
> ## 3. Struttura directory di run (memory-first)
```
00-MEMORY/  (operativa)
memory/youtube-runs/<run-id>/    checkpoint P10 + log API
memory/knowledge/<video-id>/     atomi + trace P12
memory/workflow-state/youtube/   stato canali + ReasoningBank YT
memory/errors/youtube/           video saltati, errori Fliki, gate anti-copia
```
(questa sottostruttura si crea nel vault second-brain alla prima run; l'operativa resta in 00-MEMORY con `checkpoint -

**Esempio (Sorgente)**:
> ## 3. Struttura directory di run (memory-first)
```
00-MEMORY/  (operativa)
memory/youtube-runs/<run-id>/    checkpoint P10 + log API
memory/knowledge/<video-id>/     atomi + trace P12
memory/workflow

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3. Struttura directory di run (memory-fi: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 4. SEO pack pubblicazione (yt-seo-publisher) {#a-058}
**Definizione Canonica**: ## 4. SEO pack pubblicazione (yt-seo-publisher)

**Spiegazione Estesa e Contesto Operativo**:
## 4. SEO pack pubblicazione (yt-seo-publisher)
- Titolo: pattern competitor + keyword IT ("Claude Code", "AI per lavorare", "automazione").
- Descrizione: 2 righe valore → **link Manuale (S2)** → capitoli → tag.
- CTA verbale nel primo 10%: "risorsa gratuita in descrizione" (Parte 1 del Manuale).

**Citazione Verbatim dal Sorgente**:
> ## 4. SEO pack pubblicazione (yt-seo-publisher)
- Titolo: pattern competitor + keyword IT ("Claude Code", "AI per lavorare", "automazione").
- Descrizione: 2 righe valore → **link Manuale (S2)** → capitoli → tag.
- CTA verbale nel primo 10%: "risorsa gratuita in descrizione" (Parte 1 del Manuale).

**Esempio (Sorgente)**:
> ## 4. SEO pack pubblicazione (yt-seo-publisher)
- Titolo: pattern competitor + keyword IT ("Claude Code", "AI per lavorare", "automazione").
- Descrizione: 2 righe valore → **link Manuale (S2)** → cap

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 4. SEO pack pubblicazione (yt-seo-publis: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 5. Revenue path (verità, da P6) {#a-059}
**Definizione Canonica**: ## 5. Revenue path (verità, da P6)

**Spiegazione Estesa e Contesto Operativo**:
## 5. Revenue path (verità, da P6)
Questa settimana: **0 € diretti** — il video porta lead verso S2 (lead-gen). AdSense/compounding: agosto-settembre. Chi dice altro, mente.

**Citazione Verbatim dal Sorgente**:
> ## 5. Revenue path (verità, da P6)
Questa settimana: **0 € diretti** — il video porta lead verso S2 (lead-gen). AdSense/compounding: agosto-settembre. Chi dice altro, mente.

**Esempio (Sorgente)**:
> ## 5. Revenue path (verità, da P6)
Questa settimana: **0 € diretti** — il video porta lead verso S2 (lead-gen). AdSense/compounding: agosto-settembre. Chi dice altro, mente.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 5. Revenue path (verità, da P6): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 1. Identità (dopo veto) {#a-062}
**Definizione Canonica**: ## 1. Identità (dopo veto)

**Spiegazione Estesa e Contesto Operativo**:
## 1. Identità (dopo veto)
- **Nome**: Preventa (default DEC-EST-002). Promessa: *"Il preventivo professionale in 3 minuti."*
- **Verifica dominio/handle DOPO la scelta** (mai prima — regola anti-spreco): preferenza `preventa.it`, fallback `preventa.app` / `getpreventa.com`. Esito → `decision --title "dominio Preventa"`.

**Citazione Verbatim dal Sorgente**:
> ## 1. Identità (dopo veto)
- **Nome**: Preventa (default DEC-EST-002). Promessa: *"Il preventivo professionale in 3 minuti."*
- **Verifica dominio/handle DOPO la scelta** (mai prima — regola anti-spreco): preferenza `preventa.it`, fallback `preventa.app` / `getpreventa.com`. Esito → `decision --title "dominio Preventa"`.

**Esempio (Sorgente)**:
> ## 1. Identità (dopo veto)
- **Nome**: Preventa (default DEC-EST-002). Promessa: *"Il preventivo professionale in 3 minuti."*
- **Verifica dominio/handle DOPO la scelta** (mai prima — regola anti-spre

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 1. Identità (dopo veto): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 2. Promo-kit minimo (DoD congelata) {#a-063}
**Definizione Canonica**: ## 2. Promo-kit minimo (DoD congelata)

**Spiegazione Estesa e Contesto Operativo**:
## 2. Promo-kit minimo (DoD congelata)
| Pezzo | Owner | Contenuto | Due |
|---|---|---|---|
| Case study Novacar (PDF) | claude (case-study-forge) | prova reale: prima/dopo, tempi (3 min vs ore), PDF esempio, quote cliente se disponibile | 23/07 |
| Landing Preventa | gael (empire-premium-style) | 1 pagina: demo video + PDF esempio + prezzo/licenza + CTA "attivazione" | 24/07 |
| Demo video ≤2min | gael | screen-recording GUI premium: da dati auto→PDF inviato | 24/07 |
| Lista lead import-DE | A1 scrape | dealer IT che vendono auto import dalla Germania (mobile.de): nome, sito, email/form | 25/07 |
| Sequenza outreach | A2 runtime | 3 email con case study allegato; CTA call 15' | 26/07 |

**Citazione Verbatim dal Sorgente**:
> ## 2. Promo-kit minimo (DoD congelata)
| Pezzo | Owner | Contenuto | Due |
|---|---|---|---|
| Case study Novacar (PDF) | claude (case-study-forge) | prova reale: prima/dopo, tempi (3 min vs ore), PDF esempio, quote cliente se disponibile | 23/07 |
| Landing Preventa | gael (empire-premium-style) | 1 pagina: demo video + PDF esempio + prezzo/licenza + CTA "attivazione" | 24/07 |
| Demo video ≤2min | gael | screen-recording GUI premium: da dati au

**Esempio (Sorgente)**:
> ## 2. Promo-kit minimo (DoD congelata)
| Pezzo | Owner | Contenuto | Due |
|---|---|---|---|
| Case study Novacar (PDF) | claude (case-study-forge) | prova reale: prima/dopo, tempi (3 min vs ore), PDF

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2. Promo-kit minimo (DoD congelata): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 3. Modello commerciale {#a-064}
**Definizione Canonica**: ## 3. Modello commerciale

**Spiegazione Estesa e Contesto Operativo**:
## 3. Modello commerciale
- **Setup** una tantum (sconto solo per la campagna S1 "Partenza Anticipata") + **canone mensile** (kill-switch licenze = riscossione garantita).
- ICP: concessionari IT che importano dalla Germania (preventivi complessi: IVA, immatricolazione, trasporto — Preventa li gestisce out-of-the-box).

**Citazione Verbatim dal Sorgente**:
> ## 3. Modello commerciale
- **Setup** una tantum (sconto solo per la campagna S1 "Partenza Anticipata") + **canone mensile** (kill-switch licenze = riscossione garantita).
- ICP: concessionari IT che importano dalla Germania (preventivi complessi: IVA, immatricolazione, trasporto — Preventa li gestisce out-of-the-box).

**Esempio (Sorgente)**:
> ## 3. Modello commerciale
- **Setup** una tantum (sconto solo per la campagna S1 "Partenza Anticipata") + **canone mensile** (kill-switch licenze = riscossione garantita).
- ICP: concessionari IT che 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3. Modello commerciale: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 5. Sinergie {#a-066}
**Definizione Canonica**: ## 5. Sinergie

**Spiegazione Estesa e Contesto Operativo**:
## 5. Sinergie
- S1 chiusi → testimonial citati nella landing (con permesso) → alimentano S6.
- Il runtime A2 vende da solo: stesso motore outreach, ICP nuovo (ADR-002 wrap).
- Revenue: **setup + canone** da settimana prox; questa settimana il kit si costruisce e parte la prima sequenza (25-26/07).

**Citazione Verbatim dal Sorgente**:
> ## 5. Sinergie
- S1 chiusi → testimonial citati nella landing (con permesso) → alimentano S6.
- Il runtime A2 vende da solo: stesso motore outreach, ICP nuovo (ADR-002 wrap).
- Revenue: **setup + canone** da settimana prox; questa settimana il kit si costruisce e parte la prima sequenza (25-26/07).

**Esempio (Sorgente)**:
> ## 5. Sinergie
- S1 chiusi → testimonial citati nella landing (con permesso) → alimentano S6.
- Il runtime A2 vende da solo: stesso motore outreach, ICP nuovo (ADR-002 wrap).
- Revenue: **setup + cano

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 5. Sinergie: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### 6. Metriche {#a-067}
**Definizione Canonica**: ## 6. Metriche

**Spiegazione Estesa e Contesto Operativo**:
## 6. Metriche
`s6_kit_pronto` (0/1) · lista lead (n) · email inviate/risposte (diagnostica). Lagging reale: call prenotate.
---
⛓️ P12: `WF-S6#estate-2026` · trace: DEC-EST-002, F-08, direttiva Max 19/07 ("come infinite" = nome forte)

**Citazione Verbatim dal Sorgente**:
> ## 6. Metriche
`s6_kit_pronto` (0/1) · lista lead (n) · email inviate/risposte (diagnostica). Lagging reale: call prenotate.
---
⛓️ P12: `WF-S6#estate-2026` · trace: DEC-EST-002, F-08, direttiva Max 19/07 ("come infinite" = nome forte)

**Esempio (Sorgente)**:
> ## 6. Metriche
`s6_kit_pronto` (0/1) · lista lead (n) · email inviate/risposte (diagnostica). Lagging reale: call prenotate.
---
⛓️ P12: `WF-S6#estate-2026` · trace: DEC-EST-002, F-08, direttiva Max 1

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 6. Metriche: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### workflows.yaml {#a-068}
**Definizione Canonica**: # ═══════════════════════════════════════════════════════════════════

**Spiegazione Estesa e Contesto Operativo**:
# ═══════════════════════════════════════════════════════════════════

**Citazione Verbatim dal Sorgente**:
> # ═══════════════════════════════════════════════════════════════════

**Esempio (Sorgente)**:
> # ═══════════════════════════════════════════════════════════════════

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per workflows.yaml: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ESTATE-2026 REVENUE WORKSHOP — Orchestrazione completa (machine-readable) {#a-069}
**Definizione Canonica**: # ESTATE-2026 REVENUE WORKSHOP — Orchestrazione completa (machine-readable)

**Spiegazione Estesa e Contesto Operativo**:
# ESTATE-2026 REVENUE WORKSHOP — Orchestrazione completa (machine-readable)

**Citazione Verbatim dal Sorgente**:
> # ESTATE-2026 REVENUE WORKSHOP — Orchestrazione completa (machine-readable)

**Esempio (Sorgente)**:
> # ESTATE-2026 REVENUE WORKSHOP — Orchestrazione completa (machine-readable)

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ESTATE-2026 REVENUE WORKSHOP — Orchestra: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Plan of record: 01-PLANNING/PLANNING-P7-MASTER-PLAN.md {#a-070}
**Definizione Canonica**: # Plan of record: 01-PLANNING/PLANNING-P7-MASTER-PLAN.md

**Spiegazione Estesa e Contesto Operativo**:
# Plan of record: 01-PLANNING/PLANNING-P7-MASTER-PLAN.md

**Citazione Verbatim dal Sorgente**:
> # Plan of record: 01-PLANNING/PLANNING-P7-MASTER-PLAN.md

**Esempio (Sorgente)**:
> # Plan of record: 01-PLANNING/PLANNING-P7-MASTER-PLAN.md

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Plan of record: 01-PLANNING/PLANNING-P7-: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Trace: ARCH-ESTATE-2026#estate-2026 · memory-first (ADR-EST-003) {#a-071}
**Definizione Canonica**: # Trace: ARCH-ESTATE-2026#estate-2026 · memory-first (ADR-EST-003)

**Spiegazione Estesa e Contesto Operativo**:
# Trace: ARCH-ESTATE-2026#estate-2026 · memory-first (ADR-EST-003)

**Citazione Verbatim dal Sorgente**:
> # Trace: ARCH-ESTATE-2026#estate-2026 · memory-first (ADR-EST-003)

**Esempio (Sorgente)**:
> # Trace: ARCH-ESTATE-2026#estate-2026 · memory-first (ADR-EST-003)

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Trace: ARCH-ESTATE-2026#estate-2026 · me: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ═══════════════════════════════════════════════════════════════════ {#a-072}
**Definizione Canonica**: # ═══════════════════════════════════════════════════════════════════

**Spiegazione Estesa e Contesto Operativo**:
# ═══════════════════════════════════════════════════════════════════
version: 1
project: ESTATE-2026-REVENUE
baseline: 2026-07-21
window: {start: "2026-07-21", end: "2026-07-26"}
globals:
memory:
root: 00-MEMORY
cli: "python3 00-MEMORY/memory_manager.py"
rule_zero: "task chiuso -> checkpoint; decisione -> decision; fallimento -> error"
eod_hook: "checkpoint giornaliero + metriche h19:00 (WF-MEM-EOD)"
perf_hook: "OGNI azione chiusa -> PERF record (P-LOOP T1) -> analisi/dispatch/conferma (WF-PERF-LOOP)"
constraints:
single_opus_swarm: true          # CP-20260711-002 · ADR-EST-004
swarm_queue: [S1, S2, S6, S5]    # priorità coda
secrets: {FLIKI_API_KEY: ".env locale (gitignored) — MAI nei file/prompt"}
dod_frozen: true                 # ADR-EST-005
decision_policy: default_plus_veto # ADR-EST-006: scade il veto -> default ATTIVO
decisions:                            # mirror di 00-MEMORY/decisions (source of truth)
- {id: DEC-EST-001, topic: "prezzo Manuale (B-003)", default: "EUR 67 lancio / EUR 97 listino", veto_deadline: "2026-07-21T20:00", blocks: [S2, S3, S4]}
- {id: DEC-EST-002, topic: "nome prodotto S6", default: "Preventa", veto_deadline: "2026-07-22T12:00", blocks: [S6]}
- {id: DEC-EST-004, topic: "nicchia YT #1", default: "AI/Claude IT", veto_deadline: "2026-07-24T18:00", blocks: [S5]}
workflows:
WF-MASTER:
type: orchestrator
owner: chief-forge
schedule: continuous
loop:
- "mattina: leggi dashboard + gate del giorno (07-CONTROL/DASHBOARD-E-RETRO.md)"
- "esegui corsie Max/Gael/Claude da PLANNING-P7 §2"
- "h19:00 -> WF-MEM-EOD"
- "gate scaduto -> marcare 🟢/🔴 e applicare kill criteria (PLANNING-P5 §2)"
gates: [Gate-DEC, Gate-FUNNEL, Gate-CONTATTI, Gate-S4, Gate-S5, Gate-REV]
WF-S1-CONCESSIONARI:
owner: Max; support: [claude, chief-forge]; window: 21→26/07
doD: ">=1 anticipo incassato (setup o canone)"
steps:
- {id: s1.1, who: Max, task: "lista 7 lead (nome/stato/canale/ultimo contatto)", due: "2026-07-21"}
- {id: s1.2, who: claude, task: "script WA msg1-2-3 + argomentario obiezioni", due: "2026-07-21", skill: [beast-preventivi, A8-closing]}
- {id: s1.3, who: Max, task: "contatti WA-first: 2 (21/07), 3 (22/07), 2 + follow-up (23/07)", gate: Gate-CONTATTI}
- {id: s1.4, who: Max, task: "chiusura: link pagamento setup contrattualizzato", due: "2026-07-25"}
- {id: s1.5, who: gael, task: "clone app via /nuovo-concessionario il giorno della firma", on: chiusura}
offer:
name: "Partenza Anticipata Luglio"
terms: ["setup -30% OPPURE primo mese gratis", "attivazione entro 31/07", "prezzo 2026 bloccato", "onboarding entro 20/08"]
angle: "A settembre sei GIA' operativo. Gli altri staranno installando."
fallback: {max_unavailable: "chiusura asincrona via msg-3 binario (P4 §2)"}
memory: {on_close: "metric --name s1_anticipi_chiusi --value <n>; metric --name s1_incasso_eur --value <eur>"}
WF-S2-MANUALE:
owner: chief-forge (gael); copy: claude; window: 21→26/07
depends: [DEC-EST-001]
doD: "1 landing + checkout test €1 OK + 3 email caricate (STOP — vendibile, non perfetto)"
steps:
- {id: s2.1, who: sistema, task: "DEC-001 ATTIVA entro veto 21/07 h20:00"}
- {id: s2.2, who: claude, task: "copy landing + 3 email (cro-copy-architect APSOC)", due: "2026-07-21 sera"}
- {id: s2.3, who: gael, task: "landing empire-premium-style + checkout live", due: "2026-07-22", gate: Gate-FUNNEL}
- {id: s2.4, who: gael, task: "lead-magnet Parte 1 (cap.1-3) come free download", due: "2026-07-22"}
- {id: s2.5, who: Max, task: "push su canali caldi (post/storie/contatti diretti)", due: "2026-07-23→26"}
checkout_ladder: [stripe_payment_link, gumroad, paypal_me, bonifico_typeform]
pricing: {launch: 67, list: 97, bump: "+EUR 27 pack template", currency: EUR}
memory: {on_sale: "metric --name s2_vendite_manuale --value <n>"}
WF-S3-PAGINE:
owner: gael; window: 22→26/07
doD: "crea.illtuo_impero riparte con bio→funnel + >=3 caroselli pubblicati"
steps:
- {id: s3.1, who: gael, task: "AUDIT pagine (follower/ultimo post/accessi-2FA/bio)", due: "2026-07-21", output: "07-CONTROL/AUDIT-PAGINE-20260721.md"}
- {id: s3.2, who: gael, task: "bio + link in bio -> funnel S2", due: "2026-07-22"}
- {id: s3.3, who: gael, task: "batch 7 caroselli via carousel-factory (1 run)", due: "2026-07-23"}
- {id: s3.4, who: sistema, task: "pubblicazione 1/giorno (Meta Graph API -> Buffer -> manuale)"}
rules: ["rampa anti-ban: max 1 post/giorno/pagina", "contenuti dal motore esistente, zero restyle (ADR-EST-002)"]
memory: {daily: "metric --name s3_caroselli_pubblicati --value <n>"}
WF-S4-MENTALITA:
owner: gael; window: 23→25/07; hard_rule: "condizione Max F-07: SOLO se automazione 100%, altrimenti STANDBY"
doD_gate: "E2E: batch -> QA auto -> scheduler -> report. Test run dimostrabile"
steps:
- {id: s4.1, who: gael, task: "wrap carousel-factory brand mentalita-brutale (config esistente)", due: "2026-07-23"}
- {id: s4.2, who: gael, task: "gate QA automatico su regole copy", due: "2026-07-24"}
- {id: s4.3, who: gael, task: "scheduler pubblicazione (Meta Graph API/Buffer)", due: "2026-07-24"}
- {id: s4.4, who: gael, task: "report engagement settimanale automatico", due: "2026-07-24", gate: Gate-S4}
monetization: "link in bio -> lead-magnet -> S2 (niente long-term-only)"
no_go: "dichiarare STANDBY con error --wf WF-S4 e archiviare a settimana prox"
WF-S5-YOUTUBE:
owner: youtube-dept; window: 24→25/07; depends: [DEC-EST-004, Gate-S5]
doD: "1 video renderizzato + pubblicato + trace P12 + checkpoint (basta)"
stages:                              # pipeline 9-stage Empire Studio (dossier S5)
- {stage: 0, name: memory-bootstrap, agent: strategy-dept}
- {stage: 1, name: ingestion, agent: yt-channel-ingester / video-single-ingester}
- {stage: 2, name: frame-extraction, tools: [ffmpeg]}
- {stage: 3, name: visione-reale, agent: video-watcher}
- {stage: 4, name: knowledge-atoms, agent: knowledge-extractor, trace: P12}
- {stage: 5, name: verifica, agents: [visual-verifier, compliance-auditor]}
- {stage: 6, name: forge-render, agents: [content-forge-invoker, yt-fliki-renderer]}
- {stage: 7, name: publish, agents: [wiki-writer, yt-seo-publisher]}
- {stage: 8, name: improve, agent: update-proposer -> ReasoningBank}
- {stage: 9, name: memory-close, agent: checkpoint-manager}
render_ladder: [fliki_api, "script+stock+tts+ffmpeg", deferred]
revenue_path: "lead-gen via link Manuale in descrizione (sempre), AdSense/compounding dopo"
rules: ["chiave Fliki solo .env", "1 video solo questa settimana", "ogni run -> memory_manager checkpoint"]
WF-S6-REBRAND-PROMO:
owner: chief-forge; decisions: [DEC-EST-002]; window: 22→26/07
doD: "1 landing Preventa + 1 demo video <=2min + 1 case study PDF"
steps:
- {id: s6.1, who: Max, task: "veto/ok nome (30 sec)", due: "2026-07-22 h12:00"}
- {id: s6.2, who: gael, task: "verifica dominio/handle DOPO la scelta", due: "2026-07-22"}
- {id: s6.3, who: claude, task: "case study Novacar (case-study-forge: tempi/PDF/consegna reali)", due: "2026-07-23"}
- {id: s6.4, who: gael, task: "landing empire-premium-style + demo video 2min", due: "2026-07-24"}
- {id: s6.5, who: A1, task: "scrape dealer IT import-DE (mobile.de) -> lista lead", due: "2026-07-25"}
- {id: s6.6, who: A2, task: "runtime outreach con case study (sequenza email)", due: "2026-07-26"}
testimonial: "i 7 di S1 chiusi diventano i primi casi pubblici del rebrand"
WF-MEM-EOD:
owner: memory-architect; schedule: "daily 19:00"
steps: ["raccolta metriche del giorno (tracker P6)", "aggiornamento dashboard gate", "checkpoint EOD", "segnalazione slittamenti -> error"]
WF-MEM-RETRO:
owner: strategy-dept; schedule: "2026-07-26"
steps: ["status memoria + numeri veri", "per stream: ✅/⚠️/❌ + causa radice", "pattern vincenti -> reasoning-bank", "sync note fisse -> second-brain", "CP RETRO-20260726"]
WF-PERF-LOOP:
owner: performance-cell (memory-dir, sponsor TRUTH-CMD); trigger: "post-task di OGNI workflow, ogni singola volta"
spec: 03-WORKFLOWS/WF-PERF-LOOP.md; agents: 04-AGENTS/PERFORMANCE-CELL.md
cycle: [T0 azione chiusa, T1 capture PERF record (file write, fuori quota), T2 analisi 5D entro EOD (debug/soluzione/struttura/scope/efficiency), T3 synthesize -> ReasoningBank DRAFT, T4 dispatch TIP/RULE-NOTE/MUTATION-PROP, T5 confirm alla prossima PERF: confirmed->pattern ufficiale | recurred->mutation obbligatoria]
storage: [00-MEMORY/performances, 00-MEMORY/feedback]
rules: ["non blocca mai (non e' un verificatore)", "anti-nagging: stesso TIP non ripetuto entro 3 task", "FB senza conferma >5 task -> riesumato in EOD-SYNC", "solo pattern confirmed precaricati dal pre_task hook"]
traceability: {spec: "ogni WF cita DEC-EST-xxx e CP-xxx che lo attivano (P12)"}

**Citazione Verbatim dal Sorgente**:
> # ═══════════════════════════════════════════════════════════════════
version: 1
project: ESTATE-2026-REVENUE
baseline: 2026-07-21
window: {start: "2026-07-21", end: "2026-07-26"}
globals:
memory:
root: 00-MEMORY
cli: "python3 00-MEMORY/memory_manager.py"
rule_zero: "task chiuso -> checkpoint; decisione -> decision; fallimento -> error"
eod_hook: "checkpoint giornaliero + metriche h19:00 (WF-MEM-EOD)"
perf_hook: "OGNI azione chiusa -> PERF record

**Esempio (Sorgente)**:
> # ═══════════════════════════════════════════════════════════════════
version: 1
project: ESTATE-2026-REVENUE
baseline: 2026-07-21
window: {start: "2026-07-21", end: "2026-07-26"}
globals:
memory:
roo

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ════════════════════════════════════════: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### test_fliki_api(): {#a-074}
**Definizione Canonica**: def test_fliki_api():

**Spiegazione Estesa e Contesto Operativo**:
def test_fliki_api():
key = os.environ.get('FLIKI_API_KEY')
if not key:
print('[WARN] FLIKI_API_KEY non trovata in .env o variabili di sistema.')
return False
print('[INFO] Verifica connessione a Fliki API...')
return True
if __name__ == '__main__':
test_fliki_api()

**Citazione Verbatim dal Sorgente**:
> def test_fliki_api():
key = os.environ.get('FLIKI_API_KEY')
if not key:
print('[WARN] FLIKI_API_KEY non trovata in .env o variabili di sistema.')
return False
print('[INFO] Verifica connessione a Fliki API...')
return True
if __name__ == '__main__':
test_fliki_api()

**Esempio (Sorgente)**:
> def test_fliki_api():
key = os.environ.get('FLIKI_API_KEY')
if not key:
print('[WARN] FLIKI_API_KEY non trovata in .env o variabili di sistema.')
return False
print('[INFO] Verifica connessione a Flik

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per test_fliki_api():: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### now(): return datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S") {#a-076}
**Definizione Canonica**: def now(): return datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

**Spiegazione Estesa e Contesto Operativo**:
def now(): return datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

**Citazione Verbatim dal Sorgente**:
> def now(): return datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

**Esempio (Sorgente)**:
> def now(): return datetime.datetime.now().strftime("%Y-%m-%d %H:%M:%S")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per now(): return datetime.datetime.now().st: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### today(): return datetime.datetime.now().strftime("%Y%m%d") {#a-077}
**Definizione Canonica**: def today(): return datetime.datetime.now().strftime("%Y%m%d")

**Spiegazione Estesa e Contesto Operativo**:
def today(): return datetime.datetime.now().strftime("%Y%m%d")

**Citazione Verbatim dal Sorgente**:
> def today(): return datetime.datetime.now().strftime("%Y%m%d")

**Esempio (Sorgente)**:
> def today(): return datetime.datetime.now().strftime("%Y%m%d")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per today(): return datetime.datetime.now().: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### nsure(): {#a-078}
**Definizione Canonica**: def ensure():

**Spiegazione Estesa e Contesto Operativo**:
def ensure():
for sd in SUBDIRS: (ROOT / sd).mkdir(parents=True, exist_ok=True)
if not INDEX.exists():
INDEX.write_text(
"# 🧠 MEMORY-INDEX — DIGITAL-EMPIRE / ESTATE-2026\n\n"
"> Indice vivo di ogni atomo di memoria. Aggiornato automaticamente da memory_manager.py.\n"
"> Regola zero: **task chiuso -> checkpoint**. Mai decidere senza registrare.\n\n"
"| # | Tipo | ID | Titolo | File | Data |\n|---|---|---|---|---|---|\n", encoding="utf-8")

**Citazione Verbatim dal Sorgente**:
> def ensure():
for sd in SUBDIRS: (ROOT / sd).mkdir(parents=True, exist_ok=True)
if not INDEX.exists():
INDEX.write_text(
"# 🧠 MEMORY-INDEX — DIGITAL-EMPIRE / ESTATE-2026\n\n"
"> Indice vivo di ogni atomo di memoria. Aggiornato automaticamente da memory_manager.py.\n"
"> Regola zero: **task chiuso -> checkpoint**. Mai decidere senza registrare.\n\n"
"| # | Tipo | ID | Titolo | File | Data |\n|---|---|---|---|---|---|\n", encoding="utf-8")

**Esempio (Sorgente)**:
> def ensure():
for sd in SUBDIRS: (ROOT / sd).mkdir(parents=True, exist_ok=True)
if not INDEX.exists():
INDEX.write_text(
"# 🧠 MEMORY-INDEX — DIGITAL-EMPIRE / ESTATE-2026\n\n"
"> Indice vivo di ogni at

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per nsure():: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### next_id(kind): {#a-079}
**Definizione Canonica**: def next_id(kind):

**Spiegazione Estesa e Contesto Operativo**:
def next_id(kind):
pref = PREFIX[kind]
d = ROOT / kind
n = 0
for f in d.glob(f"{pref}-*.md"):
m = re.match(rf"^{re.escape(pref)}-(\d+)", f.name)
if m: n = max(n, int(m.group(1)))
return f"{pref}-{n+1:03d}"

**Citazione Verbatim dal Sorgente**:
> def next_id(kind):
pref = PREFIX[kind]
d = ROOT / kind
n = 0
for f in d.glob(f"{pref}-*.md"):
m = re.match(rf"^{re.escape(pref)}-(\d+)", f.name)
if m: n = max(n, int(m.group(1)))
return f"{pref}-{n+1:03d}"

**Esempio (Sorgente)**:
> def next_id(kind):
pref = PREFIX[kind]
d = ROOT / kind
n = 0
for f in d.glob(f"{pref}-*.md"):
m = re.match(rf"^{re.escape(pref)}-(\d+)", f.name)
if m: n = max(n, int(m.group(1)))
return f"{pref}-{n+1:

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per next_id(kind):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ug(t): {#a-080}
**Definizione Canonica**: def slug(t):

**Spiegazione Estesa e Contesto Operativo**:
def slug(t):
s = re.sub(r"[^a-z0-9]+", "-", t.lower()).strip("-")
return s[:48] or "atom"

**Citazione Verbatim dal Sorgente**:
> def slug(t):
s = re.sub(r"[^a-z0-9]+", "-", t.lower()).strip("-")
return s[:48] or "atom"

**Esempio (Sorgente)**:
> def slug(t):
s = re.sub(r"[^a-z0-9]+", "-", t.lower()).strip("-")
return s[:48] or "atom"

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ug(t):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _index(kind, atom_id, title, path): {#a-081}
**Definizione Canonica**: def add_index(kind, atom_id, title, path):

**Spiegazione Estesa e Contesto Operativo**:
def add_index(kind, atom_id, title, path):
lines = INDEX.read_text(encoding="utf-8").splitlines()
n = sum(1 for l in lines if l.startswith("|") and "-" in l and "Tipo" not in l)
lines.append(f"| {n} | {kind} | {atom_id} | {title} | {path.relative_to(ROOT)} | {now()} |")
INDEX.write_text("\n".join(lines) + "\n", encoding="utf-8")

**Citazione Verbatim dal Sorgente**:
> def add_index(kind, atom_id, title, path):
lines = INDEX.read_text(encoding="utf-8").splitlines()
n = sum(1 for l in lines if l.startswith("|") and "-" in l and "Tipo" not in l)
lines.append(f"| {n} | {kind} | {atom_id} | {title} | {path.relative_to(ROOT)} | {now()} |")
INDEX.write_text("\n".join(lines) + "\n", encoding="utf-8")

**Esempio (Sorgente)**:
> def add_index(kind, atom_id, title, path):
lines = INDEX.read_text(encoding="utf-8").splitlines()
n = sum(1 for l in lines if l.startswith("|") and "-" in l and "Tipo" not in l)
lines.append(f"| {n} |

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _index(kind, atom_id, title, path):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### tom(kind, title, body): {#a-082}
**Definizione Canonica**: def atom(kind, title, body):

**Spiegazione Estesa e Contesto Operativo**:
def atom(kind, title, body):
ensure()
atom_id = next_id(kind)
p = ROOT / kind / f"{atom_id}-{slug(title)}.md"
header = (f"---\nid: {atom_id}\ntype: {kind}\ntitle: \"{title}\"\ncreated: {now()}\n"
f"trace: \"{atom_id}#estate-2026\"\nproject: ESTATE-2026-REVENUE\n---\n\n")
p.write_text(header + f"# {atom_id} — {title}\n\n" + body + "\n", encoding="utf-8")
add_index(kind, atom_id, title, p)
print(f"✅ {atom_id} -> {p.relative_to(ROOT.parent)}")
return p

**Citazione Verbatim dal Sorgente**:
> def atom(kind, title, body):
ensure()
atom_id = next_id(kind)
p = ROOT / kind / f"{atom_id}-{slug(title)}.md"
header = (f"---\nid: {atom_id}\ntype: {kind}\ntitle: \"{title}\"\ncreated: {now()}\n"
f"trace: \"{atom_id}#estate-2026\"\nproject: ESTATE-2026-REVENUE\n---\n\n")
p.write_text(header + f"# {atom_id} — {title}\n\n" + body + "\n", encoding="utf-8")
add_index(kind, atom_id, title, p)
print(f"✅ {atom_id} -> {p.relative_to(ROOT.parent)}")
retur

**Esempio (Sorgente)**:
> def atom(kind, title, body):
ensure()
atom_id = next_id(kind)
p = ROOT / kind / f"{atom_id}-{slug(title)}.md"
header = (f"---\nid: {atom_id}\ntype: {kind}\ntitle: \"{title}\"\ncreated: {now()}\n"
f"tr

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per tom(kind, title, body):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_init(a): {#a-083}
**Definizione Canonica**: def cmd_init(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_init(a):
ensure()
(ROOT / "RETRO-PROTOCOLLO.md").write_text(
"# Protocollo RETRO (G7 → domenica 26/07)\n\n"
"1. `status` -> numeri veri (metriche vs target P6).\n"
"2. Per ogni stream: ✅ funzionato / ⚠️ parziale / ❌ fallito + causa.\n"
"3. Pattern vincenti -> `pattern` (ReasoningBank). Errori -> `error`.\n"
"4. Decisioni nuove -> `decision`. Output: RETRO-20260726 checkpoint finale.\n", encoding="utf-8")
print("✅ Memory ecosystem inizializzato:", "\n   - " + "\n   - ".join(SUBDIRS))

**Citazione Verbatim dal Sorgente**:
> def cmd_init(a):
ensure()
(ROOT / "RETRO-PROTOCOLLO.md").write_text(
"# Protocollo RETRO (G7 → domenica 26/07)\n\n"
"1. `status` -> numeri veri (metriche vs target P6).\n"
"2. Per ogni stream: ✅ funzionato / ⚠️ parziale / ❌ fallito + causa.\n"
"3. Pattern vincenti -> `pattern` (ReasoningBank). Errori -> `error`.\n"
"4. Decisioni nuove -> `decision`. Output: RETRO-20260726 checkpoint finale.\n", encoding="utf-8")
print("✅ Memory ecosystem iniziali

**Esempio (Sorgente)**:
> def cmd_init(a):
ensure()
(ROOT / "RETRO-PROTOCOLLO.md").write_text(
"# Protocollo RETRO (G7 → domenica 26/07)\n\n"
"1. `status` -> numeri veri (metriche vs target P6).\n"
"2. Per ogni stream: ✅ funzi

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_init(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_checkpoint(a): {#a-084}
**Definizione Canonica**: def cmd_checkpoint(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_checkpoint(a):
atom("checkpoints", a.task, f"- **WF/Task:** {a.task}\n- **Note:** {a.note}\n- **Stato:** {a.stato}")

**Citazione Verbatim dal Sorgente**:
> def cmd_checkpoint(a):
atom("checkpoints", a.task, f"- **WF/Task:** {a.task}\n- **Note:** {a.note}\n- **Stato:** {a.stato}")

**Esempio (Sorgente)**:
> def cmd_checkpoint(a):
atom("checkpoints", a.task, f"- **WF/Task:** {a.task}\n- **Note:** {a.note}\n- **Stato:** {a.stato}")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_checkpoint(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_decision(a): {#a-085}
**Definizione Canonica**: def cmd_decision(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_decision(a):
atom("decisions", a.title,
f"- **Status:** {a.status}\n- **Owner:** {a.owner}\n- **Contesto:** {a.context}\n"
f"- **Decisione:** {a.decision}\n- **Veto window (scade):** {a.veto or '—'}\n"
f"- **Regola:** scaduto il veto senza obiezioni -> il default diventa decisione operativa (status: ATTIVA).")

**Citazione Verbatim dal Sorgente**:
> def cmd_decision(a):
atom("decisions", a.title,
f"- **Status:** {a.status}\n- **Owner:** {a.owner}\n- **Contesto:** {a.context}\n"
f"- **Decisione:** {a.decision}\n- **Veto window (scade):** {a.veto or '—'}\n"
f"- **Regola:** scaduto il veto senza obiezioni -> il default diventa decisione operativa (status: ATTIVA).")

**Esempio (Sorgente)**:
> def cmd_decision(a):
atom("decisions", a.title,
f"- **Status:** {a.status}\n- **Owner:** {a.owner}\n- **Contesto:** {a.context}\n"
f"- **Decisione:** {a.decision}\n- **Veto window (scade):** {a.veto o

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_decision(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_plan(a): {#a-086}
**Definizione Canonica**: def cmd_plan(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_plan(a):
atom("plans", f"{a.level}-{Path(a.file).stem}",
f"- **Livello planning:** {a.level}\n- **File:** `{a.file}`\n- **Note:** {a.note}")

**Citazione Verbatim dal Sorgente**:
> def cmd_plan(a):
atom("plans", f"{a.level}-{Path(a.file).stem}",
f"- **Livello planning:** {a.level}\n- **File:** `{a.file}`\n- **Note:** {a.note}")

**Esempio (Sorgente)**:
> def cmd_plan(a):
atom("plans", f"{a.level}-{Path(a.file).stem}",
f"- **Livello planning:** {a.level}\n- **File:** `{a.file}`\n- **Note:** {a.note}")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_plan(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_brainstorm(a): atom("brainstorms", a.title, f"{a.note}\n") {#a-087}
**Definizione Canonica**: def cmd_brainstorm(a): atom("brainstorms", a.title, f"{a.note}\n")

**Spiegazione Estesa e Contesto Operativo**:
def cmd_brainstorm(a): atom("brainstorms", a.title, f"{a.note}\n")

**Citazione Verbatim dal Sorgente**:
> def cmd_brainstorm(a): atom("brainstorms", a.title, f"{a.note}\n")

**Esempio (Sorgente)**:
> def cmd_brainstorm(a): atom("brainstorms", a.title, f"{a.note}\n")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_brainstorm(a): atom("brainstorms", a.: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_error(a):      atom("errors", f"{a.wf}-failure", f"- **WF:** {a.wf}\n- **Erro {#a-088}
**Definizione Canonica**: def cmd_error(a):      atom("errors", f"{a.wf}-failure", f"- **WF:** {a.wf}\n- **Errore:** {a.note}\n- **Fix/Fallback:** da completare in RETRO\n")

**Spiegazione Estesa e Contesto Operativo**:
def cmd_error(a):      atom("errors", f"{a.wf}-failure", f"- **WF:** {a.wf}\n- **Errore:** {a.note}\n- **Fix/Fallback:** da completare in RETRO\n")

**Citazione Verbatim dal Sorgente**:
> def cmd_error(a):      atom("errors", f"{a.wf}-failure", f"- **WF:** {a.wf}\n- **Errore:** {a.note}\n- **Fix/Fallback:** da completare in RETRO\n")

**Esempio (Sorgente)**:
> def cmd_error(a):      atom("errors", f"{a.wf}-failure", f"- **WF:** {a.wf}\n- **Errore:** {a.note}\n- **Fix/Fallback:** da completare in RETRO\n")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_error(a):      atom("errors", f"{a.wf: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_metric(a): {#a-089}
**Definizione Canonica**: def cmd_metric(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_metric(a):
atom("metrics", a.name, f"- **Metrica:** {a.name}\n- **Valore:** {a.value} {a.unit or ''}\n- **Target:** vedi 01-PLANNING/PLANNING-P6\n")

**Citazione Verbatim dal Sorgente**:
> def cmd_metric(a):
atom("metrics", a.name, f"- **Metrica:** {a.name}\n- **Valore:** {a.value} {a.unit or ''}\n- **Target:** vedi 01-PLANNING/PLANNING-P6\n")

**Esempio (Sorgente)**:
> def cmd_metric(a):
atom("metrics", a.name, f"- **Metrica:** {a.name}\n- **Valore:** {a.value} {a.unit or ''}\n- **Target:** vedi 01-PLANNING/PLANNING-P6\n")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_metric(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_pattern(a): {#a-090}
**Definizione Canonica**: def cmd_pattern(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_pattern(a):
atom("reasoning-bank", a.title, f"- **Pattern:** {a.title}\n- **Evidenza:** {a.evidence}\n- **Riutilizzo:** automatico nei WF futuri (WF-*-IMPROVE)\n")

**Citazione Verbatim dal Sorgente**:
> def cmd_pattern(a):
atom("reasoning-bank", a.title, f"- **Pattern:** {a.title}\n- **Evidenza:** {a.evidence}\n- **Riutilizzo:** automatico nei WF futuri (WF-*-IMPROVE)\n")

**Esempio (Sorgente)**:
> def cmd_pattern(a):
atom("reasoning-bank", a.title, f"- **Pattern:** {a.title}\n- **Evidenza:** {a.evidence}\n- **Riutilizzo:** automatico nei WF futuri (WF-*-IMPROVE)\n")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_pattern(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_retro(a):      atom("checkpoints", "RETRO-" + a.note[:40], a.note) {#a-091}
**Definizione Canonica**: def cmd_retro(a):      atom("checkpoints", "RETRO-" + a.note[:40], a.note)

**Spiegazione Estesa e Contesto Operativo**:
def cmd_retro(a):      atom("checkpoints", "RETRO-" + a.note[:40], a.note)

**Citazione Verbatim dal Sorgente**:
> def cmd_retro(a):      atom("checkpoints", "RETRO-" + a.note[:40], a.note)

**Esempio (Sorgente)**:
> def cmd_retro(a):      atom("checkpoints", "RETRO-" + a.note[:40], a.note)

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_retro(a):      atom("checkpoints", "R: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_perf(a): {#a-092}
**Definizione Canonica**: def cmd_perf(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_perf(a):
atom("performances", f"{a.agent}-{a.task}",
f"- **Agente:** {a.agent} (casta: da registry)\n- **Task/WF:** {a.task} / {a.wf or '—'}\n"
f"- **Esito:** {a.result}\n- **TTD (h):** {a.ttd or 'n/d'}\n- **First-pass verifica:** {a.firstpass or 'n/d'}\n"
f"- **Verificatore:** {a.verifier or '—'}\n- **Debug (errori/retry/escalation):** {a.debug}\n"
f"- **Note:** {a.note}\n"
f"- **Scorecard 5D:** da compilare da perf-analyst → correctness/solution/structure/scope-fit/efficiency (1-5) + gate traceability\n"
f"- **Feedback collegati:** auto-link dal dispatcher (FB-*). Chiusura loop: confirmed|recurred alla prossima PERF della stessa famiglia.")

**Citazione Verbatim dal Sorgente**:
> def cmd_perf(a):
atom("performances", f"{a.agent}-{a.task}",
f"- **Agente:** {a.agent} (casta: da registry)\n- **Task/WF:** {a.task} / {a.wf or '—'}\n"
f"- **Esito:** {a.result}\n- **TTD (h):** {a.ttd or 'n/d'}\n- **First-pass verifica:** {a.firstpass or 'n/d'}\n"
f"- **Verificatore:** {a.verifier or '—'}\n- **Debug (errori/retry/escalation):** {a.debug}\n"
f"- **Note:** {a.note}\n"
f"- **Scorecard 5D:** da compilare da perf-analyst → correctness

**Esempio (Sorgente)**:
> def cmd_perf(a):
atom("performances", f"{a.agent}-{a.task}",
f"- **Agente:** {a.agent} (casta: da registry)\n- **Task/WF:** {a.task} / {a.wf or '—'}\n"
f"- **Esito:** {a.result}\n- **TTD (h):** {a.ttd

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_perf(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_feedback(a): {#a-093}
**Definizione Canonica**: def cmd_feedback(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_feedback(a):
atom("feedback", f"{a.agent}-{a.ftype}",
f"- **Tipo:** {a.ftype}  (TIP micro-input | RULE-NOTE a regolatore | MUTATION-PROP a comandante)\n"
f"- **Da:** {a.source}\n- **A (destinatario):** {a.agent}\n"
f"- **Micro-input da ricordare:** {a.note}\n- **Generato da performance:** {a.perf or '—'}\n"
f"- **Status:** open → ack obbligatorio del destinatario → alla prossima PERF: **confirmed** (problema non ricorre) | **recurred** (escalation: mutation/pairing)\n"
f"- **Regola anti-nagging:** stesso TIP allo stesso agente non ripetuto entro 3 task.")

**Citazione Verbatim dal Sorgente**:
> def cmd_feedback(a):
atom("feedback", f"{a.agent}-{a.ftype}",
f"- **Tipo:** {a.ftype}  (TIP micro-input | RULE-NOTE a regolatore | MUTATION-PROP a comandante)\n"
f"- **Da:** {a.source}\n- **A (destinatario):** {a.agent}\n"
f"- **Micro-input da ricordare:** {a.note}\n- **Generato da performance:** {a.perf or '—'}\n"
f"- **Status:** open → ack obbligatorio del destinatario → alla prossima PERF: **confirmed** (problema non ricorre) | **recurred** (e

**Esempio (Sorgente)**:
> def cmd_feedback(a):
atom("feedback", f"{a.agent}-{a.ftype}",
f"- **Tipo:** {a.ftype}  (TIP micro-input | RULE-NOTE a regolatore | MUTATION-PROP a comandante)\n"
f"- **Da:** {a.source}\n- **A (destina

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_feedback(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_search(a): {#a-094}
**Definizione Canonica**: def cmd_search(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_search(a):
term = a.term.lower(); hits = []
for sd in SUBDIRS:
for f in (ROOT / sd).glob("*.md"):
txt = f.read_text(encoding="utf-8", errors="ignore")
if term in txt.lower():
line = next((l.strip() for l in txt.splitlines() if term in l.lower() and l.strip().startswith(("- **", "#"))), "")
hits.append(f"- `{f.relative_to(ROOT)}` — {line[:110]}")
print(f"🔎 '{a.term}': {len(hits)} risultati\n" + ("\n".join(hits) if hits else "   nessun risultato"))

**Citazione Verbatim dal Sorgente**:
> def cmd_search(a):
term = a.term.lower(); hits = []
for sd in SUBDIRS:
for f in (ROOT / sd).glob("*.md"):
txt = f.read_text(encoding="utf-8", errors="ignore")
if term in txt.lower():
line = next((l.strip() for l in txt.splitlines() if term in l.lower() and l.strip().startswith(("- **", "#"))), "")
hits.append(f"- `{f.relative_to(ROOT)}` — {line[:110]}")
print(f"🔎 '{a.term}': {len(hits)} risultati\n" + ("\n".join(hits) if hits else "   nessun risu

**Esempio (Sorgente)**:
> def cmd_search(a):
term = a.term.lower(); hits = []
for sd in SUBDIRS:
for f in (ROOT / sd).glob("*.md"):
txt = f.read_text(encoding="utf-8", errors="ignore")
if term in txt.lower():
line = next((l.st

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_search(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### md_status(a): {#a-095}
**Definizione Canonica**: def cmd_status(a):

**Spiegazione Estesa e Contesto Operativo**:
def cmd_status(a):
ensure(); print("🧠 MEMORY STATUS — ESTATE-2026\n")
for sd in SUBDIRS:
files = sorted((ROOT / sd).glob("*.md"))
print(f"  {sd:16s} {len(files):3d} atomi" + (f"  · ultimo: {files[-1].name}" if files else ""))
mets = sorted((ROOT / "metrics").glob("*.md"))
if mets:
print("\n📊 Ultime metriche:")
for f in mets[-8:]:
v = re.search(r"\*\*Valore:\*\* (.+)", f.read_text(encoding="utf-8"))
print(f"   - {f.stem}: {v.group(1) if v else '?'}")

**Citazione Verbatim dal Sorgente**:
> def cmd_status(a):
ensure(); print("🧠 MEMORY STATUS — ESTATE-2026\n")
for sd in SUBDIRS:
files = sorted((ROOT / sd).glob("*.md"))
print(f"  {sd:16s} {len(files):3d} atomi" + (f"  · ultimo: {files[-1].name}" if files else ""))
mets = sorted((ROOT / "metrics").glob("*.md"))
if mets:
print("\n📊 Ultime metriche:")
for f in mets[-8:]:
v = re.search(r"\*\*Valore:\*\* (.+)", f.read_text(encoding="utf-8"))
print(f"   - {f.stem}: {v.group(1) if v else '?'

**Esempio (Sorgente)**:
> def cmd_status(a):
ensure(); print("🧠 MEMORY STATUS — ESTATE-2026\n")
for sd in SUBDIRS:
files = sorted((ROOT / sd).glob("*.md"))
print(f"  {sd:16s} {len(files):3d} atomi" + (f"  · ultimo: {files[-1].

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per md_status(a):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### main(): {#a-096}
**Definizione Canonica**: def main():

**Spiegazione Estesa e Contesto Operativo**:
def main():
ap = argparse.ArgumentParser(description="Digital Empire — Memory Ecosystem Manager")
sub = ap.add_subparsers(dest="cmd", required=True)
sub.add_parser("init").set_defaults(fn=cmd_init)
p = sub.add_parser("checkpoint"); p.add_argument("--task", required=True); p.add_argument("--note", default=""); p.add_argument("--stato", default="completato"); p.set_defaults(fn=cmd_checkpoint)
p = sub.add_parser("decision"); p.add_argument("--title", required=True); p.add_argument("--status", default="PROPOSTA"); p.add_argument("--owner", default="Max"); p.add_argument("--context", default=""); p.add_argument("--decision", required=True); p.add_argument("--veto", default=""); p.set_defaults(fn=cmd_decision)
p = sub.add_parser("plan"); p.add_argument("--file", required=True); p.add_argument("--level", required=True); p.add_argument("--note", default=""); p.set_defaults(fn=cmd_plan)
p = sub.add_parser("brainstorm"); p.add_argument("--title", required=True); p.add_argument("--note", default=""); p.set_defaults(fn=cmd_brainstorm)
p = sub.add_parser("error"); p.add_argument("--wf", required=True); p.add_argument("--note", default=""); p.set_defaults(fn=cmd_error)
p = sub.add_parser("metric"); p.add_argument("--name", required=True); p.add_argument("--value", required=True); p.add_argument("--unit", default=""); p.set_defaults(fn=cmd_metric)
p = sub.add_parser("pattern"); p.add_argument("--title", required=True); p.add_argument("--evidence", default=""); p.set_defaults(fn=cmd_pattern)
p = sub.add_parser("retro"); p.add_argument("--note", required=True); p.set_defaults(fn=cmd_retro)
p = sub.add_parser("perf"); p.add_argument("--agent", required=True); p.add_argument("--task", required=True); p.add_argument("--wf", default=""); p.add_argument("--result", default="success"); p.add_argument("--ttd", default=""); p.add_argument("--firstpass", default=""); p.add_argument("--verifier", default=""); p.add_argument("--debug", default="nessuno"); p.add_argument("--note", default=""); p.set_defaults(fn=cmd_perf)
p = sub.add_parser("feedback"); p.add_argument("--agent", required=True); p.add_argument("--ftype", default="TIP"); p.add_argument("--source", default="perf-analyst"); p.add_argument("--perf", default=""); p.add_argument("--note", required=True); p.set_defaults(fn=cmd_feedback)
p = sub.add_parser("search"); p.add_argument("term"); p.set_defaults(fn=cmd_search)
p = sub.add_parser("status"); p.set_defaults(fn=cmd_status)
a = ap.parse_args(); a.fn(a)
if __name__ == "__main__":
main()

**Citazione Verbatim dal Sorgente**:
> def main():
ap = argparse.ArgumentParser(description="Digital Empire — Memory Ecosystem Manager")
sub = ap.add_subparsers(dest="cmd", required=True)
sub.add_parser("init").set_defaults(fn=cmd_init)
p = sub.add_parser("checkpoint"); p.add_argument("--task", required=True); p.add_argument("--note", default=""); p.add_argument("--stato", default="completato"); p.set_defaults(fn=cmd_checkpoint)
p = sub.add_parser("decision"); p.add_argument("--title"

**Esempio (Sorgente)**:
> def main():
ap = argparse.ArgumentParser(description="Digital Empire — Memory Ecosystem Manager")
sub = ap.add_subparsers(dest="cmd", required=True)
sub.add_parser("init").set_defaults(fn=cmd_init)
p 

**Esempio (Sorgente)**:
> # ─── Main ─────────────────────────────────────────────────────────────────────

**Esempio (Sorgente)**:
> def main():
dry_run = "--dry-run" in sys.argv
auto    = "--auto" in sys.argv
with open(INPUT_PATH, encoding="utf-8") as f:
all_emails = json.load(f)
da_inviare = [
e for e in all_emails
if e.get("stat

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per main():: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Carica .env senza dipendenza da python-dotenv {#a-097}
**Definizione Canonica**: """

**Spiegazione Estesa e Contesto Operativo**:
"""
Genera email 100% personalizzate per ogni lead — framework APSOC+V.
Input:  leads_nuove_nicchie.csv  (37 lead unici)
Output: emails_ready.json
Ogni email usa i mini-dettagli specifici del lead:
sito datato, trovato su Instagram, solo telefono, solo WhatsApp, trovato in PDF, ecc.
Questi dettagli entrano nell'apertura [A] e nell'oggetto — mai frasi generiche.
Run:
python prepare_emails.py
python prepare_emails.py --resume   # salta lead già pronti in emails_ready.json
"""
import csv
import json
import os
import re
import sys
import time
from pathlib import Path

**Citazione Verbatim dal Sorgente**:
> """
Genera email 100% personalizzate per ogni lead — framework APSOC+V.
Input:  leads_nuove_nicchie.csv  (37 lead unici)
Output: emails_ready.json
Ogni email usa i mini-dettagli specifici del lead:
sito datato, trovato su Instagram, solo telefono, solo WhatsApp, trovato in PDF, ecc.
Questi dettagli entrano nell'apertura [A] e nell'oggetto — mai frasi generiche.
Run:
python prepare_emails.py
python prepare_emails.py --resume   # salta lead già pro

**Esempio (Sorgente)**:
> """
Genera email 100% personalizzate per ogni lead — framework APSOC+V.
Input:  leads_nuove_nicchie.csv  (37 lead unici)
Output: emails_ready.json
Ogni email usa i mini-dettagli specifici del lead:
si

**Esempio (Sorgente)**:
> # Carica .env senza dipendenza da python-dotenv
_env = Path(__file__).parent / ".env"
if _env.exists():
for _line in _env.read_text(encoding="utf-8").splitlines():
if "=" in _line and not _line.starts

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Carica .env senza dipendenza da python-d: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _aggiungi_cta(corpo: str) -> str: {#a-098}
**Definizione Canonica**: def _aggiungi_cta(corpo: str) -> str:

**Spiegazione Estesa e Contesto Operativo**:
def _aggiungi_cta(corpo: str) -> str:

**Citazione Verbatim dal Sorgente**:
> def _aggiungi_cta(corpo: str) -> str:

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _aggiungi_cta(corpo: str) -> str:: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### DELIVERABILITY: link VIETATO nella prima email fredda (attiva filtri antispam). {#a-099}
**Definizione Canonica**: # DELIVERABILITY: link VIETATO nella prima email fredda (attiva filtri antispam).

**Spiegazione Estesa e Contesto Operativo**:
# DELIVERABILITY: link VIETATO nella prima email fredda (attiva filtri antispam).

**Citazione Verbatim dal Sorgente**:
> # DELIVERABILITY: link VIETATO nella prima email fredda (attiva filtri antispam).

**Esempio (Sorgente)**:
> # DELIVERABILITY: link VIETATO nella prima email fredda (attiva filtri antispam).

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per DELIVERABILITY: link VIETATO nella prima: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Il link va SOLO nel follow-up #2 (giorno 7). {#a-100}
**Definizione Canonica**: # Il link va SOLO nel follow-up #2 (giorno 7).

**Spiegazione Estesa e Contesto Operativo**:
# Il link va SOLO nel follow-up #2 (giorno 7).

**Citazione Verbatim dal Sorgente**:
> # Il link va SOLO nel follow-up #2 (giorno 7).

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Il link va SOLO nel follow-up #2 (giorno: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Questa funzione ora è un no-op per la prima email. {#a-101}
**Definizione Canonica**: # Questa funzione ora è un no-op per la prima email.

**Spiegazione Estesa e Contesto Operativo**:
# Questa funzione ora è un no-op per la prima email.

**Citazione Verbatim dal Sorgente**:
> # Questa funzione ora è un no-op per la prima email.

**Esempio (Sorgente)**:
> # Questa funzione ora è un no-op per la prima email.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Questa funzione ora è un no-op per la pr: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── CLI args (opzionali, per backward compatibility) ───────────────────────── {#a-103}
**Definizione Canonica**: # ─── CLI args (opzionali, per backward compatibility) ─────────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── CLI args (opzionali, per backward compatibility) ─────────────────────────
_argv = sys.argv[1:]
for _i in range(len(_argv)):
if _argv[_i] == "--csv" and _i + 1 < len(_argv):
CSV_PATH = _argv[_i + 1]
elif _argv[_i] == "--output" and _i + 1 < len(_argv):
OUTPUT_PATH = _argv[_i + 1]

**Citazione Verbatim dal Sorgente**:
> # ─── CLI args (opzionali, per backward compatibility) ─────────────────────────
_argv = sys.argv[1:]
for _i in range(len(_argv)):
if _argv[_i] == "--csv" and _i + 1 < len(_argv):
CSV_PATH = _argv[_i + 1]
elif _argv[_i] == "--output" and _i + 1 < len(_argv):
OUTPUT_PATH = _argv[_i + 1]

**Esempio (Sorgente)**:
> # ─── CLI args (opzionali, per backward compatibility) ─────────────────────────
_argv = sys.argv[1:]
for _i in range(len(_argv)):
if _argv[_i] == "--csv" and _i + 1 < len(_argv):
CSV_PATH = _argv[_i 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── CLI args (opzionali, per backward co: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── Mappatura settore → parola clienti/pazienti ───────────────────────────── {#a-104}
**Definizione Canonica**: # ─── Mappatura settore → parola clienti/pazienti ─────────────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── Mappatura settore → parola clienti/pazienti ─────────────────────────────
_PAROLA = {
"fisioterapista": "pazienti",
"avvocato":       "clienti",
"psicologo":      "pazienti",
"commercialista": "clienti",
"palestra":       "iscritti",
"yoga":           "iscritti",
"fitness":        "iscritti",
"crossfit":       "iscritti",
"estetica":       "clienti",
"spa":            "clienti",
"medico":         "pazienti",
"dentista":       "pazienti",
"chirurgia":      "pazienti",
"clinica":        "pazienti",
}
_VALORE = {
"fisioterapista": (
"Il 65% delle prenotazioni fisioterapia avviene dopo le 19, quando gli studi sono "
"chiusi. Un form di prenotazione online cattura quei pazienti automaticamente, "
"senza dover rispondere al telefono fuori orario."
),
"avvocato": (
"Un intake form digitale riduce l'onboarding da 3 ore a 20 minuti per ogni nuovo "
"cliente. Non serve software costoso: bastano un form online e un documento "
"condiviso su Google Drive."
),
"psicologo": (
"Il 70% di chi cerca supporto psicologico lo fa la sera, tra le 21 e le 23. "
"È il momento in cui si sentono finalmente pronti a chiedere aiuto. "
"Un form di contatto attivo in quella finestra vale quanto tre mesi di passaparola."
),
"commercialista": (
"Eliminare lo scambio documenti via email riduce 10-15 ore di back-and-forth a "
"settimana. Un portale clienti semplice — anche con strumenti gratuiti — "
"risolve questo in modo definitivo."
),
"palestra": (
"L'80% degli abbonamenti persi avviene perché chi voleva iscriversi non è riuscito "
"a prenotare una prova gratuita al primo tentativo. Se non trovano il form subito, "
"raramente tornano."
),
"yoga": (
"L'80% degli abbonamenti persi avviene perché chi voleva iscriversi non è riuscito "
"a prenotare una prova al primo tentativo. Se il form non c'è al primo accesso, "
"raramente tornano."
),
"crossfit": (
"L'80% degli abbonamenti persi avviene perché chi voleva iscriversi non è riuscito "
"a prenotare una prova gratuita al primo tentativo. Se il form non c'è, raramente tornano."
),
"estetica": (
"Le prenotazioni last-minute riempiono il 30% degli slot vuoti ogni settimana, "
"ma solo se il sistema di prenotazione è attivo 24 ore. "
"Una cliente che non trova disponibilità alle 23 prenota da qualcun altro la mattina dopo."
),
"spa": (
"Le prenotazioni last-minute riempiono il 30% degli slot vuoti ogni settimana, "
"ma solo se il sistema è attivo 24 ore. "
"Chi non trova il form alle 23 prenota altrove la mattina."
),
"medico": (
"Il 62% delle ricerche mediche avviene tra le 18 e le 22, quando gli studi sono chiusi. "
"Senza prenotazione online in quel momento, quei pazienti finiscono dove trovano "
"il pulsante 'prenota adesso'."
),
"dentista": (
"Il 62% delle ricerche odontoiatriche avviene fuori orario di studio. "
"Senza prenotazione online visibile, quei pazienti finiscono allo studio "
"che ha il form direttamente su Google."
),
"chirurgia": (
"Il 62% delle ricerche mediche avviene tra le 18 e le 22, quando gli studi sono chiusi. "
"Senza form online, quei pazienti vanno dove trovano il pulsante 'prenota adesso'."
),
"clinica": (
"Il 62% delle ricerche mediche avviene tra le 18 e le 22, quando gli studi sono chiusi. "
"Senza prenotazione online, quei pazienti finiscono dove trovano il pulsante 'prenota adesso'."
),
}
_DEFAULT_VALORE = (
"Un sistema di contatto digitale cattura i lead che arrivano fuori orario — "
"che sono spesso la maggioranza. Senza di esso, quegli utenti non tornano."
)

**Citazione Verbatim dal Sorgente**:
> # ─── Mappatura settore → parola clienti/pazienti ─────────────────────────────
_PAROLA = {
"fisioterapista": "pazienti",
"avvocato":       "clienti",
"psicologo":      "pazienti",
"commercialista": "clienti",
"palestra":       "iscritti",
"yoga":           "iscritti",
"fitness":        "iscritti",
"crossfit":       "iscritti",
"estetica":       "clienti",
"spa":            "clienti",
"medico":         "pazienti",
"dentista":       "pazienti",
"c

**Esempio (Sorgente)**:
> # ─── Mappatura settore → parola clienti/pazienti ─────────────────────────────
_PAROLA = {
"fisioterapista": "pazienti",
"avvocato":       "clienti",
"psicologo":      "pazienti",
"commercialista": "

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── Mappatura settore → parola clienti/p: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _match(mapping: dict, nicchia: str, default: str) -> str: {#a-105}
**Definizione Canonica**: def _match(mapping: dict, nicchia: str, default: str) -> str:

**Spiegazione Estesa e Contesto Operativo**:
def _match(mapping: dict, nicchia: str, default: str) -> str:
n = nicchia.lower()
for k, v in mapping.items():
if k in n:
return v
return default

**Citazione Verbatim dal Sorgente**:
> def _match(mapping: dict, nicchia: str, default: str) -> str:
n = nicchia.lower()
for k, v in mapping.items():
if k in n:
return v
return default

**Esempio (Sorgente)**:
> def _match(mapping: dict, nicchia: str, default: str) -> str:
n = nicchia.lower()
for k, v in mapping.items():
if k in n:
return v
return default

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _match(mapping: dict, nicchia: str, defa: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── Costruisce il mini-dettaglio specifico per il prompt ──────────────────── {#a-106}
**Definizione Canonica**: # ─── Costruisce il mini-dettaglio specifico per il prompt ────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── Costruisce il mini-dettaglio specifico per il prompt ────────────────────

**Citazione Verbatim dal Sorgente**:
> # ─── Costruisce il mini-dettaglio specifico per il prompt ────────────────────

**Esempio (Sorgente)**:
> # ─── Costruisce il mini-dettaglio specifico per il prompt ────────────────────

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── Costruisce il mini-dettaglio specifi: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _nota_contesto(lead: dict) -> str: {#a-107}
**Definizione Canonica**: def _nota_contesto(lead: dict) -> str:

**Spiegazione Estesa e Contesto Operativo**:
def _nota_contesto(lead: dict) -> str:
note    = lead.get("note", "").lower()
website = lead.get("website", "").lower()
if "instagram" in note:
return (
"TROVATO SU INSTAGRAM: Il professionista non ha un sito web. "
"Trovato su Instagram cercando nel settore. "
"L'unico modo di contattarli è via DM o telefono."
)
if "facebook" in website and "sito" not in note:
return (
"SOLO PAGINA FACEBOOK: Nessun sito web proprietario. "
"La presenza online è solo una pagina Facebook. "
"Nessun form di contatto diretto."
)
if "social" in note:
return (
"SOLO CONTATTI SOCIAL: Nessun sito web. "
"La presenza digitale è unicamente social. "
"Per contattarli bisogna usare DM o telefono."
)
if any(y in note for y in ["2010", "2011", "2012", "2013", "2014", "2015"]):
anno = next(y for y in ["2010","2011","2012","2013","2014","2015"] if y in note)
return (
f"SITO DATATO ({anno}): Il sito web è fermo al {anno}. "
"Design obsoleto, probabilmente non ottimizzato per mobile, "
"nessun form di contatto moderno."
)
if "pdf" in note:
return (
"TROVATO IN UN PDF: Questo professionista non ha una presenza web propria. "
"Il nominativo è emerso da un documento PDF. "
"Online è praticamente invisibile a chi cerca oggi."
)
if "nessun modulo" in note or "nessun form" in note:
return (
"SITO SENZA FORM: Il sito esiste ma non ha nessun modulo di contatto. "
"Chi vuole fissare un appuntamento deve cercare il numero e chiamare."
)
if "solo telefono" in note or "solo email" in note:
return (
"CONTATTO SOLO ANALOGICO: L'unico canale è il telefono (o email). "
"Nessun form, nessuna prenotazione online. "
"Chi arriva fuori orario non ha alternativa."
)
if "whatsapp" in note:
return (
"PRENOTAZIONE VIA WHATSAPP: Per prenotare bisogna scrivere un messaggio WhatsApp. "
"Funziona solo quando qualcuno è disponibile a rispondere in tempo reale."
)
if "nessuno" in website or not website.strip():
return (
"NESSUN SITO WEB: Questo professionista non ha un sito. "
"Trovato tramite contatto diretto. Online è invisibile a chi cerca."
)
if "vetrina" in note:
return (
"SITO SOLO VETRINA: Il sito è puramente informativo. "
"Nessun form, nessuna prenotazione. "
"Chi vuole contattarli deve chiamare."
)
if "basic" in note or "base" in note:
return (
"SITO BASE: Esiste un sito ma è essenziale. "
"Nessun sistema di prenotazione, probabilmente solo informazioni e telefono."
)
if "informativo" in note:
return (
"SITO INFORMATIVO: Il sito elenca i servizi ma non ha modo di "
"contattarli o prenotare direttamente online."
)
return f"STATO DIGITALE: {lead.get('note', 'sito base, solo telefono')}."

**Citazione Verbatim dal Sorgente**:
> def _nota_contesto(lead: dict) -> str:
note    = lead.get("note", "").lower()
website = lead.get("website", "").lower()
if "instagram" in note:
return (
"TROVATO SU INSTAGRAM: Il professionista non ha un sito web. "
"Trovato su Instagram cercando nel settore. "
"L'unico modo di contattarli è via DM o telefono."
)
if "facebook" in website and "sito" not in note:
return (
"SOLO PAGINA FACEBOOK: Nessun sito web proprietario. "
"La presenza online è 

**Esempio (Sorgente)**:
> def _nota_contesto(lead: dict) -> str:
note    = lead.get("note", "").lower()
website = lead.get("website", "").lower()
if "instagram" in note:
return (
"TROVATO SU INSTAGRAM: Il professionista non ha

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _nota_contesto(lead: dict) -> str:: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── User prompt per il Writer ──────────────────────────────────────────────── {#a-108}
**Definizione Canonica**: # ─── User prompt per il Writer ────────────────────────────────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── User prompt per il Writer ────────────────────────────────────────────────

**Citazione Verbatim dal Sorgente**:
> # ─── User prompt per il Writer ────────────────────────────────────────────────

**Esempio (Sorgente)**:
> # ─── User prompt per il Writer ────────────────────────────────────────────────

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── User prompt per il Writer ──────────: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### build_prompt(lead: dict) -> str: {#a-109}
**Definizione Canonica**: def build_prompt(lead: dict) -> str:

**Spiegazione Estesa e Contesto Operativo**:
def build_prompt(lead: dict) -> str:
nome    = lead["page_name"]
nicchia = lead.get("nicchia", "professionista")
citta   = lead.get("citta",   "Italia")
website = lead.get("website", "nessuno")
parola  = _match(_PAROLA, nicchia, "clienti")
valore  = _match(_VALORE, nicchia, _DEFAULT_VALORE)
dettaglio = _nota_contesto(lead)
return f"""Scrivi la cold email per questo lead:
Business: {nome}
Settore: {nicchia}
Parola corretta per i loro clienti/pazienti: "{parola}"
Città: {citta}
Sito web: {website}
Booking online: NO
MINI-DETTAGLIO DA USARE NELL'APERTURA E NELL'OGGETTO:
{dettaglio}
INSIGHT SETTORE per la sezione [V] VALORE CONCRETO:
{valore}
ISTRUZIONI SPECIFICHE:
1. [A] ATTENZIONE — prima frase: usa il mini-dettaglio sopra. Come sono stati trovati o lo stato preciso del loro sito. Non iniziare con "Ho cercato {nicchia}" genericamente — entra nel dettaglio specifico di {nome}.
2. OGGETTO — deve riflettere il caso specifico di {nome}. Non generico. Usa il mini-dettaglio.
3. Usa "{parola}" ogni volta che parli dei loro clienti/utenti/pazienti.
4. Adatta il pain point al settore {nicchia} a {citta}.
5. [V] adatta l'insight settore al loro caso specifico (non incollarlo identico).
6. MAI riferimenti a dentisti, studi dentistici, odontoiatria — questo è un {nicchia}.
7. MAI trattini come separatori. MAI "noi/vogliamo/offriamo". Solo "io/ho/mi".
Restituisci SOLO il JSON con oggetto, oggetto_b, oggetto_c e corpo."""

**Citazione Verbatim dal Sorgente**:
> def build_prompt(lead: dict) -> str:
nome    = lead["page_name"]
nicchia = lead.get("nicchia", "professionista")
citta   = lead.get("citta",   "Italia")
website = lead.get("website", "nessuno")
parola  = _match(_PAROLA, nicchia, "clienti")
valore  = _match(_VALORE, nicchia, _DEFAULT_VALORE)
dettaglio = _nota_contesto(lead)
return f"""Scrivi la cold email per questo lead:
Business: {nome}
Settore: {nicchia}
Parola corretta per i loro clienti/pazie

**Esempio (Sorgente)**:
> def build_prompt(lead: dict) -> str:
nome    = lead["page_name"]
nicchia = lead.get("nicchia", "professionista")
citta   = lead.get("citta",   "Italia")
website = lead.get("website", "nessuno")
parola

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per build_prompt(lead: dict) -> str:: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── Helpers ───────────────────────────────────────────────────────────────── {#a-110}
**Definizione Canonica**: # ─── Helpers ─────────────────────────────────────────────────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── Helpers ─────────────────────────────────────────────────────────────────

**Citazione Verbatim dal Sorgente**:
> # ─── Helpers ─────────────────────────────────────────────────────────────────

**Esempio (Sorgente)**:
> # ─── Helpers ─────────────────────────────────────────────────────────────────

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── Helpers ────────────────────────────: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _parse_json(raw: str) -> dict | None: {#a-111}
**Definizione Canonica**: def _parse_json(raw: str) -> dict | None:

**Spiegazione Estesa e Contesto Operativo**:
def _parse_json(raw: str) -> dict | None:
start = raw.find("{")
if start < 0:
return None
try:
result, _ = json.JSONDecoder().raw_decode(raw[start:])
return result
except json.JSONDecodeError:
pass

**Citazione Verbatim dal Sorgente**:
> def _parse_json(raw: str) -> dict | None:
start = raw.find("{")
if start < 0:
return None
try:
result, _ = json.JSONDecoder().raw_decode(raw[start:])
return result
except json.JSONDecodeError:
pass

**Esempio (Sorgente)**:
> def _parse_json(raw: str) -> dict | None:
start = raw.find("{")
if start < 0:
return None
try:
result, _ = json.JSONDecoder().raw_decode(raw[start:])
return result
except json.JSONDecodeError:
pass

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _parse_json(raw: str) -> dict | None:: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Fallback regex {#a-112}
**Definizione Canonica**: # Fallback regex

**Spiegazione Estesa e Contesto Operativo**:
# Fallback regex

**Citazione Verbatim dal Sorgente**:
> # Fallback regex

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Fallback regex: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _rx(text, key): {#a-113}
**Definizione Canonica**: def _rx(text, key):

**Spiegazione Estesa e Contesto Operativo**:
def _rx(text, key):
m = re.search(rf'"{key}"\s*:\s*"((?:[^"\\]|\\.)*)"', text, re.DOTALL)
return m.group(1).replace("\\n", "\n").replace('\\"', '"') if m else ""
r = {k: _rx(raw, k) for k in ("oggetto", "oggetto_b", "oggetto_c", "corpo")}
return r if r.get("oggetto") and r.get("corpo") else None

**Citazione Verbatim dal Sorgente**:
> def _rx(text, key):
m = re.search(rf'"{key}"\s*:\s*"((?:[^"\\]|\\.)*)"', text, re.DOTALL)
return m.group(1).replace("\\n", "\n").replace('\\"', '"') if m else ""
r = {k: _rx(raw, k) for k in ("oggetto", "oggetto_b", "oggetto_c", "corpo")}
return r if r.get("oggetto") and r.get("corpo") else None

**Esempio (Sorgente)**:
> def _rx(text, key):
m = re.search(rf'"{key}"\s*:\s*"((?:[^"\\]|\\.)*)"', text, re.DOTALL)
return m.group(1).replace("\\n", "\n").replace('\\"', '"') if m else ""
r = {k: _rx(raw, k) for k in ("oggetto

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _rx(text, key):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### oad_existing() -> tuple: {#a-114}
**Definizione Canonica**: def load_existing() -> tuple:

**Spiegazione Estesa e Contesto Operativo**:
def load_existing() -> tuple:
"""Returns (all_items_dict, ready_only_dict). Preserves sent/error statuses."""
if not os.path.exists(OUTPUT_PATH):
return {}, {}
with open(OUTPUT_PATH, encoding="utf-8") as f:
all_items = json.load(f)
all_dict   = {r["email"].lower(): r for r in all_items}
ready_dict = {k: v for k, v in all_dict.items() if v.get("status") == "ready"}
return all_dict, ready_dict

**Citazione Verbatim dal Sorgente**:
> def load_existing() -> tuple:
"""Returns (all_items_dict, ready_only_dict). Preserves sent/error statuses."""
if not os.path.exists(OUTPUT_PATH):
return {}, {}
with open(OUTPUT_PATH, encoding="utf-8") as f:
all_items = json.load(f)
all_dict   = {r["email"].lower(): r for r in all_items}
ready_dict = {k: v for k, v in all_dict.items() if v.get("status") == "ready"}
return all_dict, ready_dict

**Esempio (Sorgente)**:
> def load_existing() -> tuple:
"""Returns (all_items_dict, ready_only_dict). Preserves sent/error statuses."""
if not os.path.exists(OUTPUT_PATH):
return {}, {}
with open(OUTPUT_PATH, encoding="utf-8")

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per oad_existing() -> tuple:: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### oad_ready() -> dict: {#a-115}
**Definizione Canonica**: def load_ready() -> dict:

**Spiegazione Estesa e Contesto Operativo**:
def load_ready() -> dict:
_, ready = load_existing()
return ready

**Citazione Verbatim dal Sorgente**:
> def load_ready() -> dict:
_, ready = load_existing()
return ready

**Esempio (Sorgente)**:
> def load_ready() -> dict:
_, ready = load_existing()
return ready

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per oad_ready() -> dict:: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Costruisce rotation e writer (per _sanitize_corpo) {#a-116}
**Definizione Canonica**: # Costruisce rotation e writer (per _sanitize_corpo)

**Spiegazione Estesa e Contesto Operativo**:
# Costruisce rotation e writer (per _sanitize_corpo)
writer   = EmailWriterAgent(os.getenv("OPENROUTER_API_KEY", ""))
rotation = writer.rotation
if not rotation:
print("ERRORE: nessun client AI. Controlla .env (GROQ_API_KEY / OPENROUTER_API_KEY)")
sys.exit(1)

**Citazione Verbatim dal Sorgente**:
> # Costruisce rotation e writer (per _sanitize_corpo)
writer   = EmailWriterAgent(os.getenv("OPENROUTER_API_KEY", ""))
rotation = writer.rotation
if not rotation:
print("ERRORE: nessun client AI. Controlla .env (GROQ_API_KEY / OPENROUTER_API_KEY)")
sys.exit(1)

**Esempio (Sorgente)**:
> # Costruisce rotation e writer (per _sanitize_corpo)
writer   = EmailWriterAgent(os.getenv("OPENROUTER_API_KEY", ""))
rotation = writer.rotation
if not rotation:
print("ERRORE: nessun client AI. Contr

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Costruisce rotation e writer (per _sanit: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Legge CSV — deduplicazione per email {#a-117}
**Definizione Canonica**: # Legge CSV — deduplicazione per email

**Spiegazione Estesa e Contesto Operativo**:
# Legge CSV — deduplicazione per email
leads, seen = [], set()
with open(CSV_PATH, encoding="utf-8") as f:
for row in csv.DictReader(f):
em = row.get("email", "").strip().lower()
if em and "@" in em and em not in seen:
seen.add(em)
leads.append(row)
existing_all, existing_ready = load_existing()
existing  = existing_ready if resume else {}

**Citazione Verbatim dal Sorgente**:
> # Legge CSV — deduplicazione per email
leads, seen = [], set()
with open(CSV_PATH, encoding="utf-8") as f:
for row in csv.DictReader(f):
em = row.get("email", "").strip().lower()
if em and "@" in em and em not in seen:
seen.add(em)
leads.append(row)
existing_all, existing_ready = load_existing()
existing  = existing_ready if resume else {}

**Esempio (Sorgente)**:
> # Legge CSV — deduplicazione per email
leads, seen = [], set()
with open(CSV_PATH, encoding="utf-8") as f:
for row in csv.DictReader(f):
em = row.get("email", "").strip().lower()
if em and "@" in em a

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Legge CSV — deduplicazione per email: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Always preserve sent/failed items; skip leads already done {#a-118}
**Definizione Canonica**: # Always preserve sent/failed items; skip leads already done

**Spiegazione Estesa e Contesto Operativo**:
# Always preserve sent/failed items; skip leads already done
skip_set  = {e for e, r in existing_all.items() if r.get("status") in ("sent", "send_failed")}
da_fare   = [l for l in leads if l["email"].strip().lower() not in existing
and l["email"].strip().lower() not in skip_set]
print(f"\nLead totali   : {len(leads)}")
print(f"Già pronti    : {len(existing)}")
print(f"Già inviati   : {len(skip_set)}")
print(f"Da generare   : {len(da_fare)}")
print(f"Modello       : {rotation[0][1]}\n")

**Citazione Verbatim dal Sorgente**:
> # Always preserve sent/failed items; skip leads already done
skip_set  = {e for e, r in existing_all.items() if r.get("status") in ("sent", "send_failed")}
da_fare   = [l for l in leads if l["email"].strip().lower() not in existing
and l["email"].strip().lower() not in skip_set]
print(f"\nLead totali   : {len(leads)}")
print(f"Già pronti    : {len(existing)}")
print(f"Già inviati   : {len(skip_set)}")
print(f"Da generare   : {len(da_fare)}")
prin

**Esempio (Sorgente)**:
> # Always preserve sent/failed items; skip leads already done
skip_set  = {e for e, r in existing_all.items() if r.get("status") in ("sent", "send_failed")}
da_fare   = [l for l in leads if l["email"].

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Always preserve sent/failed items; skip : applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Preserve ALL existing statuses (sent, ready, error) to avoid overwrites {#a-119}
**Definizione Canonica**: # Preserve ALL existing statuses (sent, ready, error) to avoid overwrites

**Spiegazione Estesa e Contesto Operativo**:
# Preserve ALL existing statuses (sent, ready, error) to avoid overwrites
results = list(existing_all.values())
errors  = 0
for i, lead in enumerate(da_fare, 1):
email_dest = lead["email"].strip()
nome       = lead["page_name"]
print(f"[{i:02d}/{len(da_fare)}] {nome[:50]}")
print(f"         {email_dest}")
messages = [
{"role": "system", "content": WRITER_SYSTEM_PROMPT},
{"role": "user",   "content": build_prompt(lead)},
]
raw = call_ai(rotation, messages, max_tokens=1600, temperature=0.65,
label=f"E{i:02d}")
if not raw:
print(f"         ERRORE: nessuna risposta AI")
errors += 1
results.append({
"email": email_dest, "page_name": nome,
"nicchia": lead.get("nicchia", ""), "oggetto": "",
"corpo": "", "status": "error_no_response",
})
_flush(results)
continue
parsed = _parse_json(raw)
if not parsed:
print(f"         ERRORE: JSON non parsabile")
errors += 1
results.append({
"email": email_dest, "page_name": nome,
"nicchia": lead.get("nicchia", ""), "oggetto": "",
"corpo": "", "status": "error_parse",
})
_flush(results)
continue
corpo   = writer._sanitize_corpo(parsed.get("corpo", ""))
corpo   = _aggiungi_cta(corpo)
oggetto = parsed.get("oggetto", "")
parole  = len(corpo.split()) if corpo else 0
if oggetto and corpo and parole >= 150:
print(f"         OK [{parole}p] {oggetto[:60]}")
results.append({
"email":     email_dest,
"page_name": nome,
"nicchia":   lead.get("nicchia", ""),
"citta":     lead.get("citta", ""),
"oggetto":   oggetto,
"oggetto_b": parsed.get("oggetto_b", ""),
"oggetto_c": parsed.get("oggetto_c", ""),
"corpo":     corpo,
"status":    "ready",
})
else:
print(f"         WARN: troppo corta ({parole}p) o oggetto mancante")
errors += 1
results.append({
"email": email_dest, "page_name": nome,
"nicchia": lead.get("nicchia", ""),
"oggetto": oggetto, "corpo": corpo,
"status": f"error_short_{parole}p",
})
_flush(results)
time.sleep(1.2)
pronte = sum(1 for r in results if r["status"] == "ready")
print(f"\n{'='*55}")
print(f"RISULTATO: {pronte}/{len(leads)} email pronte | {errors} errori")
print(f"Salvato  : {OUTPUT_PATH}")
print(f"{'='*55}")
if pronte > 0:
print(f"\nReviedi   : python review_emails.py")
print(f"Poi invia : python send_ready.py")

**Citazione Verbatim dal Sorgente**:
> # Preserve ALL existing statuses (sent, ready, error) to avoid overwrites
results = list(existing_all.values())
errors  = 0
for i, lead in enumerate(da_fare, 1):
email_dest = lead["email"].strip()
nome       = lead["page_name"]
print(f"[{i:02d}/{len(da_fare)}] {nome[:50]}")
print(f"         {email_dest}")
messages = [
{"role": "system", "content": WRITER_SYSTEM_PROMPT},
{"role": "user",   "content": build_prompt(lead)},
]
raw = call_ai(rotation, 

**Esempio (Sorgente)**:
> # Preserve ALL existing statuses (sent, ready, error) to avoid overwrites
results = list(existing_all.values())
errors  = 0
for i, lead in enumerate(da_fare, 1):
email_dest = lead["email"].strip()
nom

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Preserve ALL existing statuses (sent, re: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _flush(results: list): {#a-120}
**Definizione Canonica**: def _flush(results: list):

**Spiegazione Estesa e Contesto Operativo**:
def _flush(results: list):
with open(OUTPUT_PATH, "w", encoding="utf-8") as f:
json.dump(results, f, ensure_ascii=False, indent=2)
if __name__ == "__main__":
main()

**Citazione Verbatim dal Sorgente**:
> def _flush(results: list):
with open(OUTPUT_PATH, "w", encoding="utf-8") as f:
json.dump(results, f, ensure_ascii=False, indent=2)
if __name__ == "__main__":
main()

**Esempio (Sorgente)**:
> def _flush(results: list):
with open(OUTPUT_PATH, "w", encoding="utf-8") as f:
json.dump(results, f, ensure_ascii=False, indent=2)
if __name__ == "__main__":
main()

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _flush(results: list):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── MULTI-ACCOUNT ROUND-ROBIN ─────────────────────────────────────────────── {#a-123}
**Definizione Canonica**: # ─── MULTI-ACCOUNT ROUND-ROBIN ───────────────────────────────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── MULTI-ACCOUNT ROUND-ROBIN ───────────────────────────────────────────────

**Citazione Verbatim dal Sorgente**:
> # ─── MULTI-ACCOUNT ROUND-ROBIN ───────────────────────────────────────────────

**Esempio (Sorgente)**:
> # ─── MULTI-ACCOUNT ROUND-ROBIN ───────────────────────────────────────────────

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── MULTI-ACCOUNT ROUND-ROBIN ──────────: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Per ogni account Gmail occorre una App Password (NON la password normale). {#a-124}
**Definizione Canonica**: # Per ogni account Gmail occorre una App Password (NON la password normale).

**Spiegazione Estesa e Contesto Operativo**:
# Per ogni account Gmail occorre una App Password (NON la password normale).

**Citazione Verbatim dal Sorgente**:
> # Per ogni account Gmail occorre una App Password (NON la password normale).

**Esempio (Sorgente)**:
> # Per ogni account Gmail occorre una App Password (NON la password normale).

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Per ogni account Gmail occorre una App P: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Come ottenerla: Google Account → Sicurezza → Verifica in due passaggi → App pass {#a-125}
**Definizione Canonica**: # Come ottenerla: Google Account → Sicurezza → Verifica in due passaggi → App password

**Spiegazione Estesa e Contesto Operativo**:
# Come ottenerla: Google Account → Sicurezza → Verifica in due passaggi → App password

**Citazione Verbatim dal Sorgente**:
> # Come ottenerla: Google Account → Sicurezza → Verifica in due passaggi → App password

**Esempio (Sorgente)**:
> # Come ottenerla: Google Account → Sicurezza → Verifica in due passaggi → App password

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Come ottenerla: Google Account → Sicurez: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Crea una App Password con nome "Mail" e copia le 16 lettere (es. "aaaa bbbb cccc {#a-126}
**Definizione Canonica**: # Crea una App Password con nome "Mail" e copia le 16 lettere (es. "aaaa bbbb cccc dddd").

**Spiegazione Estesa e Contesto Operativo**:
# Crea una App Password con nome "Mail" e copia le 16 lettere (es. "aaaa bbbb cccc dddd").

**Citazione Verbatim dal Sorgente**:
> # Crea una App Password con nome "Mail" e copia le 16 lettere (es. "aaaa bbbb cccc dddd").

**Esempio (Sorgente)**:
> # Crea una App Password con nome "Mail" e copia le 16 lettere (es. "aaaa bbbb cccc dddd").

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Crea una App Password con nome "Mail" e : applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Sostituisci ACCOUNT2_EMAIL / ACCOUNT3_EMAIL con le email reali e le relative App {#a-127}
**Definizione Canonica**: # Sostituisci ACCOUNT2_EMAIL / ACCOUNT3_EMAIL con le email reali e le relative App Password.

**Spiegazione Estesa e Contesto Operativo**:
# Sostituisci ACCOUNT2_EMAIL / ACCOUNT3_EMAIL con le email reali e le relative App Password.
GMAIL_ACCOUNTS = [
{"user": "max.infoproducer@gmail.com",  "password": "kkgj pnsh vupw rily"},
{"user": "ACCOUNT2_EMAIL",              "password": "ACCOUNT2_APP_PASSWORD"},
{"user": "ACCOUNT3_EMAIL",              "password": "ACCOUNT3_APP_PASSWORD"},
]

**Citazione Verbatim dal Sorgente**:
> # Sostituisci ACCOUNT2_EMAIL / ACCOUNT3_EMAIL con le email reali e le relative App Password.
GMAIL_ACCOUNTS = [
{"user": "max.infoproducer@gmail.com",  "password": "kkgj pnsh vupw rily"},
{"user": "ACCOUNT2_EMAIL",              "password": "ACCOUNT2_APP_PASSWORD"},
{"user": "ACCOUNT3_EMAIL",              "password": "ACCOUNT3_APP_PASSWORD"},
]

**Esempio (Sorgente)**:
> # Sostituisci ACCOUNT2_EMAIL / ACCOUNT3_EMAIL con le email reali e le relative App Password.
GMAIL_ACCOUNTS = [
{"user": "max.infoproducer@gmail.com",  "password": "kkgj pnsh vupw rily"},
{"user": "AC

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Sostituisci ACCOUNT2_EMAIL / ACCOUNT3_EM: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Solo account con email reale (esclude placeholder) {#a-128}
**Definizione Canonica**: # Solo account con email reale (esclude placeholder)

**Spiegazione Estesa e Contesto Operativo**:
# Solo account con email reale (esclude placeholder)
_ACTIVE_ACCOUNTS = [a for a in GMAIL_ACCOUNTS if "@" in a["user"] and a["user"] != "ACCOUNT2_EMAIL" and a["user"] != "ACCOUNT3_EMAIL"]
INPUT_PATH     = next((sys.argv[sys.argv.index("--input")+1] for i,a in enumerate(sys.argv) if a=="--input"), "emails_ready.json")
DELAY_SECONDS  = 90    # ~40 email/ora — più sicuro per reputazione account

**Citazione Verbatim dal Sorgente**:
> # Solo account con email reale (esclude placeholder)
_ACTIVE_ACCOUNTS = [a for a in GMAIL_ACCOUNTS if "@" in a["user"] and a["user"] != "ACCOUNT2_EMAIL" and a["user"] != "ACCOUNT3_EMAIL"]
INPUT_PATH     = next((sys.argv[sys.argv.index("--input")+1] for i,a in enumerate(sys.argv) if a=="--input"), "emails_ready.json")
DELAY_SECONDS  = 90    # ~40 email/ora — più sicuro per reputazione account

**Esempio (Sorgente)**:
> # Solo account con email reale (esclude placeholder)
_ACTIVE_ACCOUNTS = [a for a in GMAIL_ACCOUNTS if "@" in a["user"] and a["user"] != "ACCOUNT2_EMAIL" and a["user"] != "ACCOUNT3_EMAIL"]
INPUT_PATH  

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Solo account con email reale (esclude pl: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### DELIVERABILITY: limite giornaliero per singola mailbox Gmail. {#a-129}
**Definizione Canonica**: # DELIVERABILITY: limite giornaliero per singola mailbox Gmail.

**Spiegazione Estesa e Contesto Operativo**:
# DELIVERABILITY: limite giornaliero per singola mailbox Gmail.

**Citazione Verbatim dal Sorgente**:
> # DELIVERABILITY: limite giornaliero per singola mailbox Gmail.

**Esempio (Sorgente)**:
> # DELIVERABILITY: limite giornaliero per singola mailbox Gmail.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per DELIVERABILITY: limite giornaliero per s: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Superare 50/giorno brucia la reputazione e causa "Message blocked". {#a-130}
**Definizione Canonica**: # Superare 50/giorno brucia la reputazione e causa "Message blocked".

**Spiegazione Estesa e Contesto Operativo**:
# Superare 50/giorno brucia la reputazione e causa "Message blocked".

**Citazione Verbatim dal Sorgente**:
> # Superare 50/giorno brucia la reputazione e causa "Message blocked".

**Esempio (Sorgente)**:
> # Superare 50/giorno brucia la reputazione e causa "Message blocked".

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Superare 50/giorno brucia la reputazione: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Per volumi maggiori: aggiungere più account Gmail (round-robin orizzontale). {#a-131}
**Definizione Canonica**: # Per volumi maggiori: aggiungere più account Gmail (round-robin orizzontale).

**Spiegazione Estesa e Contesto Operativo**:
# Per volumi maggiori: aggiungere più account Gmail (round-robin orizzontale).
DAILY_LIMIT_PER_ACCOUNT = 50
BLACKLIST = {
"assistenza.pazienti@studiobittante.com",
}

**Citazione Verbatim dal Sorgente**:
> # Per volumi maggiori: aggiungere più account Gmail (round-robin orizzontale).
DAILY_LIMIT_PER_ACCOUNT = 50
BLACKLIST = {
"assistenza.pazienti@studiobittante.com",
}

**Esempio (Sorgente)**:
> # Per volumi maggiori: aggiungere più account Gmail (round-robin orizzontale).
DAILY_LIMIT_PER_ACCOUNT = 50
BLACKLIST = {
"assistenza.pazienti@studiobittante.com",
}

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Per volumi maggiori: aggiungere più acco: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### ─── SMTP ──────────────────────────────────────────────────────────────────── {#a-132}
**Definizione Canonica**: # ─── SMTP ────────────────────────────────────────────────────────────────────

**Spiegazione Estesa e Contesto Operativo**:
# ─── SMTP ────────────────────────────────────────────────────────────────────

**Citazione Verbatim dal Sorgente**:
> # ─── SMTP ────────────────────────────────────────────────────────────────────

**Esempio (Sorgente)**:
> # ─── SMTP ────────────────────────────────────────────────────────────────────

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per ─── SMTP ───────────────────────────────: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _invia(destinatario: str, oggetto: str, corpo: str, account_idx: int = 0) -> boo {#a-133}
**Definizione Canonica**: def _invia(destinatario: str, oggetto: str, corpo: str, account_idx: int = 0) -> bool:

**Spiegazione Estesa e Contesto Operativo**:
def _invia(destinatario: str, oggetto: str, corpo: str, account_idx: int = 0) -> bool:
account  = _ACTIVE_ACCOUNTS[account_idx % len(_ACTIVE_ACCOUNTS)]
user     = account["user"]
password = account["password"]
try:
msg = MIMEMultipart("alternative")
msg["From"]    = f"Max <{user}>"
msg["To"]      = destinatario
msg["Subject"] = oggetto
msg.attach(MIMEText(corpo, "plain", "utf-8"))
with smtplib.SMTP_SSL("smtp.gmail.com", 465, timeout=15) as srv:
srv.login(user, password)
srv.send_message(msg)
return True
except Exception as e:
print(f"         ERRORE SMTP: {e}")
return False

**Citazione Verbatim dal Sorgente**:
> def _invia(destinatario: str, oggetto: str, corpo: str, account_idx: int = 0) -> bool:
account  = _ACTIVE_ACCOUNTS[account_idx % len(_ACTIVE_ACCOUNTS)]
user     = account["user"]
password = account["password"]
try:
msg = MIMEMultipart("alternative")
msg["From"]    = f"Max <{user}>"
msg["To"]      = destinatario
msg["Subject"] = oggetto
msg.attach(MIMEText(corpo, "plain", "utf-8"))
with smtplib.SMTP_SSL("smtp.gmail.com", 465, timeout=15) as srv:
s

**Esempio (Sorgente)**:
> def _invia(destinatario: str, oggetto: str, corpo: str, account_idx: int = 0) -> bool:
account  = _ACTIVE_ACCOUNTS[account_idx % len(_ACTIVE_ACCOUNTS)]
user     = account["user"]
password = account["p

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _invia(destinatario: str, oggetto: str, : applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### _flush(data: list): {#a-134}
**Definizione Canonica**: def _flush(data: list):

**Spiegazione Estesa e Contesto Operativo**:
def _flush(data: list):
with open(INPUT_PATH, "w", encoding="utf-8") as f:
json.dump(data, f, ensure_ascii=False, indent=2)

**Citazione Verbatim dal Sorgente**:
> def _flush(data: list):
with open(INPUT_PATH, "w", encoding="utf-8") as f:
json.dump(data, f, ensure_ascii=False, indent=2)

**Esempio (Sorgente)**:
> def _flush(data: list):
with open(INPUT_PATH, "w", encoding="utf-8") as f:
json.dump(data, f, ensure_ascii=False, indent=2)

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per _flush(data: list):: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Preview prime 3 {#a-135}
**Definizione Canonica**: # Preview prime 3

**Spiegazione Estesa e Contesto Operativo**:
# Preview prime 3
print("ANTEPRIMA prime 3 email:\n")
for e in da_inviare[:3]:
print(f"  [{e['nicchia']}] {e['page_name']}")
print(f"  To     : {e['email']}")
print(f"  Oggetto: {e['oggetto']}")
print()
if dry_run:
print("DRY-RUN: nessuna email inviata. Rimuovi --dry-run per inviare.")
return
if not auto:
risposta = input(f"Inviare {len(da_inviare)} email? [s/N] ").strip().lower()
if risposta not in ("s", "si", "sì", "y", "yes"):
print("Annullato.")
return
else:
print(f"AUTO: invio {len(da_inviare)} email senza conferma interattiva.\n")

**Citazione Verbatim dal Sorgente**:
> # Preview prime 3
print("ANTEPRIMA prime 3 email:\n")
for e in da_inviare[:3]:
print(f"  [{e['nicchia']}] {e['page_name']}")
print(f"  To     : {e['email']}")
print(f"  Oggetto: {e['oggetto']}")
print()
if dry_run:
print("DRY-RUN: nessuna email inviata. Rimuovi --dry-run per inviare.")
return
if not auto:
risposta = input(f"Inviare {len(da_inviare)} email? [s/N] ").strip().lower()
if risposta not in ("s", "si", "sì", "y", "yes"):
print("Annullato

**Esempio (Sorgente)**:
> # Preview prime 3
print("ANTEPRIMA prime 3 email:\n")
for e in da_inviare[:3]:
print(f"  [{e['nicchia']}] {e['page_name']}")
print(f"  To     : {e['email']}")
print(f"  Oggetto: {e['oggetto']}")
print

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Preview prime 3: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Applica limite giornaliero {#a-136}
**Definizione Canonica**: # Applica limite giornaliero

**Spiegazione Estesa e Contesto Operativo**:
# Applica limite giornaliero
if len(da_inviare) > DAILY_LIMIT_PER_ACCOUNT:
print(f"ATTENZIONE: {len(da_inviare)} email pronte, ma limite giornaliero = {DAILY_LIMIT_PER_ACCOUNT}.")
print(f"Invio solo le prime {DAILY_LIMIT_PER_ACCOUNT}. Esegui di nuovo domani per le restanti.\n")
da_inviare = da_inviare[:DAILY_LIMIT_PER_ACCOUNT]
print(f"\nInizio invio ({len(da_inviare)} email, cap {DAILY_LIMIT_PER_ACCOUNT}/giorno)...\n")
inviati = 0
errori  = 0
for i, item in enumerate(da_inviare, 1):
dest    = item["email"].strip()
oggetto = item["oggetto"]
corpo   = item["corpo"]
nome    = item["page_name"]
account_idx = i - 1
account_user = _ACTIVE_ACCOUNTS[account_idx % len(_ACTIVE_ACCOUNTS)]["user"]
print(f"[{i:02d}/{len(da_inviare)}] {nome[:45]}")
print(f"         To     : {dest}")
print(f"         Oggetto: {oggetto[:60]}")
print(f"         From   : {account_user}")
ok = _invia(dest, oggetto, corpo, account_idx=account_idx)
if ok:
inviati += 1
item["status"] = "sent"
print(f"         OK Inviata")
else:
errori += 1
item["status"] = "send_failed"
_flush(all_emails)
if i < len(da_inviare):
print(f"         Attendo {DELAY_SECONDS}s...")
time.sleep(DELAY_SECONDS)
print(f"\n{'='*55}")
print(f"RISULTATO: {inviati} inviate | {errori} errori")
print(f"Log aggiornato: {INPUT_PATH}")
print(f"{'='*55}\n")
if __name__ == "__main__":
main()

**Citazione Verbatim dal Sorgente**:
> # Applica limite giornaliero
if len(da_inviare) > DAILY_LIMIT_PER_ACCOUNT:
print(f"ATTENZIONE: {len(da_inviare)} email pronte, ma limite giornaliero = {DAILY_LIMIT_PER_ACCOUNT}.")
print(f"Invio solo le prime {DAILY_LIMIT_PER_ACCOUNT}. Esegui di nuovo domani per le restanti.\n")
da_inviare = da_inviare[:DAILY_LIMIT_PER_ACCOUNT]
print(f"\nInizio invio ({len(da_inviare)} email, cap {DAILY_LIMIT_PER_ACCOUNT}/giorno)...\n")
inviati = 0
errori  = 0
for

**Esempio (Sorgente)**:
> # Applica limite giornaliero
if len(da_inviare) > DAILY_LIMIT_PER_ACCOUNT:
print(f"ATTENZIONE: {len(da_inviare)} email pronte, ma limite giornaliero = {DAILY_LIMIT_PER_ACCOUNT}.")
print(f"Invio solo l

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Applica limite giornaliero: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Funzione Operativa {#a-140}
**Definizione Canonica**: ## Funzione Operativa

**Spiegazione Estesa e Contesto Operativo**:
## Funzione Operativa
- Estrae e verifica i 9 principi ricorrenti e l'8-step didactic loop dai video di Andrei Pascu.
- Applica la formula 'AP VIDEO SYSTEM' (timeline 12-15 minuti per i video YouTube di Digital Empire).
- Compila il Log Evidenza di `checklist_APSOC.md` durante l'ingestione di Empire Studio per alimentare lo Swipe Bank aziendale.

**Citazione Verbatim dal Sorgente**:
> ## Funzione Operativa
- Estrae e verifica i 9 principi ricorrenti e l'8-step didactic loop dai video di Andrei Pascu.
- Applica la formula 'AP VIDEO SYSTEM' (timeline 12-15 minuti per i video YouTube di Digital Empire).
- Compila il Log Evidenza di `checklist_APSOC.md` durante l'ingestione di Empire Studio per alimentare lo Swipe Bank aziendale.

**Esempio (Sorgente)**:
> ## Funzione Operativa
- Estrae e verifica i 9 principi ricorrenti e l'8-step didactic loop dai video di Andrei Pascu.
- Applica la formula 'AP VIDEO SYSTEM' (timeline 12-15 minuti per i video YouTube 

**Esempio (Sorgente)**:
> ## Funzione Operativa
- Gestisce le chiamate di chiusura quando un concessionario o un cliente high-ticket chiede di parlare a voce.
- Utilizza la tecnica di rottura di schema nei primi 8 secondi (Pat

**Esempio (Sorgente)**:
> ## Funzione Operativa
Trasforma qualsiasi concetto di business o prodotto (Preventa, Manuale Claude Code, servizi agenzia) in copy chirurgico da cecchino.
- Applica la `checklist_APSOC.md` e garantisc

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Funzione Operativa: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Responsabilità e Compiti (Da PLANNING-P7) {#a-142}
**Definizione Canonica**: ## Responsabilità e Compiti (Da PLANNING-P7)

**Spiegazione Estesa e Contesto Operativo**:
## Responsabilità e Compiti (Da PLANNING-P7)
1. **Copywriting APSOC (S1/S2):** Scrittura degli script WhatsApp, email di lancio, cold call e landing page ad alto tasso di conversione.
2. **Sviluppo & Refactoring:** Creazione di script Python, batch e utility tecniche su richiesta di Max o per supportare Gael.
3. **Governo Memoria EOD:** Creazione quotidiana del checkpoint (`memory_manager.py`) e aggiornamento della dashboard serale.
4. **Pattern Mining & Ingestione:** Utilizzo di `Empire Studio Suite` e delle skill (`andrei-pascu-system`, `content-forge`) per estrarre intelligenza dai competitor.

**Citazione Verbatim dal Sorgente**:
> ## Responsabilità e Compiti (Da PLANNING-P7)
1. **Copywriting APSOC (S1/S2):** Scrittura degli script WhatsApp, email di lancio, cold call e landing page ad alto tasso di conversione.
2. **Sviluppo & Refactoring:** Creazione di script Python, batch e utility tecniche su richiesta di Max o per supportare Gael.
3. **Governo Memoria EOD:** Creazione quotidiana del checkpoint (`memory_manager.py`) e aggiornamento della dashboard serale.
4. **Pattern 

**Esempio (Sorgente)**:
> ## Responsabilità e Compiti (Da PLANNING-P7)
1. **Copywriting APSOC (S1/S2):** Scrittura degli script WhatsApp, email di lancio, cold call e landing page ad alto tasso di conversione.
2. **Sviluppo & 

**Esempio (Sorgente)**:
> ## Responsabilità e Compiti (Da PLANNING-P7)
1. **Negoziazione S1 (Concessionari - Preventa):** Invio dei 3 messaggi WhatsApp e gestione delle chiamate (solo se richieste) con i 7 titolari di salone.


**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Responsabilità e Compiti (Da PLANNING-P7: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### Responsabilità e Compiti (Da PLANNING-P7 e STATO-EMPIRE) {#a-147}
**Definizione Canonica**: ## Responsabilità e Compiti (Da PLANNING-P7 e STATO-EMPIRE)

**Spiegazione Estesa e Contesto Operativo**:
## Responsabilità e Compiti (Da PLANNING-P7 e STATO-EMPIRE)
1. **Setup Funnel S2 (Manuale Claude Code):** Configurazione landing, checkout ladder (Stripe -> Gumroad -> PayPal.me) e verifica test pagamento €1.
2. **App Delivery S1 (Preventa):** Clona e configura l'istanza multi-tenant per il nuovo concessionario via comando `/nuovo-concessionario` il giorno stesso della chiusura del contratto.
3. **Pipeline Automazione Content (S3/S4/S5):** Esecuzione batch `carousel-factory` per i 7 caroselli, scheduler Meta API / Buffer e test API Fliki.
4. **Manutenzione Core Engine:** Sviluppo di `app.py`, `build_exe.bat` e gestione del server locale.

**Citazione Verbatim dal Sorgente**:
> ## Responsabilità e Compiti (Da PLANNING-P7 e STATO-EMPIRE)
1. **Setup Funnel S2 (Manuale Claude Code):** Configurazione landing, checkout ladder (Stripe -> Gumroad -> PayPal.me) e verifica test pagamento €1.
2. **App Delivery S1 (Preventa):** Clona e configura l'istanza multi-tenant per il nuovo concessionario via comando `/nuovo-concessionario` il giorno stesso della chiusura del contratto.
3. **Pipeline Automazione Content (S3/S4/S5):** Esecuz

**Esempio (Sorgente)**:
> ## Responsabilità e Compiti (Da PLANNING-P7 e STATO-EMPIRE)
1. **Setup Funnel S2 (Manuale Claude Code):** Configurazione landing, checkout ladder (Stripe -> Gumroad -> PayPal.me) e verifica test pagam

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Responsabilità e Compiti (Da PLANNING-P7: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---

## Funnel Promozionali S1-S6 (Concessionari, YouTube, Mentalità, Rebrand) {#c-002}
*I 6 flussi verticali estivi per l'acquisizione e conversione del traffico.*

### WF-S1-CONCESSIONARI.md {#a-027}
**Definizione Canonica**: # WF-S1 — CONCESSIONARI: Partenza Anticipata Luglio 🥇

**Spiegazione Estesa e Contesto Operativo**:
# WF-S1 — CONCESSIONARI: Partenza Anticipata Luglio 🥇
> Stream: S1 · Window: 21→26/07 · Owner: Max · DoD: **≥1 anticipo incassato**
> Trace: DEC-EST-003 · Gate: Gate-CONTATTI (23/07 h12), Gate-REV (26/07) · Config: workflows.yaml#WF-S1

**Citazione Verbatim dal Sorgente**:
> # WF-S1 — CONCESSIONARI: Partenza Anticipata Luglio 🥇
> Stream: S1 · Window: 21→26/07 · Owner: Max · DoD: **≥1 anticipo incassato**
> Trace: DEC-EST-003 · Gate: Gate-CONTATTI (23/07 h12), Gate-REV (26/07) · Config: workflows.yaml#WF-S1

**Esempio (Sorgente)**:
> # WF-S1 — CONCESSIONARI: Partenza Anticipata Luglio 🥇
> Stream: S1 · Window: 21→26/07 · Owner: Max · DoD: **≥1 anticipo incassato**
> Trace: DEC-EST-003 · Gate: Gate-CONTATTI (23/07 h12), Gate-REV (26

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-S1-CONCESSIONARI.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### 📱 Variante A — Lead Caldi / Già Contattati (I 7 Concessionari in lista) {#a-030}
**Definizione Canonica**: ### 📱 Variante A — Lead Caldi / Già Contattati (I 7 Concessionari in lista)

**Spiegazione Estesa e Contesto Operativo**:
### 📱 Variante A — Lead Caldi / Già Contattati (I 7 Concessionari in lista)
**MSG-1 — Riattivazione + Prova Tangibile (Hook provocatorio/curiosità, h9:30)**
> Ciao [Nome]! Tutto bene in salone? Ti scrivo al volo perché l'app di preventivazione istantanea che ti accennavo è ufficialmente live: [NOVACAR] la usa ogni giorno e ha tagliato i tempi di preventivo da 40 minuti a 120 secondi, con PDF brandizzato firmabile su tablet. Ti giro un esempio reale di preventivo che ha chiuso ieri su una vettura da 24k? *(allegare PDF Novacar)*
**MSG-2 — Offerta "Partenza Anticipata Luglio" (Value Gap + Scarsità Etica, dopo risposta o h18:00)**
> Ottimo. Visto che eri tra i primi interessati, ho riservato per il tuo salone la **Partenza Anticipata Luglio**: setup scontato a **€343** (invece di €490) e canone bloccato a **€149/mese**, con onboarding completo ad agosto quando il salone è tranquillo. Motivo di business: a settembre tu sei GIÀ operativo dal primo giorno di rientro e chiudi mentre gli altri concorrenti stampano ancora foglietti A4 a penna.
**MSG-3 — Domanda Binaria ad Alto Contenimento Obiezioni (Giorno dopo, h10:00)**
> Ti blocco uno dei due slot di configurazione anticipata di luglio rimasti, oppure preferisci partire a settembre con il setup a listino pieno (€490)? (Per noi va benissimo in entrambi i casi, mi serve solo sapere come programmare il team tecnico di Gael 🙂)

**Citazione Verbatim dal Sorgente**:
> ### 📱 Variante A — Lead Caldi / Già Contattati (I 7 Concessionari in lista)
**MSG-1 — Riattivazione + Prova Tangibile (Hook provocatorio/curiosità, h9:30)**
> Ciao [Nome]! Tutto bene in salone? Ti scrivo al volo perché l'app di preventivazione istantanea che ti accennavo è ufficialmente live: [NOVACAR] la usa ogni giorno e ha tagliato i tempi di preventivo da 40 minuti a 120 secondi, con PDF brandizzato firmabile su tablet. Ti giro un esempio rea

**Esempio (Sorgente)**:
> ### 📱 Variante A — Lead Caldi / Già Contattati (I 7 Concessionari in lista)
**MSG-1 — Riattivazione + Prova Tangibile (Hook provocatorio/curiosità, h9:30)**
> Ciao [Nome]! Tutto bene in salone? Ti scr

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 📱 Variante A — Lead Caldi / Già Contatta: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### WF-S2-MANUALE.md {#a-036}
**Definizione Canonica**: # WF-S2 — MANUALE CLAUDE CODE: da asset fermo a prodotto in vendita

**Spiegazione Estesa e Contesto Operativo**:
# WF-S2 — MANUALE CLAUDE CODE: da asset fermo a prodotto in vendita
> Stream: S2 · Window: 21→26/07 · Owner: chief-forge/Gael · Dipende da: **DEC-EST-001** (veto 21/07 h20:00)
> DoD congelata: **landing + checkout (test €1 OK) + 3 email caricate**. Stop. (ADR-EST-005)

**Citazione Verbatim dal Sorgente**:
> # WF-S2 — MANUALE CLAUDE CODE: da asset fermo a prodotto in vendita
> Stream: S2 · Window: 21→26/07 · Owner: chief-forge/Gael · Dipende da: **DEC-EST-001** (veto 21/07 h20:00)
> DoD congelata: **landing + checkout (test €1 OK) + 3 email caricate**. Stop. (ADR-EST-005)

**Esempio (Sorgente)**:
> # WF-S2 — MANUALE CLAUDE CODE: da asset fermo a prodotto in vendita
> Stream: S2 · Window: 21→26/07 · Owner: chief-forge/Gael · Dipende da: **DEC-EST-001** (veto 21/07 h20:00)
> DoD congelata: **landi

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-S2-MANUALE.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### WF-S3-S4-PAGINE-MENTALITA.md {#a-044}
**Definizione Canonica**: # WF-S3/S4 — PAGINE LANCIO + MENTALITA.BRUTALE (riattivazione con funnel)

**Spiegazione Estesa e Contesto Operativo**:
# WF-S3/S4 — PAGINE LANCIO + MENTALITA.BRUTALE (riattivazione con funnel)
> Stream: S3+S4 · Owner: Gael · Motore: **carousel-factory** (wrap ADR-003, brand mentalita-brutale già configurato)
> Regola cardine: ogni pagina riattivata = UN funnel + UNA metrica. Niente "pubblicare per pubblicare".

**Citazione Verbatim dal Sorgente**:
> # WF-S3/S4 — PAGINE LANCIO + MENTALITA.BRUTALE (riattivazione con funnel)
> Stream: S3+S4 · Owner: Gael · Motore: **carousel-factory** (wrap ADR-003, brand mentalita-brutale già configurato)
> Regola cardine: ogni pagina riattivata = UN funnel + UNA metrica. Niente "pubblicare per pubblicare".

**Esempio (Sorgente)**:
> # WF-S3/S4 — PAGINE LANCIO + MENTALITA.BRUTALE (riattivazione con funnel)
> Stream: S3+S4 · Owner: Gael · Motore: **carousel-factory** (wrap ADR-003, brand mentalita-brutale già configurato)
> Regola 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-S3-S4-PAGINE-MENTALITA.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### PARTE B — S4 mentalita.brutale (23→25/07) — HARD RULE {#a-049}
**Definizione Canonica**: ## PARTE B — S4 mentalita.brutale (23→25/07) — HARD RULE

**Spiegazione Estesa e Contesto Operativo**:
## PARTE B — S4 mentalita.brutale (23→25/07) — HARD RULE
> **Condizione Max (vincolante): riparte SOLO se 100% automatica.** Se il gate E2E non passa entro 24/07 h20:00 → STANDBY dichiarato (`error --wf WF-S4`), nessuna pubblicazione manuale.

**Citazione Verbatim dal Sorgente**:
> ## PARTE B — S4 mentalita.brutale (23→25/07) — HARD RULE
> **Condizione Max (vincolante): riparte SOLO se 100% automatica.** Se il gate E2E non passa entro 24/07 h20:00 → STANDBY dichiarato (`error --wf WF-S4`), nessuna pubblicazione manuale.

**Esempio (Sorgente)**:
> ## PARTE B — S4 mentalita.brutale (23→25/07) — HARD RULE
> **Condizione Max (vincolante): riparte SOLO se 100% automatica.** Se il gate E2E non passa entro 24/07 h20:00 → STANDBY dichiarato (`error --

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per PARTE B — S4 mentalita.brutale (23→25/07: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### WF-S5-YOUTUBE.md {#a-054}
**Definizione Canonica**: # WF-S5 — YOUTUBE FLIKI: canale auto-producente (Empire Studio integrato)

**Spiegazione Estesa e Contesto Operativo**:
# WF-S5 — YOUTUBE FLIKI: canale auto-producente (Empire Studio integrato)
> Stream: S5 · Window: 24→25/07 · Owner: YouTube Department · **Scope questa settimana: UN video end-to-end** (P4: niente scaling)
> Metodo: competitor in ALTRA lingua che performano → rifatti in ITALIANO → pubblicati con link Manuale in descrizione.
> Dipendenze: DEC-EST-004 (nicchia, veto 24/07) · Gate-S5 (test chiave Fliki 23/07).

**Citazione Verbatim dal Sorgente**:
> # WF-S5 — YOUTUBE FLIKI: canale auto-producente (Empire Studio integrato)
> Stream: S5 · Window: 24→25/07 · Owner: YouTube Department · **Scope questa settimana: UN video end-to-end** (P4: niente scaling)
> Metodo: competitor in ALTRA lingua che performano → rifatti in ITALIANO → pubblicati con link Manuale in descrizione.
> Dipendenze: DEC-EST-004 (nicchia, veto 24/07) · Gate-S5 (test chiave Fliki 23/07).

**Esempio (Sorgente)**:
> # WF-S5 — YOUTUBE FLIKI: canale auto-producente (Empire Studio integrato)
> Stream: S5 · Window: 24→25/07 · Owner: YouTube Department · **Scope questa settimana: UN video end-to-end** (P4: niente scal

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-S5-YOUTUBE.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### WF-S6-REBRAND-PROMO.md {#a-061}
**Definizione Canonica**: # WF-S6 — REBRAND & PROMO: da PreventivoForge a **PREVENTA**

**Spiegazione Estesa e Contesto Operativo**:
# WF-S6 — REBRAND & PROMO: da PreventivoForge a **PREVENTA**
> Stream: S6 · Window: 22→26/07 · Owner: chief-forge · Dipende da: **DEC-EST-002** (default Preventa, veto 22/07 h12:00)
> Perché è oro: prodotto VIVO (Novacar live), fabbrica clone in ore, kill-switch = riscossione garantita. I 7 di S1 = primi testimonial.

**Citazione Verbatim dal Sorgente**:
> # WF-S6 — REBRAND & PROMO: da PreventivoForge a **PREVENTA**
> Stream: S6 · Window: 22→26/07 · Owner: chief-forge · Dipende da: **DEC-EST-002** (default Preventa, veto 22/07 h12:00)
> Perché è oro: prodotto VIVO (Novacar live), fabbrica clone in ore, kill-switch = riscossione garantita. I 7 di S1 = primi testimonial.

**Esempio (Sorgente)**:
> # WF-S6 — REBRAND & PROMO: da PreventivoForge a **PREVENTA**
> Stream: S6 · Window: 22→26/07 · Owner: chief-forge · Dipende da: **DEC-EST-002** (default Preventa, veto 22/07 h12:00)
> Perché è oro: pr

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per WF-S6-REBRAND-PROMO.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---

## Ruoli & Spec Agenti Operativi (Max, Gael, Claude, Andrei, Closer A8, CRO) {#c-003}
*I perimetri di responsabilità, prompt canonici e regole di handoff degli agenti.*

### 1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default) {#a-003}
**Definizione Canonica**: ## 1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)

**Spiegazione Estesa e Contesto Operativo**:
## 1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)
| ID | Decisione | Default | Veto scade | Blocca |
|----|-----------|---------|------------|--------|
| **DEC-EST-001** | Prezzo Manuale (chiude B-003) | **€67 lancio fino 31/07, poi €97** · lead-magnet = Parte 1 gratis · order-bump template €27 | 21/07 **h20:00** ⚠️ OGGI | S2 S3 S4 |
| **DEC-EST-002** | Nome nuovo prodotto preventivi | **Preventa** (dominio verificato DOPO scelta) | 22/07 h12:00 | S6 |
| **DEC-EST-004** | Nicchia YouTube #1 | **AI/Claude in italiano** (stesso ICP del Manuale → cross-sell) | 24/07 h18:00 | S5 |
| DEC-EST-003 ✅ | Ribasatura calendario 21→26 | ATTIVA (di sistema) | — | tutto |

**Citazione Verbatim dal Sorgente**:
> ## 1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)
| ID | Decisione | Default | Veto scade | Blocca |
|----|-----------|---------|------------|--------|
| **DEC-EST-001** | Prezzo Manuale (chiude B-003) | **€67 lancio fino 31/07, poi €97** · lead-magnet = Parte 1 gratis · order-bump template €27 | 21/07 **h20:00** ⚠️ OGGI | S2 S3 S4 |
| **DEC-EST-002** | Nome nuovo prodotto preventivi | **Preventa** (dominio verificato DO

**Esempio (Sorgente)**:
> ## 1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)
| ID | Decisione | Default | Veto scade | Blocca |
|----|-----------|---------|------------|--------|
| **DEC-EST-001** | Pr

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 1. DECISIONI PRE-CONFEZIONATE (Max: 30 s: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [WF-S1-CONCESSIONARI.md](#a-027)
- `applies_in` -> [📱 Variante A — Lead Caldi / Già Contattati (I 7 Concessionari in lista)](#a-030)
- `applies_in` -> [📧 Variante B — Lead Freddi da Google Maps (Outreach Email/LinkedIn)](#a-031)

---
### 🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00) {#a-005}
**Definizione Canonica**: ### 🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)

**Spiegazione Estesa e Contesto Operativo**:
### 🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)
| Data | Task | Output | Stream |
|------|------|--------|--------|
| **21/07** | ① veto/ok DEC-001 (30s) ② lista 7 lead: nome, stato, canale, ultimo contatto (20') ③ invio WA a lead 1-2 con script | prezzo ATTIVO + lista + 2 contatti | S1 |
| **22/07** | WA a lead 3-4-5 (msg1-2-3) · veto/ok DEC-002 | 5/7 contattati + nome ATTIVO | S1 S6 |
| **23/07** | WA a lead 6-7 + follow-up 1-2 · gestione obiezioni con argomentario | **7/7 contattati (GATE)** | S1 |
| **24/07** | follow-up + prime chiusure · veto/ok DEC-004 | chiusure in corso + nicchia ATTIVA | S1 S5 |
| **25/07** | chiusure: contratto + fattura setup (link pagamento nel kit vendita) · ok promo-kit (10') | **≥1 anticipo incassato** | S1 S6 |
| **26/07** | chiusure residue · RETRO con Claude (30') | numeri veri consegnati | tutti |

**Citazione Verbatim dal Sorgente**:
> ### 🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)
| Data | Task | Output | Stream |
|------|------|--------|--------|
| **21/07** | ① veto/ok DEC-001 (30s) ② lista 7 lead: nome, stato, canale, ultimo contatto (20') ③ invio WA a lead 1-2 con script | prezzo ATTIVO + lista + 2 contatti | S1 |
| **22/07** | WA a lead 3-4-5 (msg1-2-3) · veto/ok DEC-002 | 5/7 contattati + nome ATTIVO | S1 S6 |
| **23/07** | WA a lead 6-7 + follow-u

**Esempio (Sorgente)**:
> ### 🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)
| Data | Task | Output | Stream |
|------|------|--------|--------|
| **21/07** | ① veto/ok DEC-001 (30s) ② lista 7 lead: nome, st

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 🔵 MAX (business — massimo 90 min/giorno,: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `see_also` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `see_also` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `see_also` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### 🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3) {#a-006}
**Definizione Canonica**: ### 🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)

**Spiegazione Estesa e Contesto Operativo**:
### 🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)
| Data | Task | Output |
|------|------|--------|
| **21/07** | chiudere CF-R8 (30') → **AUDIT pagine** `AUDIT-PAGINE-20260721.md` (follower, ultimo post, accessi/2FA, bio/link) → verifica account checkout | audit + checkout ok |
| **22/07** | **FUNNEL S2 LIVE**: landing empire-premium-style + checkout (fallback ladder pronto) + 3 email · bio→funnel su crea.illtuo_impero · test pagamento €1 | **Gate-FUNNEL 🟢** |
| **23/07** | batch 7 caroselli S3 (carousel-factory) + pubblicazione #1 · test chiave Fliki → esito in memoria | S3 riparte · **Gate-S5** deciso |
| **24/07** | **gate E2E mentalita.brutale**: batch→QA auto→scheduler→report. Se passa → ON 25/07; se no → STANDBY dichiarato · promo-kit S6 (landing Preventa) · code-freeze h20:00 | **Gate-S4** deciso · kit pronto |
| **25/07** | **WF-YT**: 1 video end-to-end (Fliki o ladder fallback) + pubblicazione + trace P12 | video live + checkpoint |
| **26/07** | buffer fix · RETRO con metriche vere | CP settimanale |

**Citazione Verbatim dal Sorgente**:
> ### 🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)
| Data | Task | Output |
|------|------|--------|
| **21/07** | chiudere CF-R8 (30') → **AUDIT pagine** `AUDIT-PAGINE-20260721.md` (follower, ultimo post, accessi/2FA, bio/link) → verifica account checkout | audit + checkout ok |
| **22/07** | **FUNNEL S2 LIVE**: landing empire-premium-style + checkout (fallback ladder pronto) + 3 email · bio→funnel su crea.illtuo_impero · t

**Esempio (Sorgente)**:
> ### 🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)
| Data | Task | Output |
|------|------|--------|
| **21/07** | chiudere CF-R8 (30') → **AUDIT pagine** `AUDIT-PAGINE-20260721.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 🟣 GAEL (build — sequenza rigida, diritto: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `see_also` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `see_also` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)
- `applies_in` -> [WF-S1-CONCESSIONARI.md](#a-027)

---
### 🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera) {#a-007}
**Definizione Canonica**: ### 🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)

**Spiegazione Estesa e Contesto Operativo**:
### 🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)
- Script offerta S1 (WA msg1-2-3 + argomentario obiezioni) — WF-S1 · pronto per Max 21/07.
- Copy landing + 3 email S2 (cro-copy-architect APSOC) — appena DEC-001 è ATTIVA.
- Case study Novacar (case-study-forge) — 23/07.
- Dashboard EOD h19:00 + checkpoint memoria — ogni giorno.

**Citazione Verbatim dal Sorgente**:
> ### 🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)
- Script offerta S1 (WA msg1-2-3 + argomentario obiezioni) — WF-S1 · pronto per Max 21/07.
- Copy landing + 3 email S2 (cro-copy-architect APSOC) — appena DEC-001 è ATTIVA.
- Case study Novacar (case-study-forge) — 23/07.
- Dashboard EOD h19:00 + checkpoint memoria — ogni giorno.

**Esempio (Sorgente)**:
> ### 🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)
- Script offerta S1 (WA msg1-2-3 + argomentario obiezioni) — WF-S1 · pronto per Max 21/07.
- Copy landing + 3 email S2 (cro-

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 🤖 CLAUDE (esecuzione diretta, su comando: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `see_also` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `see_also` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `see_also` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `see_also` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)

---
### 3. Argomentario Obiezioni (Ribaltamento con Metodo Andrei Pascu) {#a-033}
**Definizione Canonica**: ## 3. Argomentario Obiezioni (Ribaltamento con Metodo Andrei Pascu)

**Spiegazione Estesa e Contesto Operativo**:
## 3. Argomentario Obiezioni (Ribaltamento con Metodo Andrei Pascu)
| Obiezione | Risposta APSOC / Value Gap |
|---|---|
| *"Ci sentiamo a settembre / A luglio siamo chiusi"* | *"Nessun problema sul sentirci a settembre. Però il bonus setup a -30% scade il 31/07. Se blocchiamo la configurazione oggi, a settembre tu parti vendendo dal primo minuto. Se ci sentiamo a settembre, perdi le prime 2 settimane di vendite calde per installare."* |
| *"Costa troppo (€343 setup + €149/mese)"* | *"Facciamo i conti da imprenditori: qual è il margine medio che hai sulla vendita di un'auto usata o nuova? Almeno €1.000-€2.000. Se Preventa ti salva anche solo UNA singola vendita ogni due mesi perché il cliente firma subito su tablet invece di uscire a pensarci, il software si è ripagato 10 volte. Non è un costo, è l'assicurazione sui tuoi margini."* |
| *"I miei venditori sono abituati a Excel / carta"* | *"Esattamente per questo l'abbiamo disegnata con 3 bottoni giganti su iPad/tablet. Un bambino di 10 anni sa fare un preventivo con Preventa. Se sanno usare WhatsApp, sanno usare Preventa in 2 minuti."* |
| *"Mandami una mail / Devo pensarci"* | *"Certo, ti mando la scheda riassuntiva. Ti tengo lo slot promozionale bloccato fino a venerdì alle 18:00, poi passa al prossimo concessionario in lista. Preferisci che te la mandi su WhatsApp o sulla mail del salone?"* |
---

**Citazione Verbatim dal Sorgente**:
> ## 3. Argomentario Obiezioni (Ribaltamento con Metodo Andrei Pascu)
| Obiezione | Risposta APSOC / Value Gap |
|---|---|
| *"Ci sentiamo a settembre / A luglio siamo chiusi"* | *"Nessun problema sul sentirci a settembre. Però il bonus setup a -30% scade il 31/07. Se blocchiamo la configurazione oggi, a settembre tu parti vendendo dal primo minuto. Se ci sentiamo a settembre, perdi le prime 2 settimane di vendite calde per installare."* |
| *"Cost

**Esempio (Sorgente)**:
> ## 3. Argomentario Obiezioni (Ribaltamento con Metodo Andrei Pascu)
| Obiezione | Risposta APSOC / Value Gap |
|---|---|
| *"Ci sentiamo a settembre / A luglio siamo chiusi"* | *"Nessun problema sul s

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3. Argomentario Obiezioni (Ribaltamento : applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### 3. Setup tecnico (Gael, 22/07) {#a-041}
**Definizione Canonica**: ## 3. Setup tecnico (Gael, 22/07)

**Spiegazione Estesa e Contesto Operativo**:
## 3. Setup tecnico (Gael, 22/07)
1. Landing dal motore site-* esistente (zero rebrand).
2. Checkout ladder finché uno passa il **test €1**.
3. Download: PDF completo dietro pagamento; Parte 1 con email-gate (lista per sequenza).
4. Verifica acquisto end-to-end → `checkpoint --task WF-S2 --note "funnel live"` (Gate-FUNNEL 🟢).

**Citazione Verbatim dal Sorgente**:
> ## 3. Setup tecnico (Gael, 22/07)
1. Landing dal motore site-* esistente (zero rebrand).
2. Checkout ladder finché uno passa il **test €1**.
3. Download: PDF completo dietro pagamento; Parte 1 con email-gate (lista per sequenza).
4. Verifica acquisto end-to-end → `checkpoint --task WF-S2 --note "funnel live"` (Gate-FUNNEL 🟢).

**Esempio (Sorgente)**:
> ## 3. Setup tecnico (Gael, 22/07)
1. Landing dal motore site-* esistente (zero rebrand).
2. Checkout ladder finché uno passa il **test €1**.
3. Download: PDF completo dietro pagamento; Parte 1 con ema

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 3. Setup tecnico (Gael, 22/07): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)
- `prerequisite` -> [2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check](#a-029)

---
### AGENTE-ANDREI-PASCU-MINER.md {#a-139}
**Definizione Canonica**: # AGENTE / RUOLO: ANDREI PASCU PATTERN MINER (Competitor Intelligence)

**Spiegazione Estesa e Contesto Operativo**:
# AGENTE / RUOLO: ANDREI PASCU PATTERN MINER (Competitor Intelligence)
> **Reparto:** 10-MEMORY / 08-INTELLIGENCE · **Asset collegato:** `andrei-pascu-system/`

**Citazione Verbatim dal Sorgente**:
> # AGENTE / RUOLO: ANDREI PASCU PATTERN MINER (Competitor Intelligence)
> **Reparto:** 10-MEMORY / 08-INTELLIGENCE · **Asset collegato:** `andrei-pascu-system/`

**Esempio (Sorgente)**:
> # AGENTE / RUOLO: ANDREI PASCU PATTERN MINER (Competitor Intelligence)
> **Reparto:** 10-MEMORY / 08-INTELLIGENCE · **Asset collegato:** `andrei-pascu-system/`

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per AGENTE-ANDREI-PASCU-MINER.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### AGENTE-CLAUDE.md {#a-141}
**Definizione Canonica**: # AGENTE / RUOLO: CLAUDE (AI Core & Pair Programmer)

**Spiegazione Estesa e Contesto Operativo**:
# AGENTE / RUOLO: CLAUDE (AI Core & Pair Programmer)
> **Ecosistemi:** 07-FORGE / 10-MEMORY · **Focus:** Scrittura chirurgica di codice, copy APSOC, automazione comandi.

**Citazione Verbatim dal Sorgente**:
> # AGENTE / RUOLO: CLAUDE (AI Core & Pair Programmer)
> **Ecosistemi:** 07-FORGE / 10-MEMORY · **Focus:** Scrittura chirurgica di codice, copy APSOC, automazione comandi.

**Esempio (Sorgente)**:
> # AGENTE / RUOLO: CLAUDE (AI Core & Pair Programmer)
> **Ecosistemi:** 07-FORGE / 10-MEMORY · **Focus:** Scrittura chirurgica di codice, copy APSOC, automazione comandi.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per AGENTE-CLAUDE.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### AGENTE-CLOSER-A8.md {#a-144}
**Definizione Canonica**: # AGENTE / RUOLO: CLOSER A8 (Negoziazione & Chiamata a Freddo)

**Spiegazione Estesa e Contesto Operativo**:
# AGENTE / RUOLO: CLOSER A8 (Negoziazione & Chiamata a Freddo)
> **Reparto:** 01-AGENCY (Vendita) · **Strumento:** Script Cold Call 60s & Chiusura Contratti

**Citazione Verbatim dal Sorgente**:
> # AGENTE / RUOLO: CLOSER A8 (Negoziazione & Chiamata a Freddo)
> **Reparto:** 01-AGENCY (Vendita) · **Strumento:** Script Cold Call 60s & Chiusura Contratti

**Esempio (Sorgente)**:
> # AGENTE / RUOLO: CLOSER A8 (Negoziazione & Chiamata a Freddo)
> **Reparto:** 01-AGENCY (Vendita) · **Strumento:** Script Cold Call 60s & Chiusura Contratti

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per AGENTE-CLOSER-A8.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### AGENTE-CRO-COPY-ARCHITECT.md {#a-145}
**Definizione Canonica**: # AGENTE / RUOLO: CRO COPY ARCHITECT (Skill & Guild)

**Spiegazione Estesa e Contesto Operativo**:
# AGENTE / RUOLO: CRO COPY ARCHITECT (Skill & Guild)
> **Reparto:** 04-MARKETING (Copywriting & Funnel) · **Framework:** APSOC (Attention-Problem-Solution-Offer-Close)

**Citazione Verbatim dal Sorgente**:
> # AGENTE / RUOLO: CRO COPY ARCHITECT (Skill & Guild)
> **Reparto:** 04-MARKETING (Copywriting & Funnel) · **Framework:** APSOC (Attention-Problem-Solution-Offer-Close)

**Esempio (Sorgente)**:
> # AGENTE / RUOLO: CRO COPY ARCHITECT (Skill & Guild)
> **Reparto:** 04-MARKETING (Copywriting & Funnel) · **Framework:** APSOC (Attention-Problem-Solution-Offer-Close)

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per AGENTE-CRO-COPY-ARCHITECT.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### AGENTE-GAEL.md {#a-146}
**Definizione Canonica**: # AGENTE / RUOLO: GAEL (Socio & Chief Engine Room)

**Spiegazione Estesa e Contesto Operativo**:
# AGENTE / RUOLO: GAEL (Socio & Chief Engine Room)
> **Ecosistemi:** 06-PLATFORM / 09-OPERATIONS · **Focus:** Costruzione e stabilità del runtime e delle app reali.

**Citazione Verbatim dal Sorgente**:
> # AGENTE / RUOLO: GAEL (Socio & Chief Engine Room)
> **Ecosistemi:** 06-PLATFORM / 09-OPERATIONS · **Focus:** Costruzione e stabilità del runtime e delle app reali.

**Esempio (Sorgente)**:
> # AGENTE / RUOLO: GAEL (Socio & Chief Engine Room)
> **Ecosistemi:** 06-PLATFORM / 09-OPERATIONS · **Focus:** Costruzione e stabilità del runtime e delle app reali.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per AGENTE-GAEL.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---
### AGENTE-MAX.md {#a-148}
**Definizione Canonica**: # AGENTE / RUOLO: MAX (Founder & CEO)

**Spiegazione Estesa e Contesto Operativo**:
# AGENTE / RUOLO: MAX (Founder & CEO)
> **Ecosistemi:** Tutti · **Tempo dedicato:** Max 90 min/giorno (chirurgico, zero dispersione)
> **Focus Estate 2026:** Incasso immediato, negoziazione S1 con i 7 concessionari, estetica UI/UX premium.

**Citazione Verbatim dal Sorgente**:
> # AGENTE / RUOLO: MAX (Founder & CEO)
> **Ecosistemi:** Tutti · **Tempo dedicato:** Max 90 min/giorno (chirurgico, zero dispersione)
> **Focus Estate 2026:** Incasso immediato, negoziazione S1 con i 7 concessionari, estetica UI/UX premium.

**Esempio (Sorgente)**:
> # AGENTE / RUOLO: MAX (Founder & CEO)
> **Ecosistemi:** Tutti · **Tempo dedicato:** Max 90 min/giorno (chirurgico, zero dispersione)
> **Focus Estate 2026:** Incasso immediato, negoziazione S1 con i 7

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per AGENTE-MAX.md: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [7. REGOLE DEFINITIVE v2](#a-012)
- `prerequisite` -> [Regole di orchestrazione](#a-016)
- `prerequisite` -> [4. REGOLE DI CONVIVENZA (con l'ecosistema v4)](#a-024)

---

## Automazioni Eseguibili & Scripting Python/Bash (02-AUTOMAZIONI-E-SCRIPTS) {#c-004}
*Gli script di scraping, invio massivo email/whatsapp e gestione memoria automatizzata.*

### 📧 Variante B — Lead Freddi da Google Maps (Outreach Email/LinkedIn) {#a-031}
**Definizione Canonica**: ### 📧 Variante B — Lead Freddi da Google Maps (Outreach Email/LinkedIn)

**Spiegazione Estesa e Contesto Operativo**:
### 📧 Variante B — Lead Freddi da Google Maps (Outreach Email/LinkedIn)
**Oggetto:** Il preventivo A4 che ti fa perdere clienti in salone ([Nome Salone])
> Ciao [Nome/Titolare], ho visto il vostro salone su Google e il parco auto su [Sito/Autoscout].
> Una domanda secca da collega imprenditore: quante volte un cliente esce dal vostro salone dicendo "ci penso" perché ha in mano un preventivo cartaceo o un foglio Excel confuso e va a confrontare i prezzi altrove?
> Abbiamo sviluppato **Preventa**, l'app su tablet che permette ai tuoi venditori di generare preventivi di lusso firmabili in 2 minuti. Novacar la sta usando e ha aumentato la chiusura in salone del +35%.
> Ti mando un video di 60 secondi che ti mostra come esce il preventivo per una vettura come le vostre?
---

**Citazione Verbatim dal Sorgente**:
> ### 📧 Variante B — Lead Freddi da Google Maps (Outreach Email/LinkedIn)
**Oggetto:** Il preventivo A4 che ti fa perdere clienti in salone ([Nome Salone])
> Ciao [Nome/Titolare], ho visto il vostro salone su Google e il parco auto su [Sito/Autoscout].
> Una domanda secca da collega imprenditore: quante volte un cliente esce dal vostro salone dicendo "ci penso" perché ha in mano un preventivo cartaceo o un foglio Excel confuso e va a confrontare i 

**Esempio (Sorgente)**:
> ### 📧 Variante B — Lead Freddi da Google Maps (Outreach Email/LinkedIn)
**Oggetto:** Il preventivo A4 che ti fa perdere clienti in salone ([Nome Salone])
> Ciao [Nome/Titolare], ho visto il vostro sal

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 📧 Variante B — Lead Freddi da Google Map: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### 4. Sequenza outreach (A2, 3 email) {#a-065}
**Definizione Canonica**: ## 4. Sequenza outreach (A2, 3 email)

**Spiegazione Estesa e Contesto Operativo**:
## 4. Sequenza outreach (A2, 3 email)
- **E1**: problema (preventivi lenti/disordinati per import) + case study PDF allegato.
- **E2** (+2g): demo video 2min + "lo installiamo in un giorno sui tuoi dati".
- **E3** (+4g): offerta identica a S1 (scadenza 31/07) + CTA binaria.

**Citazione Verbatim dal Sorgente**:
> ## 4. Sequenza outreach (A2, 3 email)
- **E1**: problema (preventivi lenti/disordinati per import) + case study PDF allegato.
- **E2** (+2g): demo video 2min + "lo installiamo in un giorno sui tuoi dati".
- **E3** (+4g): offerta identica a S1 (scadenza 31/07) + CTA binaria.

**Esempio (Sorgente)**:
> ## 4. Sequenza outreach (A2, 3 email)
- **E1**: problema (preventivi lenti/disordinati per import) + case study PDF allegato.
- **E2** (+2g): demo video 2min + "lo installiamo in un giorno sui tuoi da

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 4. Sequenza outreach (A2, 3 email): applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### fliki_youtube_test.py {#a-073}
**Definizione Canonica**: # Script di verifica API Fliki e render video di test (WF-S5)

**Spiegazione Estesa e Contesto Operativo**:
# Script di verifica API Fliki e render video di test (WF-S5)
import os

**Citazione Verbatim dal Sorgente**:
> # Script di verifica API Fliki e render video di test (WF-S5)
import os

**Esempio (Sorgente)**:
> # Script di verifica API Fliki e render video di test (WF-S5)
import os

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per fliki_youtube_test.py: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### memory_manager.py {#a-075}
**Definizione Canonica**: #!/usr/bin/env python3

**Spiegazione Estesa e Contesto Operativo**:
#!/usr/bin/env python3
"""
DIGITAL EMPIRE — ESTATE-2026 · Memory Ecosystem Manager
=======================================================
Ecosistema di memoria a due livelli (operativo -> long-term) che salva:
checkpoint, decisioni, piani, brainstorming, architetture, errori, metriche,
retro e ReasoningBank (pattern vincenti). Ogni atomo ha ID P12-tracciabile.
Pattern wrappato da 05-SKILLS/master-build-architecture/scripts/memory_manager.py
(ADR-EST-002: si wrappa, non si riscrive). Esteso con: brainstorms/, errors/,
metrics/, reasoning-bank/, veto-window per le decisioni, hook per ruflo.
Uso:
python memory_manager.py init
python memory_manager.py checkpoint --task WF-S1 --note "script offerta pronto"
python memory_manager.py decision --title "Prezzo Manuale" --status PROPOSTA \
--owner Max --context "B-003 aperto da giugno" --decision "EUR67 lancio / EUR97 listino" \
--veto "2026-07-21 20:00"
python memory_manager.py plan --file 01-PLANNING/PLANNING-P7-MASTER-PLAN.md --level P7 --note "plan of record"
python memory_manager.py brainstorm --title "nome prodotto" --note "shortlist Preventa/Lampo/Quotix"
python memory_manager.py error --wf WF-YT-RENDER --note "Fliki 429 rate limit"
python memory_manager.py metric --name anticipi_chiusi --value 2
python memory_manager.py pattern --title "WA-first chiude senza call" --evidence "3/3 risposte in 2h"
python memory_manager.py retro --note "settimana 21-26/07"
python memory_manager.py perf --agent forge-builder --task WF-S2-landing --result success --ttd 3 --firstpass 0
python memory_manager.py feedback --agent funnel-engineer --ftype TIP --note "test EUR1 prima di dichiarare live" --perf PERF-001
python memory_manager.py search "prezzo"
python memory_manager.py status
"""
import argparse, datetime, json, re, sys
from pathlib import Path
ROOT = Path(__file__).resolve().parent
SUBDIRS = ["checkpoints", "decisions", "plans", "brainstorms", "architectures",
"sessions", "errors", "metrics", "reasoning-bank",
"performances", "feedback"]
INDEX = ROOT / "MEMORY-INDEX.md"
PREFIX = {"checkpoints": "CP", "decisions": "DEC-EST", "plans": "PLAN",
"brainstorms": "BS", "architectures": "ARCH", "sessions": "SES",
"errors": "ERR", "metrics": "MET", "reasoning-bank": "RB",
"performances": "PERF", "feedback": "FB"}

**Citazione Verbatim dal Sorgente**:
> #!/usr/bin/env python3
"""
DIGITAL EMPIRE — ESTATE-2026 · Memory Ecosystem Manager
=======================================================
Ecosistema di memoria a due livelli (operativo -> long-term) che salva:
checkpoint, decisioni, piani, brainstorming, architetture, errori, metriche,
retro e ReasoningBank (pattern vincenti). Ogni atomo ha ID P12-tracciabile.
Pattern wrappato da 05-SKILLS/master-build-architecture/scripts/memory_manager.py
(ADR

**Esempio (Sorgente)**:
> #!/usr/bin/env python3
"""
DIGITAL EMPIRE — ESTATE-2026 · Memory Ecosystem Manager
=======================================================
Ecosistema di memoria a due livelli (operativo -> long-term) 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per memory_manager.py: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### Lasciata per compatibilità con gli script che la chiamano. {#a-102}
**Definizione Canonica**: # Lasciata per compatibilità con gli script che la chiamano.

**Spiegazione Estesa e Contesto Operativo**:
# Lasciata per compatibilità con gli script che la chiamano.
return corpo

**Citazione Verbatim dal Sorgente**:
> # Lasciata per compatibilità con gli script che la chiamano.
return corpo

**Esempio (Sorgente)**:
> # Lasciata per compatibilità con gli script che la chiamano.
return corpo

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Lasciata per compatibilità con gli scrip: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### run_checkpoint_eod.bat {#a-121}
**Definizione Canonica**: @echo off

**Spiegazione Estesa e Contesto Operativo**:
@echo off
echo ==============================================
echo ESECUZIONE CHECKPOINT GIORNALIERO EOD (h19:00)
echo ==============================================
python memory_manager.py checkpoint --task WORKFLOW-ESTATE --note "Aggiornamento serale metriche e gate"
pause

**Citazione Verbatim dal Sorgente**:
> @echo off
echo ==============================================
echo ESECUZIONE CHECKPOINT GIORNALIERO EOD (h19:00)
echo ==============================================
python memory_manager.py checkpoint --task WORKFLOW-ESTATE --note "Aggiornamento serale metriche e gate"
pause

**Esempio (Sorgente)**:
> @echo off
echo ==============================================
echo ESECUZIONE CHECKPOINT GIORNALIERO EOD (h19:00)
echo ==============================================
python memory_manager.py checkpoin

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per run_checkpoint_eod.bat: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### send_outreach_ready.py {#a-122}
**Definizione Canonica**: """

**Spiegazione Estesa e Contesto Operativo**:
"""
Invia le email da emails_ready.json.
Legge il file, mostra un riepilogo, chiede conferma, poi invia una per una.
Aggiorna lo status in tempo reale dopo ogni invio.
Run:
python send_ready.py
python send_ready.py --dry-run   # mostra lista senza inviare
"""
import json
import smtplib
import sys
import time
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText

**Citazione Verbatim dal Sorgente**:
> """
Invia le email da emails_ready.json.
Legge il file, mostra un riepilogo, chiede conferma, poi invia una per una.
Aggiorna lo status in tempo reale dopo ogni invio.
Run:
python send_ready.py
python send_ready.py --dry-run   # mostra lista senza inviare
"""
import json
import smtplib
import sys
import time
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText

**Esempio (Sorgente)**:
> """
Invia le email da emails_ready.json.
Legge il file, mostra un riepilogo, chiede conferma, poi invia una per una.
Aggiorna lo status in tempo reale dopo ogni invio.
Run:
python send_ready.py
python

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per send_outreach_ready.py: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---
### send_s1_whatsapp_auto.py {#a-137}
**Definizione Canonica**: # Script Python per verifica APSOC e invio guidato messaggi WhatsApp (WF-S1)

**Spiegazione Estesa e Contesto Operativo**:
# Script Python per verifica APSOC e invio guidato messaggi WhatsApp (WF-S1)
import os, sys

**Citazione Verbatim dal Sorgente**:
> # Script Python per verifica APSOC e invio guidato messaggi WhatsApp (WF-S1)
import os, sys

**Esempio (Sorgente)**:
> # Script Python per verifica APSOC e invio guidato messaggi WhatsApp (WF-S1)
import os, sys

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per send_s1_whatsapp_auto.py: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `applies_in` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `applies_in` -> [🔵 MAX (business — massimo 90 min/giorno, finestre h9:30 e h18:00)](#a-005)
- `applies_in` -> [🟣 GAEL (build — sequenza rigida, diritto di slittamento S5→S4→S6→S3)](#a-006)
- `applies_in` -> [🤖 CLAUDE (esecuzione diretta, su comando — batch copy UNICO 21/07 sera)](#a-007)

---

## Metodologie di Persuasione & Regole di Mandato (APSOC, CPB, Articolo 8) {#c-005}
*I framework di copy persuasivo, validazione APSOC >= 92% e auto-contenimento del monorepo.*

### 7. REGOLE DEFINITIVE v2 {#a-012}
**Definizione Canonica**: ## 7. REGOLE DEFINITIVE v2

**Spiegazione Estesa e Contesto Operativo**:
## 7. REGOLE DEFINITIVE v2
1. **Revenue prima di infra** — i conflitti si risolvono per €/h (P4).
2. **DEC-001 chiusa OGGI** — per decisione o per default: il blocco n.1 non sopravvive alle h20:00.
3. **ADR-003 ovunque**: motori attivi si wrappano, mai riscrivere.
4. **Chiave Fliki solo in `.env`** locale, gitignorata. Mai nei prompt.
5. **Nessuna pagina riattivata senza funnel + metrica**.
6. **Un solo swarm Opus alla volta** — coda in `workflows.yaml`.
7. **Task chiuso → checkpoint in memoria**. Venerdì h20:00 code-freeze. Domenica RETRO con numeri veri.
8. **Solo date assolute** (mai "G1/G2": il 21/07 è martedì e le etichette relative si rompono — A-02).
9. **Vendibile > perfetto**: ogni deliverable ha DoD congelata; superarla richiede una decisione registrata.

**Citazione Verbatim dal Sorgente**:
> ## 7. REGOLE DEFINITIVE v2
1. **Revenue prima di infra** — i conflitti si risolvono per €/h (P4).
2. **DEC-001 chiusa OGGI** — per decisione o per default: il blocco n.1 non sopravvive alle h20:00.
3. **ADR-003 ovunque**: motori attivi si wrappano, mai riscrivere.
4. **Chiave Fliki solo in `.env`** locale, gitignorata. Mai nei prompt.
5. **Nessuna pagina riattivata senza funnel + metrica**.
6. **Un solo swarm Opus alla volta** — coda in `workflow

**Esempio (Sorgente)**:
> ## 7. REGOLE DEFINITIVE v2
1. **Revenue prima di infra** — i conflitti si risolvono per €/h (P4).
2. **DEC-001 chiusa OGGI** — per decisione o per default: il blocco n.1 non sopravvive alle h20:00.
3.

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 7. REGOLE DEFINITIVE v2: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [PLANNING-P7-MASTER-PLAN.md](#a-001)
- `prerequisite` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `prerequisite` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `prerequisite` -> [2. CALENDARIO ESECUTIVO 21→26 LUGLIO](#a-004)

---
### Regole di orchestrazione {#a-016}
**Definizione Canonica**: ## Regole di orchestrazione

**Spiegazione Estesa e Contesto Operativo**:
## Regole di orchestrazione
1. **Revenue-first**: contesa risorse → vince il task con €/h più alto (P4 tabella).
2. **Coda swarm**: max 1 pesante (Opus) → ordine S1 > S2 > S6 > S5; il resto degradato a esecuzione singola.
3. **Gate discipline**: al deadline il gate si marca 🟢/🔴 e si applica il kill-criterio (P5 §2). Nessun gate "quasi verde".
4. **Default-decision enforcement**: veto scaduto → la decisione passa ad ATTIVA → si nota in dashboard e si procede.
5. **Zero-surprise memory**: qualsiasi cosa chiuda/cambi/fallisca → atomo di memoria corrispondente.

**Citazione Verbatim dal Sorgente**:
> ## Regole di orchestrazione
1. **Revenue-first**: contesa risorse → vince il task con €/h più alto (P4 tabella).
2. **Coda swarm**: max 1 pesante (Opus) → ordine S1 > S2 > S6 > S5; il resto degradato a esecuzione singola.
3. **Gate discipline**: al deadline il gate si marca 🟢/🔴 e si applica il kill-criterio (P5 §2). Nessun gate "quasi verde".
4. **Default-decision enforcement**: veto scaduto → la decisione passa ad ATTIVA → si nota in dashboard e

**Esempio (Sorgente)**:
> ## Regole di orchestrazione
1. **Revenue-first**: contesa risorse → vince il task con €/h più alto (P4 tabella).
2. **Coda swarm**: max 1 pesante (Opus) → ordine S1 > S2 > S6 > S5; il resto degradato 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Regole di orchestrazione: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [PLANNING-P7-MASTER-PLAN.md](#a-001)
- `prerequisite` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `prerequisite` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `prerequisite` -> [2. CALENDARIO ESECUTIVO 21→26 LUGLIO](#a-004)

---
### 4. REGOLE DI CONVIVENZA (con l'ecosistema v4) {#a-024}
**Definizione Canonica**: ## 4. REGOLE DI CONVIVENZA (con l'ecosistema v4)

**Spiegazione Estesa e Contesto Operativo**:
## 4. REGOLE DI CONVIVENZA (con l'ecosistema v4)
1. **P-LOOP non è un verificatore**: non blocca nulla, non ha gate. Analizza DOPO, non giudica PRIMA. (Casta distinta dai verificatori — che approvano l'output; il loop migliora l'attore.)
2. **Budget rumore**: capture=T1 fuori quota (file write); TIP/RULE-NOTE = P3 (vanno nel digest EOD); MUTATION-PROP = P2 (4h hold). Max riceve la sintesi del loop SOLO nel digest e al COUNCIL.
3. **Niente surveillance-state**: i punteggi 5D servono a migliorare il SISTEMA (prompt, regole, assegnazioni), non a punire. Feed negativi → causano mutation del PROMPT, mai "colpa" (coerente con failure-modes come first-class).
4. **Chiusura garantita**: se un FB resta >5 task senza conferma/ricorrenza → TRUTH-CMD lo riesuma in EOD-SYNC (task marcio).
5. **ReasoningBank è il deposito finale**: solo i pattern `confirmed` entrano come ufficiali e vengono precaricati dal pre_task hook nei task futuri della stessa famiglia. È qui che il miglioramento diventa memoria operativa.

**Citazione Verbatim dal Sorgente**:
> ## 4. REGOLE DI CONVIVENZA (con l'ecosistema v4)
1. **P-LOOP non è un verificatore**: non blocca nulla, non ha gate. Analizza DOPO, non giudica PRIMA. (Casta distinta dai verificatori — che approvano l'output; il loop migliora l'attore.)
2. **Budget rumore**: capture=T1 fuori quota (file write); TIP/RULE-NOTE = P3 (vanno nel digest EOD); MUTATION-PROP = P2 (4h hold). Max riceve la sintesi del loop SOLO nel digest e al COUNCIL.
3. **Niente surveil

**Esempio (Sorgente)**:
> ## 4. REGOLE DI CONVIVENZA (con l'ecosistema v4)
1. **P-LOOP non è un verificatore**: non blocca nulla, non ha gate. Analizza DOPO, non giudica PRIMA. (Casta distinta dai verificatori — che approvano 

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 4. REGOLE DI CONVIVENZA (con l'ecosistem: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [PLANNING-P7-MASTER-PLAN.md](#a-001)
- `prerequisite` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `prerequisite` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `prerequisite` -> [2. CALENDARIO ESECUTIVO 21→26 LUGLIO](#a-004)

---
### 2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Check {#a-029}
**Definizione Canonica**: ## 2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Checklist ≥92%)

**Spiegazione Estesa e Contesto Operativo**:
## 2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Checklist ≥92%)
> **Regola APSOC (da Sistema Andrei Pascu importato 22/07):** Emozione prima, logica dopo. Valore percepito vs Costo. Ogni messaggio deve passare il gate della `checklist_APSOC.md` prima dell'invio.

**Citazione Verbatim dal Sorgente**:
> ## 2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Checklist ≥92%)
> **Regola APSOC (da Sistema Andrei Pascu importato 22/07):** Emozione prima, logica dopo. Valore percepito vs Costo. Ogni messaggio deve passare il gate della `checklist_APSOC.md` prima dell'invio.

**Esempio (Sorgente)**:
> ## 2. Script WhatsApp in 3 messaggi + Email (Chiusura Asincrona APSOC — Score Checklist ≥92%)
> **Regola APSOC (da Sistema Andrei Pascu importato 22/07):** Emozione prima, logica dopo. Valore percepit

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2. Script WhatsApp in 3 messaggi + Email: applicazione immediata nel workflow aziendale Empire.

**Connessioni & Cross-Reference (P8)**:
- `prerequisite` -> [PLANNING-P7-MASTER-PLAN.md](#a-001)
- `prerequisite` -> [0. VERITÀ AGGIORNATA (post-audit P1)](#a-002)
- `prerequisite` -> [1. DECISIONI PRE-CONFEZIONATE (Max: 30 secondi l'una, o vale il default)](#a-003)
- `prerequisite` -> [2. CALENDARIO ESECUTIVO 21→26 LUGLIO](#a-004)

---
### 2-bis. Script Chiamata a Freddo (Cold Call — Formula Cecchino APSOC, 60 secondi) {#a-032}
**Definizione Canonica**: ## 2-bis. Script Chiamata a Freddo (Cold Call — Formula Cecchino APSOC, 60 secondi)

**Spiegazione Estesa e Contesto Operativo**:
## 2-bis. Script Chiamata a Freddo (Cold Call — Formula Cecchino APSOC, 60 secondi)
> **Quando usare:** Su richiesta del lead, oppure se su WhatsApp non risponde al MSG-1 entro 48h.
> **Tono:** Fratello maggiore / collega sul campo, zero venditore aggressivo, zero scuse ("disturbo?").
**[0:00 - 0:08] A — ATTENTION (Pattern Interrupt & Rottura di Schema)**
> *"Buongiorno [Nome], sono Max di Digital Empire. Ti chiamo al volo senza girarci intorno: non ti voglio vendere auto, ti chiamo perché ho visto le recensioni del vostro salone e voglio farti una domanda secca di 10 secondi sul vostro processo di preventivazione. Hai 10 secondi reali o ti richiamo tra un'ora?"*
**[0:08 - 0:25] P — PROBLEM (Pain Agitation chirurgico sul dolore del venditore)**
> *"Perfetto. Oggi nei saloni multimarca il 40% dei potenziali acquirenti esce dicendo 'ci penso' perché il venditore ci mette mezz'ora a calcolare rate, permuta e finanziamento su Excel o su carta, e il cliente va a comprare dal concorrente che gli dà una risposta istantanea. Succede anche da voi quando c'è ressa il sabato mattina?"*
**[0:25 - 0:45] S + O — SOLUTION & VALUE GAP ANCHORING**
> *"Esatto. Per questo abbiamo creato Preventa: un'app per tablet che fa il preventivo PDF di lusso brandizzato [Nome Salone] in esattamente 120 secondi. Novacar la sta usando da questo mese. La cosa bella è che stiamo facendo la 'Partenza Anticipata Luglio': installiamo ora con setup scontato a €343 invece di €490, così a settembre il tuo team è già operativo al 100% senza perdere giorni di installazione durante il rientro."*
**[0:45 - 0:60] C — CLOSE (Micro-impegno e CTA singola a basso attrito)**
> *"Non ti chiedo di decidere ora al telefono su due piedi. Su che numero WhatsApp ti posso mandare il video di 60 secondi che fa vedere come esce il preventivo su tablet per una BMW o un'Audi del vostro salone? Te lo giro, lo guardi con calma e se ne vale la pena ci sentiamo giovedì 10 minuti."*
---

**Citazione Verbatim dal Sorgente**:
> ## 2-bis. Script Chiamata a Freddo (Cold Call — Formula Cecchino APSOC, 60 secondi)
> **Quando usare:** Su richiesta del lead, oppure se su WhatsApp non risponde al MSG-1 entro 48h.
> **Tono:** Fratello maggiore / collega sul campo, zero venditore aggressivo, zero scuse ("disturbo?").
**[0:00 - 0:08] A — ATTENTION (Pattern Interrupt & Rottura di Schema)**
> *"Buongiorno [Nome], sono Max di Digital Empire. Ti chiamo al volo senza girarci intorno: 

**Esempio (Sorgente)**:
> ## 2-bis. Script Chiamata a Freddo (Cold Call — Formula Cecchino APSOC, 60 secondi)
> **Quando usare:** Su richiesta del lead, oppure se su WhatsApp non risponde al MSG-1 entro 48h.
> **Tono:** Fratel

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 2-bis. Script Chiamata a Freddo (Cold Ca: applicazione immediata nel workflow aziendale Empire.

---
### Landing — struttura (copy: cro-copy-architect APSOC, 21/07 sera) {#a-039}
**Definizione Canonica**: ### Landing — struttura (copy: cro-copy-architect APSOC, 21/07 sera)

**Spiegazione Estesa e Contesto Operativo**:
### Landing — struttura (copy: cro-copy-architect APSOC, 21/07 sera)
1. **H1**: "203 pagine per comandare Claude Code come un professionista." + sub: "Da zero a workflow autonomi: il manuale operativo in italiano."
2. Prova: indice dei contenuti + 3 screenshot interni + Parte 1 gratis.
3. Offerta: €67 barrato €97 · "prezzo di lancio fino al 31/07" · bump template.
4. CTA unica: checkout. Niente menu, niente distrazioni (DoD "vendibile").

**Citazione Verbatim dal Sorgente**:
> ### Landing — struttura (copy: cro-copy-architect APSOC, 21/07 sera)
1. **H1**: "203 pagine per comandare Claude Code come un professionista." + sub: "Da zero a workflow autonomi: il manuale operativo in italiano."
2. Prova: indice dei contenuti + 3 screenshot interni + Parte 1 gratis.
3. Offerta: €67 barrato €97 · "prezzo di lancio fino al 31/07" · bump template.
4. CTA unica: checkout. Niente menu, niente distrazioni (DoD "vendibile").

**Esempio (Sorgente)**:
> ### Landing — struttura (copy: cro-copy-architect APSOC, 21/07 sera)
1. **H1**: "203 pagine per comandare Claude Code come un professionista." + sub: "Da zero a workflow autonomi: il manuale operativo

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Landing — struttura (copy: cro-copy-arch: applicazione immediata nel workflow aziendale Empire.

---
### 6. Regole non negoziabili {#a-060}
**Definizione Canonica**: ## 6. Regole non negoziabili

**Spiegazione Estesa e Contesto Operativo**:
## 6. Regole non negoziabili
chiave solo `.env` · zero stub (validator 0 violazioni) · memory-first dopo OGNI run · tracciabilità P12 su ogni atomo · revenue-before-perfection (ogni video porta al Manuale) · 1 video basta questa settimana.
---
⛓️ P12: `WF-S5#estate-2026` · trace: DEC-EST-004, F-05, R-05, R-07 · gates: Gate-S5 23/07, checkpoint stage 9

**Citazione Verbatim dal Sorgente**:
> ## 6. Regole non negoziabili
chiave solo `.env` · zero stub (validator 0 violazioni) · memory-first dopo OGNI run · tracciabilità P12 su ogni atomo · revenue-before-perfection (ogni video porta al Manuale) · 1 video basta questa settimana.
---
⛓️ P12: `WF-S5#estate-2026` · trace: DEC-EST-004, F-05, R-05, R-07 · gates: Gate-S5 23/07, checkpoint stage 9

**Esempio (Sorgente)**:
> ## 6. Regole non negoziabili
chiave solo `.env` · zero stub (validator 0 violazioni) · memory-first dopo OGNI run · tracciabilità P12 su ogni atomo · revenue-before-perfection (ogni video porta al Man

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per 6. Regole non negoziabili: applicazione immediata nel workflow aziendale Empire.

---
### verify_apsoc(text): {#a-138}
**Definizione Canonica**: def verify_apsoc(text):

**Spiegazione Estesa e Contesto Operativo**:
def verify_apsoc(text):
checks = {
'A (Attention)': '?' in text or '!' in text or 'NOVACAR' in text or 'preventivo' in text.lower(),
'P (Problem)': '40 minuti' in text or 'A4' in text or 'attesa' in text.lower() or 'concorren' in text.lower(),
'S (Solution)': '120 secondi' in text or 'PDF' in text or 'tablet' in text.lower() or 'Preventa' in text,
'O (Offer)': '343' in text or '149' in text or 'Partenza Anticipata' in text or 'scontato' in text.lower(),
'C (Close)': 'giugno' in text.lower() or 'luglio' in text.lower() or 'settembre' in text.lower() or 'preferisci' in text.lower() or 'slot' in text.lower()
}
passed = [k for k, v in checks.items() if v]
print('=== DIAGNOSTICA CHECKLIST APSOC (Andrei Pascu) ===')
for k, v in checks.items():
print(f'[{chr(10003) if v else "X"}] {k}')
print(f'Punteggio indicativo: {len(passed)}/5 ({len(passed)*20}%)')
if len(passed) >= 4:
print('ESITO: GO (Messaggio conforme al protocollo)')
else:
print('ESITO: NO-GO (Arricchire con leva dolore/valore)')
if __name__ == '__main__':
msg = """Ciao! Ti scrivo perché l'app preventivi è live: NOVACAR la usa e ha tagliato i tempi da 40 minuti a 120 secondi con PDF brandizzato su tablet. Ti riservo la Partenza Anticipata Luglio: setup €343 invece di €490 e canone €149/mese, così a settembre sei già operativo e non perdi clienti con fogli A4. Ti blocco uno dei due slot rimasti o preferisci partire a settembre a listino pieno?"""
verify_apsoc(msg)

**Citazione Verbatim dal Sorgente**:
> def verify_apsoc(text):
checks = {
'A (Attention)': '?' in text or '!' in text or 'NOVACAR' in text or 'preventivo' in text.lower(),
'P (Problem)': '40 minuti' in text or 'A4' in text or 'attesa' in text.lower() or 'concorren' in text.lower(),
'S (Solution)': '120 secondi' in text or 'PDF' in text or 'tablet' in text.lower() or 'Preventa' in text,
'O (Offer)': '343' in text or '149' in text or 'Partenza Anticipata' in text or 'scontato' in text.l

**Esempio (Sorgente)**:
> def verify_apsoc(text):
checks = {
'A (Attention)': '?' in text or '!' in text or 'NOVACAR' in text or 'preventivo' in text.lower(),
'P (Problem)': '40 minuti' in text or 'A4' in text or 'attesa' in t

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per verify_apsoc(text):: applicazione immediata nel workflow aziendale Empire.

---
### Regole di Operatività {#a-143}
**Definizione Canonica**: ## Regole di Operatività

**Spiegazione Estesa e Contesto Operativo**:
## Regole di Operatività
- Rispettare rigorosamente la **Regola Art.8 del Mandato:** ogni workflow creato o citato deve avere una cartella reale, tangibile e autocontenuta con tutti i flussi, Python, MD, Agenti e Skill.
- Mai inventare numeri o claim senza prova CPB.

**Citazione Verbatim dal Sorgente**:
> ## Regole di Operatività
- Rispettare rigorosamente la **Regola Art.8 del Mandato:** ogni workflow creato o citato deve avere una cartella reale, tangibile e autocontenuta con tutti i flussi, Python, MD, Agenti e Skill.
- Mai inventare numeri o claim senza prova CPB.

**Esempio (Sorgente)**:
> ## Regole di Operatività
- Rispettare rigorosamente la **Regola Art.8 del Mandato:** ogni workflow creato o citato deve avere una cartella reale, tangibile e autocontenuta con tutti i flussi, Python, 

**Esempio (Sorgente)**:
> ## Regole di Operatività
- Non modifica il copy o il design estetico UI/UX (di competenza Max/Claude).
- Esegue sequenze di build rigide rispettando la Definition of Done congelata (ADR-EST-005).

**Esempio (Sorgente)**:
> ## Regole di Operatività
- Non scrive codice di backend o infrastruttura runtime (delega a Gael e Claude).
- Non perde tempo su metriche di vanity (visualizzazioni vuote): conta solo l'incasso e i pre

**➕ Esempio Operativo Aggiuntivo (`expansion principle`)**:
➕ Esempio operativo per Regole di Operatività: applicazione immediata nel workflow aziendale Empire.

---