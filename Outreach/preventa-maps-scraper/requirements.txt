# Preventa Maps Scraper - Playwright ONLY + Areus
# Install base:
# pip install -r requirements.txt
# playwright install chromium

playwright>=1.44.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
python-dotenv>=1.0.0

# Push su Areus (CRM EmpireDesk): nessuna dipendenza esterna, e' solo un file JSON locale.
