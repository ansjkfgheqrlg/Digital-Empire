### Scena 22 · `frame-0062.png` · 6:06
Canvas Excalidraw (excalidraw.com) zoomato sulla parte destra del diagramma "Cos'è un Agente AI": si vede il box "Strumenti" con lista Google (icona lente), WolframAlpha (icona stella), Python (icona logo Python), API Meteo (icona nuvola); a sinistra si intravede il bordo del box viola "Agente AI" (testo tagliato "gente AI") con una freccia sopra e il cerchietto arancione "IP". Il pannello sinistro di Excalidraw mostra i controlli Stroke/Background/Stroke width/Opacity/Layers, colore stroke selezionato arancione. Nell'area vuota a destra è appena visibile il tratto iniziale di un disegno nuovo (piccola "V" arancione intorno a x=700,y=375) — l'autore sta per iniziare un nuovo schizzo. Webcam del relatore in basso a sinistra.
Voce: *"l'agente AI viene direzionato dal vostro prompt. Che cosa vuol dire? Vuol dire che se io sono l'utente e ho come obiettivo il fatto di arrivare qui"* — introduce il concetto di direzionamento del prompt verso un obiettivo, prima di disegnare lo schema.

### Scena 23 · `frame-0067.png` · 6:36
Sullo stesso canvas, a destra del box Strumenti, è ora disegnato uno schema completo a mano libera: un omino stilizzato arancione (bastoncino con testa tonda) a sinistra da cui partono due coni di traiettorie che convergono/divergono verso una bandierina arancione a destra (obiettivo): un cono STRETTO in blu (linea diritta + linea ondulata sotto) che punta dritto alla bandiera, e un cono LARGO in rosso che si apre molto e supera/manca il bersaglio (le due linee rosse divergono ben oltre la bandiera). Vicino alla bandiera è disegnata una piccola "X" blu su un triangolino arancione.
Voce: *"se il mio prompt è direzionato nel modo corretto, ok? avrò una possibile traiettoria di questo tipo [...] il mio prompt dirà lei hei, vai in questa direzione fino a che poi non colpisci l'obiettivo. Se invece il mio agente è direzionato male [...] abbiamo la possibilità che il prompt manchi il nostro obiettivo e poi continui all'infinito"* — il cono blu stretto = prompt ben direzionato che colpisce l'obiettivo, il cono rosso largo = prompt mal direzionato che manca l'obiettivo e itera all'infinito.

### Scena 24 · `frame-0072.png` · 7:06
Stesso schema, arricchito: dentro il cono rosso largo è ora disegnata una linea a zig-zag rossa (tipo elettrocardiogramma/onda triangolare) che rimbalza ripetutamente tra i due bordi del cono senza mai centrare la bandiera — rappresenta le iterazioni sprecate di un agente mal direzionato. In alto a destra del cono rosso compare una "X" rossa su un altro segmento a "V" che esce dal cono (un secondo tentativo fallito che esce fuori target). In basso, appena sotto il disegno, iniziano a comparire due caratteri verdi: un simbolo "$" e una virgola/apostrofo — l'inizio dell'elenco delle due variabili di costo.
Voce: *"le due variabili che noi abbiamo per decidere quanto un prompt sia efficace, quindi quando un agente stia aiutando il nostro business o no, è nell'investimento che noi facciamo appunto in dollari che rientrano dall'utilizzo di questo agente"* — introduce la prima variabile: l'investimento in dollari.

### Scena 25 · `frame-0077.png` · 7:36
Stesso schema; sotto il disegno del cono ora si legge per intero, in verde, la scritta a mano: **"$, token"** e sulla riga sotto **"└> costo ↓↓"** (freccia/diramazione che introduce la parola "costo" seguita da due trattini/frecce discendenti, verosimilmente ad indicare "va abbassato"). ➕ la grafia delle due frecce dopo "costo" è ambigua (potrebbero essere due "↓" o due lettere corsive) — dichiarato per onestà, il resto del testo è leggibile con certezza.
Voce: *"o in fatturato aumentato, oppure in token, perché questi sono il nostro costo. Quindi, ovviamente vogliamo averlo il minore possibile"* — seconda variabile: i token, che sono il costo da minimizzare.

### Scena 26 · `frame-0079.png` · 7:48
Il canvas si allarga (zoom-out): ora si vede l'INTERO diagramma "Cos'è un Agente AI" con titolo in alto. Al centro box viola "Agente AI" collegato con doppia freccia a sinistra a un omino con cappello a cilindro ("Utente") tramite due frecce etichettate in verde "Richiesta" (utente→agente) e "Risposta" (agente→utente); sopra "Agente AI" scende una freccia dal box "Istruzioni" ("Un template di prompt") con accanto il cerchietto arancione "IP"; sotto "Agente AI" due frecce scendono verso due box: "LLM (ragionamento)" (icona cervello/chip) e "Memoria" (icona database); a destra doppia freccia verso il box "Strumenti" (Google, WolframAlpha, Python, API Meteo) già visto nelle scene precedenti. In alto a sinistra, dietro il pannello Excalidraw, si intravedono frammenti di scritte di un altro schizzo più vecchio, parzialmente coperti: "...HAT", "OCALI", "ORM" — ➕ probabilmente "CHAT", "LOCALE/LOCALI", "PLATFORM", ma illeggibili per intero perché tagliati dal pannello laterale. In basso a destra, rimpicciolito, si vede ora lo schema coni rosso/blu con l'omino e la bandiera della scena precedente, segno che è la stessa lavagna infinita.
Voce: *"Come ultimo concetto conclusivo [...] Un altro concetto, dopo avere introdotto il nostro prompt e avere detto che questa è l'IP, quindi la proprietà intellettuale che avremo nel futuro"* — commenta il diagramma d'insieme appena mostrato per intero, ricollegando Istruzioni/IP, Agente AI, LLM, Memoria e Strumenti.

### Scena 27 · `frame-0084.png` · 8:18
Stesso diagramma completo "Cos'è un Agente AI". Unica differenza rispetto alla scena precedente: sotto la parola "LLM" nel box "LLM (ragionamento)" è comparsa una sottolineatura blu ondulata (nello stile della sottolineatura arancione già presente sotto "Un template di prompt"), ad evidenziare visivamente il box mentre se ne parla. Il resto del diagramma (Utente/Richiesta/Risposta/Agente AI/Istruzioni/IP/Memoria/Strumenti) è invariato.
Voce: *"Quindi ad ogni agente, come vedremo dopo, andremo a collegare un LM, quindi c'è CVT, Clod, Gemini o chi per esso, a seconda del caso che avremo in esame"* — spiega il box LLM appena sottolineato: ogni agente viene collegato a un modello (GPT, Claude, Gemini...).

### Scena 28 · `frame-0089.png` · 8:48
Nuove annotazioni sul diagramma: (1) accanto alle frecce "Richiesta"/"Risposta" tra Utente e Agente AI compaiono due parentesi graffe nere disegnate a mano libera (aperte verso l'esterno, senza testo accanto ancora leggibile); (2) una freccia arancione curva parte dal box "LLM (ragionamento)" e punta verso l'alto dentro il box "Agente AI", a indicare il collegamento diretto LLM→ragionamento dell'agente; (3) in alto a destra, sopra il box "Strumenti", una linea nera scende da una scritta a mano ancora incompleta, leggibile solo come "To" + tratto non finito (in corso di scrittura). ➕ la scritta in scena 28 è parziale/in corso — completata nella scena successiva.
Voce: *"il motivo per cui lo facciamo è perché questi Llm di per se stessi non sono super intelligenti, ma possono processare bene input testuali"* — giustifica la freccia LLM→Agente AI appena disegnata.

### Scena 29 · `frame-0094.png` · 9:18
La scritta in alto a destra ora è completa e leggibile: **"Tool"**, con la linea che punta al box "Strumenti". Sotto il box "Strumenti" compare un nuovo elenco puntato in verde: **"• Read"** e **"• Bash"**. Il resto del diagramma è invariato rispetto alla scena precedente.
Voce: *"abbiamo anche la possibilità di avere uno strumento che si chiama read, uno strumento che si chiama bash e quindi sotto al agente esistono, diciamo, molte cose che sono state concesse [...] nell'infrastruttura da parte di antropic, open AI e via dicendo"* — cita esplicitamente i tool "Read" e "Bash" appena scritti come esempi di strumenti nativi (tipo quelli di Claude Code).

### Scena 30 · `frame-0099.png` · 9:48
Nuove annotazioni a destra del box "Strumenti": una parentesi quadra nera raggruppa l'elenco Google/WolframAlpha/Python/API Meteo, con una freccia che punta a una scritta nera **"API"**; poco sotto, in verde, un simbolo **"$"**. Elenco "Read"/"Bash" e resto del diagramma invariati.
Voce: *"questi servizi esterni, ovviamente noi sappiamo che ci accediamo tramite specifiche API, quindi una documentazione specifica e noi in cambio dell'accesso a questi servizi di terzi abbiamo che dobbiamo dare una un piccolo riscontro economico"* — l'annotazione "API" + "$" rappresenta il costo di accesso ai servizi esterni via API.

### Scena 31 · `frame-0104.png` · 10:18
Sotto il simbolo verde "$" (a destra del box Strumenti) compaiono due trattini/sottolineature orizzontali verdi in sequenza e una freccia verde rivolta verso il basso. Tutto il resto del diagramma resta invariato rispetto alla scena precedente.
Voce: *"il nostro quantitativo di denaro che noi spendiamo per qualcosa dipende da il numero di token che consumiamo che dipende dal numero di chiamate che facciamo all'API di un servizio a terzo. Quindi ovviamente più chiamiamo un servizio terzo, quindi più volte lo utilizziamo, peggio è per noi perché pagheremo molto di più"* — la freccia verde verso il basso accompagna il concetto "più chiamate = più si spende / peggio è".
