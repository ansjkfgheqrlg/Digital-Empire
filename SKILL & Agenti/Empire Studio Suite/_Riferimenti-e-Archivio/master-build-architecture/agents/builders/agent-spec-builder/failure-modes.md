# Agent-Spec-Builder — Failure Modes (P09 First-Class + CS01-04 + AP + Build History + Advisor)

**Purpose:** Comprehensive failure modes table for the agent-spec-builder. Per P09-failure-modes-first-class.md (every agent + global log), Content-Forge CS03 (self-imp mistake), CS04 (bugs in real test), advisor (anti-stuffing, context vs engineering), our ANALYSIS-AND-IMPROVEMENT-PLAN.md (initial 3/10 artifacts, persistence fail irony, stub violations), clones (content-forge anti-patterns, bugs), knowledge-pack AP01-09 (once populated) + P09 + PT07 + CS. Table format: ID | Failure | Symptom | Prevention | Detection | Recovery | Trace Sources. >=7 entries. Include own build failures (P09 self-ref). Contribute to memory/failure-modes-log/ + global.

**Failure Modes Table:**

| ID | Failure | Symptom | Prevention | Detection | Recovery | Trace Sources |
|----|---------|---------|------------|-----------|----------|---------------|
| FM-AS-001 | Missing canonical files (e.g. only 6/7, no memory.md) | Output "looks like agent" but unusable (no memory ecosystem, can't validate complete, violates PT05/P06/P10). User has to guess structure. | Strict shape in system-prompt + tools (BUILD order, 7 files mandatory), min thresholds in evals, invariant 5 (depth + shapes), PT05 enforcement in every handoff. | target-schema-validator-agent (C3), coverage-verifier (file count), self-critique in playbook step 5, bash find |wc -l ==7, failure-mode-validator. | Add missing files in iterate vN, log DEC "fixed canonical", update evals case AS-008, handoff to packaging only after pass. Add to this table. | PT05 full ( "ha tutti i 7 file? sì/no" ), P06 (shapes as contracts), P08 (depth), P10 (memory.md mandatory), our ANALYSIS (initial 12 stubs with 1 file each, 3/10 artifacts), content-forge agent-builder-agent.md (BUILD order + self-critique), schemas/agent.schema.json (required_files). |
| FM-AS-002 | Shallow content / summaries instead of expansion (P03/P08/P11 violation) | agent.md 100 words "see history", system-prompt vague "use Ruflo", no full extracts, playbook 1 example, failure table 2 entries. Violates P08 "depth over breadth", user "impeccabile", P03 "mai riassume — espande sempre". | Invariant 2/5/6 (no-summary, depth, shapes), system-prompt "expand every atom... Label ➕", evals cases that fail on shallow (AS-005), P08 in handoff context, plan-builder for depth planning, skill-depth-agent O1 pass mandatory before final. | skill-depth-agent (O1), evals (min words), failure-detector (P09), qa validators, bash wc -l + grep for "see history" or short sections, human critique. | Iterate: re-research sources, expand all files (add 200+ words, full quotes + inline expansion), add traces, log CP "depth pass v2", update evals/plan. In P5 full O1-O5. | P03-no-summary-expansion.md (full), P08-depth-over-breadth.md (full), P11-anti-summary-cultural.md, PT05 (min words), content-forge "no-summary-expansion" + Stage 4 MKD, Skill-Creator (progressive disclosure but full in files), our ANALYSIS ( "stubs are pure summaries", "rich kernel never made it to disk"), CS01/CS04 (real expansion needed). |
| FM-AS-003 | No memory updates / persistence fail (P10 + user core req + DEC-002 violation) | Claims "memory ecosystem" in SKILL/INDEX but no actual CP/DEC/SES files (dirs empty), INDEX text only, no manager runs, no two-layer, no update after steps. Ironic for memory skill. Violates "fin da subito", "si aggiorna dopo il singolo step", P10, P12, Context-Eng, Ruflo memory, Content-Forge logs. | Invariant 1 (memory from 0), memory.md in every 7 files with exact protocol (screenshot + two-layer + cycle + manager + both targets + append INDEX + run after every), tools include MemoryManager, evals AS-002/AS-004 enforce, P10 in every handoff + system-prompt, DEC-002 memory-first, CP-013 restore as lesson, manager.py full + tested. | memory-ecosystem-builder (if available), failure-detector + silent-observer (PT07), qa (failure-mode-validator), bash ls memory/checkpoints/ |wc -l, manager --search, INDEX grep for recent, human "is memory live?". | Create missing CPs/DECs from INDEX text (as in CP-013), run manager on both, append INDEX, sync, log DEC "fixed persistence", update memory.md + evals, P5 SI pass on self. Add to table. | P10-self-improvement-loops.md (full), user screenshot (exact structure), DEC-002 "memory-ecosystem-first", CP-013 "memory files restored", ANALYSIS ( "Memory Ecosystem Failure: core user req only partially met... no actual files... persistence failure"), Context-Eng advisor (two-layer + Research→Plan→Reset), Ruflo (memory plugins + AgentDB), Content-Forge (failure-modes-log + SI in CS03/CS04), memory_manager.py (code + usage). |
| FM-AS-004 | Context stuffing / no Research→Plan→Reset (advisor violation) | Prompt bloated with all sources at once, no synthesis, no RESET, output polluted, low density, hallucinations on boundaries. Violates advisor "anti-stuffing", two-layer, cycle, Context Manifest. | Invariant 8 (Research → Plan → Reset → Implement), system-prompt step 1-3 (research chaotic ok, synthesize PLAN, RESET, implement from plan + long-term only), two-layer in memory.md + tools (short for current, long INDEX for history), tight handoff boundaries ("Context boundary: ONLY these atoms"), advisor full extract in system-prompt, evals AS-004, P5 depth with humanizer. | Self-critique ( "is context reset?"), failure-detector, qa, human "too long/polluted?", advisor self-check if installed. | RESET explicitly (clear short-term, use plan), re-synthesize from long-term INDEX only, log CP "RESET performed for [slug]", iterate with tighter boundary, use two-layer more. | Context-Engineering-Advisor/SKILL.md (full: "Research → Plan → Reset → Implement", "Context Manifest", "5Qs", "anti-stuffing", two-layer memory examples, falsification), P12 (trace), P03 (expansion but clean), our ANALYSIS ( "no actual Context Manifest or two-layer in memory files"), CP-013+ (restore used two-layer), advisor examples. |
| FM-AS-005 | Meta drift / no self-improvement (P10/P13/PT08/CS03 violation) | Builder produces good agents but itself not improved over time; no loops, no feeding own output back, no P10 on self, drift from initial vision (like CS03: "self-improvement mistake" no observer). Violates P13 "meta-recursive", PT08 "skill-that-produces-skills", user meta, P10 loops. | Invariant 10 (meta-recursive), system-prompt "This spec can be fed back to produce v2", playbook Example 6 (meta self-build), evals AS-006 (self-improve case), P5 SI pass (deploy failure-detector + phase-planner + silent-observer + triage on self artifacts), memory updates on self during every build (P10), catalog/ANALYSIS updates, PT08 in handoff. | failure-detector-agent + silent-observer (PT07), phase-planner, continuous-improver (once added), evals "run" on self, human "has builder improved?", grep for "v1" vs "v2" in files, CP count on self. | Feed v1 + new CPs/ANALYSIS + new sources back to self (or v2 builder), produce v2 with improvements (deeper Ruflo, more depth, fixes), log DEC "meta v2 from v1 + P13", update CATALOG/ANALYSIS, P10 loops in memory, P5 full SI. | P10 (full loops), P13-meta-recursive-applicability.md, PT08-meta-recursive-skill.md, CS03-the-self-improvement-mistake.md (full: "the mistake was assuming SI without observer"), PT07-silent-observer.md, our ANALYSIS ( "P13 meta not executed (no self-improvement artifacts from the build)", "P10 no actual loops"), CP-025+ (autonomous progress), ruflo (SONA/hooks for background learning), content-forge (SI team + failure-detector). |
| FM-AS-006 | Wrong topology / poor delegation (P07/PT01/Ruflo violation) | Builder tries to do conductor work or other L2 (e.g. builds full swarm instead of delegating to swarm-builder), or wrong handoff (loose boundaries, no DEC log). Violates P07 three-level, PT01 conductor-subagents, Ruflo queen delegation, PT02 handoff. | Invariant 4 (three-level + delegate), system-prompt "You are L2 specialist. Never do conductor... Delegate ASK to question-designer... Handoff with exact protocol", tools include SpawnSubAgent, playbook examples of delegation (Example 1-3), handoff DEC mandatory in every, evals AS-003 (swarm related), P7 in context. | conductor (L1 observes), qa (coverage for delegation), failure-detector, human "did you delegate ASK?", check DEC logs for handoffs, grep for "I did the ASK myself". | Handoff immediately to correct sub (e.g. question-designer), log DEC "corrected delegation", update playbook with lesson, re-build if needed, P5 add more delegation in prompt. | P07-three-level-architecture.md (L1 Kernel/Conductor, L2 Specialists, L3 Tools full), PT01-conductor-with-subagents.md (full), Ruflo (queen, delegation, federation), content-forge (conductor spawns builders, handoff), PT02 (handoff rules), our CPs (e.g. CP-017 "Conductor deepened", delegation in batch), SKILL.md (three-level). |
| FM-AS-007 | No traceability / orphan atoms (P12/PT09 violation) | Sections in files with no "Trace: ..." links, content not linked to Pxx/PTxx/clones/advisor/skill-creator/user/CP, KG broken, coverage <100%, violates P12 "every atom links back", PT09 multi-source. | Invariant 7 (traceability), system-prompt "every atom... links... >=3 per section", tools WriteFile requires trace_header, evals require traces_per_section, playbook examples have traces, P12 in every, ingestion-agent KG if available, qa coverage-verifier mandatory before handoff. | coverage-verifier-agent (P12 + C1), target-schema-validator, failure-detector, grep "Trace:" count, manual review, KG query. | Add traces to all sections (re-write with links), log CP "added traces for P12", update evals, handoff only after coverage pass, P5 full coverage check. | P12-traceability-source-to-output.md (full), PT09-multi-source-with-traceability.md, P03 (expansion with links), content-forge (KG in stages, traceability in agents), our ANALYSIS ( "Traceability strong in text, weak in artifacts", "INDEX claims without files"), CPs 013+ (restore added traces), SKILL.md (trace section), user query ( "traceability" ). |
| FM-AS-008 | Ignoring own failures / no P09 in process (P09/CS03/CS04/ANALYSIS violation) | Build has failures (e.g. persistence, stubs) but not logged in failure-modes.md or memory/failure-modes-log/, no SI triggered, repeats mistakes (CS03 no observer). Violates P09 "failure modes first-class", CS04 "bugs found in real test". | Invariant 6 (failure first-class), system-prompt "log your own build failures", failure-modes.md mandatory in 7, tools log to failure-modes-log/, evals AS-005 (failure case), P5 SI pass with failure-detector + triage + phase-planner + silent-observer on self, memory updates for failures, CS03/04 in playbook. | failure-detector-agent, silent-observer, phase-planner, qa failure-mode-validator, grep failure-modes-log/, human "did we log the persistence fail?", CP count for failures. | Log the failure as FM-xxx in this table + global log + CP/DEC "failure detected in self build: [desc]", trigger SI (spawn failure-detector), iterate fix, update evals/playbook with recovery, P10 loop. | P09-failure-modes-first-class.md (full), CS03-the-self-improvement-mistake.md (full), CS04-bugs-found-in-real-test.md (full), advisor (anti-patterns), our ANALYSIS ( "no actual failure-modes.md files or populated logs. Build's own persistence failure not documented in files", "AP08 no-failure-mode-doc"), CPs 013/025 (persistence fix logged), content-forge (failure-modes-log/ in clone, SI agents). |
| FM-AS-009 | Breaking meta-recursive / no PT08 (P13/PT08 violation) | Builder produces agents but the skill as whole not used to build/improve itself or future skills; no self-ref in specs, no feeding back, violates PT08 "meta-recursive-skill", P13, user "meta-recursive builder". | Invariant 10, system-prompt "meta-recursive: this can be used to build agents that build agents", playbook Example 6 (self v2), evals AS-006, P5 depth/SI on self, update CATALOG/ANALYSIS with self as example, PT08 in handoff + memory, meta-recursive-builder (once added) for full. | meta-recursive checks in evals, conductor observes, failure-detector, grep for "meta" or "v2" or "feed back", human "is this skill building itself?", CP on self improvements. | Explicitly feed current spec + new reqs back (as in Example 6), produce v2 or improved artifacts, log DEC "PT08 applied: self v2", update this failure-modes + memory, P5 full meta pass. | P13-meta-recursive-applicability.md (full), PT08-meta-recursive-skill.md (full), PT01 (conductor subagents meta), Skill-Creator (packaging for meta), our ANALYSIS ( "P13 meta not executed (no self-improvement artifacts from the build)"), CP-026 (autonomous continuation using self), user "meta-recursive", content-forge (skill-that-produces-skills via pipeline). |

**Additional Failure Modes (to expand in P5 / when more sources):**
- FM-AS-010: Wrong BUILD order (tools not first) — symptom: system-prompt bad because no tools defined. Prevention: strict in prompt + evals. Trace: content-forge agent-builder-agent.md ( "BUILD (ordine OBBLIGATORIO — tools.md prima del system prompt)" ).
- FM-AS-011: No Ruflo integration for swarm agents — symptom: agent spec misses npx ruflo, memory_store, queen. Prevention: Ruflo extracts in prompt for relevant. Trace: ruflo full + PT01.
- FM-AS-012: Anti-patterns not called out (e.g. free-form, over-canonicalize) — from PT05 anti + advisor + pack AP. Prevention: failure-modes + system-prompt anti section. Trace: PT05 anti, advisor, pack 03-anti-patterns (when populated).
- FM-AS-013: Evals not discriminating / not run — symptom: cases pass even without spec. Prevention: Skill-Creator "must fail without", evals in P5 loop. Trace: Skill-Creator, content-forge evals.
- FM-AS-014: No sync top/embedded memory — symptom: divergence in CPs/INDEX. Prevention: sync step in memory.md + manager + rsync in tools. Trace: CP-013 restore, our sync protocol.
- FM-AS-015: Violate user invariants in produced agents (e.g. no 10 invariants in SP) — symptom: produced agents don't enforce memory/MKD/no-summary etc. Prevention: invariant 1-10 in every system-prompt. Trace: SKILL.md 10 invariants, user query.

**Global Failure Modes Log Contribution:**
This table + any runtime failures during builds must be appended to memory/failure-modes-log/ (or global in skill). Use Content-Forge style + P09. Silent-observer (PT07) + failure-detector will monitor.

**Prevention/Recovery Summary (P09 + P10):**
- Prevention: invariants in prompt, canonical shapes, memory.md protocol, evals discriminating, qa gates, depth pass.
- Detection: validators + SI agents + self-critique + human + grep/ wc.
- Recovery: iterate vN (P01), log everything (P09/P10/P12), SI loops (P10), feed back (PT08), P5 full depth/SI/validate.
- All failures from our build history (stubs, persistence, ANALYSIS 3/10, shallow) are included as lessons (P09 self-ref + CS04).

**Trace for this failure-modes.md:** Full table expanded from:
- P09-failure-modes-first-class.md (full definition + why + how + table shape)
- CS01-the-mkd-discovery.md, CS03-the-self-improvement-mistake.md (full text), CS04-bugs-found-in-real-test.md (full)
- PT07-silent-observer.md, PT09, PT05 (anti-patterns section)
- Context-Engineering-Advisor/SKILL.md (full anti-stuffing, context vs engineering, examples of failures)
- content-forge2.0 (failure-modes-log/ dir + CS*.md + agent-builder-agent.md self-critique + bugs, anti-patterns in references)
- Our ANALYSIS-AND-IMPROVEMENT-PLAN.md (full criticism: "Memory Ecosystem Failure", "persistence during build", "no actual failure-modes.md", "P09 no actual... ", "AP08", "6.5/10 intent 3/10 artifacts", "stubs", "no SI artifacts"; also plan items for P09)
- CPs 013 (persistence fix), 017-025 (build process failures logged), 026 (this update)
- Past agent failure-modes.md (conductor, memory-ecosystem-builder, etc as templates + their tables)
- Ruflo (failure handling in swarm)
- Skill-Creator (evals for failure cases)
- User: screenshot (memory as prevention for persistence), "ok procedi", piano di sviluppo (memory iterativa, principles), KP-PLAN (P09 section)
- Glossary (failure modes, shape, canonical, meta-recursive)
All entries have full trace. No summary. P09 first-class. Will be populated to global log + used in P5 SI pass.

**Status:** v1 with 9+ entries (exceeds 7). Includes own build history as P09 self-ref. Will be expanded in P5 (more from real runs, new AP from pack, SI feedback) + added to failure-modes-log/.

**End of failure-modes.md. Memory updated on creation (CP for this + prior).**