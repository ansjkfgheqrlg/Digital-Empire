# Principles-Manager Failure-Modes (P09 First-Class + CS01-CS04 + Our ANALYSIS P Violations + Build Issues)

**P09 Mandate:** Every agent has failure-modes.md with table "failure | symptom | prevention | detection | recovery". Log to failure-modes-log/ (P10 SI). Global contrib (this agent's FMs feed SI for whole skill). Table from P09 + CS01-C04 (real content-forge history) + AP01-09 + our ANALYSIS (per-P violations as live FM) + CPs/DECs (build issues) + clones (Ruflo/Content-Forge failures) + advisor (stuffing) + skill-creator (shallow) + user complaint (visibility/structure as P02/P08/P15 fail).

**Table (8+ entries, full per P09):**

| ID | Failure | Symptom | Prevention | Detection | Recovery |
|----|---------|---------|------------|-----------|----------|
| FM-PM-001 | P10 not applied (no memory updates after steps, like initial ANALYSIS persistence fail) | No CPs/DECs/INDEX appends after actions; memory/ subdirs empty or only text claims in INDEX (no backing files); user "non vedo... non c'è memoria live"; dogfood broken (memory skill has no memory) | Mandate in prompt/tools/playbook/memory.md: "ALWAYS run manager --checkpoint both targets + append INDEX both + sync after EVERY step/tool/handoff"; two-layer (SES short + INDEX long + principle-state); Research→Plan→Reset→Implement (P10 in cycle); Ruflo AgentDB hooks for auto; SI conditional (P10/PT07); architectures/ dir for principle-state | validate tool + scripts/validator.py --check-memory (count CPs >=3 post step, both layers); failure-detector (P10 threshold >=1 blocker); user query "show memory updates"; INDEX audit (compare claims vs files); CP count in principle-state | Run manager --checkpoint/ --decision both (like CP-013 restore); sync CPs/DECs; update principle-state {"P10": {"applied":true, "cps":[...]}}; create missing (e.g. architectures/); log FM to failure-modes-log/logged/; triage (SI2); generate fix PLAN (SI3); re-validate; P01 iterate (new vN with P10 enforced). Example: our CP-004-... + CP-013 + every autonomous CP/DEC = recovery. |
| FM-PM-002 | P03/P11 summary instead of expansion (shallow stubs like early agents/ANALYSIS) | Outputs are summaries ("stub", "refer to history"); no extracts/tables/trees/code from P files/clones/advisor/skill-creator/our CPs/ANALYSIS/user; thin MKD/agents (P03 violation); user "non vedo... non c'è SKILL.md/references/agents" (structure hidden in summary) | No-summary invariant in prompt (P03/P11): "ALWAYS expand atoms from sources into richer (full extracts, tables, examples from our build, ➕ inventions like principle-state)"; P03 in all tools/playbook/evals; depth mandate (P08); MKD first (PT10/Content-Forge Stage 4); progressive disclosure (P02: lean kernel, depth in refs/agents); use ReadPrinciple full format | validate tool (no "summary" words, extract count >=3 per section); qa coverage-verifier (P03 atoms from sources present); user "è solo un summary"; failure-detector (P03 threshold); evals grade low on depth/extracts; SKILL/ANALYSIS audit | Expand in-place or new vN (P01); re-write with full extracts (like this principles-manager.md with P01/P10 full + ANALYSIS + CPs + user + clones); add tables/trees/code; log FM; SI triage; P03 re-enforce in prompt; re-eval. Example: early ANALYSIS stubs → this rich creation (P03 applied). |
| FM-PM-003 | P08 shallow / depth not over breadth (like early ANALYSIS 3/10 artifacts, stubs vs 5-10p) | Agents/flows <2 pages; no 5-10+ page depth; prioritize breadth (many stubs) over depth (core P not deep); user "non c'è buona struttura... non ci sono tutti gli agenti... flussi" (shallow) | P08 mandate: "Depth over breadth (P08): 5-10+ pages per artifact (this agent ~deep with extracts/tables); prioritize core (P01/P07/P09/P10/P12 first) before more; one-by-one (this after 13 core)"; P08 in prompt/tools/playbook/evals/failure-modes; use plan-builder for depth PLAN; O1-O5 optimizers in P5; PT05 7 files (depth in each) | evals (depth score <5/10); qa target-schema (shallow); user complaint on structure; failure-detector (P08); count lines/sections in outputs; principle-state coverage low on depth | Depth pass (P5: use skill-depth + this + optimizers); expand in-place (add extracts/tables/examples from sources + our CPs/ANALYSIS); P01 iterate (vN with depth); one-by-one add (this principles-manager as depth example); re-validate. Example: early 12 stubs → 13+ full 7-file + this deep (P08 start). |
| FM-PM-004 | P12 weak traceability (like early ANALYSIS "trace strong in text, weak in artifacts") | No headers with sources; no links to Pxx/PT/CS/clones/advisor/skill-creator/user/our CPs/ANALYSIS; atoms not traceable (e.g. "from history" instead of exact file + line); KG missing; user can't audit "dov'è la skill" | P12 mandate: "Trace every atom to sources (P12 + PT09 + Content-Forge KG): >=3 per section (Pxx, clones, advisor, skill-creator, user complaint, our CPs/ANALYSIS/DEC, pack)"; headers in all files (Timestamp/Phase/Linked Principles/Traceability); KG planned (kg_builder.py); every tool/playbook/evals/memory.md has "Trace (P12): ..."; use ReadOurLessons/ReadPrinciple with trace | validate tool (trace links count); qa coverage-verifier (P12); user "non vedo... non c'è traccia"; failure-detector (P12); grep for "Trace:" in outputs; principle-state trace coverage | Add headers/trace sections (like this playbook "Trace: P10/P12 + ANALYSIS + CP-004 + user + sources"); re-write with exact cites; build KG; log FM; P01 iterate; re-audit. Example: early text claims → this with full "Trace (P12): ..." + CPs/DECs headers + SKILL/ANALYSIS/ README. |
| FM-PM-005 | P13 meta not applied (like ANALYSIS "P13 not executed until autonomous") | No self-ref; skill not used to build/improve self (no "feed this back to v2"); no meta examples in playbook/memory (e.g. "our autonomous is P13"); PT08 violation | P13/PT08 mandate: "Meta-recursive (P13 + PT08 + Skill-Creator): This skill builds itself (our autonomous continuation is meta example); principles-manager can be used to improve this skill's own principle application in v2; feed self (our ANALYSIS/CPs) as input for v2; meta self-ref in prompt/playbook/memory ('feed this back to produce v3')"; PT08 in SKILL/catalog; use meta-recursive-builder; P13 in evals (PM-005) | evals (meta score low); failure-detector (P13); grep "meta" / "self-ref" / "v2" in outputs; user "non meta-recursive"; principle-state P13 coverage | Add meta self-ref (like this prompt/playbook/memory: "meta self-ref in creation"); use on self (this agent creation post DEC-010 + autonomous = P13 in action); update ANALYSIS/SKILL with v2 improvements from this; log FM; P01 iterate (vN with meta); re-eval. Example: early "meta claimed but not executed" → this + autonomous (P13 applied, examples in playbook). |
| FM-PM-006 | P15 trigger not optimized (like user complaint "la skill si deve chiamare Master build Architecture" + visibility issues as P15 fail) | Wrong name (prior "master-architect"); triggers not pushy (per Skill-Creator); user can't invoke ("dov'è la skill", "non vedo..."); no "flussi di principi" / "agenti per principi" / "Master build Architecture" in SKILL/README/ANALYSIS | P15 mandate: "Trigger-Design-as-Product-Design (P15): SKILL.md triggers (natural phrases like 'architect this...', 'flussi di principi', 'Master build Architecture') optimized per Skill-Creator; this agent trigger on 'manage principles for...', 'flussi di principi per...', 'agenti per principi'; name 'Master build Architecture' (user override); visibility in SKILL/README (P02/P08)"; P15 in prompt/evals (PM-001/005); description optimization; user feedback loop | user complaint (exact "la skill si deve chiamare..."); evals (P15 score); failure-detector; "non vedo" queries; principle-state P15 coverage | Update name in SKILL/frontmatter/README/ANALYSIS (like CP-036/DEC); add triggers/flussi section in SKILL/README (like this + visibility fixes); optimize description; log FM; P01 iterate (vN with P15); re-invoke test. Example: prior name → "Master build Architecture" + flussi in SKILL/README + this agent (P15 addressed). |
| FM-PM-007 | P07/P01 three-level + iterative not in flussi (like early no L3 domain for principles, no vN) | Flussi flat (no L1/L2/L3); no multiple PLAN-vN for principle flows; no conductor/ team for principles-team; P07/PT01/P01 violation | P07/P01/PT01 mandate: "Three-level (P07 + PT01): L1 Kernel/Conductor (SKILL + conductor), L2 Specialists (25+ in agents/ + pipeline/builders/qa/meta/opt/SI/domain), L3 Tools (scripts/ + Ruflo MCP + memory); principles as L3 domain + flussi (this + principle-codifier + 5 domain + workflow/team for pipelines/teams); P01 (multiple vN + changelog for flussi)"; in prompt/playbook; use conductor/team-builder/workflow-builder | validate (three-level dirs/flows); evals (P07 score); failure-detector; no L3 in output; principle-state P07 coverage | Create L3 domain agents (this principles-manager as L3); compose flussi with team-builder (principles-team) + workflow-builder (principles-pipeline); P01 iterate (vN for flussi); log FM; re-validate. Example: early no L3 for principles → this + 5 domain + catalog L3 (P07 applied). |
| FM-PM-008 | P09/P10 ignore own failures (like CS03 self-imp mistake + our initial persistence + ANALYSIS "no FM files") | No FM table in this agent; no log to failure-modes-log/; SI not spawned for P violations in build (e.g. P10 fail); P09/P10 violation | P09/P10/PT07 mandate: "FM as first-class (P09): Every agent has failure-modes.md table (this one does, 8+ from P09/CS/ANALYSIS/CPs); log to failure-modes-log/ (P10 SI); SI agents (failure-detector + planned triage/phase-planner) observe/triage/plan; silent/conditional (P14/PT07); P10 loops (memory updates + SI)"; in prompt/tools/playbook/evals/memory.md; global contrib | failure-detector (P09/P10); no table in agent; no FM in log; evals (P09 score); user "no FM doc" (AP08); principle-state P09 coverage | Add table (this file, 8+); log FM for build issues (e.g. initial P10); spawn SI (handoff failure-detector); update failure-modes-log/INDEX; P01 iterate; re-audit. Example: early "no FM files" (AP08) → this + all new agents have tables + CP-013 recovery + SI in P10 flow (P09/P10 applied). |

**Additional FMs (from AP + CS + clones + advisor + our CPs):**
- FM-PM-009: P02 visibility fail (like user "non vedo... non c'è SKILL.md/references/scripts/agents/flussi/dov'è la skill" = P02/P08/P15 violation) → prevention: README + SKILL visibility section + ls maps + explicit flussi/agents for principles/patterns/case (this + siblings + CATALOG); detection: user query; recovery: this README/SKILL update (CP-004-created-readme... + CP-035).
- FM-PM-010: AP01/AP02 (scaffold as deliverable + no FM doc, from advisor + our initial + P09) → prevention: full 7 files + this FM table + packaged/ + memory live; etc.
- FM-PM-011: CS03 self-imp mistake (P10 without real observer/SI = drift, from CS03 + our persistence) → prevention: SI in P10 flow (failure-detector + this); recovery: CP-013 + this agent + validator.
- FM-PM-012: CS04 bugs in real-test (P09/P10 without validation = bugs like persistence, from CS04 + our initial) → prevention: validator.py + qa + real test invoke + P09 FM; recovery: our autonomous audit + fixes.
- FM-PM-013: P12/P13 no self examples (like ANALYSIS "trace weak in artifacts", "P13 not executed") → prevention: meta self-ref + our CPs/ANALYSIS as examples in playbook/memory (this has "our autonomous as P13"); recovery: this creation.
- FM-PM-014: P15 name/trigger mismatch (prior "master-architect" vs user "Master build Architecture") → prevention: name in SKILL/README/ANALYSIS/CPs (updated CP-036); recovery: name update + DEC.
- FM-PM-015: Ruflo/Content-Forge/Advisor extract fail (no swarm/memory/cycle in flussi) → prevention: extracts in prompt/playbook (this has full from Ruflo swarm/P10 memory, Content-Forge SI/P01, Advisor two-layer/cycle); recovery: re-extract + update.

**Global Log Contrib (P10/P09):** This agent's FMs (e.g. FM-PM-001 if P10 slip in future) go to failure-modes-log/logged/ (via failure-detector handoff); triage/plan by SI; feed P01 next vN + principle-state.

**Prevention Culture (P09/P10/PT07):** All FMs have prevention (mandates in prompt/tools/playbook/memory.md); detection (tools/validator/qa/SI); recovery (P01 iterate + manager + SI). Silent default (P14). Our build as example (initial violations logged in ANALYSIS → recovered in autonomous + this + CPs/DECs).

**Trace (P12):** Table from P09 full + CS01-C04 full + AP01-09 + ANALYSIS "per-P violations" + "Weaknesses" + "Implemented" + our CPs/DECs/INDEX (build issues + recoveries e.g. CP-013) + SKILL/ README (P violations addressed) + clones (Ruflo/Content-Forge failures) + advisor (stuffing) + skill-creator (shallow) + user complaint (visibility as P02/P08/P15) + pack P09/CS. Produced by this agent creation (autonomous + plan-builder + memory-ecosystem-builder + conductor + ANALYSIS Priority 4 + user feedback).

*FM table is living (append from future CPs via P10 SI). Enforce P09 in all. Meta: use to prevent in self v2.*