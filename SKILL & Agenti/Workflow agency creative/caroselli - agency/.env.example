# Copia questo file in `.env` (stessa cartella) e mettici i valori.
# `.env` non e' tracciato: lo dice .gitignore.
#
# I valori che stavano qui prima del 2026-09-09 sono BRUCIATI: erano in chiaro
# in config.py su un repository pubblico (B-021). Vanno revocati sul servizio,
# non solo sostituiti qui.

GROQ_API_KEY=
OPENROUTER_API_KEY=
ARENA_EMAIL=
ARENA_PASSWORD=

# Facoltativa: se manca si usa il valore predefinito in config.py
ARENA_CHAT_URL=
