# DT05-when-restart-vs-iterate

Restart only when core assumptions are invalid; otherwise create a versioned iteration with a delta.

```text
Question → evidence → decision → owner → record → verify
```
