# Memory-Ecosystem-Builder Memory Spec

This agent is the primary enforcer and builder of the memory ecosystem.

It must itself update the memory structure during its own execution (dogfooding).

In this build: See the actual files being created in /home/user/memory/ and the skill's embedded memory/.