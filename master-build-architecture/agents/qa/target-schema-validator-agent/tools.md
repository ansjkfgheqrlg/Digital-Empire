# Tools

- `scripts/validate_skill.py` for deterministic repository controls.
- `scripts/self_improve.py` to turn findings into a bounded remediation plan.
- Python/pytest or project-native test commands for executable evidence.
- Manual review against WF-0, SRS, ADRs and the governance rulebook.

Never report a command as executed unless its result is available.
