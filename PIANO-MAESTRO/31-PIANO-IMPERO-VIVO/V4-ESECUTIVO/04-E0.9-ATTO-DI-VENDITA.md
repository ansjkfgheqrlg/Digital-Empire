# 04 — E0.9: L'ATTO DI VENDITA

> Chi: **Max decide** (il prezzo e il ruolo del Manuale), **EMPERATOR esegue** (allineamento,
> aggancio, blocco di descrizione, registrazione) · Ore V3: 2-4 · Tetto: 6 · Dipende da: **E0a**
> (i due Payment Link) e **E0.5 passo 2.8** (il gate INCASSO via HTTP) — **ma solo per il passo
> 2.4**: il decreto, l'allineamento del prezzo e il blocco di descrizione non aspettano nessuno ·
> Percorsi in scrittura: `Crea siti/Siti CCM/checkout.config.json`, `Crea siti/Siti CCM/manuale.html`
> (righe 280-283), `Crea siti/Siti CCM/pagamento.html` (solo il blocco JSON, via `--apply`),
> `empire/tools/checkout.py` (un comando nuovo, passo 2.2), `Crea siti/Siti CCM/blocco-descrizione-video.txt`
> (da creare), `company/Memory/decisions/ADR-030-prezzo-manuale-per-decreto.md` (da creare),
> `company/Memory/BACKLOG.md` (riga 10), `PIANO-MAESTRO/29-ECOSISTEMA-LANCI/00-LEGGIMI.md` (riga 309),
> `company/Memory/tesoreria/entrate.jsonl` (**solo** se arriva una vendita vera) · Politica di guasto:
> **fail-closed sul prezzo** (config, pagina e Stripe devono dire lo stesso numero: se divergono, la
> pagina non va a tier 1) · **fail-open sul link nei video** (un video senza link non ferma gli altri)
> STATO: COMPLETO (EMPERATOR, 2026-09-11 — scritto a mano, un documento per volta)

**Cosa fa questo scaglione, in una riga:** dà un prezzo dichiarato al Manuale Claude Code, lo mette
nello stesso numero in tre posti (config, pagina, Payment Link), e collega i video che escono al
prodotto — così che il fusibile R5 (V3 §2/§12) possa scattare **per un verdetto di mercato** e non
per costruzione. È nato in V3 §2 perché in V2 i due Payment Link di E0 non avevano un prodotto
prezzato dietro e 23 dei 26 pezzi caricabili sono video senza prezzo per natura.

**Il disco ha smentito il piano su tre punti, e V4 li corregge qui:**

1. **«Un link nella descrizione dei 23 video» è sbagliato per 7 video su 23.** Misurato l'11/09
   (`python scripts/ultimo_metro.py` + `head -3 YOUTUBE-AUTOMATION-FACTORY/VIDEO-PRONTI/video-NN/copy.md`):
   i 7 video di `VIDEO-PRONTI/` (video-01…07) sono del canale **Legami d'Amore** (psicologia delle
   relazioni, sorgente `@PsicologiaFemminile-f8c`) — un pubblico che non c'entra niente con un
   manuale su Claude Code. Il link al Manuale va **solo sui 16 video Claude** (i ROSSI da 139-143 gg
   in `Lancio corso skill beast/Page/Leo/da pubblicare`: `youtube page claude code.mp4`, `file spec
   CL.mp4`, `the beadt claude…`, ecc.), che escono in **E8**. Per i 7 di Legami d'Amore l'esito è
   **NON VALUTATO** con motivo «nessun prodotto di quel canale da collegare oggi» (V3 §7, tre esiti),
   e nasce una riga di BACKLOG. Il gate «`grep -c` tende a 23» diventa **tende a 16**.
2. **`checkout.py` non ha un comando per cambiare il prezzo.** `python empire/tools/checkout.py
   --help` (11/09): solo `--check`, `--apply`, `--accendi-stripe`, `--scadenza`. Il prezzo 67/97/27
   sta nel config **e** cablato a mano in `manuale.html:280-281` (`€67`, `€97`); `pagamento.html`
   invece lo legge dal blocco JSON che `--apply` inietta (`render_pagamento`, `checkout.py:202`).
   Se Max decreta un prezzo diverso dal default, serve un comando: è la **specifica del passo 2.2**.
   Se Max conferma il default, il comando non serve oggi e si rimanda (non si costruisce ciò che
   non si usa — L4).
3. **`manuale.html:283` dice «prezzo di lancio fino al 31/07» ed è testo fisso**: `--scadenza`
   (E0a passo 2.3) aggiorna il JSON di `pagamento.html`, **non** questa riga (`render_manuale`,
   `checkout.py:175-183`, tocca solo i tre link). Dopo E0a la pagina di vendita mostra ancora una
   data scaduta. Si sistema qui, nel passo 2.3, perché la riga è del prezzo e il prezzo è di E0.9.

E una correzione al testo di V3 §12 già annotata in EMP-D9HD: il fusibile R5 non legge `tipo:
vendita` (campo che **non esiste**). Il record di `tesoreria.py entrata` ha le chiavi `id, data,
importo, valuta, da, motore, stato, nota, registrato` (`scripts/tesoreria.py:128-138`): **R5 legge
`motore == "corsi"` e `stato == "incassato"`**. Passo 2.6.

---

## 0. Cosa NON è bloccato da questo scaglione (ADR-028)

Se E0.9 si ferma (Max non decreta, Stripe non risponde, il comando `--prezzo` non nasce oggi),
**procede comunque tutto questo**:
- **E0a** (i 5 pezzi escono con o senza link: sono Legami d'Amore, il link non li riguarda).
- **E0.5, E0b, E0.6, E0.7, E1, E2, F3, E3.0, E3, E4, E5a, E5-bis, E5c, E6, E7**: nessuno legge
  il prezzo del Manuale. Verificato: `grep -rl "prezzo_lancio\|checkout.config" --include=*.py .`
  → solo `empire/tools/checkout.py` e `empire/controllo.py:72` (che legge un booleano, non il prezzo).
- **E8** procede per i 3 libri KDP e per il caricamento dei 16 video Claude; aspetta E0.9 **solo
  per la riga di link in descrizione** — e anche quella si aggiunge dopo, a video già pubblico
  (una descrizione YouTube si modifica in qualsiasi momento). Quindi E8 non aspetta neanche quella.
- **LANCI v4** (esecutore Gael — **non dipende da Gael**: LANCI adotta i pezzi usciti quando il suo
  primo flusso chiude, V3 §11 decisione 9; E0.9 non gli chiede niente e non aspetta niente da lui).

**Cosa aspetta davvero E0.9:** nulla nel piano. Il fusibile R5 di fine E3 legge «canale di vendita
apparecchiato» = prodotto prezzato + Payment Link vivo + collegato a pezzi usciti: senza E0.9 il
fusibile scatta per costruzione. **Non è un blocco: è il motivo per cui E0.9 sta prima di E1.**

**Cosa NON fa E0.9**, dichiarato: non crea i Payment Link (E0a), non costruisce il gate HTTP (E0.5
passo 2.8), non carica video (E0a/E8), non istruisce il lancio del Manuale dentro LANCI (O1-O6 di
`29-ECOSISTEMA-LANCI/03-FLUSSO-OFFERTA.md` restano di LANCI: qui si prende **solo** la decisione
O1/ruolo e il default di prezzo, con la stessa forma reversibile).

---

## 1. Prerequisiti — verificati con un comando, non dichiarati

| # | Prerequisito | Comando | Output atteso | Exit |
|---|---|---|---|---|
| P1 | Il default di prezzo è sul disco | `python -c "import json;d=json.load(open('Crea siti/Siti CCM/checkout.config.json'));print(d['prezzo_lancio_eur'],d['prezzo_listino_eur'],d['bump_eur'],d['scadenza_lancio'])"` | `67 97 27 2026-07-31` (misurato 11/09; dopo E0a 2.3 la data cambia) | 0 |
| P2 | La pagina di vendita dice lo stesso prezzo del config | `grep -n 'id="price-display"[^>]*>€67\|id="original-price-display"[^>]*>€97' "Crea siti/Siti CCM/manuale.html"` | due righe: **280** e **281** (misurato 11/09) | 0 |
| P3 | I prezzi che l'azienda si è già data sono sul tavolo (O2 del flusso offerta: «un prezzo proposto senza guardare i prezzi già dati non è istruito») | `grep -n "Claude Code Mastery" "competitor/Andrei Pascu/ANDREI-PASCU-DOSSIER-COMPLETO.md"` · `grep -n "297" PIANO-MAESTRO/29-ECOSISTEMA-LANCI/03-FLUSSO-OFFERTA.md` | **397 €** (dossier Pascu, tabella §4 riga «Claude Code Mastery», cattura per errore) · **297-497 €** (la wiki, citata in `03-FLUSSO-OFFERTA.md` O2) · **67/97 €** (config) — tre prezzi, tre date, mai sullo stesso tavolo | 0 |
| P4 | I Payment Link esistono (E0a chiusa) — **serve solo al passo 2.4** | `python empire/tools/checkout.py --check` | `tier 1` (dopo E0a) · oggi `tier 2 - fallback ordine attivo` | 0 |
| P5 | Il gate INCASSO via HTTP esiste (E0.5 passo 2.8) — **serve solo al passo 2.4** | `python -m empire controllo \| grep -A2 INCASSO` | la riga `stato:` dice `HTTP 200` o `link assente`; oggi dice `tier 2 — solo ordine via mail` (legge il flag, `empire/controllo.py:72`) | 0 |
| P6 | Il libro delle entrate esiste ed è vuoto (nessuna vendita finta prima) | `wc -c company/Memory/tesoreria/entrate.jsonl` | `0` (misurato 11/09) — se non è 0, aprire il file e verificare che ogni riga sia un euro vero | 0 |
| P7 | La decisione 1 di LANCI («vende o regala») è ancora aperta | `sed -n 309p PIANO-MAESTRO/29-ECOSISTEMA-LANCI/00-LEGGIMI.md` | la riga della decisione **1** senza ✅; scadenza 7 gg dal 05/09 → **12/09**, default `vendita` | 0 |
| P8 | Il gate di regressione dell'ospite `checkout.py` ha una base misurata | `python -m pytest empire/tests/test_seed.py -q` | **29 passed, 1 failed** (11/09): il fallito è `test_adr001_canonical_ecosystems_are_ten` — pre-esistente, è il perimetro 16-non-15 di E0.5 passo 2.10, **non** c'entra con checkout | 1 (per il test pre-esistente) |

---

## 2. I passi — numerati, ognuno con il comando esatto

### 2.1 — Il decreto di Max (Max, 5 minuti; se tace, il default)

Una riga in chat, o nel file della decisione, in questa forma esatta:

```
Manuale Claude Code: ruolo = vendita | regalo ; prezzo lancio = NN € ; listino = NN € ; bump = NN €
```

**Default se Max tace entro la scadenza di LANCI (12/09):** `vendita · 67 · 97 · 27` — la forma di
V2 §26 e V3 §11 decisione 8: *il Manuale a 67 € finché il team prezzi (B-003) non esiste; quando
nascerà, lo rivede lui, non annulla la vendita nel frattempo*. Il ruolo `vendita` è il default di
O1 di `03-FLUSSO-OFFERTA.md` («la strada da cui si torna indietro»): un prodotto venduto può
diventare regalo, un regalo non torna in vendita.

**È una sola decisione, non due.** B-002 (BACKLOG riga 10) e la decisione 1 di LANCI (riga 309)
sono la stessa domanda scritta in due posti: E0.9 la chiude una volta, e i due puntatori
rimandano alla stessa ADR (passo 2.7).

EMPERATOR scrive `company/Memory/decisions/ADR-030-prezzo-manuale-per-decreto.md` (**da creare in
questo passo**; il numero segue ADR-029, l'ultimo su disco l'11/09) con questo contenuto, i campi
`<…>` compilati dal decreto:

```
# ADR-030 — Prezzo e ruolo del Manuale Claude Code per decreto (default reversibile)

- Data: <AAAA-MM-GG> · Stato: ATTIVA · Scaglione: E0.9 (V4 doc 04)
- Decide: Max · Esegue: EMPERATOR

## Decisione
Ruolo: <vendita|regalo>. Prezzo di lancio <NN> EUR, listino <NN> EUR, bump <NN> EUR.
Scadenza del prezzo di lancio: <AAAA-MM-GG> (da checkout.config.json dopo E0a 2.3).
ruolo_scelto_per_silenzio: <true|false> (true se Max non ha risposto entro il 12/09).
ruolo_revisione_il: <AAAA-MM-GG> (default: 30 giorni dopo la data di questa ADR).

## Prezzi precedenti trovati (O2 del flusso offerta, obbligatorio)
| fonte | valore | data | perche' ci discostiamo |
| checkout.config.json | 67/97 EUR | <data commit del config: git log -1 --format=%ad -- "Crea siti/Siti CCM/checkout.config.json"> | e' il default |
| dossier Andrei Pascu §4 (cattura "Claude Code Mastery") | 397 EUR | 2026-09 | <motivo> |
| wiki (citata in 03-FLUSSO-OFFERTA.md O2) | 297-497 EUR | 2026-03/04 | <motivo> |

## Perche' non viola "il prezzo non si decide a mano" (B-002)
E' un default reversibile nella forma di V2 §26, non una decisione definitiva. Il team prezzi
(B-003) quando nasce lo rivede; nel frattempo la vendita non si annulla.

## Conseguenze
- E0.9 allinea config, manuale.html e Payment Link a questo numero.
- Il fusibile R5 (V3 §12) legge entrate.jsonl con motore=corsi e stato=incassato.
- Chiude B-002 (BACKLOG) e la decisione 1 di 29-ECOSISTEMA-LANCI/00-LEGGIMI.md.
```

### 2.2 — Allineare il prezzo nel config e nella pagina (EMPERATOR, 1 minuto se default; 30-45 minuti se cambia)

**Caso A — il decreto conferma 67/97/27:** nessun edit. P1 e P2 lo provano già. Si passa a 2.3.

**Caso B — il decreto cambia almeno un numero.** Oggi non esiste un comando: **specifica di comando
da costruire in questo passo**, dentro `empire/tools/checkout.py`:

- **Nome:** `--prezzo LANCIO LISTINO BUMP` (tre interi in euro, es. `--prezzo 47 97 27`).
- **Cosa fa:** valida che siano interi positivi e `LANCIO <= LISTINO`; scrive `prezzo_lancio_eur`,
  `prezzo_listino_eur`, `bump_eur` in `checkout.config.json`; lancia `run_apply` (che già
  propaga il JSON in `pagamento.html`); in più `render_manuale` (`checkout.py:175`) riceve due
  sostituzioni nuove, con regex nella forma delle tre già presenti (`checkout.py:170-172`):
  `(<span id="price-display"[^>]*>€)\d+(</span>)` → `prezzo_lancio_eur` e
  `(<span id="original-price-display"[^>]*>€)\d+(</span>)` → `prezzo_listino_eur`.
- **Output:** `prezzo -> lancio NN / listino NN / bump NN` + le righe di `--apply`.
- **Exit code:** 0 se scritto e propagato; 1 su valori non validi (`CheckoutConfigError`, stessa
  classe usata da `_valida_link_stripe`, `checkout.py:367`).
- **Contratto d'innesto (V3 §4):** file ospite `empire/tools/checkout.py` — vivo (importato da
  `empire/tests/test_seed.py`; nessun altro `.py` lo importa, `grep -rln "tools.checkout" --include=*.py .`).
  Politica: **fail-closed** (nessuna scrittura parziale: prima si valida tutto, poi si scrive il
  config, poi `--apply`; se `--apply` fallisce il config resta scritto e `--check` lo segnala).
  Gate di regressione dell'ospite: `python -m pytest empire/tests/test_seed.py -q` → **29 passed
  come prima** (il fallito pre-esistente resta 1, P8). Rientro: `git checkout -- empire/tools/checkout.py "Crea siti/Siti CCM/"`.
  Migrazione chiamanti: nessuna (comando nuovo, nessun chiamante).
- Poi: `python empire/tools/checkout.py --prezzo <LANCIO> <LISTINO> <BUMP>` e `--check`.

### 2.3 — La riga «prezzo di lancio fino al …» smette di essere testo fisso (EMPERATOR, 10 minuti)

`manuale.html:283` oggi: `<p class="text-xs text-[#FF5A26] font-semibold mb-6 uppercase
tracking-wider">prezzo di lancio fino al 31/07</p>`. Si aggiunge a `render_manuale` una quarta
sostituzione: `(prezzo di lancio fino al )\d{2}/\d{2}` → `GG/MM` da `config["scadenza_lancio"]`
(stessa struttura delle regex a `checkout.py:170-172`). Poi:

```
python empire/tools/checkout.py --apply
grep -n "prezzo di lancio fino al" "Crea siti/Siti CCM/manuale.html"
```
Atteso: la riga 283 porta la data di `scadenza_lancio` (quella messa da E0a 2.3, es. `15/10`).
Se E0a 2.3 non è ancora stata fatta, la riga resta `31/07` **e lo si scrive** — non si sposta la
scadenza da qui (è un passo di E0a, un solo proprietario per file: L6).

Contratto d'innesto: lo stesso di 2.2 (stesso file, stessa funzione, stesso gate di regressione,
stesso rientro). Fail-open sul testo: se la regex non trova la riga, `--apply` non fallisce e
`--check` non lo vede — per questo il gate G1 di §3 controlla la riga con `grep`.

### 2.4 — Il Payment Link porta lo stesso prezzo (Max 2 minuti; poi il gate HTTP) — **dipende da E0a ed E0.5 2.8**

1. Max apre `dashboard.stripe.com` → Payment Links → il link **base** creato in E0a 2.1: il prezzo
   mostrato deve essere `prezzo_lancio_eur`,00 €; il **bump** `bump_eur`,00 €.
2. **Se il decreto ha cambiato il prezzo (caso B di 2.2):** un Payment Link Stripe non cambia
   prezzo — Max ne crea uno nuovo con il prezzo nuovo (stessa procedura di E0a 2.1), **disattiva**
   il vecchio (Stripe: «Deactivate»), e EMPERATOR rilancia
   `python empire/tools/checkout.py --accendi-stripe "https://buy.stripe.com/<BASE-NUOVO>" "https://buy.stripe.com/<BUMP>"`.
   Il vecchio disattivato risponde 404/410: è esattamente ciò che il gate HTTP di E0.5 2.8
   rileva — se qualcuno dimentica il rilancio, INCASSO torna rosso da solo.
3. Verifica a macchina che i link rispondano: `python -m empire controllo | grep -A1 INCASSO` →
   `HTTP 200` su entrambi (**dopo** E0.5 2.8). Se E0.5 non è ancora chiusa: `curl -sI
   "https://buy.stripe.com/<BASE>" | head -1` → `HTTP/2 200`, e nel checkpoint si scrive
   «verificato con curl a mano, non col gate: il gate arriva con E0.5 2.8» — ammesso una volta,
   non due (V3 §2: «a mano e lo si dichiara» è il caso che ha generato E0b, non lo si ripete).
4. **Il prezzo sul Payment Link non è verificabile a macchina** da questo repository: leggerlo
   richiede l'API Stripe con una chiave segreta che non esiste nel perimetro (nessun `STRIPE_`
   in nessun `.env`: `grep -rl "STRIPE_SECRET\|sk_live\|sk_test" --include=.env* . 2>/dev/null`
   → vuoto, 11/09). Esito: **NON VALUTATO a macchina**, Max lo dichiara nel checkpoint con la
   frase «Payment Link base a NN €, bump a NN €, letti su Stripe il <data>». Non entra nel
   denominatore del gate (V3 §7).

### 2.5 — Il blocco di descrizione: i 16 video Claude puntano al prodotto (EMPERATOR 10 minuti; l'incollaggio è di E8)

Si crea **un solo file sorgente**, così E8 copia e non riscrive: `Crea siti/Siti CCM/blocco-descrizione-video.txt`
(**da creare in questo passo**), con esattamente questo testo (l'URL è `rails.stripe_base.url`
dal config, mai scritto a mano):

```
---
Il Manuale Claude Code (PDF completo, prezzo di lancio NN EUR fino al GG/MM):
https://buy.stripe.com/<BASE>
Domande: max.infoproducer@gmail.com
---
```

Comando che lo genera dal config (così NN, GG/MM e l'URL non divergono mai dalla pagina):

```
python -c "import json;c=json.load(open('Crea siti/Siti CCM/checkout.config.json'));u=c['rails']['stripe_base']['url'];assert u.startswith('https://buy.stripe.com/'),'link base assente: prima E0a';s=c['scadenza_lancio'];open('Crea siti/Siti CCM/blocco-descrizione-video.txt','w',encoding='utf-8').write('---\nIl Manuale Claude Code (PDF completo, prezzo di lancio %d EUR fino al %s/%s):\n%s\nDomande: max.infoproducer@gmail.com\n---\n'%(c['prezzo_lancio_eur'],s[8:10],s[5:7],u))"
```
Se il link base non c'è ancora (E0a aperta), l'`assert` ferma il comando: **fail-closed sul
blocco** — meglio nessun blocco che un blocco senza link.

**Dove va il blocco, per pezzo (fail-open: un video senza blocco non ferma gli altri):**

| Pezzi | Quanti | Cosa si fa | Esito atteso |
|---|---|---|---|
| I 16 `.mp4` in `Lancio corso skill beast/Page/Leo/da pubblicare` (Claude Code / skill beast) | 16 | E8 costruisce il kit (titolo, descrizione, tag) e **appende il blocco in coda alla descrizione** di ognuno, prima dell'upload | `grep -c` tende a **16** |
| I 7 `VIDEO-PRONTI/video-01…07` (Legami d'Amore) | 7 | **niente**: il pubblico è un altro (P: `head -3 …/copy.md` → `@PsicologiaFemminile-f8c`) | **NON VALUTATO** — motivo «nessun prodotto del canale Legami d'Amore da collegare» |
| I 3 libri KDP | 3 | niente: hanno il loro prezzo su Amazon (E8) | fuori perimetro |

**Voce nuova in `company/Memory/BACKLOG.md`** (le voci da B-058 in poi sono paragrafi puntati in
coda, non righe di tabella — forma di `BACKLOG.md:393`; numero = ultimo + 1, l'11/09 l'ultimo è
B-067): `- **B-068 — NUOVA (<data>).** Legami d'Amore: 7 video pronti in VIDEO-PRONTI/ e nessun
prodotto/lead magnet del canale a cui collegarli. E0.9 (V4 doc 04) li ha dichiarati NON VALUTATO;
decidere se e come il canale monetizza (AdSense / affiliazione / prodotto proprio). Dopo E8.`

### 2.6 — La prima riga vera in `entrate.jsonl` — **solo quando arriva una vendita, mai prima**

Il comando reale, con i campi reali (`python scripts/tesoreria.py entrata --help`, 11/09):

```
python scripts/tesoreria.py entrata --importo 67 --da "<email cliente da Stripe>" --per corsi --stato incassato --nota "Manuale Claude Code, Stripe <id pagamento pi_...>"
```

Scrive in `company/Memory/tesoreria/entrate.jsonl` un record con `motore: "corsi"` e
`stato: "incassato"` (`scripts/tesoreria.py:128-138`). **È questa la coppia che il fusibile R5 legge**
— non `tipo: vendita` (V3 §12 va letto così; corretto qui, REGOLA PUNTATORI). Il bump si registra
come **seconda riga** (`--importo 27 … --nota "bump, stesso ordine <id>"`), non sommato: due
prodotti, due righe, così il rapporto (`tesoreria.py report`) distingue quante volte il bump si
prende.

Non si registra un `previsto` per «la vendita che ci aspettiamo»: E0.5 passo 2.2 lo dice già —
si accende il libro con un euro vero o non lo si accende.

### 2.7 — Chiudere i due puntatori alla decisione (EMPERATOR, 2 minuti)

- `company/Memory/BACKLOG.md` riga 10, colonna stato: `⬜` → `✅ default per decreto: ADR-030 (E0.9); il team prezzi (B-003) rivede`.
- `PIANO-MAESTRO/29-ECOSISTEMA-LANCI/00-LEGGIMI.md` riga 309, prima cella: il numero **1** in
  grassetto diventa barrato (la stessa sintassi che la riga 310 usa per la decisione 2 chiusa) e
  nella cella «Decisione» in testa: `✅ **<data> — ADR-030**, default \`vendita\``.
- Se `ruolo_scelto_per_silenzio: true`: in cima a `STATO-EMPIRE.md` una riga ADR-026 «decisione
  presa per silenzio, revocabile fino al <ruolo_revisione_il>» — così Max la vede, come O1 impone
  («si avvisa»).

---

## 3. Il gate — comando, condizione di fallimento, exit code (L9)

```
# G1 - un solo prezzo: config == manuale.html (price-display, original-price-display, riga "fino al")
python -c "import json,re,sys;c=json.load(open('Crea siti/Siti CCM/checkout.config.json'));h=open('Crea siti/Siti CCM/manuale.html',encoding='utf-8').read();p=re.search(r'id=\"price-display\"[^>]*>\D*(\d+)',h);l=re.search(r'id=\"original-price-display\"[^>]*>\D*(\d+)',h);f=re.search(r'prezzo di lancio fino al (\d{2})/(\d{2})',h);s=c['scadenza_lancio'];ok=p and l and f and int(p.group(1))==c['prezzo_lancio_eur'] and int(l.group(1))==c['prezzo_listino_eur'] and f.group(1)==s[8:10] and f.group(2)==s[5:7];print('G1',('PASSA' if ok else 'NON PASSA'),p and p.group(1),l and l.group(1),f and f.group(0),s);sys.exit(0 if ok else 1)"
#   exit 0 = i tre numeri della pagina sono quelli del config · exit 1 = divergono (fail-closed: non si va avanti)

# G2 - la decisione esiste ed e' compilata (nessun segnaposto)
python -c "import sys,io;t=io.open('company/Memory/decisions/ADR-030-prezzo-manuale-per-decreto.md',encoding='utf-8').read();sys.exit(0 if '<' not in t.split('## Prezzi precedenti')[0] and 'Ruolo:' in t else 1)"
#   exit 0 = ADR-030 presente, nessun "<...>" nella sezione Decisione · exit 1 = manca o incompleta

# G3 - i due link rispondono (dopo E0.5 2.8; prima: curl a mano, dichiarato una volta)
python -m empire controllo | grep -A1 INCASSO | grep -q "HTTP 200" ; echo "G3 exit $?"
#   exit 0 = entrambi 200 · exit 1 = link assente / 404 / 410 / gate HTTP non ancora costruito

# G4 - il blocco di descrizione esiste e porta l'URL vero del config
python -c "import json,sys;c=json.load(open('Crea siti/Siti CCM/checkout.config.json'));u=c['rails']['stripe_base']['url'];t=open('Crea siti/Siti CCM/blocco-descrizione-video.txt',encoding='utf-8').read();sys.exit(0 if u and u in t and str(c['prezzo_lancio_eur']) in t else 1)"
#   exit 0 = blocco coerente col config · exit 1 = manca, o URL/prezzo diversi

# G5 - il fusibile R5 e' armato con la coppia giusta (informativo: stampa, non blocca E0.9)
python -c "import json;n=sum(1 for r in open('company/Memory/tesoreria/entrate.jsonl',encoding='utf-8') if r.strip() and json.loads(r).get('motore')=='corsi' and json.loads(r).get('stato')=='incassato');print('vendite Manuale incassate:',n)"
#   stampa 0 oggi; e' l'esito di mercato che R5 riporta a fine E3, non un gate di E0.9

# G6 - i due puntatori chiusi
grep -q "ADR-030" company/Memory/BACKLOG.md && grep -q "ADR-030" PIANO-MAESTRO/29-ECOSISTEMA-LANCI/00-LEGGIMI.md ; echo "G6 exit $?"
#   exit 0 = B-002 e la decisione 1 di LANCI rimandano alla stessa ADR · exit 1 = un puntatore e' rimasto aperto
```

**Condizione di chiusura di E0.9:** G1 **e** G2 **e** G4 **e** G6 a exit 0, **più** G3 (che aspetta
E0.5 2.8: finché non esiste, E0.9 si chiude «con G3 verificato a mano» scritto nel checkpoint, e
si riapre G3 come primo comando del cruscotto il giorno che E0.5 chiude). Il prezzo sul Payment
Link è **NON VALUTATO** a macchina (2.4 punto 4) e Max lo dichiara: non è nel denominatore.

---

## 4. Politica di guasto e piano di rientro

| Guasto | Politica | Cosa si fa | Cosa si dichiara |
|---|---|---|---|
| Max non decreta entro il 12/09 | non è un guasto: è il default | `vendita · 67 · 97 · 27`, `ruolo_scelto_per_silenzio: true`, `ruolo_revisione_il` a +30 gg; riga ADR-026 in cima a STATO-EMPIRE | «prezzo per silenzio, revocabile fino al <data>» |
| Il decreto cambia il prezzo e il comando `--prezzo` non nasce oggi | fail-closed sul tier | il config **non** si tocca a mano (due prezzi in giro sono peggio di uno vecchio); la pagina resta al prezzo di prima; E0.9 resta aperta sul passo 2.2 | «E0.9 ferma su 2.2 (comando da costruire): E8 e tutto il resto procedono» |
| `--prezzo` o `--apply` scrivono male la pagina (regex che non trova, G1 rosso) | fail-closed | `git checkout -- empire/tools/checkout.py "Crea siti/Siti CCM/"`, si corregge la regex, si rilancia `--apply`, G1 | nel checkpoint, con la riga di `manuale.html` che ha ingannato la regex |
| Stripe: il link nuovo nasce ma il vecchio resta attivo | fail-open sul vecchio | il vecchio incassa al prezzo vecchio finché Max non lo disattiva: si registra comunque in `entrate.jsonl` con la nota «prezzo precedente, link non disattivato» | nel checkpoint + riga BACKLOG se resta aperto più di un giorno |
| Il gate HTTP di E0.5 2.8 non è ancora costruito | non è un guasto di E0.9 | `curl -sI` a mano, **una** volta, dichiarato; G3 si riapre a E0.5 chiusa | «G3 a mano, gate arriva con E0.5» |
| `blocco-descrizione-video.txt` generato prima dei link (E0a aperta) | fail-closed | l'`assert` di 2.5 ferma la generazione; si rilancia dopo E0a | niente: il file non nasce |
| Una vendita arriva e nessuno la registra | il danno peggiore di E0.9 | il rapporto Stripe (dashboard → Payments) è la fonte: si registra a posteriori con `--data AAAA-MM-GG` del pagamento vero | nel checkpoint: quante righe recuperate |

**Il danno peggiore possibile di E0.9:** un cliente che paga un prezzo diverso da quello scritto
sulla pagina (config ≠ Stripe). È per questo che il prezzo è fail-closed e il tier non sale finché
G1 non passa e Max non ha letto il numero su Stripe. Il secondo danno: un video Legami d'Amore con
un link a un manuale di programmazione — evitato per costruzione (2.5: i 7 non ricevono il blocco).

---

## 5. Le forze — chi fa cosa, e il prompt d'ingaggio già scritto

**Nessuna forza.** Il decreto è di Max (5 minuti), Stripe è di Max (2 minuti); i comandi 2.2, 2.3,
2.5, 2.7 li lancia EMPERATOR in prima persona: sono 40-60 minuti di lavoro su un solo file vivo
(`checkout.py`), con un gate di regressione che gira in 5 secondi. Delegare a uno scagnozzo un
edit di due regex su un file che protegge la cassa costa più in verifica di quanto risparmi.

**Se in futuro il comando `--prezzo` diventa più grosso** (rail multipli, valute): allora sì, una
sentinella sonnet con perimetro «solo `empire/tools/checkout.py` + `empire/tests/test_seed.py`,
un test per comando, scrittura incrementale: prima il test, poi la funzione, salva, poi il
prossimo». Oggi no.

---

## 6. Cosa si scrive in Memory alla chiusura

- **Checkpoint:** `python scripts/checkpoint.py cp --titolo "E0.9 chiusa: Manuale a NN EUR per decreto (ADR-030), prezzo unico config/pagina/Stripe, blocco descrizione per i 16 video Claude"` — dentro: il decreto testuale, `ruolo_scelto_per_silenzio`, i prezzi precedenti messi sul tavolo (397 / 297-497 / 67), la frase di Max sul prezzo letto su Stripe, l'esito di G3 (gate o curl), i 7 video NON VALUTATO col motivo, B-068.
- **Riga per `STATO-EMPIRE.md`** (in cima): `## ✅ <data> — E0.9: il Manuale ha un prezzo (NN EUR, ADR-030) e un solo numero in tre posti; 16 video Claude puntano al prodotto — CP-...` + se per silenzio, la riga ADR-026 sopra.
- **ADR:** `ADR-030-prezzo-manuale-per-decreto.md` (passo 2.1). Nessun'altra.
- **BACKLOG:** B-002 → ✅ (rimanda ad ADR-030) · B-068 nuova (Legami d'Amore senza prodotto) · se `--prezzo` non nasce: B-069 «`checkout.py --prezzo` da costruire, specifica in V4 doc 04 passo 2.2».
- **I sei numeri del cruscotto (V2 §23), prima e dopo:**
  - `controllo x/6`: **invariato** rispetto a E0a (INCASSO era già verde per flag dopo E0a; E0.9 non aggiunge un gate a `controllo`, cambia *cosa c'è dietro* il link)
  - `OPERATIVO %`: **61/439 = 13,9%** (invariato: nessun agente toccato)
  - `tracce (origine=hook)`: **0** (invariato)
  - `byte entrate.jsonl`: **0** prima → **0** dopo, salvo vendita vera (si scrive il numero reale, mai quello sperato)
  - `pezzi fermi (ultimo_metro)`: **invariato** da E0a (E0.9 non carica: prepara il blocco che E8 incolla)
  - `vivo%`: non calcolabile (il comando nasce in E0.5)
