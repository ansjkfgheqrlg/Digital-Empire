# 02 — E0.5: GLI AGGANCI A COSTO ZERO

> Chi: **EMPERATOR** · Ore V3: 4-6 · Tetto: 9 · Dipende da: **nessuno** · Percorsi in scrittura:
> `scripts/checkpoint.py`, `scripts/ultimo_metro.py`, `scripts/gate_rotazione.py` (nuovo),
> `empire/` (nuovo sottocomando `vivo`, riga in `controllo`), `company/Memory/tesoreria/*.jsonl`,
> `company/runtime/`, `company/metrics/`, `marketing/handoffs/`, `platform/handoffs/`,
> `company/REGISTRO-IMPRESA.md`, `company/Ecosistemi/REGISTRO-NUMERI.md`,
> `EmpireDesk/modules/metrics.py`, `company/Ecosistemi/13-ARENA-APEX/orchestrator.py`,
> `SKILL & Agenti/Workflow pubblicazione automatica/` · Politica di guasto: **fail-closed per
> passo** (ogni passo ha il suo gate; un passo che fallisce non si dichiara fatto e non ferma
> gli altri nove — sono indipendenti)
> STATO: COMPLETO (EMPERATOR, 2026-09-11)

**Cosa fa:** i dieci agganci che i censimenti quotano in minuti e che V1 aveva messo dietro 50-70
ore di costruzione (V2 §21-E0.5): otto di V2, più due dall'Appendice B di V3 (sanare i numeri
degli ecosistemi, riparare `13-ARENA-APEX`). Tutti insieme sono un pomeriggio, e sbloccano il
cruscotto quotidiano (V2 §23), la Tesoreria, e il gate finale del piano (`empire vivo --json`).

**Tre cose che il disco ha smentito rispetto a V2/V3, e che questo documento corregge:**
1. `python -m empire trace` **esiste** (25 tracce, 4 sottocomandi) — V3 Appendice B lo dava
   assente. Il buco vero è il campo `origine` e i filtri: sono di F3, non di E0.5.
2. `scripts/ultimo_metro.py` **non ha `--json`** — V2 §21-E0 e §23 lo usano nei gate. Diventa il
   passo 2.9 qui (serve al cruscotto).
3. `company/REGISTRO-NUMERI.md` **non esiste a quel percorso**: è `company/Ecosistemi/
   REGISTRO-NUMERI.md`, e il doppio 08 è già dichiarato lì (righe 23-24: *«collide … non risolto,
   non bloccante, da sanare quando si tocca una delle due»*). Si tocca ora.

---

## 0. Cosa NON è bloccato da questo scaglione (ADR-028)

E0.5 non è prerequisito di E0a, E0.6, E0.7 (salvo il punto 2.7: `empire vivo` è letto dal
cruscotto, non dagli hook), E1, E2, E3.0, E3, F3. **Aspettano E0.5:** E0b (`gate_rotazione.py`,
passo 2.8) e E0.9 (il gate INCASSO via HTTP, passo 2.8).

Dentro E0.5, **i dieci passi sono indipendenti**: se il 2.7 (`empire vivo`, il più lungo) non
chiude oggi, gli altri nove chiudono lo stesso e si dichiara «E0.5 9/10, resta `vivo`».

---

## 1. Prerequisiti — verificati con un comando, non dichiarati

| # | Prerequisito | Comando | Atteso (misurato 2026-09-11) | Exit |
|---|---|---|---|---|
| P1 | La Tesoreria è costruita ma spenta | `ls -la company/Memory/tesoreria/` | `entrate.jsonl` **0 byte**, `spese.jsonl` **0 byte** (dal 3/9), `README.md` | 0 |
| P2 | `checkpoint.py cp` non ha `costi` | `grep -c "costi" scripts/checkpoint.py` | **0** | 0 |
| P3 | Le caselle mancano | `for d in company/runtime company/metrics marketing/handoffs platform/handoffs; do test -e "$d" && echo ESISTE $d \|\| echo MANCA $d; done` | **MANCA** tutte e quattro; `company/Memory/pubblicati.json` assente (lo crea `ultimo_metro --segna`) | — |
| P4 | Il motore libri è orfano del registro | `grep -c "libri-performanti" company/REGISTRO-IMPRESA.md` | **0** | 1 |
| P5 | `metrics.py` punta a una cartella con refuso | `sed -n 20p EmpireDesk/modules/metrics.py` | `"Workfolw crea caroselli à" / "carousel-factory"` — **la cartella esiste** (`ls -d`) ma è il motore vecchio, non `SKILL & Agenti/Workflow agency creative/caroselli - agency` (l'unico verificato, CP-20260806-005) | 0 |
| P6 | Il publisher muore all'import | `cd "SKILL & Agenti/Workflow pubblicazione automatica" && python -c "import main_orchestrator"` | `OpenAIError: Missing credentials` (V2 §8) — **da riverificare in questo passo: dopo E0b la chiave sarà nuova** | 1 |
| P7 | `empire vivo` non esiste | `python -m empire vivo --help` | `invalid choice: 'vivo'` | 2 |
| P8 | Sedici cartelle, un numero doppio | `ls company/Ecosistemi/ \| grep -c "^[0-9]"` · `ls -d company/Ecosistemi/08-*` | **16** · `08-INTELLIGENCE` e `08-STREAM-S7-BOT` | 0 |
| P9 | `13-ARENA-APEX` è rotta | `python company/Ecosistemi/13-ARENA-APEX/orchestrator.py --help` | `UnicodeEncodeError: 'charmap' codec can't encode character '\u2192'` | 1 |
| P10 | `trace` esiste, `origine` no | `python -m empire trace stato` · `grep -c "origine" empire/trace.py` | `totale: 25` · **0** | 0 |

---

## 2. I passi — numerati, ognuno con il comando esatto

### 2.1 — La chiave nel `.env` del publisher + istanziazione dentro una funzione (1 h)

**Ordine reale:** questo passo si chiude **dopo E0b** (la chiave OpenRouter nuova), ma la parte
di codice si fa ora. In `SKILL & Agenti/Workflow pubblicazione automatica/main_orchestrator.py`:
1. Trovare la riga che istanzia il client a livello di modulo: `grep -n "OpenAI(\|OpenRouter(\|api_key" main_orchestrator.py`.
2. Spostare l'istanziazione dentro la funzione che la usa (o in un `def _client():` con cache):
   così `import main_orchestrator` non muore se la chiave manca — muore la **chiamata**, con un
   messaggio che dice quale variabile manca (stesso pattern già applicato a `config.py` di
   caroselli in CP-20260909-HV8Q: *«una credenziale mancante ferma il programma dicendo quale
   variabile manca»*).
3. Nel `.env` accanto: la riga `OPENROUTER_API_KEY=` resta vuota finché E0b non consegna la
   chiave nuova.
**Contratto d'innesto:** ospite = il publisher (V2 §8: `pubblica.py` e `ig_carousel_publish.py`
già verificati funzionanti, dry-run di default). Gate di regressione: `python pubblica.py
--dry-run` (verificare il nome del flag con `--help`) → exit 0 prima e dopo. Rientro: `git
checkout -- main_orchestrator.py`. Politica: fail-closed (un import che muore è meglio di un
publisher che parte con chiave vuota e stampa PASS finti — la Legge Suprema).

### 2.2 — La prima riga in `entrate.jsonl` (1 riga)

Non si inventa un'entrata. Si registra la prima **vera** — o, se non ce n'è una entro la chiusura
di E0.5, si registra la **spesa** vera più recente in `spese.jsonl` (un abbonamento, un credito
API) con il comando reale:
```
python scripts/tesoreria.py spesa --help      # gli argomenti reali, prima di scrivere
python scripts/tesoreria.py spesa <argomenti>  # una spesa vera, con la prova (fattura/estratto)
```
La regola dei soldi (`registro.yaml:1830`, V2 §9): *ogni euro nasce qui e sale in Tesoreria*.
**Attenzione al fusibile R5 (V3 §2/§12):** una riga di spesa NON conta come vendita — R5 legge
`tipo: vendita`, non «file non vuoto». Qui si accende il libro, non si dichiara un incasso.

### 2.3 — Il campo `costi` obbligatorio in `scripts/checkpoint.py` (1 h) — CON FINESTRA

**Edit in `scripts/checkpoint.py:382`** (il parser del sottocomando `cp`):
```
prima:
    k = sub.add_parser("cp", help="conia un CP di lavoro con codice irripetibile")
    k.add_argument("--titolo", required=True)
dopo:
    k = sub.add_parser("cp", help="conia un CP di lavoro con codice irripetibile")
    k.add_argument("--titolo", required=True)
    k.add_argument("--costi", default=None,
                   help="costo del task: token/EUR/minuti, es. '250k token, 0 EUR, 40 min'")
```
e in `nuovo_cp(a.titolo)` → `nuovo_cp(a.titolo, costi=a.costi)`, con `nuovo_cp` che scrive la
riga `- **Costi:** <valore>` nel template del CP (oggi è un segnaposto che 303 checkpoint hanno
lasciato vuoto, V2 §21-E0.5 punto 3).

**Il contratto d'innesto (V3 §4), compilato — è il caso-scuola:**
- **Migrazione dei chiamanti:** `checkpoint.py cp` è girato 303 volte ed è **obbligatorio per
  ogni sessione** (CLAUDE.md REGOLA ZERO). Il giorno dell'innesto, una chat di Gael o di Max
  aperta ieri chiude un task col vecchio comando. **Finestra di 3 giorni:** `--costi` assente →
  il comando **accetta**, stampa `AVVISO: manca --costi, dal <data+3gg> sara' obbligatorio`, e
  scrive `- **Costi:** (non dichiarato)`. Dal quarto giorno: `required=True`. La data si scrive
  nel codice, non a memoria.
- **Avviso alle sessioni parallele:** riga ⚠️ in cima a `STATO-EMPIRE.md` il giorno stesso:
  `checkpoint.py cp: da <data> serve --costi — per 3 giorni solo avviso`.
- **Gate di regressione:** `python scripts/checkpoint.py cp --titolo "prova innesto"` → conia
  normalmente, con l'avviso; `python scripts/test_checkpoint.py` se esiste (verificare con `ls
  scripts/test_*`) → verde come prima.
- **Rientro:** `git checkout -- scripts/checkpoint.py`. Politica: fail-open per 3 giorni
  (avviso), poi fail-closed (rifiuto) — motivato: un checkpoint senza costo è un dato mancante;
  un checkpoint **non scritto** perché il comando ha rifiutato una sessione ignara è un task
  perso per la Memory, il danno peggiore dei due.

### 2.4 — Le cinque caselle create (30 min)

```
mkdir -p company/runtime company/metrics marketing/handoffs/log platform/handoffs/log
```
- `company/Memory/pubblicati.json` **non si crea a mano**: lo crea `ultimo_metro.py --segna` al
  primo uso (E0a 2.4); crearlo vuoto a mano cambierebbe il ramo `errore_lettura` (riga 79).
- In ogni cartella nuova un `README.md` di 3 righe: chi scrive qui, chi legge, quale hook la
  svuota (LC-1: la casella senza consumatore è la casella di Osservabilità, V2 §5 causa 2). Le 5
  Guild e 5 Sentinelle che dichiarano deliverable in `company/runtime/` (V2 §5) restano com'erano:
  ora hanno dove finire.
- Gate: `test -d company/runtime && test -d company/metrics && test -d marketing/handoffs/log
  && test -d platform/handoffs/log` → exit 0.

### 2.5 — Il motore libri nel registro + i due gusci marcati ARCHIVIO (30 min)

1. In `company/REGISTRO-IMPRESA.md`, nella tabella dei motori/workflow: una riga per
   `company/Ecosistemi/02-INFO-BUSINESS/Workflow/libri-performanti-multiagente/` — proprietario
   (Gael, esecutore KDP — è una **riga di anagrafe**, non un compito: questo passo **non dipende
   da Gael**, INV-GAEL, lo scrive EMPERATOR), controllore (EMPERATOR), origine (FORGE, CP-20260803-006), governo
   (Mandato: INFO-BUSINESS), stato VIVO (`python -m engine.kdp --help` → exit 0).
2. In `company/skills-map.yaml`: le due voci `Workflow-libri/` (ferma da 169 gg) e `KDP -
   prodottti digitali/` (151 gg) prendono `stato: ARCHIVIO` + `ragione: guscio senza codice,
   sostituito da libri-performanti-multiagente` + `archivio_origine: true` (L8: ARCHIVIO è uno
   stato, con ragione e puntatore — mai cancellare, L1).
3. Gate: `grep -c "libri-performanti-multiagente" company/REGISTRO-IMPRESA.md` → ≥ 1 ·
   `grep -B2 -A3 "Workflow-libri" company/skills-map.yaml | grep -c ARCHIVIO` → 1.

### 2.6 — `metrics.py` di EmpireDesk sulla cartella caroselli giusta (30 min)

**Edit in `EmpireDesk/modules/metrics.py:20`:**
```
prima:  "caroselli_factory": REPO_ROOT / "Workfolw crea caroselli à" / "carousel-factory",
dopo:   "caroselli_factory": REPO_ROOT / "SKILL & Agenti" / "Workflow agency creative" / "caroselli - agency",
```
**Nota di verità:** la cartella col refuso **esiste** (`ls -d` → sì); non è un percorso rotto, è
il motore sbagliato — CP-20260803-008 ha stabilito che *il motore giusto è ArenaAI
(`caroselli - agency/`), non carousel-factory*. Prima di cambiare: `python -c "from
EmpireDesk.modules import metrics; print(metrics._caroselli(metrics.SOURCES['caroselli_factory']))"`
sui due percorsi, e si scrive nel checkpoint quanti caroselli conta l'uno e l'altro.
Contratto d'innesto: ospite = EmpireDesk (VIVO, 4 motori agganciati). Gate di regressione: il
selftest di EmpireDesk (`python EmpireDesk/selftest.py` o equivalente — verificare con `ls
EmpireDesk/*.py`) → verde. Rientro: `git checkout -- EmpireDesk/modules/metrics.py`.

### 2.7 — `python -m empire vivo --json` — SPECIFICA DEL COMANDO DA COSTRUIRE (2-3 h)

**Dove si registra:** come gli altri 25 sottocomandi di `python -m empire` (aprire `empire/cli.py`
o `empire/__main__.py`: il parser con `sub.add_parser(...)`; `vivo` si aggiunge accanto a
`controllo`, che è il modello: legge stato, stampa, non scrive).

**Argomenti:** `--json` (obbligatorio per il cruscotto; senza, tabella leggibile) · `--nodo <id>`
(un solo nodo) · `--classe ecosistema|motore|organo` (filtro).

**Cosa legge:** l'anagrafe **dinamica** (V3 §3): tutti i nodi delle classi censite registrati
alla data del calcolo — `company/Ecosistemi/*/` per gli ecosistemi (16 oggi, dopo 2.8 saranno
16 con numeri unici), `dati/censimento-04b-motori.md` §tabella per i 25 motori finché non
esiste un registro-motori (deliverable di E5c), `dati/censimento-01b-organi.md` per i 14 organi.
**Non** una lista scritta nel codice.

**Cosa calcola, per nodo:** le otto condizioni V-a..V-d, C-a..C-d (V2 §4) — ognuna con **tre
esiti**: `PASSA | NON_PASSA | NON_VALUTATO`, e per ogni `NON_VALUTATO` la stringa `perche`
(obbligatoria: senza, il comando rifiuta di stampare quel nodo — è la regola di V3 §7 corretta
dall'Appendice B). Il primo riempimento è l'**Appendice B di V3**: i 120 esiti misurati il
2026-09-10 diventano il file `dati/vivo-baseline.json` che il comando legge come stato iniziale
(`{"nodo": "15-LANCI", "V-a": "PASSA", ..., "C-d": {"esito": "NON_VALUTATO", "perche": "un
consumatore interno conta come reale? — definizione da V4"}}`).

**Output `--json`:**
```
{
  "data": "2026-09-11",
  "denominatore": <nodi censiti - nodi ARCHIVIO - nodi con almeno un NON_VALUTATO>,
  "numeratore": <nodi con 8/8 PASSA>,
  "percentuale": <numeratore/denominatore, 1 decimale>,
  "archivio": [ "08-STREAM-S7-BOT", "12-STREAM-S7-BOT", ... ],     (L8, decisione 5 V2 §26)
  "non_valutati": [ {"nodo": "...", "condizione": "C-d", "perche": "..."} ],
  "nodi": [ { "nodo": "...", "classe": "...", "esiti": {...}, "chiuso": false } ]
}
```
**Exit code:** 0 se il calcolo è riuscito (anche con percentuale 0); 1 se un nodo `NON_VALUTATO`
non ha `perche`; 2 se l'anagrafe non è leggibile.

**Test contro lo scheletro vuoto (H4):** con `dati/vivo-baseline.json` vuoto il comando deve
stampare `denominatore: 0` e **exit 2**, non `100%`.

**Gate del passo:** `python -m empire vivo --json | python -c "import sys,json;d=json.load(sys.stdin);print(d['numeratore'],'/',d['denominatore'],'non_valutati:',len(d['non_valutati']))"`
→ atteso oggi **0 / <denominatore>, non_valutati: 6** (i sei dell'Appendice B, ognuno col perché).

### 2.8 — `scripts/gate_rotazione.py` e il gate INCASSO via HTTP (1-2 h)

**`scripts/gate_rotazione.py` — specifica:** per ciascuno dei tre servizi (Brevo, OpenRouter,
Instagram) legge la chiave **vecchia** da un file fuori repo indicato con `--vecchie <path>`
(mai nel repo, mai in chat) e la **nuova** dal `.env` corrispondente; fa una chiamata in sola
lettura al servizio (endpoint di `me`/account) con entrambe; **PASSA** se vecchia → 401 e nuova →
200; stampa una riga per servizio; exit 0 solo se 3/3. Nessun log della chiave: solo le prime 4
cifre. È il gate di `03-E0b-ROTAZIONE.md`.

**Gate INCASSO via HTTP — edit in `empire/controllo` (trovare il modulo con `grep -rn
"stripe_base" empire/`):** la riga INCASSO smette di leggere `rails.stripe_base.attivo` e fa
`HEAD` sui due URL del config; verde solo se entrambi rispondono 200 (un Payment Link Stripe
disattivato risponde 404/410). Se `url` è vuoto → rosso con motivo `link assente`. È C2 R-18
[FATALE] chiuso per davvero (V2 §21-E0 nota).

### 2.9 — `ultimo_metro.py --json` (30 min) — mancava, e il cruscotto lo pretende

In `scripts/ultimo_metro.py:379` (l'argparse): `ap.add_argument("--json", action="store_true")`;
in `main`, se `--json`: `print(json.dumps({"data":..., "fermi": len(trovati), "caricabili": n,
"manca_un_pezzo": m, "piu_vecchio_gg": ..., "pezzi": trovati}, ensure_ascii=False))` e
**nessun'altra stampa** (il cruscotto fa `| jq`). Gate: `python scripts/ultimo_metro.py --json |
python -c "import sys,json;print(json.load(sys.stdin)['caricabili'])"` → un intero.

### 2.10 — Sanare i numeri degli ecosistemi (30 min) — PRIMA di 2.7

`company/Ecosistemi/REGISTRO-NUMERI.md` righe 23-24 e 28 dichiarano: `08` occupato due volte
(`08-INTELLIGENCE`, `08-STREAM-S7-BOT`), e `12-STREAM-S7-BOT` è la stessa cosa di
`08-STREAM-S7-BOT`. Decisione 5 di V2 §26 (default): **08 e 12 STREAM-S7 in ARCHIVIO con
onore**. Quindi:
1. `08-STREAM-S7-BOT` → è l'**archivio d'origine** del bot Solana (la «cartella vuota che vuota
   non era», L1 — 00-LEGGIMI.md §2): resta dov'è, ma nel registro prende `stato: ARCHIVIO`,
   `ragione: archivio d'origine di 12-STREAM-S7-BOT`, `fuori_dal_denominatore: true`.
2. `08` resta a `INTELLIGENCE` (unico). La riga 24 del registro si riscrive: `| 08 | STREAM-S7-BOT
   | ARCHIVIO — archivio d'origine di 12, fuori dal denominatore (ADR-025 decisione 5, E0.5) |`.
3. **Nessun `git mv`**: la cartella non si sposta (L1, e altre sessioni potrebbero puntarci —
   REGOLA PUNTATORI). Cambia lo stato nel registro, non il percorso.
4. Gate: `python -c "import re;t=open('company/Ecosistemi/REGISTRO-NUMERI.md',encoding='utf-8').read();print(t.count('| 08 |'))"`
   → 2 righe, di cui una con `ARCHIVIO`; e `empire vivo --json | jq '.archivio'` contiene
   `08-STREAM-S7-BOT`.

### 2.11 — `13-ARENA-APEX/orchestrator.py`: il fix di tre righe (10 min)

L'errore (P9): `UnicodeEncodeError` su `→` (U+2192) stampato su console cp1252. Il fix **esiste
già** in `company/Ecosistemi/11-APEX-7-CORE/main.py:12-14`:
```
if hasattr(sys.stdout, "reconfigure"):
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")
```
Si copiano queste tre righe in testa a `13-ARENA-APEX/orchestrator.py`, subito dopo `import sys`
(stesso split già deciso in CP-20260813-002: *gli entry point forzano UTF-8*). Contratto
d'innesto: ospite = ARENA-APEX (13 file py, `censimento-01a`); gate di regressione = `python
orchestrator.py --help` → exit 0 e stampa l'usage; rientro = `git checkout --`. Politica:
fail-closed (se dopo il fix c'è un secondo errore, si scrive quale — Appendice B lo dava
«rotto», non «a metà»: si misura fino in fondo).

---

## 3. Il gate — comando, condizione di fallimento, exit code (L9)

```
test -s company/Memory/tesoreria/spese.jsonl -o -s company/Memory/tesoreria/entrate.jsonl   # 2.2: un file NON vuoto (exit 0)
python scripts/checkpoint.py cp --titolo "prova gate E0.5" 2>&1 | grep -c "AVVISO: manca --costi"   # 2.3: 1 (finestra), poi dal 4o giorno il comando rifiuta
test -d company/runtime && test -d company/metrics && test -d marketing/handoffs/log && test -d platform/handoffs/log   # 2.4: exit 0
grep -c "libri-performanti-multiagente" company/REGISTRO-IMPRESA.md                          # 2.5: >= 1
python -m empire vivo --json | python -c "import sys,json;d=json.load(sys.stdin);assert d['denominatore']>0;print('ok',d['percentuale'])"   # 2.7: ok, exit 0
python scripts/gate_rotazione.py --help                                                       # 2.8: usage, exit 0 (il gate vero gira in E0b)
python -m empire controllo | grep INCASSO                                                     # 2.8: la riga dice "HTTP" o "link assente", non un flag
python scripts/ultimo_metro.py --json | python -c "import sys,json;print(json.load(sys.stdin)['caricabili'])"   # 2.9: un intero
grep -c "ARCHIVIO" company/Ecosistemi/REGISTRO-NUMERI.md                                      # 2.10: >= 1 sulla riga di 08-STREAM-S7-BOT
python company/Ecosistemi/13-ARENA-APEX/orchestrator.py --help                                # 2.11: exit 0
cd "SKILL & Agenti/Workflow pubblicazione automatica" && python -c "import main_orchestrator" # 2.1: exit 0 (l'import non muore piu')
```
**Condizione di chiusura:** 10 passi su 10 con il loro gate verde, **oppure** N/10 dichiarati per
nome nel checkpoint (§0: sono indipendenti). Il tetto è 9 ore: sforato, ci si ferma e si riporta
(V2 §21 regola di tetto).

---

## 4. Politica di guasto e piano di rientro

Ogni passo è un innesto piccolo su un file diverso: **fail-closed per passo, fail-open fra i
passi**. Un passo che non passa il suo gate non si dichiara e si rientra con `git checkout --
<file>` (tutti i file toccati sono tracciati; i nuovi — `gate_rotazione.py`, le caselle, `vivo`
— si tolgono con `git clean` mirato sul percorso, mai `-fd` sulla radice: altre sessioni hanno
file non ancora committati, R4 di V2 §24).

**Il passo con il danno peggiore è 2.3** (`checkpoint.py`: ogni sessione lo usa): per questo ha
la finestra di 3 giorni e l'avviso in STATO-EMPIRE — è la migrazione dei chiamanti di V3 §4,
applicata al caso che l'ha generata (A-1 della Critica 2).

**Convivenza col daemon di sync:** ogni passo = un commit proprio, subito (mai lo stato misto di
due passi a metà nel commit automatico). Non si sospende il daemon per E0.5 (si sospende solo per
E1/E2, dove le operazioni sono a due tempi su 36 coppie).

---

## 5. Le forze — chi fa cosa, e il prompt d'ingaggio già scritto

**EMPERATOR da solo**, in sequenza (V2 §18: produzione sequenziale). Nessun passo giustifica una
forza: il più lungo (2.7) è un sottocomando da 150-250 righe che va scritto **leggendo** l'Appendice
B, e leggere l'Appendice B è esattamente ciò che una sentinella nuova rifarebbe da zero.

**Unica eccezione ammessa:** uno **scagnozzo (haiku)** per i tre README delle caselle (2.4), con
questo prompt, copiabile:

> Scrivi tre file `README.md` di 3 righe l'uno in `company/runtime/`, `company/metrics/`,
> `marketing/handoffs/log/` (repo `c:\Users\Utente\Desktop\qui tutto\Digital Empire`). Ogni
> README dice, in italiano: (1) chi scrive in questa cartella, (2) chi la legge ogni giorno,
> (3) quale hook o comando la svuota. I tre contenuti, uno per cartella: `runtime/` = «le 5
> Guild e le 5 Sentinelle scrivono qui i loro deliverable (V2 §5); li legge `python -m empire
> inspect`; li svuota nessuno — è un archivio»; `metrics/` = «`company/metrics/runs.jsonl`,
> scritto dall'emettitore F3 (V4 09), letto dal cruscotto quotidiano (V2 §23), mai svuotato»;
> `marketing/handoffs/log/` = «i verbali degli handoff verso 04-MARKETING, scritti dal Bus F2
> (V4 12), letti dal proprietario di MARKETING all'apertura di sessione». Non toccare nessun
> altro file. Scrivi un file, salvalo, poi il successivo.

---

## 6. Cosa si scrive in Memory alla chiusura

- **Checkpoint:** `python scripts/checkpoint.py cp --titolo "E0.5 chiusa N/10: Tesoreria accesa, costi in checkpoint, caselle, vivo --json, gate rotazione, numeri sanati, ARENA-APEX riparata" --costi "<token>, 0 EUR, <min>"` — sì, **con `--costi`**: è il primo checkpoint che usa il campo appena innestato (mangia la propria medicina).
- **Riga per `STATO-EMPIRE.md`** in cima: il numero di passi chiusi, i tre fatti che il disco ha smentito (trace esiste; `--json` mancava; REGISTRO-NUMERI è in `Ecosistemi/`), e **l'avviso della finestra `--costi`** con la data.
- **ADR:** nessuna nuova. La decisione 5 di V2 §26 (08/12 in ARCHIVIO) viene **applicata** qui, non decisa: si cita.
- **Correzione di puntatore (REGOLA PUNTATORI):** in `V3-PIANO-ASSESTATO.md` Appendice B punto 6 e in `00-INDICE.md` §1, `company/REGISTRO-NUMERI.md` → `company/Ecosistemi/REGISTRO-NUMERI.md`. Fatta in questo stesso documento (§ intestazione), da propagare al checkpoint.
- **I sei numeri del cruscotto (V2 §23), prima → dopo:**
  - `controllo x/6`: 2/6 (o 3/6 se E0a è chiusa) → **invariato** (E0.5 cambia *come* si misura INCASSO, non il risultato)
  - `OPERATIVO %`: 61/439 = 13,9% → invariato
  - `tracce (origine=hook)`: 0 → 0 (nasce con F3)
  - `byte entrate.jsonl`/`spese.jsonl`: **0 → > 0** (2.2)
  - `pezzi fermi (ultimo_metro --json)`: 31/26 → il numero stampato dal flag nuovo (2.9)
  - `vivo%`: **non calcolabile → 0,0%** con denominatore > 0 e 6 `non_valutati` nominati (2.7) — è il primo numero vero del piano: **0 chiusi su N**, come V2 §3 dichiarava e l'Appendice B ha misurato
