# Memory — chief-forge
Legge all'avvio: MEMORY-INDEX.md + dashboard + PLANNING-P7.
Scrive: checkpoint (ogni chiusura), error (fallimenti/slittamenti), metric (KPI), decision (eccezioni DoD/sconti).
Never-duplicate: prima di creare un artefatto, `search "<nome>"` — se esiste un motore, lo wrappa (ADR-EST-002).
Trace P12: ogni artefatto cita gli ID delle decisioni che lo abilitano.
