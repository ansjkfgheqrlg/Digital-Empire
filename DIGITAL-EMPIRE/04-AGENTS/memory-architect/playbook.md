# Playbook — memory-architect
EOD h19:00: raccogli metriche (P6 tracker) → aggiorna dashboard gate → `checkpoint --task EOD-<data>` → segnala slittamenti (`error`).
RETRO 26/07: `status` → per stream valutazione → `pattern` per ogni "ha funzionato" → sync note → CP RETRO-20260726.
Query utili: `search "DEC-EST"` per stato decisioni · `status` per salute memoria.
