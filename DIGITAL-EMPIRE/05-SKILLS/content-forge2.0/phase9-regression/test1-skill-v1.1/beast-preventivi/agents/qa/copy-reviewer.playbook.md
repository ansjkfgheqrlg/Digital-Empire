# Playbook — copy-reviewer

## 1. happy — Scenario tipico 1

**Orchestrator**: Spawn copy-reviewer con input X.

**Agent copy-reviewer**:
```
Processo X applicando le regole del manuale APSOC sezione Y.
Output: [...]
Handoff: passa a next-agent con flag Z.
```

**Risultato atteso**: output conforme schema, tempo <5 min, no LLM-speak.

## 2. happy — Scenario tipico 2

**Orchestrator**: Spawn copy-reviewer con input X.

**Agent copy-reviewer**:
```
Processo X applicando le regole del manuale APSOC sezione Y.
Output: [...]
Handoff: passa a next-agent con flag Z.
```

**Risultato atteso**: output conforme schema, tempo <5 min, no LLM-speak.

## 3. happy — Scenario tipico 3

**Orchestrator**: Spawn copy-reviewer con input X.

**Agent copy-reviewer**:
```
Processo X applicando le regole del manuale APSOC sezione Y.
Output: [...]
Handoff: passa a next-agent con flag Z.
```

**Risultato atteso**: output conforme schema, tempo <5 min, no LLM-speak.

## 4. edge — Scenario tipico 4

**Orchestrator**: Spawn copy-reviewer con input X.

**Agent copy-reviewer**:
```
Processo X applicando le regole del manuale APSOC sezione Y.
Output: [...]
Handoff: passa a next-agent con flag Z.
```

**Risultato atteso**: output conforme schema, tempo <5 min, no LLM-speak.

## 5. edge — Scenario tipico 5

**Orchestrator**: Spawn copy-reviewer con input X.

**Agent copy-reviewer**:
```
Processo X applicando le regole del manuale APSOC sezione Y.
Output: [...]
Handoff: passa a next-agent con flag Z.
```

**Risultato atteso**: output conforme schema, tempo <5 min, no LLM-speak.

## 6. failure — Scenario tipico 6

**Orchestrator**: Spawn copy-reviewer con input X.

**Agent copy-reviewer**:
```
Processo X applicando le regole del manuale APSOC sezione Y.
Output: [...]
Handoff: passa a next-agent con flag Z.
```

**Risultato atteso**: output conforme schema, tempo <5 min, no LLM-speak.
