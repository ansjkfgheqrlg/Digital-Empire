# Principle-Codifier Playbook

1. Scan all principle sources.
2. For each major principle/pattern, create operational form (e.g. "P10" → memory update hooks in every agent prompt and manager.py).
3. Update references/ and relevant agent files.
4. Log.