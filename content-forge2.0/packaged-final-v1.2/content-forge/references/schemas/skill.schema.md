# `skill.schema` — Human-Readable Version

> Schema canonico per `skill`. Coppia con `skill.schema.json` (validabile da `scripts/schema_validator.py`).

## Scopo

(da espandere in Fase 4)
Documenta la forma canonica dell'entità `skill` prodotta da Forge.

## Campi obbligatori

(da espandere in Fase 4)

## Campi opzionali

(da espandere in Fase 4)

## Esempio minimo

```json
{}
```

## Esempio completo

```json
{}
```

## Validazione

```bash
python scripts/schema_validator.py --target skill --output-dir <path>
```

## Note evolutive

Versionato in semver in chiave `schema_version` del JSON Schema corrispondente.
