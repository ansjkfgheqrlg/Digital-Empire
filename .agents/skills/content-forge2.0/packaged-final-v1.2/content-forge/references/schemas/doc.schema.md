# `doc.schema` — Human-Readable Version

> Schema canonico per `doc`. Coppia con `doc.schema.json` (validabile da `scripts/schema_validator.py`).

## Scopo

(da espandere in Fase 4)
Documenta la forma canonica dell'entità `doc` prodotta da Forge.

## Campi obbligatori

(da espandere in Fase 4)

## Campi opzionali

(da espandere in Fase 4)

## Esempio minimo

```json
{}
```

## Esempio completo

```json
{}
```

## Validazione

```bash
python scripts/schema_validator.py --target doc --output-dir <path>
```

## Note evolutive

Versionato in semver in chiave `schema_version` del JSON Schema corrispondente.
