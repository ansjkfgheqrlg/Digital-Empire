# 📊 OBSERVABILITY — Metriche e auto-miglioramento

> **Backbone component.** Vede tutto quello che succede nella holding.
> Da costruire in F8 (dopo che gli agenti reali sono running).

## Funzione

| Sistema | Funzione | Quando costruire |
|---|---|---|
| Metrics | KPI per agente/team/ecosistema | F8 |
| Dashboard | vista aggregata holding (ispirata a `dashboard.sh` CF) | F8 |
| `neural_train` | addestra da pattern ReasoningBank | F8 |
| `autopilot` | suggerisce ottimizzazioni proattive | F9+ |
| Cost attribution | associa ogni spesa API a agente/task/ecosistema | F2 (parziale via CFO) |

## Output atteso (F8)

- Dashboard CLI: stato holding, KPI per ecosistema, costi sessione
- Alert proattivi: "AGENCY ha reply rate < 3% da 5 giorni → diagnosi CMO"
- Loop auto-miglioramento: osserva → giudica → distilla → propone hire/retire

## Stato: DA COSTRUIRE (F8)
