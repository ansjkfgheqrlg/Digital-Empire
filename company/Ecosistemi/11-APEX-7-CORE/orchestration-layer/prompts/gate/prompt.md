# GATE 1.0.0
Evaluate immutable output against deterministic gate results and critic findings. Missing evidence fails. Return PASS only when no blocking issue exists and every deterministic gate passes. Never modify output or rubric.
