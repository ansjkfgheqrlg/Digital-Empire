# CP-20260701-001 — PreventivoForge Half B (Contenuto · Output · Qualità) COMPLETA

- **Data:** 2026-07-01
- **Ecosistema/Reparto:** 01-AGENCY / Cliente Prof Autocad — PreventivoForge (Half B, Gael)
- **Task:** Costruire Half B del workflow PreventivoForge (mobile.de DE → preventivo IT PDF) dal
  contratto `Clienti/Prof Autocad/preventivo-forge/HANDOFF-GAEL.md`. Cambio priorità di Max
  (STOP scelta ecosistema → task unico). Metodo 9 passi (ADR-006).
- **Esito:** ✅ completato — DoD raggiunta e verificata end-to-end.

## Output (path reali)
Codice (S3/S5/QA):
- `implementation/translate_copy.py` — S3 traduzione+copy deterministica (glossario + composizione dai fatti)
- `implementation/glossary_de_it.py` — glossario automotive DE→IT (~150 termini) + rilevamento tedesco indipendente
- `implementation/render_pdf.py` — S5 render motore-agnostico (Playwright→WeasyPrint), foto base64
- `templates/preventivo.html` — template Jinja2 professionale (9 sezioni)
- `implementation/qa_gate.py` — Gate A/B/C/D bloccanti (ricalcolo prezzo indipendente, rilevamento DE indipendente)

RULES (RBI): `rules/R3-translation-copy.md`, `rules/R5-pdf-render.md`, `rules/R6-qa-gate.md`

Agenti CF-grade (6 × 7 file = 42 + CATALOG):
- `agents/operativi/op-translator-copy/` · `agents/operativi/op-pdf-renderer/`
- `agents/verifica/qa-extraction-verifier/` · `qa-translation-verifier/` · `qa-price-verifier/` · `qa-output-reviewer/`
- `agents/CATALOG.md`

## Verifica (GATE + TEST)
- **Struct gate:** 6 agenti × 7 file, 0 mancanti, 0 magri (<200b) → PASS. Totale 43 file agenti.
- **py_compile** dei 4 moduli → OK.
- **Test end-to-end reale** `python run.py --manual <html BMW 320d> --foto <dir>`:
  scraper→parser→GateA→S3→S4(pricer Max)→S5 → `preventivo_bmw-320d.pdf` 63 KB (motore Playwright).
- **Gate A/B/C/D = ALL GREEN** (0 residui tedeschi, prezzo 26.900→30.707 € ricalcolato indipendente, PDF completo).
- **Ispezione visiva del PDF** (2 pagine): layout pulito, tutte le 9 sezioni, prezzo nel titolo, gallery ok.

## Decisioni prese
- **Traduzione deterministica di default** (glossario + copy dai fatti strutturati), gancio LLM
  `TRANSLATE_BACKEND=llm` OFF (Mandato Art.4.3 dry-run: nessuna spesa API senza ok Max). Nessun ADR nuovo (rispetta ADR-003/Art.4.3).
- **Motore PDF = Playwright** (WeasyPrint richiede GTK nativo, non praticabile su Windows); fallback WeasyPrint mantenuto.
- Foto sempre incorporate in base64 locale (mai hotlink).

## Confine rispettato (ADR-003 / handoff)
NON toccati: `run.py`, `schema/*` (CONGELATO), scraper/parser/pricer/dealers/common (Half A di Max).
Solo file nuovi in `implementation/`, `templates/`, `rules/`, `agents/`.

## Lezioni/errori
- Umlaut traslitterati ASCII (ue/oe/ae) comuni negli export → aggiunta `_restore_umlauts()` per il lookup glossario.
- Nomi colore costruttore (Kosmosschwarz/Alpinweiß) esclusi dalle specs: tradurli è errato e genera falsi residui.
- Ambiente Gael privo di deps: installati playwright+chromium+jsonschema+requests+bs4+lxml+dotenv.

## Costi
API: €0 (traduzione deterministica, nessuna chiamata LLM). Solo install pacchetti locali (~114 MB chromium).

## Pending per Max (non bloccante)
Wiring gate B/C/D in `run.py` (3 chiamate dopo S3/S4/S5, passando `dealer`) — è Half A, da fare a Max come da handoff §Integrazione.

## Prossimo passo
Half B chiusa. Attendere Max per il wiring gate in run.py + primo run su URL mobile.de REALE.
Poi (se Max sblocca): ripresa scelta ecosistema Gael (05-MULTI-BUSINESS / split 06).
