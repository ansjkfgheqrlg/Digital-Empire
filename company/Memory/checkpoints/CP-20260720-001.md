# CP-20260720-001 — EMPIRE DESK: PIVOT AREUS (l'app riparte dalla piattaforma Aureus Agency OS)

- **Data:** 2026-07-20
- **Autore:** Max (via Claude)
- **Ordine Max:** la UI launcher v0.1/v2 è bocciata ("graficamente fa schifo, struttura sbagliata" —
  era pattern PreventivoForge, ma Empire Desk è l'app GESTIONALE del team). Si riparte dalla piattaforma
  già creata da Max: repo `ansjkfgheqrlg/Gestionale-Team---Areus-Piattaforma-By-Digital-Empire`
  ("Aureus Agency OS"). Trasformarla in APP SENZA toccare la grafica; le funzioni/automazioni si
  aggiungono SOLO in fase 2. Ownership: Max = grafica/UI/UX, Gael = tutto il resto.

- **Esito:** ✅ U0 completato (import + fix build + anteprima app funzionante)

- **Output:**
  1. **`EmpireDesk/platform/`** — Aureus vendored dal repo GitHub (commit `83854cf`, "feat: Add
     YouTube and Amazon KDP modules", 2025-12-17). React 19 + Vite + react-router + recharts +
     lucide; 26 componenti (~8.400 righe); dati in localStorage (`utils/database`).
     node_modules/dist gitignorati (dopo pull: `npm install`).
  2. **Fix build (NON grafico):** `index.html` dell'export AI-Studio non referenziava `/index.tsx`
     → build produceva dist VUOTA (solo index.html, 2 moduli). Aggiunta la riga
     `<script type="module" src="/index.tsx"></script>` (presente nella copia Desktop funzionante).
     Dopo il fix: `npm run build` → bundle 970 kB. **Anteprima verificata** servendo `dist/` in
     locale + finestra `chrome --app` (aperta a Max).
  3. Dossier 17: nuova sezione **§0-bis PIVOT AREUS** (supera §1-§5 per la parte UI) con task
     U0/U1 (Max) e G1/G2/G3 (Gael). STATO-EMPIRE riscritto in cima.

- **Verifiche:** repo GitHub = fonte canonica confermata da Max (3 copie esistenti: repo, Desktop
  "AREUS/Areus" con .env+dist deployata, "Digital Empire new" = progetto diverso/landing 17 componenti).
  Repo App.tsx (462 righe) > copia Desktop (185) → repo più completo.

- **Note tecniche per il futuro:**
  - `index.html` carica Tailwind e React/deps da CDN `aistudiocdn.com` → serve internet all'avvio
    app. Vendorizzazione = fase 2 (owner Max, zero impatti grafici).
  - Moduli A1-A3 (metrics/revenue/licenze) restano validi come route backend; i loro `panel_html`
    sono provvisori — la UI operativa si rifà dentro Aureus in fase 2 (U1).
  - La vecchia `ui/index.html` (Empire Premium) resta come fallback `/legacy` finché G1 non è live.

- **Prossimo:** GAEL → G1 (app.py serve platform/dist + chrome-app) → G2 (exe). MAX → U1 in fase 2.
