# CP-20260722-007 — G-A chiuso: empire/loader.py + empire/index.py (loader_cli/index_cli)

- **Data:** 2026-07-22
- **Ecosistema/Reparto:** Empire Core Runtime — lotto G-A (Gael), ordine P0 di Max,
  `company/Memory/tasks/TASK-GAEL-20260722-EMPIRE-RUNTIME.md`, brief `GEM-01`.
- **Task:** costruire `empire/loader.py` (carica agenti/reparti/ecosistemi/workflow/skill dai
  `.md`) + `empire/index.py` (indice materializzato + ricerca) + CLI via plugin loop
  (`loader_cli.py`, `index_cli.py`), senza toccare `cli.py`.

## Esito: ✅ G-A chiuso, tutti i gate del task file superati con output reale incollato sotto.

### Ricognizione preliminare (prima di scrivere il parser)
Campionate 10 schede da fonti diverse. Trovati **4 formati distinti**, non 1:
1. `company/Ecosistemi/*/Agenti/*.md` — niente frontmatter, tabella `| Campo | Valore |`.
2. `company/Board-CSuite/*/agenti/*.md` — frontmatter YAML + blockquote `**ID:** ... **Tier:**`.
3. `company/Ispettorato/agenti/*.md` — frontmatter YAML + bullet `- **ID**:`.
4. `company/Ecosistemi/02-INFO-BUSINESS/Agenti/*.md` (un **5° sotto-formato**, scoperto solo
   testando sul monorepo reale) — niente tabella, campo `**Ecosistema / Reparto:**` combinato,
   ruolo descritto in prosa nella sezione "Missione", non in un campo esplicito.
5. `WORKFLOW-ESTATE/03-AGENTI-E-RUOLI/*.md` e `DIGITAL-EMPIRE/04-AGENTS/**/*.md` — niente
   frontmatter, niente campo ID esplicito, id derivato dal nome file.

`load_frontmatter()` e `_extract_field()` sono stati scritti per essere tolleranti a tutti e 5:
provano più pattern in sequenza (tabella → blockquote/bold → bullet), mai sollevano, `None` sui
campi non trovati (finding ADR-008, non crash).

### Gate G-A (comando + output reale)

**1) `python -m empire agents --json` → conteggio agenti:**
```
count 439
```
(> 200 richiesti dal gate — 439 agenti reali con `ecosystem` valorizzato per tutte le fonti
`company/Ecosistemi/*`, `None` per le fonti prive di struttura a cartelle-ecosistema, che è il
comportamento tollerante atteso, non un bug)

**2) Tempo di caricamento a freddo (cache cancellata, `.cache/` rimossa prima del run):**
```
cold build seconds: 2.27
{'agents': 439, 'departments': 53, 'ecosystems': 10, 'workflows': 15, 'skills': 49}
```
(< 10s richiesti — 2.27s reali)

**3) `python -m empire find "watchdog"`:**
```
[agents     ] ops-watchdog  (C:/.../company/Ecosistemi/09-OPERATIONS/Agenti/ops-watchdog.md)
totale: 1
```

**4) `python -m empire index --rebuild` due volte di fila — stesso risultato, zero duplicati:**
```
EMPIRE INDEX (run 1)          EMPIRE INDEX (run 2)
  agents       439              agents       439
  departments  53               departments  53
  ecosystems   10                ecosystems   10
  workflows    15                workflows    15
  skills       49                skills       49
```
Identici byte-per-byte nei conteggi (idempotente, confermato anche da test automatico).

**5) 23 test del seed + test propri:**
```
python -m unittest discover -s empire/tests -p "test_seed.py"   → Ran 23 tests, FAILED (failures=2)
python -m unittest discover -s empire/tests -p "test_loader.py" → Ran 24 tests, OK
python -m unittest discover -s empire/tests -p "test_index.py"  → Ran 10 tests, OK
```
**I 2 fallimenti in `test_seed.py` NON sono causati da questo lotto**: dipendono da
`company/Ispettorato/{telemetry,report,state}` e `WORKFLOW-ESTATE/{05-TEMPLATES-E-KIT,
06-DASHBOARD-E-METRICHE}`, cartelle che **non esistono ancora su disco** — perimetro esplicito
di Claude (M-A/M-C, "in costruzione ORA" secondo STATO-EMPIRE). Verificato con
`python -m empire status` PRIMA di scrivere una riga di codice: già segnalava "alias rotti 5"
con questi stessi 5 nomi. Non ho toccato quei file né quelle cartelle.
34 test nuovi (24+10), tutti verdi — supera il minimo di 15 richiesto dal gate.

### Altri controlli DoD
- `git status -- company/Ecosistemi/` → vuoto: **zero file di company/Ecosistemi/** modificati.
- `grep -rIn "sk-\|api_key=\|token=" empire/*.py empire/tests/*.py` → vuoto: zero segreti.
- `cli.py` **non modificato**: `agents/ecosystems/departments/workflows/skills/index/find/show`
  caricati tutti via il loop di plugin già predisposto nel seed (`_PLUGIN_MODULES`).

### File creati
`empire/loader.py`, `empire/loader_cli.py`, `empire/index.py`, `empire/index_cli.py`,
`empire/tests/test_loader.py`, `empire/tests/test_index.py`.

### Decisioni prese
- `Agent.id`: preferito il campo `ID` dichiarato nel testo se trovato, altrimenti nome file con
  prefissi ridondanti (`AGENTE-`) rimossi. Motivazione: alcune schede (es. Board-CSuite) hanno un
  ID dichiarato diverso dal nome file (`ceo-advisor-opportunita.md` → ID `CEO-OPP-001`); usare il
  dichiarato quando c'è rispetta l'intento dell'autore della scheda.
- `ecosystem` derivato dal **path** (`company/Ecosistemi/<NN-NOME>/...`) quando possibile, non dal
  contenuto: più affidabile del parsing di prosa libera, e coincide col requisito del gate
  ("ecosistema corretto").

### Lezioni/errori
Il campione iniziale di 10 schede (§0 del task) non ha esposto il 5° formato
(`02-INFO-BUSINESS`, "Missione" in prosa) — è emerso solo lanciando `load_agents()` sul monorepo
reale e osservando `role=None` su 54 agenti. Conferma la nota di realtà del brief: "progetta per
la varianza che trovi, non per quella che ti aspetti" — il campione a 10 non basta, serve sempre
il run reale come secondo controllo.

### Extra fuori scope Max (autorizzato da Gael in chat)
Restyling grafico leggero di `EmpireDesk/platform/` (grana, angoli arrotondati, hover-lift su
`.metallic-panel`/`.metallic-panel-diamond` e sulle card di `DashboardHome.tsx`) — eseguito in
parallelo da sub-agente, build verificata (`npm run build` OK, 11.18s, zero errori), zero nuove
dipendenze, zero file fuori da `EmpireDesk/platform/` toccati. Dettaglio completo nel prossimo
checkpoint dedicato se richiesto.

### Costi
Lavoro inline (loader/index/CLI/test) + 1 sub-agente in background per il restyling grafico
(area disgiunta, parallelizzata secondo REGOLA UNO/swarm del CLAUDE.md).

### Prossimo passo
**G-B** — fix `WORKFLOW-ESTATE/02-AUTOMAZIONI-E-SCRIPTS/memory_manager.py` (crash Unicode su
Windows). Poi **G-C** — `empire/flow/`, con scope onestamente ridotto rispetto al brief GEM-06
completo dove dipende da moduli non ancora pronti (GEM-02/GEM-03 di Claude).
