# CP-20260731-001 — Fabbrica video @dosementale: copertina adattata dalla sorgente + flusso verificato

**Data:** 2026-07-31
**Area:** YOUTUBE-AUTOMATION-FACTORY
**Commit:** `06ed8094`

## Cosa è stato fatto
1. **Copertina adattata, non inventata** (richiesta di Gael): `arena_thumbnail.py` scarica la
   miniatura reale del video sorgente (`i.ytimg.com/vi/<ID>/maxresdefault.jpg` →
   `05-TEMPLATES-E-KIT/source-thumbnail/`), la allega alla chat Arena e chiede una MODIFICA
   (posa + testo) mantenendone il linguaggio visivo. Risultato reale verificato: 1672x941 (16:9),
   sfondo bianco + raggi gialli + illustrazione a matita + testo nero maiuscolo con box verdi,
   posa cambiata in equilibrio su una gamba, testo italiano corretto senza errori.
2. **Retry su chat nuova** (regola di Gael): se una chat non produce nulla, `open_new_chat()` +
   fino a 3 tentativi, invece di insistere sulla chat rotta.
3. **Residuo del progetto morto nel CODICE rimosso**: `build_prompt()` conteneva ancora hardcoded
   l'estetica "terminal/console, #fb4604, tech coding tutorial" del Manuale Claude Code — il
   prompt inviato contraddiceva il brief @dosementale già riscritto.
4. **Scene bilanciate** (`MAX_WORDS_PER_SCENE = 130`, spezzate a fine frase): un blocco da 594
   parole teneva il job Fliki bloccato in `queued` per oltre un'ora.
5. **stdout via `reconfigure()`** in tutti e 4 gli script: due `io.TextIOWrapper` sullo stesso
   buffer causavano `I/O operation on closed file` quando `fliki_client` importa `apex7_orchestrator`.
6. **Merge Git non risolto**: risolto (da sessione parallela), verificato pulito — 11/11 test verdi.

## Verificato su file reali (non sulla sola risposta API)
- Video `dopo-70-anni-camminare-non-basta-v7.mp4`: **727.08s (12min 7s)** ✅ ≥12 min
- Voce: **Calimero, MALE** ✅ (non fallback)
- Sottotitoli: **visibili, verificati su più fotogrammi** ✅
- 19 scene bilanciate, job completato senza blocchi in coda ✅

## ⚠️ Problema aperto (non risolto, decisione a Gael)
`apex7_orchestrator.py` (motore F1→F5) è **ancora interamente cablato sul funnel Manuale Claude
Code**: 20 canali AI hardcoded, `keyword = "claude code"`, tier "opportunità per il Manuale",
CTA "trova la guida completa nel Manuale", `--file-name` di default `claude-code-installazione`.
Anche `candidati-video.json`, `produzione-spec.json`, `seo-report.json`, `scheda-nicchia.md`,
`YOUTUBE-PERFORMANCE-DASHBOARD.md` contengono ancora dati del vecchio canale tech.

**Conseguenza reale:** il flusso *manuale* (script scritto a mano → `fliki_client.py` +
`arena_thumbnail.py`) è funzionante e verificato, ma lanciare l'orchestratore end-to-end
**sovrascriverebbe** `script.md`/`metadati.json`/`brief-miniatura.json` con contenuti Claude Code.

**Scelta da fare:** (a) riscrivere F1-F5 su @dosementale (build grosso, tocca anche i test che
codificano il vecchio dominio), oppure (b) tenere il flusso manuale e ridurre l'orchestratore
al solo pezzo riusato (`_parse_script_scenes`).

## RIPRESA DA
Decisione di Gael sul punto ⚠️ qui sopra.
