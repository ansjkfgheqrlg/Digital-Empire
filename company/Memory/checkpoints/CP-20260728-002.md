# CP-20260728-002 — Preventa: prezzo €2.000 tantum chiuso, scraper migrato da Google Sheets ad Areus, sync-conflict risolto

- **Data:** 2026-07-28
- **Ecosistema/Reparto:** Outreach/preventa-maps-scraper + EmpireDesk (Areus)
- **Owner decisione:** Max · **Esecuzione:** Claude

## Decisioni chiuse da Max
1. **DEC-EST-005 / M-EST-4 — Prezzo Preventa: €2.000 una tantum**, pagamento unico, nessun
   canone. Sostituisce la vecchia proposta (€490 setup + €149/mese), mai andata live.
2. **Google Sheets bocciato come CRM esterno** — "abbiamo tutto dentro Areus". I lead vivono
   nella piattaforma aziendale (Aureus Agency OS / EmpireDesk), non su un foglio esterno.
3. **M-EST-9 — Province scraper**: chiuso con default Nord+Centro in
   `05-TEMPLATES-E-KIT/cities.txt` (Milano, Bergamo, Brescia, Monza, Varese, Como, Torino,
   Verona, Bologna, Padova). Max può editare il file in qualsiasi momento.

## Propagazione prezzo (2 superfici pubbliche)
- `Crea siti/Preventa/index.html` — comment block onestà pagina + 5 punti copy (hero, sezione
  benefici, obiezioni, FAQ disdetta, footer CTA finale) riscritti per il modello una tantum.
- `agency-empire/src/sections/03b-preventa.tsx` — costanti `PREZZO_ATTIVAZIONE`/`PREZZO_CANONE`
  sostituite da `PREZZO_TANTUM = "2.000"`, card prezzo e copy "canone"/"kill-switch abbonamento"
  riscritti in "pagamento unico".
- `WORKFLOW-ESTATE/06-DASHBOARD-E-METRICHE/AZIONI-MAX.md` — voce 2 marcata chiusa, voce 3
  (Gate-CONTATTI) aggiornata con il nuovo flusso Areus.

## Migrazione Google Sheets → Areus (Outreach/preventa-maps-scraper)
Sostituito l'intero binario Sheets con un bridge a file JSON condiviso, zero credenziali:

- **Nuovo:** `02-AUTOMAZIONI-E-SCRIPTS/areus.py` (rimpiazza `sheets.py`, eliminato) —
  `upload_to_areus()` (dedup per telefono, stage iniziale `NEW`) e `mark_contacted()`.
- **Rinominato:** `03-AGENTI-E-RUOLI/integratore-sheets/` → `integratore-areus/`, classe
  `SheetsAgent` → `AreusAgent`, evento `sheets.synced` → `areus.synced`.
- **Aggiornati:** `agents.py` (Conductor: `sheets_agent`→`areus_agent`, `on_sheets_synced`→
  `on_areus_synced`), `run.py` (`--sheet-id/--sheets-creds/--sheets-worksheet/--sheets-push-alta`
  → `--no-areus/--areus-state-path/--areus-push-alta`, **push attivo di default**),
  `orchestrator.py` (stesso schema, era rotto — importava ancora `SheetsAgent`),
  `test_apex7.py` e `quality_gate.py` (kwarg/descrizioni gate aggiornate), `.env.example`,
  `LEGGIMI.md`, `requirements.txt` (rimosse dipendenze `gspread`/`google-auth`),
  `04-SKILLS-E-REFERENCE/SKILL.md`, `06-DASHBOARD-E-METRICHE/CONSOLIDATED-OUTREACH-DASHBOARD.md`.
- **Nuovo:** `contact_leads.py` ora chiama `areus.mark_contacted()` dopo ogni invio: lo stage del
  lead passa `NEW`→`CONTACTED` in Areus, non solo nel `memory_db.json` locale.

## Nuovo modulo EmpireDesk (lato Areus)
- **Nuovo:** `EmpireDesk/modules/preventa.py` — routes `preventa/elenco` (lead + conteggi per
  stage) e `preventa/aggiorna_stage` (sposta un lead NEW→CONTACTED→...→CLOSED_WON/LOST), pannello
  "Preventa — Outreach Freddo" con tabella + select stage per riga (stesso pattern di
  `taskboard.py`). Stage usa lo stesso enum `LeadStage` di `platform/types.ts`, così un domani
  l'integrazione nel Kanban `SalesPipeline.tsx` è un mapping diretto, non un redesign.
- **Nuovo:** `EmpireDesk/state/preventa_leads.json` — file condiviso (placeholder vuoto
  committato, popolato dal primo run dello scraper).
- **Aggiornato:** `platform/utils/empireApi.ts` — `preventaElenco()`/`preventaAggiornaStage()`.

**Bug trovato e fixato in corsa:** `areus.py` calcolava il path di default con
`parents[2]` invece di `parents[3]` (il file è 4 livelli sotto `Digital Empire/`, non 3) —
risolveva a `Outreach/EmpireDesk/state/...` (non esiste) invece di `EmpireDesk/state/...`.
Verificato con un test diretto che confrontava il path calcolato dai due lati (scraper vs
modulo EmpireDesk) prima di considerare la migrazione chiusa.

## Sync-conflict risolto (bloccava lettura affidabile della memoria)
All'inizio del task, `company/Memory/STATO-EMPIRE.md` e `INDEX.md` avevano marker di merge
(`<<<<<<< HEAD` / `=======` / `>>>>>>> origin/main`) committati letteralmente nel testo — un
rebase automatico di sync (vedi `SYNC-CONFLICT.txt`, ora rimosso) era fallito silenziosamente.
Trovati i marker prima di iniziare l'edit; nel frattempo il sync automatico di sessione li ha
risolti da solo (commit `merge: risolti conflitti memoria (CP-015 rinumerato, voci parallele
Gael tenute)`) — verificato con `grep` che non restassero marker, poi ho continuato senza
duplicare il lavoro. Anche questo checkpoint ha richiesto la stessa cautela: `CP-20260728-001`
era già occupato da un checkpoint parallelo di Gael (fusione Ruflo+APEX-7-CORE) — numerato qui
come `002` per evitare collisione.

## Verifiche
- `python -m pytest test_apex7.py -q` → **13/13 verdi** (invariato).
- Smoke test diretto di `areus.upload_to_areus()` + `mark_contacted()` su file temporaneo:
  dedup per telefono normalizzato funzionante, stage NEW→CONTACTED confermato.
- `EmpireDesk/modules/preventa.py` importato e `selftest()` verde.
- `python -m empire estate` → **11/13 verdi, invariato** (nessuna regressione); il blocco
  `conform WORKFLOW-ESTATE` (link morto a `EmpireDesk/state/preventa_leads.json` prima che il
  placeholder fosse committato) è stato intercettato e fixato nella stessa sessione.

## RIPRESA DA
Gael sbloccato: nessun lavoro tecnico residuo su preventa-maps-scraper. Prossimo passo è
operativo (Max): lanciare `python scraper.py --input cities.txt --limit 25 --only-alta
--areus-push-alta` per generare i primi lead reali post-pilota, poi contattarli e aggiornare
lo stage in Areus per chiudere **Gate-CONTATTI** (ancora rosso, per costruzione — nessun dato
finto). **Gate-REV** resta rosso finché non c'è un incasso reale, indipendente da questo lavoro.
