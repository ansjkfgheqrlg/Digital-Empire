# CP-20260728-008 — TASK-YT-002 chiusa: F4 Produzione, spec Fliki reale multi-scena da script.md

- **Data:** 2026-07-28
- **Ecosistema/Reparto:** YOUTUBE-AUTOMATION-FACTORY
- **Owner:** Gael · **Esecuzione:** Claude
- **Task:** `TASK-YT-002` (`company/Memory/tasks/TASK-GAEL-20260728-YOUTUBE-FACTORY.md`), secondo
  lotto YT, dipende da TASK-YT-001 (chiuso in [CP-20260728-007](CP-20260728-007.md)).

## 0. RECALL
`run_phase_4` scriveva sempre `scene_count: 5` con **1 sola scena hardcoded**
(`"Vuoi installare l'agente IA più veloce?"`), indipendentemente dallo script reale scritto in
F3. Ispezionato `script.md` reale prodotto da F3: 4 sezioni sempre marcate `## HOOK`, `## INTRO`,
`## CORPO`, `## CTA`, più una quinta `## Note SEO inline` (metadato per il writer — keyword/tag/
hook-type storici — non contenuto da narrare). Schema `produzione-spec` (`validate_schemas.py`)
richiede solo i campi top-level (`video_id/title/voice/music/hook_type/scene_count/scenes` lista),
nessun vincolo per-scena: libertà di progettare le scene senza rompere la validazione.

## 1. BUILD
Nuovo `_parse_script_scenes(script_text)`: individua gli header `## <NOME>` via regex, ritaglia il
corpo fino al prossimo header, **esclude esplicitamente "Note"** (metadato, non narrato), genera
una scena reale per ognuna delle 4 sezioni narrative con `text` = contenuto reale e `duration`
stimata da un ritmo di lettura reale (~2.5 parole/secondo), non fissa a 5.0.

`run_phase_4` riscritta: legge `working_memory["script_path"]` (fallisce onestamente con `return
False` se assente, come le altre fasi, invece di silenziare), estrae `video_id` come slug
dell'idea reale scelta in F3 (`working_memory["script_idea_title"]`, non più
`"claude-code-001"` fisso), `title` = titolo reale dell'idea, `hook_type` dal reale
`working_memory["script_idea_hook_type"]` salvato da F3.

## 2. GATE — F1→F4 con due candidati diversi, output reale a confronto
Eseguito F3+F4 due volte iniettando due video candidati reali diversi (stesso F1/F2 già gate-ato
in TASK-YT-001, non ritoccato qui): **A** = "KIMI K3 VIBE CODING Tutorial" → idea scelta
"Come installare e configurare Claude Code..."; **B** = "HOW TO CREATE AN AI VOICE AGENT..." →
idea scelta "I 5 comandi di Claude Code che i programmatori ignorano" (idea diversa, selezionata
per affinità testuale reale col titolo del video B).

```
RUN A: video_id=come-installare-e-configurare-claude-code-in-5-minuti-tutori
       title="Come installare e configurare Claude Code in 5 minuti (Tutorial Completo)"
       hook_type=Statement · scene_count=4
       scene[HOOK].duration=23.6 · scene[CORPO] cita "seo debole (score reale 17.5/25...)"

RUN B: video_id=i-5-comandi-di-claude-code-che-i-programmatori-ignorano
       title="I 5 comandi di Claude Code che i programmatori ignorano"
       hook_type=Question · scene_count=4
       scene[HOOK].duration=27.2 · scene[CORPO] senza correzione SEO (candidato B non aveva errori)
```
`scenes uguali? False` (testo/durata/video_id/title/hook_type tutti diversi tra le due run).
`scene_count` è risultato uguale (4) in **entrambe** le run: onesto, non un artefatto nascosto —
F3 emette sempre le stesse 4 sezioni narrative per costruzione (HOOK/INTRO/CORPO/CTA), quindi il
numero di scene reali coincide quando entrambi gli script hanno la stessa struttura; il valore
non è più **hardcoded a priori** (deriva dal parsing reale, cambierebbe se una sezione mancasse).
Validazione schema **PASS** su entrambe.

Rieseguito anche `python -m unittest test_youtube_apex7 -v` prima e dopo la modifica:
**11/11 verde** in entrambi i casi. Dopo il test sintetico, rieseguita una run reale pulita
(`python apex7_orchestrator.py run --phase 4`, dominio condiviso reale `youtube`) per lasciare
`05-TEMPLATES-E-KIT/produzione-spec.json` in uno stato genuino (non l'ultimo run sintetico B con
URL fittizio) prima di committare.

## 3. File toccati
- `YOUTUBE-AUTOMATION-FACTORY/02-AUTOMAZIONI-E-SCRIPTS/apex7_orchestrator.py` (`run_phase_4` +
  nuovo `_parse_script_scenes`)
- `YOUTUBE-AUTOMATION-FACTORY/05-TEMPLATES-E-KIT/produzione-spec.json` (output reale aggiornato,
  file tracciato già esistente)
- `EmpireDesk/state/taskboard.json` (`TASK-YT-002` → `fatto`)
- `company/Ecosistemi/11-APEX-7-CORE/memory/data/youtube/` (prima scrittura reale del dominio
  condiviso `youtube`, nato in TASK-YT-001 — decision log SQLite con le critiche reali di questa
  e della run precedente; coerente con `data/` già tracciato per il dominio `default`)

## Task chiusa
`TASK-YT-002` → `stato: fatto` in `EmpireDesk/state/taskboard.json`.

## RIPRESA DA
`TASK-YT-003` (F5 Pubblicazione — titolo/descrizione/tag reali da
`working_memory["script_idea_title"]` + `learned_rules.json`, oggi sempre gli stessi metadati
statici "Installare Claude Code locale"). Nessun blocco tecnico residuo su TASK-YT-002.
