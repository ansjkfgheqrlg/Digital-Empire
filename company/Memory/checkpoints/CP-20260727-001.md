# CP-20260727-001 — Risoluzione conflitto sync GitHub + fotografia stato Phase A (preventa-agents)

- **Data:** 2026-07-27
- **Ecosistema/Reparto:** 10-memory (sync) / 01-agency (preventa-maps-scraper)
- **Task:** Gael chiede "git pull, git push, aggiorna tutto" — risolvere `SYNC-CONFLICT.txt` (rebase
  fallito il 27/07 08:00) e riportare il monorepo allineato a GitHub.
- **Esito:** ✅ completato (sync) — ⚠️ trovato stato intermedio rotto in preventa-agents (non causato
  da questa sessione, ereditato dalla sessione precedente)

## Cosa è successo (sync)
Il pull --rebase falliva perché ~54 file sotto `.agents/skills/*` (+ `.codex/`, `AGENTS.md`)
esistevano **sia** come untracked locali su questo PC **sia** già committati su `origin/main`
(pushati da Max in `bbeedfca`/`a3a68b5e`/`1f9b3c54`). Git rifiuta di sovrascrivere untracked con un
checkout — da qui il conflitto.

**Risoluzione:** `git stash push -u` (isola tracked+untracked) → `git pull --rebase` pulito (3
commit di Max integrati, 1 commit locale di Gael rebasato) → `git push` (OK) → confronto mirato
`git diff HEAD stash^3 -- <path>` sui file in conflitto: **identici byte-per-byte** a quelli già su
GitHub, zero perdita. L'unico contenuto genuinamente nuovo nello stash era
`Outreach/preventa-maps-scraper/03-AGENTI-E-RUOLI/writer/` (AGENTE.md + agente.py, 810 righe) →
committato e pushato. Droppati entrambi gli stash (incluso uno orfano del 23/07 su `run.py`, già
superato dal wiring meta-optimizer successivo). `SYNC-CONFLICT.txt` cancellato come da procedura.

## Scoperta importante (non ancora in STATO-EMPIRE.md prima di questo CP)
Il commit locale `bcd4ef89` "Phase A - wipe flat agent structure" (25/07, Gael) ha **cancellato**
gli 8 file flat in `03-AGENTI-E-RUOLI/` (`AGENTE-*.md` + `agente_*.py`: scraper, qualificatore,
writer, sender, responder, integratore-sheets, gate, orchestratore) per prepararli alla
ricostruzione **cartella-per-agente** (es. `writer/AGENTE.md` + `writer/agente.py`, pattern
confermato da questo stesso commit). Solo **writer/** è stato ricostruito finora.

**Verificato a runtime:** `agents.py` (la facade ufficiale, `02-AUTOMAZIONI-E-SCRIPTS/agents.py`)
importa ancora i vecchi moduli flat (`from agente_scraper import ScraperAgent`, ecc.) →
`python -c "import agents"` fallisce con `ModuleNotFoundError: No module named 'agente_scraper'`.
**Il facade è rotto adesso.** Non è un problema di questa sessione: è lo stato in cui Gael aveva
lasciato il lavoro il 25/07 (commit di wipe senza ricostruzione né aggiornamento import).

## Output
- Sync: `SYNC-CONFLICT.txt` risolto e rimosso, main allineato a `origin/main` (commit `f1ab076d`)
- Nuovo: `Outreach/preventa-maps-scraper/03-AGENTI-E-RUOLI/writer/{AGENTE.md,agente.py}`

## Decisioni prese
Nessuna nuova ADR. Nessuna scelta di design fatta — solo recupero meccanico + fotografia dello stato.

## Lezioni/errori
Il bug "file diventano untracked-duplicati su due PC" si ripete (già visto per INDEX.md il 23/07,
CP-20260723-005): quando una skill/asset viene installata localmente su entrambi i PC in modo
indipendente (non via git) e poi uno dei due la committa, l'altro si ritrova un conflitto di
checkout al primo pull successivo. Non è un bug del motore di sync: è un promemoria che asset
scaricati da marketplace/plugin vanno committati subito, non lasciati untracked a lungo.

## Costi
Nessuno (solo comandi git).

## Prossimo passo
Ricostruire i restanti 7 agenti in formato cartella-per-agente (scraper, qualificatore, sender,
responder, integratore-sheets, gate, orchestratore) seguendo il pattern di `writer/`, POI
riallineare gli import in `agents.py` (da `from agente_scraper import ScraperAgent` a
`from scraper.agente import ScraperAgent` o equivalente) prima di considerare la Phase A chiusa —
finché `agents.py` non importa pulito, la suite di 13 test è verosimilmente rotta.
