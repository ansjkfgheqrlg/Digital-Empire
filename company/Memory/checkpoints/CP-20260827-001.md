# CP-20260827-001 — TASK-PUBLISHER-W1 — comando unico di pubblicazione (cartella caroselli -> canale), dry-run verificato su Instagram

- **Data:** 2026-08-27
- **Autore:** Claude
- **Ecosistema/Reparto:** 03-CONTENT-FACTORY
- **Task:** TASK-PUBLISHER-W1
- **Esito:** completato
- **Output:** —
- **Decisioni prese:** nessuna
- **Lezioni/errori:** nessuna
- **Costi:** —
- **Prossimo passo:** —

---

- **Data:** 2026-08-27
- **Ecosistema/Reparto:** 03-CONTENT-FACTORY / Workflow pubblicazione automatica
- **Task:** TASK-PUBLISHER-W1 â€” consolidare il workflow di pubblicazione multi-canale in un comando richiamabile
- **Esito:** âœ… completato (gate soddisfatto sul ramo "dry-run verificato": il live resta bloccato da un login manuale che non spetta a un agente)
- **Output:**
  - `SKILL & Agenti/Workflow pubblicazione automatica/pubblica.py` (nuovo, il comando)
  - `SKILL & Agenti/Workflow pubblicazione automatica/DIAGNOSI-PUBLISHER.md` (nuovo, lo stato reale del folder)
  - `SKILL & Agenti/Workflow pubblicazione automatica/_diagnostica/sessione_instagram_*.png` (prove)
- **Decisioni prese:** nessun ADR nuovo. Applicati ADR-003 (wrap, mai riscrittura) e il `REGOLE.md` locale del folder (si edita solo dentro `Workflow pubblicazione automatica/`).

---

## GATE â€” comando + esito (incollato, non riassunto)

```
cd "SKILL & Agenti/Workflow pubblicazione automatica"
python pubblica.py "../Workflow agency creative/Arsenale Caroselli/Preventa/2026-08-27_quanto-tempo-perdi-a-fare-un-preventivo"
```

```
========================================================================
 PUBBLICA - DRY-RUN VERIFICATO
========================================================================
Cartella : ...\Arsenale Caroselli\Preventa\2026-08-27_quanto-tempo-perdi-a-fare-un-preventivo
Canale   : instagram -> @digitalempireagency.e

-- 1. CONTROLLO CONTENUTO --
   [OK] slide_01.png        1080x1080  0.91MB
   [OK] slide_02.png        1080x1080  0.84MB
   [OK] slide_03.png        1080x1080  0.87MB
   [OK] slide_04.png        1080x1080  0.86MB
   [OK] slide_05.png        1080x1080  0.87MB
   [OK] slide_06.png        1080x1080  0.89MB
   [OK] caption.txt        924 caratteri, 7 hashtag
   => 6 slide + caption: contenuto valido per Instagram.

-- 2. CANALE --
   [OK] 'instagram' ha un publisher reale (publisher reale con login + carosello + esito onesto).

-- 3. SESSIONE BROWSER --
   [X]  NON autenticato: form di login presente. url=https://www.instagram.com/ | screenshot=sessione_instagram_20260827-201606.png

-- 4. ESITO --
   Dry-run: non e' stato pubblicato nulla (nessun post creato).
   Contenuto e canale sono verificati; la SESSIONE no.
   Serve un login una tantum:
      python Instagram/setup_session.py

VERDETTO: PASS PARZIALE - contenuto pronto, sessione da fare (nessun PASS finto)
EXIT=2
```

L'input Ã¨ **l'output reale di TASK-CAROSELLI-W1** chiuso stamattina (CP-20260825-003): le
due task ora si toccano davvero, non solo sulla carta.

**La verifica di sessione non Ã¨ una stampa**: apre Chrome reale, va su instagram.com,
salva uno screenshot in `_diagnostica/`. Il browser Ã¨ partito davvero (Chrome
151.0.7922.174) e ha caricato la pagina vera â€” quello che manca Ã¨ solo la sessione.

### Gli altri 5 run del gate (le strade che devono FALLIRE)

| Test | Atteso | Ottenuto |
|---|---|---|
| cartella prodotto `Preventa/` (nessuna slide diretta) | elenca le 3 sottocartelle candidate | FAIL, exit 1 âœ… |
| carosello reale `2026-08-06` (senza `caption.txt`) | blocca | `[X] caption.txt assente`, exit 1 âœ… |
| cartella inesistente | blocca | exit 1 âœ… |
| `--canale tiktok` | blocca col motivo | `NON e' pronto: import config rotto`, exit 1 âœ… |
| **`--live` senza sessione** | **rifiuta di provare** | `--live rifiutato: la sessione non risulta autenticata`, exit 1 âœ… |

L'ultima riga Ã¨ la piÃ¹ importante: il `--live` **non Ã¨ stato tentato**, di proposito.
Nessun post reale Ã¨ mai stato creato in questa sessione.

---

## Cosa ho trovato (verificato eseguendo, non leggendo)

Il folder era in stato peggiore di come si presentava. Dettaglio completo in
`DIAGNOSI-PUBLISHER.md`; i 5 difetti che contano:

1. **`push_social.py` â€” il publisher che `CLAUDE.md` dichiara OBBLIGATORIO â€” Ã¨ una
   simulazione.** Eseguito davvero: stampa `Pubblicazione completata con successo
   (SIMULATA)!` ed esce **0**. La `requests.post` Ã¨ commentata e il payload non
   contiene nemmeno i media. Ãˆ il PASS finto peggiore: chi si fida dell'exit code
   crede di aver pubblicato. â†’ **B-024**
2. **`main_orchestrator.py` non parte proprio**: `OpenAIError: Missing credentials` a
   *import time* (`ai_client.py` istanzia `OpenAI(...)` a livello di modulo). E stampa
   `FLUSSO COMPLETATO CON SUCCESSO!` **incondizionatamente**, senza guardare l'esito.
   â†’ **B-025**
3. **`Instagram/instagram_publisher.py::publish()` non puÃ² fallire**: `try/except` che
   ingoia tutto e ritorna `None`. Il chiamante non distingue successo da fallimento, e
   la funzione non fa nemmeno il login. **Per questo `pubblica.py` NON usa questo
   motore** ma `scripts/ig_carousel_publish.py`, che ritorna un `bool` onesto.
4. **`scripts/ig_carousel_publish.py` era buono ma con l'ingresso morto**:
   `CAROSELLI_DIR = C:\Users\Utente\Desktop\qui tutto\...` â€” utente `Utente`, qui Ã¨
   `gdiar`. `exists() -> False`: nessun `--auto/--folder/--list` poteva funzionare.
   Non l'ho toccato (ADR-003): `pubblica.py` gli passa un path assoluto. Verificato che
   accetta la cartella Arsenale cosÃ¬ com'Ã¨: `is_ready() -> (True, '6 img OK')`.
5. **`TikTok/tiktok_publisher.py` non importa**: `import config` invece di
   `from TikTok import config`. â†’ **B-026**

**Sicurezza â€” `Instagram/config.py` ha `IG_PASSWORD` in chiaro ed Ã¨ tracciato sul repo
PUBBLICO.** Terza occorrenza dopo B-020 (Brevo) e B-021 (Arena), e come B-021 Ã¨ una
**password di account**, non una API key. â†’ **B-023**. Nota d'ordine: la password va
cambiata **prima** del login una tantum, non dopo, altrimenti invalida la `session_data`
appena creata.

**Instagram ha cambiato il login sotto i piedi**: la home loggata-fuori oggi espone
`input[name="email"]` / `input[name="pass"]`, non piÃ¹ `input[name="username"]`. Quindi
il `do_login()` automatico **non funziona** su IG 2026 (â†’ **B-027**). Trovato guardando
lo screenshot del primo run, non indovinando: il mio stesso rilevatore aveva dato "stato
ambiguo", ed era il rilevatore ad avere torto, non la pagina. Corretto e ri-verificato.

---

## Lezioni/errori

- **Il primo verdetto del mio comando era sbagliato e me l'ha detto lo screenshot.**
  Il rilevatore di sessione cercava `input[name="username"]` e ha risposto "stato
  ambiguo" su una pagina che era palesemente la schermata di login. Se non avessi
  guardato l'immagine avrei scritto una diagnosi falsa con la coscienza a posto. La
  prova visiva non Ã¨ un extra del dry-run: Ã¨ ciÃ² che rende il verdetto affidabile.
- **Il codice piÃ¹ utile del folder era anche il piÃ¹ nascosto.** `scripts/ig_carousel_publish.py`
  Ã¨ l'unico publisher completo (login + carosello + esito onesto + dedup) e nessuno lo
  usava, perchÃ© aveva un path di un'altra macchina. Il difetto non era il motore, era
  l'ingresso: la task diceva "portarlo a uno stato usabile da un comando, non ricostruirlo",
  ed era letteralmente vero.
- **Un exit code 0 non vale niente se nessuno lo verifica.** Tre pezzi diversi di questo
  folder dichiarano successo senza averne titolo (Â§1, Â§2, Â§3 sopra). Per questo
  `pubblica.py` ha tre esiti e non due: `0` PASS, `1` FAIL, **`2` PASS PARZIALE** â€”
  cosÃ¬ "contenuto pronto ma sessione mancante" non puÃ² travestirsi da successo.
- **Ho fatto ripartire l'anti-collisione dei checkpoint invece di aggirarla.**
  `python -m empire mem write` (il fix di B-009, mai usato da nessuno) non girava:
  `ModuleNotFoundError: No module named 'yaml'`. Un `pip install pyyaml` e funziona â€”
  questo checkpoint Ã¨ scritto con quel comando, non a mano. Probabilmente Ã¨ **quello**
  il motivo per cui la regola veniva ignorata da 5 sessioni: non era pigrizia, era rotta.
  Segnalazione utile a TASK-MEMORY-SYNC-W1.

## Costi

Nessun costo API: la task era diagnosi + wrapping, nessuna generazione. Un
`pip install pyyaml`.

---

## Prossimo passo / RIPRESA DA

1. **Login una tantum su Instagram** (lo deve fare un umano, non un agente â€” e serve
   l'ok di Max perchÃ© Ã¨ il suo account):
   `python Instagram/setup_session.py` â†’ poi `pubblica.py <cartella>` deve dare
   **VERDETTO: PASS** (exit 0) invece di PASS PARZIALE.
2. **Solo con ok esplicito di Max**: primo `--live` reale sul carosello Preventa del
   27 agosto. Il comando si rifiuta da solo finchÃ© la sessione non c'Ã¨, quindi non c'Ã¨
   rischio di pubblicare per sbaglio.
3. **Prima del punto 1**: chiudere **B-023** (password IG in chiaro) â€” cambiarla dopo
   aver creato la sessione la invaliderebbe.
4. Canali 2-5 di `REGOLE.md` (LinkedIn cross-post, MentalitÃ  Brutale, Codice dei
   Potenti su TikTok+IG): oggi `pubblica.py` li **dichiara non pronti** invece di
   fingere. Sbloccarli Ã¨ B-026 (import TikTok) + una sessione per canale.
