# CP-20260709-001 — Max — PreventivoForge: archivio auto-svuotamento a chiusura app

**Data:** 2026-07-09 · **Base:** ultimo su main `063cd27` (build precedente)

## Richiesta Max
"pulisci l'archivio e l'archivio si deve svuotare ogni volta che chiudo l'app."

## Fatto
1. **`implementation/archivio.py`** → nuova funzione `clear()`: rmtree `pdf/`+`foto/` + unlink `index.json`.
   Mai bloccante. **NON tocca i PDF nella cartella di output** (quelli restano al cliente) — svuota solo la galleria.
2. **`app.py`** → chiama `archivio.clear()` dopo la chiusura della finestra, sia in `main_webview` (dopo `webview.start()`)
   sia in `main_gui` (dopo `root.mainloop()`), entrambe in try/except.
3. **Archivi su disco svuotati ora** (dev `preventivo-forge/archivio` + dist).
4. **Exe consegna RIBUILDATO** (PyInstaller `--noconfirm --clean`, BUILD_EXIT=0, exe 2026-07-09 10:15).
   `.env` (chiavi Groq) + `LEGGIMI.txt` ripristinati dopo il `--clean`.
5. **Zip consegna rigenerato**: `Consegna-Novacar/PreventivoForge-Novacar.zip` = **117.4 MB**.

## Verificato
- Unit-test `clear()`: pieno (pdf=9/foto=9/index) → `pdf=0 foto=0 index=False`; `entries()` vuoto → `[]`.
- Zip: exe nuovo (10:15) + `.env` + `LEGGIMI.txt` presenti; modulo bundillato `archivio.py` contiene `def clear()`;
  nessuna cartella dati `archivio/` (solo il modulo in `_internal`). 1976 entry.

## Limite noto
Svuotamento a **chiusura pulita** (X). Crash / Task Manager → voci restano, spariscono alla prossima chiusura pulita.
Non è un errore: i PDF veri (output) non si toccano mai.

## Ripresa
- Consegna Novacar: zip aggiornato pronto. Guida `COME-CONSEGNARE-A-NOVACAR.md` invariata.
- Se serve la stessa modifica su altri concessionari futuri: è già nel motore (`nuovo_concessionario.py --build` copia il dist aggiornato).
