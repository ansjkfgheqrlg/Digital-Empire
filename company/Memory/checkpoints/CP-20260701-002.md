# CP-20260701-002 — PreventivoForge: gate wiring + test end-to-end + fix (Max)

**Data:** 2026-07-01 · **Autore:** Max · **Tipo:** cliente / integrazione + test

## Contesto
Dopo la consegna di Half B (Gael, CP-20260701-001), completato il pending di Max: wiring dei gate
in `run.py`, test end-to-end e verifica del PDF preventivo. Cliente Prof Autocad.

## Fatto
- **Wiring gate B/C/D + Gate A forte** in `run.py`: helper `_gate()` che invoca `qa_gate.gate_*`
  (se presente) o fallback minimo. Gate rosso → stop con exit code dedicato (5=B, 6=C, 7=D).
- **BUG trovato e risolto:** `print()` di `€`/emoji andava in `UnicodeEncodeError` su console Windows
  (cp1252). Fix: `sys.stdout/err.reconfigure(encoding="utf-8", errors="replace")` in cima a `run.py`.
- **`.gitignore`**: ignora tutta `runs/*/` (artefatti transitori) tranne `.gitkeep`.
- Incluso `PREVENTIVO-FORGE.MD` (guida semplificata).

## Test END-TO-END (modalità --manual, fixture GLA realistica JSON-LD + 3 foto)
`python run.py --manual gla.html --foto foto --dealer prof-autocad` → **EXIT 0, 4 GATE VERDI:**
- S2 parsing: Mercedes-Benz GLA 220 d 4MATIC, prezzo 33.900. Gate A ✅
- S3: 8 optional tradotti IT, 12 specs, 6 highlights, 0 tedesco residuo. Gate B ✅
- S4: **33.900 → 37.917 €** (×1.03 +1500 +1500), titolo corretto. Gate C (ricalcolo indip.) ✅
- S5: `preventivo_*.pdf` 60 KB (Playwright). Gate D ✅
- PDF ispezionato: header dealer, titolo+prezzo, scheda tecnica IT, punti forza, descrizione IT scorrevole,
  dotazioni IT, galleria, pagina "prezzo chiavi in mano" + validità 15gg. Qualità ottima.

## Limite noto (ambientale, NON bug)
Scraping LIVE mobile.de **bloccato da qui** (anti-bot + IP datacenter + headless). Atteso.
Sul PC del cliente (IP residenziale, prima volta `PLAYWRIGHT_HEADLESS=false` per consenso) o via
`--manual` (HTML salvato) funziona. Da validare nell'ambiente cliente.

## RIPRESA DA
- **Max/consegna:** validare scraping live sul PC cliente; inserire dati reali dealer (logo/telefono/email)
  in `concessionarie/prof-autocad/config.json`; allineare stile PDF al riferimento "Preventivo BMW Z4".
- **PreventivoForge: COMPLETO e funzionante end-to-end.** Impero ancora in pausa fino a GO Max.
