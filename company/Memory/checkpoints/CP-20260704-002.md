# CP-20260704-002 — Max — PreventivoForge: consegna Novacar pronta + fix GUI/Chrome

**Data:** 2026-07-04
**Autore:** Max (Half A)
**Ultimo commit main:** `71a3859`

## Fatto oggi
1. **Riserva AI traduzione ATTIVA** (€0): provider Groq gratuito (riuso chiave Outreach `GROQ_API_KEY`).
   Glossario 99% + AI sui soli residui tedeschi. Testato live (4/4 termini + auto-riparazione "Fernbedienung").
   `.env` (gitignorato) su PC Max e nelle app dealer (via fabbrica `--build`).
2. **Fix GUI**: `app.py _StreamToQueue` ora FILTRA → alla GUI solo frasi pulite (`_MILESTONES`), non il log tecnico.
3. **Fix Chrome scraping nascosto**: `cdp.py launch` headful → finestra fuori schermo (`--window-position=-32000`)
   invece di `--start-maximized`. Resta headful (Akamai ok). Testato live: Hyundai i20 scrapato, PDF ok.
4. **Pacchetto consegna Novacar**: `Consegna-Novacar/PreventivoForge-Novacar.zip` (120 MB, gitignorato) con
   exe + kill-switch + riserva AI + `LEGGIMI.txt`. Guida: `COME-CONSEGNARE-A-NOVACAR.md`.
5. **pyrightconfig.json**: silenzia i falsi "modulo non trovato" dell'editor (import runtime via sys.path).

## File Half B toccati (avvisato Gael in STATO)
- `app.py`: filtro `_StreamToQueue`, lettura `brand.json`, `_CODE_MSG` 8/9/10, guard stdout selftest, load `.env` frozen.
- `translate_copy.py`: 1 hook `_ai_fill_residuals`. Nessun altro file Half B toccato.

## Verificato
- Exe frozen ricostruita: selftest EXIT=0, licenza attiva, 6/6 gate.
- Scrape live off-screen: EXIT=0.
- Filtro GUI: 7 frasi pulite invece di 10+ righe tecniche.

## Ripresa
- **Max:** consegna a Novacar (zip pronto). Poi ISPETTORATO M1 (vedi STATO).
- **Gael:** Impero V2-2/V2-3 (ordine assoluto). Se riprende GUI/traduzione → parte dai file aggiornati.
