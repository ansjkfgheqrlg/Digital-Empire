# CP-20260730-007 — TASK-GAEL-NFT-METODO CHIUSO — RETRO finale

- **Data:** 2026-07-30
- **Ecosistema/Reparto:** 12-STREAM-S7-BOT (layer NFT, R&D speculativo)
- **Task:** `company/Memory/tasks/TASK-GAEL-20260730-STREAM-S7-NFT-METODO.md` — **COMPLETO**
  (Fase 0 + Ondata 1-4 + Fase 2, tutti i passi della sezione 8 "ordine di marcia")

## Cosa e' stato costruito
Layer NFT nuovo per Stream S7 (`nft_magiceden_client.py`, `nft_analysis_engine.py`,
`nft_monte_carlo.py`, `nft_ondata2.py`, `nft_ondata3.py`, `nft_ondata4.py` + 4 file di test +
`STUDIO-NFT-FASE0.md` + `FAILURE-MODES-NFT.md`), 100%% su dati reali Magic Eden (API pubblica,
nessuna chiave), affiancato al motore memecoin gia' chiuso senza toccare un solo file frozen
(`event_bus.py`, `memory_interface.py`, `quality_gates.py`, `gate_verifiers.py`, `gate_agent.py`,
`meta_agent.py`, `orchestrator.py`, `ruflo_adapter.py`, `analysis_engine.py`, `risk_manager.py`,
`execution_engine.py`, `position_monitor.py` — tutti invariati, verificato via `git diff`).

**Totale controlli reali superati: 89/89** (Fase 0 7/7 + Ondata1 39/39 + Ondata2 26/26 +
Ondata3 11/11 + Ondata4 13/13), zero capitale vero, zero chiamate a pagamento.

> **⚠️ CORREZIONE (stessa giornata, dopo il primo push).** Questi numeri erano stati pubblicati
> come **78/78** (25/21/10/15 per ondata) in tutti i checkpoint, in `INDEX.md`, in
> `STATO-EMPIRE.md` e nella Definition of Done del task. **Erano sbagliati**: avevo contato le
> chiamate `check()` scritte nel sorgente invece dei controlli davvero *eseguiti* — diverse
> funzioni ciclano su 3 collection e producono 3 controlli ciascuna, quindi il totale reale è
> più alto. Ricontati eseguendo davvero le suite
> (`python <suite>.py 2>&1 | grep -c "\[OK \]"`, con verifica che `KO=0`): **39 / 26 / 11 / 13**.
> Corretto ovunque. Il verdetto e le conclusioni **non cambiano** — cambia solo il conteggio,
> che era per difetto. Lezione: un numero riportato va misurato con lo stesso rigore preteso dai
> numeri del metodo, non stimato leggendo il codice.
>
> Nota: Ondata 2 è passata da 25 a 26 controlli anche per un motivo reale — il test di latenza è
> stato irrobustito (vedi sotto).

## Difetto trovato e corretto DOPO la chiusura (test fragile)
`test_nft_ondata2.py` è fallito in una riesecuzione successiva con `TimeoutError` sulla chiamata
live a Magic Eden: il test misurava la latenza reale a ogni run, quindi **una condizione di rete
transitoria faceva cadere l'intera suite** — un gate inaffidabile, difetto mio di progettazione.
Corretto separando le due cose: (a) la **logica** di misura è ora verificata sempre con uno stub
deterministico (3 controlli, zero rete, incluso il caso "rete giù"), (b) la **misura reale** resta
un tentativo che, se la rete non risponde, viene dichiarato `[SALTATO — NON misurabile adesso]`
con l'errore vero, **senza inventare un numero al suo posto** (`measure_real_latency` ritorna
`measured: False` invece di sollevare). Riverificato: 26/26 OK, misura reale riuscita a 3133.5ms,
coerente con i 3333.1ms registrati in CP-20260730-003.

## Il verdetto (quello che conta davvero)
**INVARIATO rispetto a `report-studio.md`: bocciato per produzione live.** Il layer NFT
(floor-rarity mismatch sniping su Magic Eden) risolve **parzialmente 1 problema strutturale
su 3** (il rate-limit RPC, solo sul lato scansione — 20 chiamate concorrenti reali senza 429
contro le 2 dell'RPC Solana pubblico), lascia **irrisolti** latenza competitiva (misurata
2456-3624ms, piu' lenta in ms assoluti del benchmark MEV 300-800ms) e rug/abbandono (soglia di
rottura calcolata 16.9%%, probabilita' reale ignota). L'edge sull'unica collection reale con
segnale (degods, 3 trade su 31 listing) non e' statisticamente distinguibile da zero al 95%%
di confidenza (IC95%% -2.00%%/+34.70%%), il fit fair-value e' debole E instabile (bootstrap
R² fino a 0.257 di IC95%%), e il meccanismo di uscita assunto (buyNow diretto) e' l'evento
piu' raro nell'attivita' reale osservata (1/400). **Resta R&D/paper-trading**, come da
clausola esplicita del task.

## Scoperte non richieste ma rilevanti (da non perdere)
1. Il mercato NFT reale oggi scorre su bid/pool (market-making automatizzato), non su buyNow
   diretto — l'edge modellato punta a un canale minoritario del volume reale.
2. Non tutte le collection hanno rarity rank disponibile (y00ts: 0/98) ne' tutti i campi stats
   attesi (degods: `volume7d` assente) — limiti della fonte dati da tenere a mente per qualunque
   sviluppo futuro su questa API.
3. L'API pubblica Magic Eden e' molto piu' robusta dell'RPC Solana pubblico (20 concorrenti OK
   vs 2) ma non infinita (429 reale su uso cumulato) — utile per chiunque tocchi questo layer
   in futuro.

## Divergenza git (segnalata, non risolta — fuori perimetro)
`STATO-EMPIRE.md` (blocco COORDINAMENTO in cima) documenta una divergenza reale tra HEAD locale
e `origin/main` su `YOUTUBE-AUTOMATION-FACTORY`/`Outreach`/`company/Memory` (due sessioni hanno
chiuso lo stesso lotto TASK-YT-002..007 in parallelo). Non toccata: fuori dal perimetro di
questo task, lavoro di un'altra sessione. **Il push di questo lavoro NFT creera' un secondo
capo divergente su `origin/main`** finche' qualcuno (Max o una sessione dedicata) non fa la
merge reale — segnalato qui esplicitamente prima del push, non scoperto dopo.

## Costi totali del task
Nessuno. Solo API pubbliche gratuite (Magic Eden, Solana RPC pubblico, CoinGecko) + `gh` gia'
autenticato per il clone del repo di riferimento.

## Prossimo passo (per Max/Gael)
1. Decidere se/quando riconciliare la divergenza git segnalata (non bloccante per la lettura
   di questo lavoro, ma bloccante per un push pulito futuro).
2. Se si vuole comunque procedere verso un pilot live (sconsigliato dal verdetto): servirebbe
   prima (a) un RPC provider Solana a pagamento per l'esecuzione, (b) un tasso storico reale di
   rug/abbandono su collection blue-chip, (c) piu' collection/piu' storico per un campione
   statisticamente solido — nessuno di questi 3 esiste oggi.
3. Nessuna azione di Gael richiesta per chiudere: il task e' completo secondo la sua stessa
   Definition of Done (sezione 7), verificata voce per voce.
