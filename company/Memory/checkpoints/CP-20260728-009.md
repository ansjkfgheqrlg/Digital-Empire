# CP-20260728-009 — TASK-YT-003 chiusa: F5 Pubblicazione, metadati reali (titolo/descrizione/tag/brief)

- **Data:** 2026-07-28
- **Ecosistema/Reparto:** YOUTUBE-AUTOMATION-FACTORY
- **Owner:** Gael · **Esecuzione:** Claude
- **Task:** `TASK-YT-003` (`company/Memory/tasks/TASK-GAEL-20260728-YOUTUBE-FACTORY.md`), terzo
  lotto YT, dipende da TASK-YT-001/002 (chiusi in [CP-20260728-007](CP-20260728-007.md) e
  [CP-20260728-008](CP-20260728-008.md)).

## 0. RECALL
`run_phase_5` scriveva sempre lo stesso titolo/descrizione/tag ("Installare Claude Code locale",
`["claude code", "antigravity", "digital empire"]`, descrizione placeholder), indipendentemente
dal video/idea reali. Letto `seo_score.py`: pesi trasparenti (title 25/description 25/tags 20/
thumbnail 15/subtitles 15), soglie precise (title: keyword+lunghezza 20-70; description: >=200
caratteri + prime 2 righe >=40 + keyword + link/CTA; tags: >=8 per punteggio pieno + keyword tra
i tag). Nessun vincolo per-scena/per-campo nello schema `produzione-spec`/`metadati` oltre ai
campi richiesti — libertà di costruire i contenuti senza rompere la validazione.

## 1. BUILD
- **F1** (`run_phase_1`): aggiunta una riga, `working_memory["canale_cluster"] = scelto["cluster"]`
  — il cluster/niche reale (es. "Tech-Hacker Screencast") era già calcolato ma non salvato,
  serviva come input reale per i tag di F5 ("eventuale cluster/niche reale di F1" nel task).
- **F5** (`run_phase_5`): riscritta. Rilegge `script.md` reale e riusa `_parse_script_scenes`
  (già scritto in TASK-YT-002) per ottenere HOOK/INTRO/CTA reali, ripulendo le annotazioni
  `➕ ...` (note di regia per il producer, non copy per lo spettatore).
  - **Titolo**: `working_memory["script_idea_title"]` (idea reale di F3), non più fisso.
  - **Descrizione**: HOOK + INTRO + CTA reali + una CTA esplicita finale con "iscriviti"/"link"
    per garantire il marcatore CTA richiesto da `seo_score.py`, non più placeholder.
  - **Tag**: `learned_rules.json["high_performing_tags"]` + `canale_cluster` reale di F1 +
    parole chiave reali tokenizzate dal titolo dell'idea (`_tokenize_for_matching`, già esistente)
    + la keyword `"claude code"`, deduplicati.
  - **Brief miniatura**: `concept`/`text_overlay` derivati dall'HOOK reale (prime 5 parole in
    maiuscolo per l'overlay, estratto dell'hook per il concept), non più fissi.
  - Il gate SEO ora **logga onestamente** il risultato reale di `seo_score.py` invece di
    stampare sempre "PASS": se `pass_soglia_70` è `false`, stampa `FAIL onesto` con le debolezze
    reali — non blocca la fase (nessun refiner esiste per i metadati come per lo script in F3),
    ma non nasconde più il dato.

## 2. GATE — 2 candidati diversi, output reale a confronto
Eseguito F3+F4+F5 su due candidati reali diversi (stessi A/B di TASK-YT-002):

```
RUN A: title="Come installare e configurare Claude Code in 5 minuti (Tutorial Completo)"
       tags=[antigravity, claude code, digital empire, Tech-Hacker Screencast, claude, code,
             completo, configurare, installare, minuti]
       seo_score.py --json metadati.json -> total=92.5, pass_soglia_70=true
       nota onesta: "lunghezza titolo 73 fuori 20-70" (titolo reale, non forzato a 70 char)

RUN B: title="I 5 comandi di Claude Code che i programmatori ignorano"
       tags=[antigravity, claude code, digital empire, Tech-Hacker Screencast, claude, code,
             comandi, ignorano, programmatori]
       seo_score.py --json metadati.json -> total=100.0, pass_soglia_70=true
```
`title uguale? False` · `tags uguali? False`. `seo_score.py` reale (non simulato) eseguito su
entrambi i metadati generati, `pass_soglia_70: true` su entrambe le run — nessun caso sotto
soglia incontrato in questo test, ma il ramo "FAIL onesto" è comunque implementato e pronto (non
`return False`: la fase completa comunque, il segnale onesto serve alla dashboard/audit, non è un
gate bloccante di per sé in questo lotto).

`python -m unittest test_youtube_apex7 -v` **11/11 verde** prima e dopo la modifica. Rieseguita
anche una run reale pulita (`python apex7_orchestrator.py run --phase 5`, dominio condiviso reale
`youtube`) per lasciare `05-TEMPLATES-E-KIT/metadati.json` e `brief-miniatura.json` in uno stato
genuino prima di committare.

## 3. File toccati
- `YOUTUBE-AUTOMATION-FACTORY/02-AUTOMAZIONI-E-SCRIPTS/apex7_orchestrator.py` (`run_phase_1`:
  +1 riga `canale_cluster`; `run_phase_5`: riscritta)
- `YOUTUBE-AUTOMATION-FACTORY/05-TEMPLATES-E-KIT/metadati.json` +
  `YOUTUBE-AUTOMATION-FACTORY/05-TEMPLATES-E-KIT/brief-miniatura.json` (output reale aggiornato)
- `EmpireDesk/state/taskboard.json` (`TASK-YT-003` → `fatto`)

## Task chiusa
`TASK-YT-003` → `stato: fatto` in `EmpireDesk/state/taskboard.json`.

## RIPRESA DA
`TASK-YT-004` (F6 Audit — gate onesto, niente `views_per_hour` finto; serve un manifest
`memory/published_videos.json` per video realmente pubblicati). Nessun blocco tecnico residuo su
TASK-YT-003.
