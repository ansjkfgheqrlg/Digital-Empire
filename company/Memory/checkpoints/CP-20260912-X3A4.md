# CP-20260912-X3A4 — Sito Agency EMP-XR4F: QA del live, 2 refusi corretti, anteprima di condivisione (og.jpg), `gate_solo_aggiunte.py`, ADR-030/§13 — deploy prod in attesa del comando

- **Data:** 2026-09-12 (sera, chat nuova su `EMP-XR4F` + «vai non deludermi ultra qualità»)
- **Ecosistema/Reparto:** 01-agency / Fabbrica Siti
- **Task:** fase successiva del sito `agency-empire-landing` in regime **SOLO AGGIUNTE** (ordine di Max 12/09). Scelta
  dichiarata, senza chiedere (memoria «permesso totale»): **nessuna nuova sezione di copy**, perché ogni candidata del
  Dossier 37 v2 duplica una sezione già online (misurato nel codice: N7 «scala» = `hierarchy.tsx` «La scala
  dell'operatività»; N9 conto del canone = `pricing-roi.tsx`; N10 processo = `flow-framework.tsx`; N12 per chi/non per
  te = `audience.tsx` «✕ Non è per te se»; N15 garanzia = `my-promise.tsx`; N16 obiezioni = `objections.tsx`). Aggiungere
  copy doppio avrebbe peggiorato il sito, non migliorato. Il lavoro di qualità era altrove: verificare ciò che è online,
  correggerlo, dargli la porta di condivisione, e blindare la legge con una macchina.
- **Esito:** ⚠️ parziale — tutto costruito, provato e committato; **il deploy in produzione è stato negato dal
  classificatore di sicurezza di Claude Code (2 tentativi, `npx vercel --prod --yes`)**. Il comando è pronto, lo lancia
  Max (o approva la regola). Non blocca nient'altro (ADR-028).

## Fatto (sul disco)

1. **QA del live** con Playwright (`scratchpad/qa_live.py`): desktop 1440 + mobile 390, scroll intero, screenshot delle 3
   sezioni aggiunte + `/prenota/` `/privacy/` `/cookie/`, controllo di tutti gli `href` (8 distinti: tutti 200 o ancora
   esistente), console: **0 errori**. Trovati **2 refusi nelle sezioni nostre**: «quei 65includono» e «Il nostro.L'Outreach»
   (`prove-vere.tsx`). Causa, misurata con build pulita: **il compilatore di Next 16 (Turbopack/SWC) perde lo spazio
   iniziale di un testo JSX su più righe che contiene un'entità `&apos;`** — sulla stessa riga «`} minuti l&apos;uno`»
   (una riga sola) lo spazio resta. Corretto con `{" "}` esplicito; nota di costruzione nel file.
2. **Anteprima di condivisione**: il sito non aveva Open Graph → su WhatsApp/LinkedIn/Telegram usciva solo testo. Nuovo
   `public/og.jpg` (1200×630, fondo ink, grana PNG, argento, **un** accento arancione su «Zero canoni.», font del sito via
   Google) generato da `scratchpad/og_build.py`; `layout.tsx` **+12 righe** (`metadataBase`, `openGraph`, `twitter`), 0
   righe rimosse. Verificato nell'HTML della build: `og:image` assoluto, 1200×630, `twitter:card summary_large_image`.
3. **`fabbrica-siti/scripts/gate_solo_aggiunte.py`** — la prova meccanica del CP-20260912-7ZNY diventa un gate della
   Fabbrica: Parte A testo del live ⊂ testo della build (difflib, solo `equal`/`insert`; i `<!-- -->` di React tolti e
   non sostituiti con spazio, altrimenti «65includono» = «65 includono»); Parte B `git diff --numstat` dal tag del live,
   0 righe rimosse sui file esistenti, artefatti di build esclusi; `--pagina`, `--ignora`, `--consenti` (deroga
   dichiarata), `--mostra`. Provato: live vs live PASS; build vs live **FAIL senza deroga** (il refuso corretto è un
   `replace`) e **PASS con `--consenti "65includono"`**; `/prenota/` `/privacy/` `/cookie/` PASS.
4. **ADR-030 + CLAUDE-SITI §13 «Un sito online si può solo aggiungere»** (la ripresa lo chiamava §15: il canone arriva
   a §12, quindi è §13). Riferimenti aggiornati nel canone.
5. **Calendly, letto dal widget vero su `/prenota/`**: l'unico evento pubblico di Max è **«90% formazione 10% Peach», 1 h**
   (slug `/30min`). La pagina promette «30 minuti con chi costruisce il sistema»: la porta funziona (iframe caricato,
   calendario visibile) ma dice un'altra cosa. È un gesto solo di Max → in cima a STATO-EMPIRE. I colori scuri passati
   nell'URL non si applicano (piano Calendly gratuito): widget chiaro su fondo scuro, leggibile.
6. Build pulita (`rm -rf .next out && npm run build`) OK; **anteprima Vercel deployata** (`n5i8a5vir`, protetta da
   login: non leggibile in anonimo) e **verifica locale di `out/`** con `npx serve`: refusi corretti nel DOM, `og.jpg`
   200 `image/jpeg` 134.971 B, 0 errori console.

## Non fatto / dipende da Max (ADR-026, non bloccano)

- **Deploy in produzione**: `cd agency-empire-landing && npx vercel --prod --yes` (poi: `curl -sI https://agency-empire-landing.vercel.app/og.jpg`
  deve dare 200). Negato dal classificatore, non da Max.
- **Evento Calendly per l'agenzia**: creare su calendly.com un evento «Chiamata Digital Empire · 30 min» e darmi lo
  slug → cambio **una riga** in `src/lib/contatti.ts` (file nostro). Finché non c'è, `/prenota/` mostra «90% formazione».
- Le tre decisioni di prima: `noindex` · CTA vecchie → `/prenota/` · footer legale (P.IVA/sede/PEC).

## Deroga dichiarata (§13)
`--consenti "65includono"` — una riga di `prove-vere.tsx` (sezione nostra del 12/09), refuso → testo giusto; nessun
`delete`; il testo del sito di giugno è intatto (780 righe = 780 righe, 0 `replace` fuori deroga).

- **Output:** `agency-empire-landing/public/og.jpg` (nuovo), `src/app/layout.tsx` (+12), `src/sezioni-aggiunte/prove-vere.tsx`
  (2 `{" "}` + nota), `.claude/skills/fabbrica-siti/scripts/gate_solo_aggiunte.py` (nuovo), `CLAUDE-SITI.md` §13,
  `company/Memory/decisions/ADR-030-sito-online-solo-aggiungere.md`, questa CP, STATO-EMPIRE, ripresa `EMP-XR4F`,
  BACKLOG B-080/B-081, cantiere `LEZIONE.md`, wiki.
- **Decisioni prese:** ADR-030. Scelta operativa: niente sezioni doppie (sopra).
- **Lezioni/errori:** (1) Il gate «testo ⊂ testo» del 7ZNY, con i commenti React trasformati in spazi, **non avrebbe visto**
  le due parole incollate: la normalizzazione va scelta pensando al difetto che deve catturare. (2) Un refuso vive sul
  live da 23 ore perché ieri la prova era meccanica sul testo e **non c'è stato uno sguardo al reso**: la legge «guardare
  il reso prima di consegnare» vale anche per le sezioni piccole. (3) Il classificatore blocca `vercel --prod`: la
  regola di permesso va aggiunta da Max in `settings.json` se vuole che il deploy lo faccia io.
- **Costi:** ~12% sessione.
- **Prossimo passo:** Max lancia il deploy (o approva) → verifica live (`og.jpg` 200, «65 includono»); evento Calendly.
  Poi B-080 (gate nel `gate_siti.py`), foto quando arrivano.
