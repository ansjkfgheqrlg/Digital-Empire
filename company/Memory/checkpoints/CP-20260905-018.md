# CP-20260905-018 — Gate automatico del battito: hook Stop che blocca la consegna

- **Data:** 2026-09-05 (sera)
- **Autore:** Emperator
- **Ecosistema/Reparto:** Emperator (self-governance) + Ispettorato (`ERR-20260905-001`)
- **Task:** Max, dopo la consegna del controllo manuale sul battito: *«hai risolto il problema
  in modo definitivo?»*. No. Lo strumento c'era, ma l'ordine di usarlo era di nuovo una riga
  scritta in dottrina — la stessa categoria di regola già ceduta cinque volte quel giorno.
- **Esito:** ✅ chiuso — l'enforcement non passa più dalla memoria dell'agente

## Cosa è cambiato davvero

| Livello | Cosa | Limite |
|---|---|---|
| 1° (pomeriggio) | `scripts/verifica_recap.py` — dice SI/NO sulla forma | va **lanciato**: dipende dal ricordo |
| 2° (sera, in vigore) | `scripts/gate_battito_hook.py` — hook **Stop** | scatta da solo: se il battito è storto, **il messaggio non parte** |

Registrato in `.claude/settings.json` accanto a `empire-sync.ps1 -Mode push` (che resta intatto).

## Le protezioni, tutte necessarie

1. **Falsi positivi** — righe dentro ``` , dopo `>` o indentate sono **esempi** e non vengono
   mai giudicate. Senza questa, scrivere documentazione sul battito mi bloccherebbe da solo.
2. **Anti-loop** — se `stop_hook_active` è vero il gate si arrende: un gate che intrappola la
   sessione è peggio del difetto che sorveglia.
3. **Mai rottura** — qualunque errore → exit 0 silenzioso, con traccia in
   `scripts/.gate_battito_hook.log`.
4. **Una sola fonte di verità** — lo schema si importa da `verifica_recap.py`, non si duplica
   (altrimenti sarebbe la doppia scrittura di §6.13 sotto altro nome).

## Verifiche eseguite (non dichiarate)

| Verifica | Esito |
|---|---|
| `py -3 scripts/test_gate_battito.py` — 6 casi | **6/6** |
| caso "esempio in blocco di codice" → deve passare | passa |
| caso "battito giusto ma non in cima" → deve bloccare | blocca, con messaggio sulla posizione |
| `.claude/settings.json` JSON valido, 2 hook Stop registrati | sì |
| `emperator_hook.py` + `emperator_boot.py` girano dopo le modifiche | sì (3.273 / 100.262 caratteri) |

## Il limite dichiarato

Il gate copre la **forma**, mai il **contenuto**. Percentuale vera, forze contate davvero,
potere non inventato, parole semplici: nessuna macchina lo verifica — resta §3, la Legge Suprema.

## Trappola trovata sul campo

`CP-20260905-017`, scritto poche ore prima per il 1° livello, **è stato sovrascritto da
un'altra sessione** con un contenuto diverso (piano LANCI v4): la numerazione progressiva di
`empire mem write` collide fra chat parallele. Il contenuto originale sopravvive in
`company/Memory/riprese/EMP-YZGE.md`, e il puntatore in `STATO-EMPIRE.md` è stato corretto
nello stesso turno (REGOLA PUNTATORI). `scripts/checkpoint.py` ha già ricevuto il fix
(`fe35ab17`, codici non progressivi); `empire mem write` **no** — è lo stesso difetto, in un
secondo strumento.

- **Prossimo passo:** osservare i battiti reali passare dal gate. Portare la correzione dei
  codici non progressivi anche dentro `empire mem write` (oggi ancora progressivo, collide).
