# CP-20260703-001 — PreventivoForge: GUI App PREMIUM (pywebview) — priorità #1 Max

- **Data:** 2026-07-03
- **Ecosistema/Reparto:** 01-AGENCY / Cliente Novacar srl — PreventivoForge (App, Gael)
- **Task:** Priorità #1 di Max (STATO 2026-07-02): **alzare del 2000% la qualità grafica dell'app** `app.py`.
  Layout/struttura/colore APPROVATI da Max → invariati; alzare l'ESECUZIONE a livello premium/luxury.
- **Esito:** ✅ completato — GUI premium costruita, testata (dev + .exe), validata WebView2.

## Output (path reali)
- `ui/index.html` — **UI luxury HTML/CSS** (slate + argento): font di sistema premium (Segoe UI Variable),
  gradienti sobri, filo argento metallico nell'header, ombre morbide, campi con focus-ring, bottone con
  gradiente + hover/pressione, **barra avanzamento animata**, log scuro con testo colorato per esito.
- `app.py` — finestra premium via **pywebview** + bridge Python↔JS (`_WebApi`: generate/poll/dealers,
  log live, apre il PDF). `main_webview()` con **fallback automatico a Tkinter** (`main_gui`) se pywebview/
  WebView2 non disponibile → nessun PC resta senza app. Titolo finestra → **"Novacar srl"** (fix Max).
- `preventivo-forge.spec` — bundle `ui/` + hiddenimports `webview`/`edgechromium`/`winforms`/`tkinter`.
- `implementation/glossary_de_it.py` — aggiunto "Sitzeinstellung"/"Sitzverstellung" + varianti (Gate B
  bloccava un preventivo Mercedes CLS reale su questa parola).
- **.exe ricostruito:** `dist/PreventivoForge/PreventivoForge.exe` (11 MB, pywebview bundlato, gitignorato).

## Verifica (TEST reale)
- **GUI premium confermata WebView2** sia in dev (`python app.py`) sia nell'**.exe** (processo `msedgewebview2`
  attivo, non fallback Tkinter) — verifica pulita via PowerShell (PreventivoForge.exe + WebView2 = True).
- Bridge: `dealers()`→['novacar'], `generate(bad url)`→validazione, `poll()` ok; `webview` importa; ui bundlato.
- Selftest pipeline headless → PDF via cdp, Gate D verde (nessuna regressione).
- **Prova reale precedente (2026-07-02):** Mercedes GLA 220 da mobile.de → 26 foto, prezzo 44.490→48.825 €,
  PDF generato (scraping headful supera Akamai — fix `PLAYWRIGHT_HEADLESS=false` nell'app).

## Confine rispettato (ownership)
**NON toccati PDF/template/REGOLE/render_pdf/qa_gate** — sono ownership di Max (STATO "STOP collisioni").
Lavorato SOLO su `app.py` + `ui/` + `spec` + glossario (traduzione S3). Rebase ha integrato pulito le
migliorie parallele di Max ad app.py (redirect stdout per pythonw, messaggi errore leggibili, `PF_NO_OPEN`).

## Lezioni/errori
- pywebview su Windows usa Edge **WebView2** (presente su Win10/11); il fallback Tkinter copre i rari PC senza.
- `pythonw`/.exe: `sys.stdout` è None → serve redirigere stdout (fatto da Max) per non crashare le print().
- Chrome ≥111 richiede `--remote-allow-origins=*` (già gestito lato render).

## Costi
API €0. Install locali: pywebview. Build .exe locale (gitignorata).

## Prossimo passo
Attendere feedback di Gael/Max sulla resa premium (eventuali ritocchi tonalità/spaziature/font).
PDF resta a Max. Poi (GO Max): ripresa scelta ecosistema Empire.
