# CP-20260907-PG43 — Gate A4: le tre verifiche payload L20 chiuse, scoperta bgMusicVolume mai usato

- **Data:** 2026-09-07
- **Ecosistema/Reparto:** 05-youtube / studio AI TUBE PRO (ripresa `EMP-V6DE`)
- **Task:** chiudere le "tre verifiche nuove contro il payload reale" assegnate al gate A4 da L20
  (SFX via API? timing per-media? tetto 50 scene via API?), rimaste aperte anche dopo
  CP-20260907-JVY2. Continuazione diretta di quel checkpoint, ordine di Max ("vai continua").
- **Esito:** ✅ completato (2 risposte accertate, 1 dichiarata non verificabile senza spesa reale)

## Output

Letta la documentazione ufficiale `developer.fliki.ai` (non il nostro `fliki_client.py`, che
riflette solo cio' che usiamo, non cio' che l'API offre), due volte con prompt diversi per
riscontro incrociato prima di dare un campo per accertato.

- **SFX via API: SÌ** — campo `generateSfx` (boolean) su `POST /v1/generate/video`.
- **Timing per-media: NO** — nessun campo di layer/timing nello schema documentato.
- **Tetto 50 scene: NON VERIFICABILE DALLO SCHEMA** — nessuna menzione di limiti di scena nella
  doc; puo' essere un vincolo di piano applicato silenziosamente (come i minuti). Resta aperta,
  a differenza delle prime due: serve una chiamata reale per chiuderla, non fatta oggi.
- **⭐ Scoperta non cercata:** `bgMusicVolume` (number 0-100) è anch'esso un campo reale del
  payload, mai mandato dal nostro `fliki_client.py`. La regola registrata `A4-L20-01` ("i nostri
  video non hanno musica: campo musica inesistente nel payload") resta vera nella premessa stretta
  (il NOSTRO payload di oggi) ma la causa è una **scelta mai fatta**, non un limite dell'API — le
  due frasi non sono la stessa cosa e la seconda va scritta prima che qualcuno la legga come la
  prima.
- Report: `company/Memory/studi/aitubepro/A4-metodo-ai-tube/VERIFICA-PAYLOAD-L20-GATE-A4.md`.
  `REPORT-CATEGORIA.md` §6 aggiornato nello stesso turno (REGOLA PUNTATORI).
- **Avviata in parallelo** (background, non bloccante): `corso_prepara.py --categoria "Viral
  Mastery"`, scaricamento+trascrizione delle 10 lezioni di A6, prossima categoria in coda.

## Decisioni prese

Nessun ADR nuovo. Due candidati binario B messi in coda al gate A6, **non applicati oggi**
(ADR-024, "il motore si tocca solo a gate di categoria"): aggiungere `generateSfx: true` al
payload, e valutare `bgMusicVolume` (richiede un ascolto A/B prima di attivarlo su un canale vero
— stesso principio di A4-L04-04: non si crede, si ascolta).

## Lezioni/errori

Leggere il nostro client per rispondere "cosa puo' fare l'API" è l'errore che questo checkpoint
evita apposta: `fliki_client.py` dice solo cosa **facciamo**, non cosa **si può fare**. La stessa
categoria di errore di A4-L01-03 (un controllo che dichiara vero cio' che ha scritto lui stesso),
qui evitata andando alla fonte terza (doc ufficiale) invece che al nostro codice.

## Verifiche

- Due fetch indipendenti di `developer.fliki.ai/docs/api/generate/video` (prompt diversi), campi
  coincidenti su entrambe le letture.
- Nessuna chiamata API reale eseguita (zero credito speso, zero video generato).

## Costi

Zero — solo lettura di documentazione pubblica. Nessuna sentinella attivata per questo pezzo
(ricerca diretta, non richiede visione di frame).

## Prossimo passo

**Categoria A6 «Viral Mastery» (10 lezioni)** — download+trascrizione in corso in background
(`corso_prepara.py`). Al termine: estrazione frame per lezione (`frame_extractor.py --run
corso-aitubepro/<lesson_id> --input <url>`, l'indirizzo sta in `mappa.json`), poi sentinelle in
lettura (max 2-3 in parallelo, regola gia' in memoria), regole a registro, gate A6 a 7 condizioni
— che chiude anche **D-1** e **D-2** (i due difetti della fabbrica tenuti aperti apposta dal
2026-09-04). Restano invariati, non fatti: tempo per video (`A4-L05-04`), campo `YouTube channel
ID(s)` su Fliki (`A4-L19-01`, gratis, richiede l'interfaccia Fliki — non l'API).
