# CP-20260723-003 — G-C1 riallineato al prodotto vero + G-C2 sezione PROVE Novacar (gap CRO n.1)

- **Data:** 2026-07-23
- **Ecosistema/Reparto:** 06a-PLATFORM (`agency-empire/`) — sito agency
- **Task:** dossier 25 §G-C — G-C1 sezione Preventa separata, G-C2 sezione PROVE/case-study Novacar.
- **Esito:** ✅ completato. `npm run build` verde, sezioni verificate nell'HTML prerenderizzato.
- **Output:**
  - `agency-empire/src/sections/03b-preventa.tsx` — **riscritta** (esisteva già una v1 non committata,
    vedi §Lezioni). Ora: nota di posizionamento che separa esplicitamente il tier SaaS dai workflow
    €5-15k · 3 benefici (velocità/controllo/autonomia, da `preventa-launch-kit/01_LANDING_COPY`) ·
    "come funziona" in 3 step · card prezzo · ponte a `#prove`.
  - `agency-empire/src/sections/09b-prove-novacar.tsx` — **nuova** (G-C2). 4 numeri contati +
    prima/dopo + un preventivo reale dallo storico + i 6 controlli + blocco "cosa questa prova NON dice".
  - `agency-empire/src/app/page.tsx` — `<ProveNovacar />` dopo `<Portfolio />`.
  - `agency-empire/src/components/navbar.tsx` — voci `#preventa` e `#prove` (7 voci: padding ridotto
    a `px-2.5 xl:px-3.5` perché a 1024px erano al limite dell'overflow).
- **Decisioni prese:** nessun ADR nuovo. Il prezzo Preventa (DEC-EST-005: €490 attivazione + €149/mese)
  è esposto in un blocco costanti isolato in cima a `03b-preventa.tsx` con `MOSTRA_PREZZO`: il veto
  **M-EST-4 di Max è ancora aperto** → se cambia, si tocca una riga sola; se Max preferisce non
  esporre la cifra, `MOSTRA_PREZZO = false` e la pagina rimanda alla demo.
- **Lezioni/errori:**
  - **Nessun numero inventato.** Ogni cifra della sezione PROVE è contata su disco e la fonte è
    scritta in un commento in cima al file: 65 record in `preventivo-forge/Memory/storico-preventivi/`
    (`grep -c run_id` = 65, dealer `novacar`, 3→13 luglio 2026) · 52 PDF nelle cartelle
    `preventivi_<data>` · 11 marche distinte · ~2 min per preventivo (`CONSEGNA-NOVACAR.md` §3) ·
    6 controlli (gate A/B/C/D + IMG + R, CP-20260702-003) · esempio Opel Insignia 15.950 → 19.428 €
    (`AF-20260713-150552-459563767_novacar_Opel-Insignia.json`, letto direttamente).
  - **Difetto reale trovato nella v1 di `03b-preventa.tsx`:** la copy vendeva funzioni che il motore
    **non ha** — "permuta e finanziamento inclusi nel calcolo automatico" e "il venditore inserisce
    veicolo e allestimento". PreventivoForge fa: link annuncio → scraping → traduzione IT → formula
    prezzo → PDF. Nessun modulo permuta/finanziamento, nessun inserimento manuale del veicolo. Erano
    claim presi dal launch-kit Arena (copy aspirazionale) e finiti in pagina come se fossero prodotto.
    Riscritta sul motore reale. **Da sapere: se un prospect chiede la permuta, oggi non c'è.**
  - **Onestà messa in pagina, non tolta:** la sezione PROVE dichiara apertamente che (1) non
    sosteniamo che Novacar venda di più, (2) i 65 preventivi includono i nostri collaudi su annunci
    veri e non sono il registro vendite del cliente, (3) la testimonianza firmata non c'è ancora.
    Coerente con `case-study-forge` (mai pubblicare prove non verificate) e col tono di 11b-carte-scoperte.
  - **Discrepanza con CP-20260723-002 — da chiarire con Max.** CP-002 dichiara che
    `09b-prove-novacar.tsx` era "già presente e già importato in page.tsx" da un'altra sessione.
    Misurato qui: `git status` mostrava solo `03b` untracked, `git diff page.tsx` conteneva solo
    l'import di `Preventa`, `git log --all -- 09b-prove-novacar.tsx` è **vuoto** (mai committato),
    `git stash list` vuoto, e il Write ha risposto "File created" (non "updated"). Quel file **non
    era su disco** quando ho guardato: o è andato perso prima, o il checkpoint l'ha dato per fatto.
    La versione ora su disco è quella scritta qui.
  - 2 errori di build miei, corretti: (1) l'hook formatter rimuove gli import ancora inutilizzati →
    aggiungere import e JSX nello stesso passaggio, non in due Edit separati; (2) `preventivi_*/`
    dentro un commento a blocco `/* */` lo chiude in anticipo → Turbopack "Unexpected character '→'".
  - `npm run lint` è **rotto a monte** (Next 16 ha rimosso `next lint`: interpreta "lint" come
    cartella). Pre-esistente, non causato da questo task. Il type-check gira comunque dentro `next build`.
  - `git pull` non eseguito prima del checkpoint (B-009): rifiutato per unstaged changes proprie e
    della sessione parallela. ID scelto guardando i CP già su disco (001, 002 → 003).
- **Costi:** token normali. Nessuna API a pagamento, nessun deploy.
- **Prossimo passo:** Max — (1) veto M-EST-4 sul prezzo in pagina, (2) confermare se la permuta va
  promessa (oggi non esiste: o si costruisce o resta fuori dalla copy), (3) chiarire la discrepanza
  su 09b con la sessione parallela prima di intestare gli artefatti in `REGISTRO-IMPRESA.md`/
  `skills-map.yaml` (ADR-008). Gael — G-A3 (follow-up G+2/G+5 + tracking), poi G-B1. Il sito **non è
  stato deployato**: le sezioni sono in locale, il deploy resta una decisione di Max.
