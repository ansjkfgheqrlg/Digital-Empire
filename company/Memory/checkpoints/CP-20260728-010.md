# CP-20260728-010 — TASK-YT-004 chiusa: F6 Audit, gate onesto senza metriche finte

- **Data:** 2026-07-28
- **Ecosistema/Reparto:** YOUTUBE-AUTOMATION-FACTORY
- **Owner:** Gael · **Esecuzione:** Claude
- **Task:** `TASK-YT-004` (`company/Memory/tasks/TASK-GAEL-20260728-YOUTUBE-FACTORY.md`), quarto
  lotto YT, dipende da TASK-YT-001..003 (chiusi in CP-20260728-007/008/009).

## 0. RECALL
`run_phase_6` scriveva sempre `views_per_hour: 35.5` fisso in `performance_logs.json` — il
self-improver imparava su rumore inventato, non su performance vere. Nessun video è mai stato
realmente pubblicato da questa fabbrica (F1-F5 producono script/spec/metadati, non caricano nulla
su YouTube): lo stato onesto oggi è "niente da auditare", non un numero finto.

## 1. BUILD
- Nuovo manifest `memory/published_videos.json`: lista di voci `{run_id, video_id, url,
  channel_handle, published_at}`, popolata SOLO quando un video è davvero stato caricato (da
  Gael/Max), non a ogni run.
- Nuovo `_find_published_entry()`: cerca la voce per `self.run_id`. Assente → F6 ritorna `True`
  **senza** scrivere nulla in `performance_logs.json` (log onesto, non un errore).
- Se la voce esiste ma `published_at` è più recente della soglia di maturità già usata da F2
  (`VIDEO_MATURITY_FLOOR_HOURS = 24h`, riusata qui): log onesto "troppo presto per un audit
  reale", nessuna scrittura.
- Se la voce è matura: riusa `_get_channel_videos(handle)` (stesso fetch pubblico cache-first/
  live-fallback già scritto per F2) per prendere i video reali del canale, individua il video
  tramite il nuovo `_extract_youtube_id(url)` sull'URL del manifest, calcola
  `views_per_hour = views / age_hours` reale (stesso identico calcolo di F2, non una nuova
  formula inventata). Se il video non compare nel fetch pubblico: log onesto, nessuna scrittura
  (dato reale non disponibile ≠ dato inventato).
- `ctr`/`retention_rate` impostati esplicitamente a `null`: richiedono YouTube Studio (dati
  privati), non ottenibili dal fetch pubblico usato qui — **non inventati** per riempire il
  campo. `keyword`/`tags` presi dai metadati reali di F5 (`working_memory["metadati_path"]`),
  `hook_type` da `working_memory["script_idea_hook_type"]` (F3): nessun campo del log resta
  hardcoded.
- **Fix collaterale necessario** in `self_improve.py`: `float(metrics.get("ctr", 0.0))` esplode
  con `TypeError` se il valore è esplicitamente `None` (non mancante) — cambiato in
  `float(metrics.get("ctr") or 0.0)` per gli stessi 3 campi (`views_per_hour`/`ctr`/
  `retention_rate`). Comportamento invariato su tutti i log storici reali (erano sempre float
  non-None); il fix serve solo per i nuovi log con `null` esplicito introdotti da questo lotto.

## 2. GATE — entrambi i casi, output reale

**Caso (a) — run senza manifest:**
```
$ wc -l memory/performance_logs.json   # PRIMA
95 memory/performance_logs.json
$ python apex7_orchestrator.py run --phase 6 --run-id gate-yt004-nomanifest
...
[i] Nessuna voce reale in published_videos.json per la run gate-yt004-nomanifest: video non
    ancora pubblicato. Nessuna scrittura in performance_logs.json (non è un errore).
$ wc -l memory/performance_logs.json   # DOPO
95 memory/performance_logs.json
```
Diff = **zero righe**, come richiesto dal gate.

**Caso (b) — run con manifest e video reale:** creata una voce di test in
`published_videos.json` puntando a un video REALE già in cache da F2 (canale `@ciraolone`,
`-RzPQSS3Bhw`, 19.000 viste / 72h nella cache), `published_at` impostato >24h fa:
```
$ python apex7_orchestrator.py run --phase 6 --run-id gate-yt004-published
...
[🔬 ANALYST] Audit reale: 263.89 views/ora (19000 viste in 72h, fonte cache).
...
```
Riga aggiunta a `performance_logs.json`:
```json
{
  "video_id": "test-real-fetch-gate-yt004",
  "keyword": "claude code",
  "voice": "Fabio (Italiano)",
  "hook_type": "Statement",
  "tags": ["antigravity", "claude code", "digital empire", "Low-Code Business Architect",
           "claude", "code", "completo", "configurare", "installare", "minuti"],
  "metrics": {
    "views_per_hour": 263.89,
    "ctr": null,
    "retention_rate": null,
    "curve_type": "non disponibile (richiede YouTube Studio, non ottenibile da fetch pubblico)"
  },
  "source": "cache"
}
```
`263.89`, non `35.5` — numero reale calcolato da viste/età reali del video.

**Pulizia post-gate**: `test-real-fetch-gate-yt004` non è un video realmente pubblicato da
Gael/Max (è il video di un concorrente, riusato solo per provare che il meccanismo di fetch+
calcolo funziona su dati pubblici reali) — rimossa la voce da `performance_logs.json` (tornato
a 95 righe, verificato `git diff` a zero) e rimosso `published_videos.json` (nessun video
realmente pubblicato oggi, stato naturale = assente). Rieseguito `self_improve.py` sul dataset
pulito per non lasciare bias residuo in `learned_rules.json` (unico cambiamento:
`last_updated`, `dynamic_min_vph` invariato a 35.5).

`python -m unittest test_youtube_apex7 -v` → **11/11 verde** prima e dopo la modifica.

## 3. File toccati
- `YOUTUBE-AUTOMATION-FACTORY/02-AUTOMAZIONI-E-SCRIPTS/apex7_orchestrator.py` (`run_phase_6`
  riscritta, nuovi `_find_published_entry`/`_extract_youtube_id`/`PUBLISHED_VIDEOS_PATH`)
  YOUTUBE-AUTOMATION-FACTORY/02-AUTOMAZIONI-E-SCRIPTS/self_improve.py` (fix `None`-safety)
- `EmpireDesk/state/taskboard.json` (`TASK-YT-004` → `fatto`)

Nessun file di dati reale lasciato alterato dal test sintetico (`performance_logs.json`
verificato identico byte-per-byte via `git diff`, `published_videos.json` assente come da stato
naturale pre-pubblicazione).

## Task chiusa
`TASK-YT-004` → `stato: fatto` in `EmpireDesk/state/taskboard.json`.

## RIPRESA DA
`TASK-YT-005` (Dashboard — scrivere lo stato REALE della run corrente, oggi
`run_youtube_apex7.py` scrive sempre tutto 🟢 PASS a prescindere). Nessun blocco tecnico residuo
su TASK-YT-004.
