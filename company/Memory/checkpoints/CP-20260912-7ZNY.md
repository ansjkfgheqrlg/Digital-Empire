# CP-20260912-7ZNY — Sito Agency: RIPRISTINATO al live di giugno su ordine di Max, poi SOLO 3 sezioni aggiunte + `/prenota/` — la riscrittura v2 è BOCCIATA

- **Data:** 2026-09-12
- **Ecosistema/Reparto:** 01-agency / Fabbrica Siti
- **Task:** Max, dopo il deploy del Sito Agency Vivo v2: *«hai letteralmente rovinato tutto il sito. Riporta il sito esattamente com'era
  prima che lo modificassi. Poi aggiungi le sezioni che vuoi aggiungere senza modificare quelle che già ci sono. Non puoi modificare
  niente di ciò che già c'è, puoi solamente aggiungere.»*
- **Esito:** ✅ eseguito alla lettera.
  1. **Live ripristinato** con `npx vercel promote` del deploy `pvjhy6yyw` (quello di 100 giorni prima): byte identici, H1 «Automatizziamo
     la tua operatività», `robots noindex` com'era.
  2. **Codice ripristinato al sorgente che corrisponde al live**: commit `57a0ba0b` (giugno). Verificato: build del sorgente di giugno ==
     testo del live (identico). Il "restyling F1+F2" del 2 settembre (`a745231b`, `84c73c33`) **non era mai stato online**: resta in git,
     non lo tocco, decide Max. Tag `agency-empire-landing-live-20260912`.
  3. **Solo aggiunte**: 3 sezioni nuove in `src/sezioni-aggiunte/` (`specchio` dopo Problems, `prove-vere` dopo Results, `cosa-ottieni`
     prima di FinalCTA) + pagine nuove `/prenota/` (Calendly, brand agenzia), `/privacy/`, `/cookie/` + `src/lib/{fatti,listino,contatti,legal}.ts`
     + `vivo.css` con **ogni regola scopata sotto `.vivo`**. `page.tsx` +7 righe, `layout.tsx` +1 riga, **0 righe rimosse**. Prova
     meccanica: il testo del live di prima è contenuto intatto nel nuovo (difflib: solo 3 `insert`); la sezione esistente `#risultati`
     renderizza uguale (diff pixel 3,6/255 = grana). Deploy `5cgwpu2p2`, verificato sul live.
- **Cosa resta com'era, per ordine di Max:** noindex, CTA esistenti verso `chiamata-formazione.netlify.app`, footer `href="#"`, 37 sezioni
  originali, prezzi in 3 file. Le sezioni nuove mandano a `/prenota/` (nostra); le vecchie no.
- **Decisioni prese:** **LEGGE (memoria + emperator.md da aggiornare): su un sito di Max che è online non si riscrive, non si sostituisce,
  non si "migliora" l'esistente — si AGGIUNGE soltanto, e ogni aggiunta è scopata (CSS sotto un wrapper, file nuovi, inserimenti puri in
  page.tsx). Una riscrittura intera si fa solo se Max la ordina con quelle parole.** Il «vai» sul Dossier 37 v2 NON era quell'ordine: io
  l'ho letto come licenza di rifare tutto.
- **Lezioni/errori:** (1) **Ho scambiato «vai» sul piano per «rifai il sito».** Il piano diceva "20 sezioni al posto di 37" e Max lo ha
  visto solo online. Un piano che sostituisce l'esistente va mostrato **prima** in anteprima (deploy di preview, non produzione) — mai
  produzione al primo colpo su un sito vivo. (2) **Il "prima" è il LIVE, non il sorgente**: il repo aveva un restyling mai deployato;
  se avessi ripristinato "al tag" avrei messo online un'altra versione ancora. Verificare sempre build-del-sorgente == live prima di
  chiamarlo "prima". (3) Il dossier 37 v2, EMP-2AW3 e CP-20260911-92MC restano come storia: **il sito vivo v2 è nel tag
  `canone-v3-landing`/commit `6565545f`, bocciato**.
- **Costi:** ~15% sessione.
- **Prossimo passo:** niente sul sito senza un ordine nuovo di Max. Se vuole altre sezioni: stesso metodo (aggiunta scopata).
