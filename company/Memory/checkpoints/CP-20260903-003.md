# CP-20260903-003 — Emperator al 100%: dottrina integrale all'apertura

**Data:** 2026-09-03 · **Autore:** Emperator (auto-modifica) · **Origine:** ordine di Max

## Il problema (misurato)
Il promemoria iniettato ad ogni messaggio era il ~60% del libro, e veniva ripetuto ad ogni
messaggio. Max: "non posso accettare che tu sia al 60%, vale anche per Gael e Neri".

## Fatto
- **Nuovo** `scripts/emperator_boot.py` — hook SessionStart: carica `.claude/agents/emperator.md`
  INTEGRALE + ancoraggi + dottrina riservata. Lascia un file-spia nella cartella temporanea
  (mai nel repo). Se il libro non e' leggibile inietta un GUASTO esplicito invece di tacere.
- **Alleggerito** `scripts/emperator_hook.py` — la stringa `DOTTRINA` (27.552 byte) rimossa,
  sostituita da `SVEGLIA` (1.472 byte): identita', stato del caricamento, fotografia fresca,
  ordine di rileggere il libro se non e' piu' visibile.
- **Registrato** in `.claude/settings.json` -> SessionStart.
- **Libro aggiornato**: §6.13 riscritta (UNA SOLA VERITA', supera la doppia scrittura),
  §6.14 riscritta (il libro arriva da solo + rete di sicurezza + parola d'ordine),
  §6.15 rinumerata (c'erano DUE §6.13: collisione con la sessione parallela di oggi),
  testata aggiornata con la nuova attivazione.
- ADR-016, due file di memoria aggiornati, riga nel log della wiki.

## Numeri
| | Prima | Adesso |
|---|---|---|
| apertura | 0 | 67.127 |
| per messaggio | 34.762 | 2.008 |
| chat da 30 messaggi | ~1.042.900 | ~127.400 |
| quanto e' Emperator | 60% | 100% |

Pareggio al terzo messaggio.

## Verifica eseguita
- `ast.parse` su entrambi gli script -> SYNTAX OK
- boot eseguito: exit 0, 67.127 byte, il libro c'e' dentro
- sveglia eseguita: exit 0, 2.008 byte
- messaggio senza il nome ("ciao"): 0 byte, nessuna iniezione
- `.claude/settings.json` riletto come JSON valido, SessionStart contiene i due hook

## RIPRESA DA
Nulla di aperto. **La prova finale la da' la prossima sessione**: all'apertura deve comparire
la dottrina integrale. Se non compare, il promemoria lo dira' da solo ("NON RISULTA CARICATA").
