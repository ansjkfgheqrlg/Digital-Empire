# CP-20260729-005 — TASK-YT-004 CHIUSA: F6 Audit onesto, niente metriche finte

- **Data:** 2026-07-29
- **Ecosistema/Reparto:** YOUTUBE-AUTOMATION-FACTORY (modello operativo governato dall'Estate)
- **Task:** TASK-YT-004 (P1) — F6 Audit: gate onesto, mai `views_per_hour` finti. Terzo lotto YT
  costruito dall'Estate nella stessa sessione (dopo F4 e F5).
- **Esito:** ✅ chiusa. Gate VERDE.

## Il difetto (il più grave dei tre)
`run_phase_6` appendeva SEMPRE a `performance_logs.json` un log con `views_per_hour: 35.5` (ctr 8.2,
retention 55, curve "regolare") **inventato**, indipendente da qualsiasi pubblicazione reale. Poi
lanciava `self_improve.py`: il self-improver **imparava le "regole vincenti" su rumore inventato**.
Ogni run avvelenava `learned_rules.json` (tag/hook/voci "ad alta performance" derivati dal nulla).

## Fix (additivo, stesso unico file)
`apex7_orchestrator.py`:
- Nuovo helper module-level **`_fetch_video_views(url_or_id)`**: views reali via pagina pubblica
  YouTube (regex `"viewCount":"…"`), `None` su qualunque errore — meglio nessun numero che uno finto.
- **`run_phase_6` riscritto onesto**, basato su manifest `memory/published_videos.json`
  (`run_id`, `video_id`, `url`, `published_at`):
  - nessuna voce reale per la run → **non scrive nulla**, non lancia il self-improver;
  - voce troppo recente (`published_at` < 24h) → nessuna scrittura (troppo presto per un audit);
  - voce reale abbastanza vecchia → `views_per_hour = views_reali / età_ore` (fetch pubblico); se il
    fetch fallisce → nessuna scrittura. `ctr`/`retention_rate` restano `null` (richiedono YouTube
    Studio, analytics del proprietario — non si inventano); aggiunge `views_totali`, `eta_ore`,
    `source: "fetch-pubblico-reale"`.

## Gate (comandi + output reali)
`scratchpad/gate_yt004.py` (offline, senza pubblicare nulla):
```
CASO A: nessun manifest        -> righe perf prima=6 dopo=6   [OK] 0 aggiunte
CASO B: voce troppo recente    -> righe perf prima=6 dopo=6   [OK] 0 aggiunte
GATE: VERDE
```
Regressione: `python -m unittest test_youtube_apex7` → **Ran 11 tests — OK (11/11)** (l'E2E asserisce
solo l'esistenza degli artefatti di fase, non che F6 appenda: nessuna assert rotta).
Ramo "video reale abbastanza vecchio → fetch reale" documentato ma non eseguito qui: richiede un
video DAVVERO pubblicato (oggi non esiste ancora — manca il render .mp4). Non si fabbricano numeri
per testarlo.

## Perimetro / taskboard
Toccato solo `apex7_orchestrator.py`. `EmpireDesk/state/taskboard.json`: **TASK-YT-004 → fatto**.

## Stato fabbrica YouTube dopo questa sessione
**F1-F6 ora oneste/reali.** F4 (multi-scena), F5 (metadati reali), F6 (niente metriche finte).
Resta un solo lotto P1: **TASK-YT-005** (Dashboard riflette PASS/FAIL reale; oggi la dashboard è
scritta da `run_youtube_apex7.py`, percorso fantasma, sempre 🟢 PASS). Poi P2: TASK-YT-006
(ritiro APEX-7 duplicato in Stream-S7-Bot) e TASK-YT-007 (docs).

## Prossimo passo (RIPRESA DA)
**TASK-YT-005**: aggiungere `write_dashboard()` a `Apex7Orchestrator`, chiamato a fine
`execute_workflow`, che scrive `06-DASHBOARD-E-METRICHE/YOUTUBE-PERFORMANCE-DASHBOARD.md` leggendo
`self.working_memory` reale (canale/video scelti, esito vero di ogni gate). Gate: forzare un FAIL
(topic che fa fallire il niche-gate) → dashboard mostra 🔴 FAIL, non sempre 🟢 PASS. Decidere il
destino di `run_youtube_apex7.py` (ritiro o riallineo). Regia: `empire cantiere`.
