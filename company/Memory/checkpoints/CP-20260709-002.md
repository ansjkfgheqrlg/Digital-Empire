# CP-20260709-002 — Max — PreventivoForge: regola globale prezzo (fixed_2 = guadagno nascosto)

**Data:** 2026-07-09 · **Base:** CP-20260709-001

## Richiesta Max (regola GLOBALE, ogni preventivo/concessionario) — 2 passi
1. "Immatricolazione, pratiche e trasporto" = **1.500** in UNA sola voce (fixed_1); voce "Trasporto" rimossa.
2. Il **secondo 1.500** (fixed_2 = guadagno) va **sommato alla voce "Prezzo autovettura"** (non voce a sé, non solo nel totale).

## Interpretazione (formula invariata)
`finale = round(esposto ×1.03 + fixed_1(1500) + fixed_2(1500))` → **totale immutato**.
Cambia SOLO la visualizzazione del breakdown nel PDF:
- Prezzo autovettura → `listed + fixed_2` (guadagno inglobato, indistinguibile)
- fixed_1 → voce unica "Immatricolazione, pratiche e trasporto" (1.500)
- fixed_2 → NON è una voce; è dentro "Prezzo autovettura".
Conseguenza: **le voci visibili sommano ESATTAMENTE il totale**.

## Fatto
- **`implementation/render_pdf.py` → `_price_novacar`**: "Prezzo autovettura" = `(listed or 0) + f2`;
  rimossa la voce "Trasporto"; voce fixed_1 rinominata "Immatricolazione, pratiche e trasporto".
- Unico punto di verità (usato da tutti i dealer). Template `preventivo.html` invariato (itera `price.lines`).
- Nessun gate blocca: gate_c ricalcola il totale dai parametri (non somma le voci); R-08 controlla solo presenza totale.

## Verificato
- `_price_novacar` test: TOTALE **19.428**; voci = Prezzo autovettura **17.450** · Maggiorazione (3%) 478 ·
  Immatricolazione, pratiche e trasporto 1.500. Somma voci = 19.428 (torna col totale). Nessuna voce "Trasporto".

## Consegna
- Exe ribuildato + zip `Consegna-Novacar/PreventivoForge-Novacar.zip` rigenerato.

## Coordinamento
- ⚠️ Gael: `render_pdf.py` (Half B) toccato da Max per ordine globale. Registrato in STATO-EMPIRE (lista file Half B).

## Ripresa
- Regola globale attiva su ogni preventivo. Se cambia l'importo del margine → è `fixed_2` nel config del dealer
  (`concessionarie/<id>/config.json`), resta sempre nascosto dalle voci.
