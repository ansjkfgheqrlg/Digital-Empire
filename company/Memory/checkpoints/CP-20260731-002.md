# CP-20260731-002 — Skill apex-7 dallo zip workspace: già presente e identica, nessuna aggiunta necessaria

**Data:** 2026-07-31
**Area:** 11-APEX-7-CORE / `.agents/skills`
**Commit:** `7c4fbef3` (+ push su `origin/main`)

## Richiesta
Max: «dentro `C:\Users\olhad\Downloads\workspace-019f930a-6fa5-7345-94e4-856fcd7f845b (1).zip`
c'è una skill chiamata apex-7, aggiungila su GitHub e salva tutto».

## Cosa è stato verificato (non assunto)
Lo zip (135 MB, 3873 entry) contiene 4 cartelle di primo livello: `ruflo` (2509 file),
`content-forge2.0` (1033), `master-build-architecture` (289), **`apex-7` (42)**.

I 42 file di `apex-7/` sono stati estratti in scratchpad e confrontati **file per file con
SHA256** contro `.agents/skills/apex-7/` già presente nel repo:

```
IDENTICAL: tutti i 42 file combaciano (SHA256)
0 ONLY-IN-ZIP · 0 ONLY-IN-REPO · 0 DIFFERS
```

Stato Git della skill, verificato dopo `git fetch origin main`:
- `git ls-files .agents/skills/apex-7` → **42 file tracciati** in locale
- `git ls-tree -r origin/main -- .agents/skills/apex-7` → **42 file presenti su GitHub**
- `git diff origin/main HEAD -- .agents/skills/apex-7` → **nessuna differenza**
- `check-ignore` → non ignorata

**Conclusione: la skill apex-7 era già su GitHub, identica allo zip. Non c'era nulla da
aggiungere** — copiarla di nuovo avrebbe prodotto zero diff. Struttura confermata: 7 agenti
(orchestrator, planner, analyst, writer, critic, gate-agent, meta-agent), 7 stage
(00-bootstrap → 06-output), 4 script Python, event-bus, self-evolution, 4 strategie in memory.

## Cosa è stato effettivamente salvato ("salva tutto")
Il repo era **3 commit avanti** su `origin/main` e **0 indietro** (nessuna divergenza: quella
segnalata il 2026-07-30 in STATO-EMPIRE risulta rientrata, è un fast-forward pulito).

Commit `7c4fbef3` creato con le 3 modifiche pendenti in working tree:
- `copertina-arena-candidata-1.png` — rigenerata dalla miniatura sorgente (1.62 MB → 1.90 MB)
- `arena_thumbnail_status.json` — timestamp run `2026-07-31T15:35:09`
- `.claude/settings.json` — blocco `env` spostato in testa + voci allowlist

Push su `origin/main`: **4 commit** (`06ed8094`, `f226e992`, `02162299`, `7c4fbef3`).

## Lezioni/errori
- **Verificare prima di copiare**: la richiesta presupponeva che apex-7 mancasse dal repo. Il
  confronto SHA256 ha evitato una duplicazione inutile (o peggio, un secondo percorso della
  stessa skill). Le altre 3 cartelle dello zip (`ruflo`, `content-forge2.0`,
  `master-build-architecture`) sono anch'esse già in `.agents/skills/` — lo zip è uno **snapshot
  di un workspace già sincronizzato**, non una sorgente di roba nuova.
- `.claude/settings.json` ha raccolto 4 voci allowlist generate automaticamente dai permessi di
  sessione, di cui 3 sono rumore (`PowerShell(git -C $r ...)`, `PowerShell($r = "...")`: la
  variabile non è espansa, quindi non corrisponderanno mai a un comando reale). Innocue ma
  inutili, committate come da richiesta "salva tutto".

## Costi
Nessuno (nessuna chiamata a pagamento, nessun agente spawnato).

## RIPRESA DA
Invariata rispetto a [CP-20260731-001](CP-20260731-001.md): resta aperta la decisione di Gael su
`apex7_orchestrator.py` (riscrivere F1-F5 su @dosementale **oppure** ridurre l'orchestratore al
solo `_parse_script_scenes` e tenere il flusso manuale). Nulla di questo checkpoint la modifica.

Item minore opzionale: ripulire le 3 voci allowlist rumorose in `.claude/settings.json`.
