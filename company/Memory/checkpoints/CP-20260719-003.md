# CP-20260719-003 — EMPIRE DESK: divisione lavoro metà/metà Max↔Gael + planning aggiornato

- **Data:** 2026-07-19 (pomeriggio)
- **Ecosistema/Reparto:** EmpireDesk (app .exe di tutto) — coordinamento Max/Gael
- **Task:** ordine Max: focus totale sull'app, dividere il lavoro restante a metà precisa tra Max e Gael,
  aggiornare tutto il planning prima di iniziare.
- **Esito:** ✅ completato (planning + coordinamento; il build riparte con i task assegnati)
- **Output:**
  - `PIANO-MAESTRO/17-EMPIRE-DESK-APP.md`: §3 aggiornato con stato reale (P1-P3 ✅, P4 🔴 aperto) +
    nuova §5 DIVISIONE METÀ/METÀ (task B0-B4 Gael, A1-A4 Max, contratto modulo §5.3, regole anti-collisione §5.4).
  - `company/Memory/STATO-EMPIRE.md`: nuovo blocco ordine in cima con ripresa chirurgica per Gael.
- **Verifiche fatte (stato reale al momento della divisione):**
  - Selftest EmpireDesk eseguito: **7/8**. FAIL = tile Caroselli.
  - Difetto 1: `app.py` ~r.101 `"script": "scripts/generate.js"` risolto da REPO_ROOT → file inesistente.
    Path vero verificato su disco: `Workfolw crea caroselli à/carousel-factory/scripts/generate.js`.
  - Difetto 2 (non visibile dal selftest): `generate.js` richiede `process.argv[2]` = file JSON carosello
    (senza → usage + exit 1) e `input/` non contiene JSON pronti → serve campo `input` sulla tile.
  - `dist/` assente → exe mai buildato (P4 aperto).
- **Decisioni prese:**
  - Split per ownership di FILE (pattern Half A/Half B PreventivoForge): Gael = core (app.py, ui, build)
    + moduli scheduler/notify/taskboard; Max = 4 moduli dati/business (metrics, revenue, licenze, fliki).
  - Estensioni future SOLO via `EmpireDesk/modules/` (contratto `MODULE` + `selftest()`, dossier 17 §5.3);
    dopo il seam B1 il core va in FREEZE. Unica dipendenza di Max = B1.
- **Lezioni/errori:**
  - Confusione mattutina "la task non esiste" (riferita da Gael) = **git pull fallito per rete**
    (`schannel: server closed abruptly`, visto 2 volte anche lato Max) → sessione su STATO vecchio.
    Regola: se lo stato sembra incoerente con un ordine di Max, RIPETERE il pull prima di concludere.
- **Prossimo passo:** Gael → B0 (chiudi v0.1 oggi) poi B1 (seam). Max → A1/A2 scrivibili subito a contratto.
