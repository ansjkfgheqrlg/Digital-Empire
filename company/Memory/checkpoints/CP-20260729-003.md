# CP-20260729-003 — TASK-YT-002 CHIUSA: F4 Produzione, spec Fliki multi-scena da script reale

- **Data:** 2026-07-29
- **Ecosistema/Reparto:** YOUTUBE-AUTOMATION-FACTORY (modello operativo governato dall'Estate)
- **Task:** TASK-YT-002 (P1) — F4 Produzione: generare una spec Fliki multi-scena dallo `script.md`
  reale di F3, non più una scena fissa hardcoded. Preso in mano da Claude su ordine di Max
  (l'Estate costruisce lui i modelli operativi); primo modello costruibile senza blocco secondo
  `empire cantiere`.
- **Esito:** ✅ chiusa. Gate VERDE.

## Il difetto (misurato, non dichiarato)
`run_phase_4` in `apex7_orchestrator.py` scriveva sempre:
`scene_count: 5` con **1 sola scena** (`"Vuoi installare l'agente IA più veloce?"`),
`video_id: "claude-code-001"`, `title: "Installare Claude Code locale"` — tutto costante,
indipendente da ciò che F3 aveva scritto in `script.md`. La fabbrica non produceva mai una spec
reale del video scelto.

## Fix (additivo, un solo file toccato)
`YOUTUBE-AUTOMATION-FACTORY/02-AUTOMAZIONI-E-SCRIPTS/apex7_orchestrator.py`:
- Nuova funzione **module-level `_scene_da_script(script_text)`**: deriva le scene dal contenuto
  reale. Regole calibrate sul formato scritto da `run_phase_3`: considera solo HOOK/INTRO/CORPO/CTA;
  taglia le note di regia (marcatore `➕`); ignora i metadati bullet (`- **`); ai bullet del CORPO
  toglie il prefisso timecode (`0:00-1:30 `); spezza ogni sezione in frasi → una frase = una scena
  con durata stimata dalle parole (nessun TTS qui). Nessuna scena narrabile → `[]`, il chiamante
  mette una scena-titolo di fallback (schema resta valido, niente contenuto inventato).
- `_slug_video_id(title)`: `title` reale → id url-safe stabile.
- `run_phase_4` riscritto: `title`/`hook_type` da `working_memory` (reali, da F3), scene da
  `_scene_da_script(script.md)`, `scene_count = len(scenes)`. Se `script_path` manca, avvisa
  onestamente e usa la scena-titolo di fallback.

## Gate (comandi + output reali)
Sostituzione onesta del gate F1→F4: `run_phase_4` esercitato in isolamento su 2 script formato-F3
diversi (il pipeline completo tocca fetch YouTube live; il fix vive tutto in F4). Script di gate:
`scratchpad/gate_yt002.py`.
```
RUN 1 (script.md reale):  video_id=come-installare-e-configurare-claude-code-in-5-m  scene_count=9  validate PASS
RUN 2 (idea diversa):     video_id=3-errori-che-rovinano-i-tuoi-prompt-con-claude-c  scene_count=8  validate PASS
[OK] run1/run2 multi-scena (>1)   [OK] scene_count DIVERSO (9≠8)   [OK] testo scene DIVERSO
[OK] run1/run2 validate_schemas produzione-spec PASS
GATE: VERDE
```
Regressione: `python -m unittest test_youtube_apex7 -v` → **Ran 11 tests — OK (11/11)**, l'E2E
esercita la Fase 4 nuova.

## Nota trovata durante il gate (onestà)
La prima esecuzione del gate mostrava note di regia non tagliate: era un difetto del MIO script di
test (avevo usato `+` ASCII invece del vero `➕` U+2795 che F3 scrive). Corretto il test al carattere
reale → note di regia correttamente rimosse. Il parser gestisce il marcatore reale `➕`; nessun
cambiamento al codice di produzione necessario. Residuo minore per i lotti successivi: le righe del
CORPO oggi sono descrizioni-timecode ("Hook + intro del problema"), non parlato letterale — è il
contenuto attuale di F3, non un difetto di F4. Se in futuro si vuole parlato puro nel CORPO, è F3 a
doverlo scrivere (BACKLOG, non blocca TASK-YT-002).

## Perimetro rispettato
Toccato solo `apex7_orchestrator.py` (esclusivo di questo lotto). Non toccati: motore condiviso
`11-APEX-7-CORE`, file trading Stream-S7. Blocco ⚠️ COORDINAMENTO messo prima del build
(commit `02ad904a`).

## Taskboard
`EmpireDesk/state/taskboard.json`: **TASK-YT-002 → fatto** (nota con riferimento a questo CP).

## Prossimo passo (RIPRESA DA)
Secondo `empire cantiere`, il prossimo lotto costruibile della YT-Factory è **TASK-YT-003** (F5
Pubblicazione: metadati/titolo/tag reali dal video+script scelti; oggi `run_phase_5` è hardcoded
allo stesso modo che era F4). Stesso metodo: COORDINAMENTO → build → gate (2 run → metadati.json
diversi, `seo_score.py` resta ≥70) → checkpoint → taskboard. Comando: `empire cantiere`.
