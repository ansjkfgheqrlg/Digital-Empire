# CP-20260909-6T6J — battito: sesto giro, fence obbligatorio invertito, chat satura

- **Data:** 2026-09-09
- **Ecosistema/Reparto:** 10-memory / battito Emperator (§6.11)
- **Task:** continuazione di CP-20260909-RWR2 (stessa task, "migliora il recap") — coniato
  di nuovo perche' Max ha chiesto un checkpoint per chat satura di contesto, a meta' di un
  aggiornamento dottrina non ancora finito.
- **Esito:** ⚠️ parziale — CODICE chiuso e verde, DOTTRINA quasi finita (resta un controllo).
- **Stato esatto adesso:**
  - `scripts/verifica_recap.py` e `scripts/gate_battito_hook.py`: **finiti, 11/11 test
    verdi** (`py -3 scripts/test_gate_battito.py`). Regola attuale (6º giro, la buona):
    il battito vero va **dentro un blocco ` ``` `, senza linguaggio, in cima al
    messaggio** — spazio vero per il rientro (niente più `·`), nessun bordo `┌│└─┐┘`
    (tolto al 5º giro), Forze piatta o ad ALBERO (`costruisci(..., forze=[(nome,
    [voce,...]), ...])` per sentinelle/doom bot). Il gate blocca: fuori dal blocco di
    codice, blocco non in cima, o problemi di forma dentro. Un blocco `` ``` `` con prosa
    vera **anche dopo** la chiusura resta un esempio di documentazione (ignorato).
  - `.claude/agents/emperator.md` §6.11: aggiornato fino al 6º giro incluso (esempi
    rigenerati con `costruisci()` reale, regole 8/10 riscritte, blockquote del 6º giro
    scritto). **NON ancora controllato**: se sotto "IL CONTROLLO MECCANICO" (poco più giù
    nello stesso §6.11) restano frasi tipo "mai dentro un blocco di codice" scritte prima
    del 6º giro, sono CONTRADDITTORIE e vanno corrette — prossimo passo, primo da fare.
  - Il messaggio finale a Max in chat (con lo screenshot "che schifo, i puntini") **non è
    ancora stato risposto con il battito nuovo**: l'ultima cosa mandata a Max è la domanda
    con le due opzioni (blocco di codice vs niente centraggio), lui ha scelto "blocco di
    codice", e da li' è partito tutto questo lavoro — ma non gli ho ancora RIMANDATO un
    battito di prova nella forma nuova per farglielo vedere/confermare.
- **Decisioni prese:** nessun ADR — modifica di forma su una regola gia' esistente (§6.11).
- **Lezioni/errori:**
  - Un tentativo di sostituzione con `re.search(r'```...```', ..., re.DOTALL)` su TUTTO il
    file di dottrina ha beccato il PRIMO blocco ``` del documento (un esempio "FORZE
    SCHIERATE" a riga ~130, non il §6.11 a riga ~630) e l'ha sovrascritto per errore.
    Scoperto subito con `git diff --stat` (mostrava un file cambiato a caso), corretto con
    `git checkout HEAD -- .claude/agents/emperator.md` (il sync daemon aveva gia'
    committato la versione buona) e rifatto con sostituzione per **indice di riga esatto**,
    verificato con `assert` prima di scrivere. **Regola per la prossima volta**: mai
    `re.search`/`str.index` globale su un file lungo con marcatori ripetuti (` ``` `,
    `---`, ecc.) — sempre per intervallo di riga letto e verificato appena prima.
  - Storia completa dei sei giri (perche' ognuno e' stato smentito) e' scritta per intero
    nel docstring di `verifica_recap.py` e nel §6.11 di emperator.md — non ripeterla a
    mano, leggerla da li' se serve il "perche'".
- **Costi:** nessuna chiamata API oltre alla sessione corrente.
- **Prossimo passo (in ordine):**
  1. In `emperator.md` §6.11, sezione "IL CONTROLLO MECCANICO" (e ovunque altro nella
     stessa sezione): cercare frasi residue "mai dentro un blocco di codice" o equivalenti
     scritte PRIMA del 6º giro e correggerle (ora e' l'opposto).
  2. Generare un battito di prova con `costruisci()`, avvolgerlo in ` ``` `, validarlo con
     `valida()`, e mandarlo a Max come risposta — è la conferma che manca per chiudere
     davvero questo filone.
  3. Se Max confirma: chiudere CP-20260909-RWR2 (il checkpoint originale di questa stessa
     task) e questo CP-20260909-6T6J insieme, un'unica voce di chiusura in STATO-EMPIRE.
