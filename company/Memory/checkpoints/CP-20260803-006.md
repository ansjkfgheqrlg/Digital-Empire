# CP-20260803-006 — Workflow "Libri Performanti" (multi-agente KDP) importato + modulo Aureus/EmpireDesk

- **Data:** 2026-08-03
- **Ecosistema/Reparto:** 02-INFO-BUSINESS (Workflow) + EmpireDesk (Aureus Agency OS)
- **Task:** importare lo zip `workspace-019fc6f4-cff2-7fba-b8ab-44f5a12def95.zip` (richiesta diretta di Max: "aggiungilo su GitHub e implementalo nella cartella Digital Empire e nell'app Aureus/Empire Desk")
- **Esito:** ✅ completato (con un limite dichiarato, vedi sotto)

## Cosa conteneva lo zip
Blueprint architetturale per un workflow multi-agente che pubblica libri Amazon KDP
"performanti, riproducibili, sostenibili": 782 file, 4 varianti di architettura
(`workflow_architecture/`, `official_claude_architecture/`, `architettura_sincrona/`,
`architettura_completa_7_livelli/`), 104 agenti dichiarati su 7 livelli gerarchici (L1
Supreme → L7 Micro), 19 team, 9 ecosistemi (Research/Qualification/Planning/Production/
Visual/Memory/SelfHealing/AutoImprovement + sotto-ecosistema Playwright). Non esisteva
nulla di simile nel repo (verificato con grep su `SupremeOrchestratorAgent`,
`BookOpportunityRegistry`, `architecture_manifest.json` prima di importare).

## Dove è finito (decisione presa dopo conferma di Max)
Avevo chiesto a Max se formalizzare come nuovo ecosistema 14 (ADR-009 lo richiede
esplicitamente per ogni ecosistema dal 14 in su) o tenerlo più leggero. Risposta di Max:
solo aggiungerlo, senza cerimonie. **Nessun ecosistema 14 creato** — salvato in
`company/Ecosistemi/02-INFO-BUSINESS/Workflow/libri-performanti-multiagente/` (sotto-cartella
del prodotto Ebook esistente, coerente con `Workflow/` già presente in quell'ecosistema).
Se in futuro la scala giustifica un ecosistema dedicato, resta un `git mv` + ADR-011, non
un problema oggi.

## Aureus/EmpireDesk: modulo aggiunto, non solo file copiati
`EmpireDesk/modules/libri.py` (nuovo), modellato esattamente su `modules/dash.py`:
tile `readonly` che apre `ARCHITETTURA_COMPLETA_FINALE.md`, route `libri/manifest` che
legge `architecture_manifest.json` (mai ricalcolato — Mandato Art.2), pannello con bottone,
`selftest()` che verifica i file chiave. **Verificato con `python app.py --selftest`:
21/21 PASS**, `libri` caricato correttamente insieme agli altri 9 moduli, nessuna tile in
conflitto, `platform` (Aureus) integra.

## ⚠️→✅ Limite trovato E RISOLTO (update: Max ha chiesto "sistema te")
`workflow_architecture/main.py` e `orchestrator_assembly.py` (la variante originariamente
importata) referenziano `agents/all_agents.py` e `agents/senior_and_operational.py` — file
che non esistono da nessuna parte nella consegna. Peggio: i file `L1/`/`L2/` di quella
variante dipendono da un `Agent` importato da **un'altra cartella dello zip**
(`architettura_sincrona/core.py`, campi diversi, incompatibile con
`workflow_architecture/core.py`). Questa variante resta **non eseguibile**, lasciata intatta
come riferimento.

**Trovata una seconda variante nello stesso zip, auto-consistente**: `architettura_completa_7_livelli/`
ha un proprio `core.py`, L1-L7 con file `_fixed`, e `Orchestrator/assembly_finale.py` che
importa SOLO da dentro la propria cartella — nessuna dipendenza incrociata. Portava già un
`manifest_finale_completo.json` pre-generato (prova che in origine girava). Fix applicato:
1 solo problema, lo stesso path hardcoded `/home/user/...` reso relativo a `__file__` (zero
modifiche di logica).

**Verificato con esecuzione reale, due volte**: `python assembly_finale.py` da riga di
comando (exit 0) E lanciato dall'app Aureus/EmpireDesk stessa via `/api/launch` (subprocess
reale attraverso `TileManager`, non uno shortcut) → `exit_code: 0`, `running: false`. Il
manifest rigenerato è **deterministico**: diff col precedente solo di fine riga (`git diff -w`
= zero differenze reali). Risultato reale: **95 agenti, 26 team, 18 skill, 38 memory
component, 5 flow, 9 ecosistemi** — diverso dai 104 dichiarati nell'altra variante: sono due
conteggi di due varianti diverse nello stesso zip, non lo stesso numero validato due volte.

Playwright è confermato **simulato** in entrambe le varianti (`playwright_tool.py`/`real_tool.py`:
dataclass che restituiscono `status="pending"`, commento esplicito `"# In real implementation:
playwright async navigate"`) — nessuna chiamata di rete, nessun rischio eseguendo lo script.

`modules/libri.py` aggiornato: tile passata da `readonly` a `kind=py` (lancio reale via
TileManager standard, ADR-003), la route `libri/manifest` ora legge il manifest della
variante VERIFICATA, non più quella rotta. Selftest EmpireDesk **21/21 verde**, confermato
dopo l'aggiornamento.

## Salvato anche il resto ("salva tutto")
Due commit separati, pushati su `origin/main` dopo fast-forward pulito (repo era 1 dietro,
0 avanti — sincronizzato prima di committare):
- `5dad6a89` — blueprint Libri Performanti (783 file) + modulo `libri.py`
- `f4f50f22` — resto delle modifiche pendenti in sessione: `fliki_client.py`,
  `.claude/settings.json` (root + YOUTUBE-AUTOMATION-FACTORY), nuovo scaffold
  `YOUTUBE-AUTOMATION-FACTORY/youtube_automation_factory/` (src/agents/core/integrations,
  solo `.env.example` mai `.env` — verificato assenza segreti prima di committare),
  `company/Ecosistemi/12-STREAM-S7-BOT/.claude/` (settings locali sessione NFT, controllati:
  nessun segreto, solo permessi Bash/WebFetch)

## RIPRESA DA
Task chiuso: la tile Aureus "Libri Performanti" lancia per davvero la variante verificata
(95 agenti). `workflow_architecture/main.py` resta non eseguibile e non è stato toccato (non
serviva più, essendoci già una variante gemella funzionante nello stesso zip) — se in futuro
serve ANCHE quella specifica variante (es. per i suoi 104 agenti dichiarati invece di 95),
va riscritta a mano puntando ai file reali, non tentato qui.
