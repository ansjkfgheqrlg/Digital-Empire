# CP-20260805-005 — YOUTUBE-AUTOMATION-FACTORY: i 4 gap dell'audit CP-20260804-001 costruiti e agganciati per davvero

> *Rinumerato da CP-20260805-004: collisione con il checkpoint KDP67 di Gael sullo stesso slot,
> arrivato su origin mentre scrivevo questo. Contenuto invariato.*

- **Data:** 2026-08-05
- **Area:** YOUTUBE-AUTOMATION-FACTORY
- **Owner decisione:** Max · **Esecuzione:** Claude (sessione Sonnet 5, continuazione di CP-20260804-001)
- **Esito:** ✅ costruito, testato, verificato in esecuzione reale (non solo unit-test isolati)

## Contesto
Segue [CP-20260804-001](CP-20260804-001.md): l'audit aveva trovato 4 capacità reali ma isolate
(regolatori mai chiamati da una run automatica, copertine senza controllo di originalità, copy
research mai collegata alla generazione, scouting canali/nicchie rimosso senza sostituto
advisory). Max ha confermato di procedere ("procedi"), il coordinamento con Gael è stato
verificato ad ogni sync (`git fetch` prima di ogni edit ai file caldi): **zero collisioni reali**
— l'unico file toccato da Gael in parallelo (`script-adattati/eax7OPi1q0M.md`, +6 righe) non è
mai stato tra i file che ho modificato.

## 1. Regolatori L3 agganciati per davvero (prima: solo CLI manuale)
`apex7_orchestrator.py`: nuova `_esegui_regolatori()` (carica `regolatori.py` per percorso,
stesso pattern di `cashcow_check.py`), chiamata:
- fine `run_phase_3` — nicchia + copy sul testo narrato (`REGOLATORI.testo_narrato`, esclude le
  note di produzione) + originalità se esiste un transcript reale per il video scelto;
- fine `run_phase_5` — nicchia + copy sui metadati finali (titolo/descrizione sono testo
  NUOVO generato in F5, mai controllato prima).

Ogni chiamata registra le 3 firme in `memory/firme.json` (esisteva solo come path dichiarato,
mai un file reale — vedi CP-20260804-001 §RIPRESA DA). Un BLOCCO di un regolatore ora fa
fallire davvero la fase (`return False`), stesso comportamento delle altre fasi.

**Verificato in esecuzione reale** (non solo per import): script reale di Gael
(`eax7OPi1q0M.md`) passato attraverso `run_phase_3` con lo stato di una run vera —
`regolatore-nicchia`, `regolatore-copy`, `regolatore-originalita` tutti PASS, prima firma reale
mai scritta in `firme.json` (poi svuotato di nuovo a `[]`: era una verifica, non una firma di
produzione — vedi §5).

## 2. regolatore-configurazione agganciato dove conta: prima della chiamata Fliki reale
Non in F4/F5 (`produzione-spec.json` non contiene i campi Fliki — quelli esistono solo nel
payload costruito da `fliki_client.generate_video()`): **quello** è il punto reale dove la
configurazione approvata da Gael rischiava di essere alterata, protetta finora solo da un
commento "NON MODIFICARE". Ora `verifica_configurazione(payload, flag_espliciti={"visuals":
visuals})` gira subito prima di `_request(...)`, BLOCCO reale con `SystemExit` se differisce.

**Verificato**: payload approvato → `passa`; stesso payload con un campo alterato
(`subtitlePresetId`) → `BLOCCO`. Testato senza chiamare l'API vera.

## 3. regolatore-copertina — NUOVO, prima non esisteva alcun controllo
`regolatori.py`: `verifica_copertina(source_path, generated_path)`, hash percettivo (average
hash 8×8, Pillow — già disponibile nell'ambiente, 12.1.1) fra la copertina reale del competitor
e quella generata da Arena. Sotto 10/64 bit di distanza → BLOCCO ("non sembra riadattata").
Nessuna sorgente da confrontare (prompt generico) → passa con nota, controllo non applicabile.

Agganciato in `arena_thumbnail.py`, subito dopo `harvest_images()` (l'unico punto dove esiste
per davvero un file generato da confrontare), scritto anche nello status JSON.

**Verificato su immagini reali del repo**: sorgente vs copertina Arena già generata (video
mkaNzHTBw1M) → distanza 22/64, `passa`. Immagine confrontata con se stessa → distanza 0,
`BLOCCO` (il caso che il controllo deve catturare). Nessuna sorgente → `passa` con nota.

## 4. Bridge copy_study_dosementale.py → produzione (prima: solo wiki, mai letto da una run)
`copy_study_dosementale.py` scrive ora anche `memory/copy_intelligence.json` (schemi
favorevoli/sfavorevoli strutturati, non solo il Markdown sulla wiki). Tenuto **separato** da
`learned_rules.json` di proposito: quel file viene riscritto per intero da `self_improve.py` a
ogni F6 (anche vuoto, se non ci sono ancora log di performance reali) — scriverci dentro
sarebbe stato cancellato al giro successivo. Due fonti distinte, stesso punto di consumo:
- F5 (`apex7_orchestrator.py`): `copy_intel_tags` aggiunti a `tag_candidates` prima dei tag
  YouTube finali;
- F3: quando manca uno script adattato, il brief `<videoId>.DA-SCRIVERE.md` ora include una
  sezione "Schemi di copy misurati" (USARE/EVITARE con delta % reale) — è la guida che chi
  scrive lo script vede per primo.

**Rilanciato per davvero** (`python copy_study_dosementale.py`): 36 video reali,
`copy_intelligence.json` scritto, stessi numeri dello studio wiki (salute/età +455.6%,
relazioni +338.3%, religioso −43.1%).

## 5. Due tool advisory nuovi (nessuno esisteva) — propongono, non decidono
Lo scouting multi-canale/multi-nicchia era stato **rimosso apposta** il 2026-07-31 (vedi
`apex7_orchestrator.py` righe 74-78): il gap non era un errore da correggere, era una scelta di
design da rispettare. Questi due tool sono costruiti per non contraddirla — sono READ-ONLY sulla
configurazione di produzione, nessuna run automatica li legge, nessuno tocca mai
`CANALE_TARGET`.

- **`channel_discovery.py`** — cerca DAVVERO su YouTube (Playwright, filtro nativo "Canale",
  nessun handle inventato) per query di nicchia passate a riga di comando, poi riusa
  `youtube_hunter_playwright.raccogli()` (stesso codice già testato su @dosementale) +
  `cashcow_check.py` per calcolare un indice comparabile. Scrive
  `memory/proposte_canali.json`.
  **Testato in esecuzione reale** (non mock): query "saggezza spirituale anziani" → 3 canali
  reali trovati, indici Cash Cow reali misurati (15.0–15.1), proposte scritte. Limite trovato e
  documentato nel codice: il parsing degli iscritti dai risultati di ricerca non ha ancora
  intercettato il formato reale (sempre `null` sui 3 canali di prova) — riportato onestamente
  come `null`, non inventato.
- **`niche_discovery.py`** — aggrega TUTTI i canali già in cache (`memory/channel_videos/*.json`,
  cresce ogni volta che `channel_discovery.py` gira) e riusa gli schemi TEMATICI di
  `copy_study_dosementale.SCHEMI` (salute_età, relazioni, religioso — non quelli di stile come
  "domanda" o "numero secco") per proporre su quale tema concentrare la produzione. Scrive
  `memory/proposte_nicchie.json`.
  **Testato in esecuzione reale**: 2 canali in cache, 62 video, 3 temi con verdetto (salute_età
  +127.3%, relazioni +61.5%, religioso −66.3%).

## Verificato (riepilogo)
- `python -m pytest test_youtube_apex7.py` → **10/11** (invariato prima/dopo le mie modifiche —
  vedi §Problema trovato, non causato da me).
- Sintassi pulita (`ast.parse`) su tutti i 5 file modificati + 2 nuovi.
- `run_phase_3` con stato di run reale (script Gael, video eax7OPi1q0M) → i 3 regolatori
  eseguono davvero, prima firma reale mai scritta in `firme.json`.
- `verifica_configurazione` e `verifica_copertina` testati isolatamente con dati reali del repo,
  esito atteso su caso PASS e caso BLOCCO per entrambi.
- `channel_discovery.py` e `niche_discovery.py` lanciati per davvero, non in dry-run.

## ⚠️ Problema trovato, NON causato da questa sessione — segnalato, non corretto
`test_apex7_orchestrator_e2e` fallisce (10/11 invece di 11/11) **anche con le mie modifiche
completamente stashate** (verificato con `git stash` prima/dopo): la ricalibrazione del ritmo di
lettura di Gael di ieri (185 parole/min, commit `27b1c4df`) rende lo script reale
`eax7OPi1q0M.md` (1865 parole) troppo corto per il gate dei 12 minuti — stesso identico
fallimento sia nel test sia in una run di produzione reale lanciata oggi
(`apex7_orchestrator.py run --phase 5`). Non è un bug del mio codice: i miei regolatori non
vengono nemmeno raggiunti, la fase fallisce prima al gate di durata. Non toccato (è contenuto
reale di Gael, non mio da riscrivere) — ma osservato che durante questa sessione è arrivato un
commit di Gael proprio su `eax7OPi1q0M.md` (+6 righe): probabile correzione in corso, da
riverificare alla prossima sessione con `python -m pytest`.

## ⚠️ Collisione di numerazione trovata (non mia, non risolta) — segnalata per trasparenza
Durante la sync pre-commit, `company/Memory/STATO-EMPIRE.md` e
`company/Memory/checkpoints/CP-20260805-001.md` avevano modifiche locali non mie (contenuto:
Outreach Preventa, follow-up msg2/msg3) che collidevano con una versione diversa già su origin
allo stesso path (CP-20260805-001 = PIANO KDP 67, sessione di Gael). **Non ho tentato di
riconciliarle**: isolate in uno stash etichettato (`git stash list` →
"sessione parallela (Outreach Preventa follow-up, non mia) - STATO-EMPIRE.md +
CP-20260805-001.md, da riconciliare"), nulla perso, nulla deciso al posto di chi le ha scritte.
Chi riprende quella sessione deve fare `git stash pop` (o `git stash show -p` per leggere prima)
e rinumerare il checkpoint su uno slot libero.

## RIPRESA DA
1. **Verificare se il commit di Gael su `eax7OPi1q0M.md` risolve il gate dei 12 minuti**
   (`python -m pytest test_youtube_apex7.py`, atteso 11/11 se sì).
2. **Una volta sbloccato**: lanciare una run F1→F6 completa vera e osservare i regolatori in
   azione su una run non sintetica (oggi verificato solo iniettando lo stato a mano).
3. `channel_discovery.py`: il parsing subscriber count va rifinito (oggi sempre `null` — non
   blocca il tool, ma riduce l'informazione delle proposte).
4. Decisione architetturale ancora aperta (segnalata anche in CP-20260804-001): che fare dello
   scaffold parallelo `youtube_automation_factory/` (mai installato/testato) — nessuna delle
   modifiche di oggi lo tocca, resta un secondo motore incompleto nello stesso repo.
5. Domanda di governance emersa oggi (Max): `PIANO-MAESTRO/03-ECOSISTEMA-CONTENT-FACTORY-V2.md`
   descrive un reparto formale (CF-Director, CF-R1..R8, KPI, namespace `cf/patterns`) come
   proprietario di YouTube — **non esiste sul disco**, è un piano non costruito. Oggi la fabbrica
   resta autogovernata dalla propria gerarchia interna (33 agenti). Se Max vuole il reparto
   formale davvero operativo, è un progetto a sé, non una continuazione di questo checkpoint.
