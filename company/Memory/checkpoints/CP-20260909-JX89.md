# CP-20260909-JX89 — /frantuma: il codice e' sorteggiato come un checkpoint (MT-XXXX), non un numero progressivo o un percorso

- **Data:** 2026-09-09
- **Ecosistema/Reparto:** 10-MEMORY / auto-implementazione di Emperator
- **Task:** correzione finale sul filone `/frantuma`. Dopo CP-20260909-6R2M (percorso al posto
  della frase generica), Max ha rifiutato anche il percorso: *"con ID intendo il checkpoint,
  capisci? sono la stessa cosa. Lo copio, lo metto in un'altra chat, e quella parte subito
  facendo la micro task... che c'entra il percorso? non c'entra assolutamente niente."*
- **Esito:** ✅ corretto per l'ultima volta. Il codice di una micro-task e' ora identico nella
  forma e nel meccanismo a un checkpoint di ripresa: `MT-XXXX`, quattro caratteri sorteggiati
  dallo stesso alfabeto senza ambiguita' di `checkpoint.py`, verificato contro disco+storia git
  (B-009), **non progressivo per-padre come nella prima versione** (`MT-01` era ambiguo: due
  task diverse avrebbero avuto entrambe un `MT-01`, e incollato senza dire il padre non portava
  da nessuna parte).
- **Aggiunto `trova <codice>`**: cerca il codice in TUTTE le task padre e stampa il file — il
  comando che una chat nuova lancia ricevendo solo il codice, senza altro contesto. E' il pezzo
  che rende vero "lo copio, lo metto in un'altra chat, e quella parte subito": prima non esisteva
  nessun modo di andare dal codice al contenuto.
- **Output:** `scripts/frantuma.py` (riscritto: `nuovo_codice`/sorteggio con alfabeto sicuro,
  `trova_percorso` cerca su tutte le sottocartelle, `report` mostra il codice non il percorso),
  `tests/test_frantuma.py` (13 test, riscritti sul nuovo meccanismo), `emperator.md` §6.24
  aggiornata con lo schema corretto e la citazione della correzione.
- **Decisioni prese:** nessuna ADR.
- **Lezioni/errori:**
  - **"Stessa cosa di X" va preso alla lettera, non per analogia.** Avevo interpretato "un ID
    come sempre" come "qualcosa di breve", e prima ho dato una frase, poi un percorso — entrambi
    "brevi" nel senso sbagliato. Max intendeva letteralmente lo STESSO meccanismo di
    `checkpoint.py`: sorteggiato, non progressivo, risolvibile da solo in una chat nuova. Quando
    qualcuno dice "è la stessa cosa di X" e X esiste già nel codice, il primo passo è aprire X,
    non indovinare una forma nuova che sembri simile.
  - **Un numero progressivo per-padre sembra innocuo finché non lo si stacca dal contesto.**
    `MT-01` funzionava benissimo dentro `report()` (si vede il padre in cima allo schema), ma
    fallisce esattamente nell'unico uso per cui la funzione esiste: essere copiato altrove.
  - **Tre correzioni sullo stesso dettaglio in un giorno non sono state uno spreco**: ogni giro
    ha eliminato un'ambiguità reale (frase generica → percorso → codice sorteggiato), e la prova
    a vuoto richiesta da Max ("fingi") ha fatto emergere il difetto senza costare un conio vero.
- **Costi:** nessuna chiamata a modello.
- **Prossimo passo:** nessuno aperto. La funzione è completa: due fasi, schema fisso viola,
  codice sorteggiato tipo-checkpoint, comando `trova` per il recupero cross-chat.
