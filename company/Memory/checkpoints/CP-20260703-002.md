# CP-20260703-002 — Max — PreventivoForge: consegna abbonabile pronta

**Data:** 2026-07-03
**Autore:** Max (Half A)
**Cliente:** Novacar srl

## Fatto
1. **Gate IMG + Gate R cablati in `run.py`** (bloccanti dopo Gate D; exit 8=foto/R-09, 9=REGOLE-SACRE). Chiuso pending #1.
2. **Storico automatico** in `Memory/storico-preventivi/` (PDF + sidecar JSON) a ogni run consegnato. Chiuso pending #3.
3. **Kill-switch abbonamento** `implementation/licenza.py`: controllo online (`LICENSE_URL`/`dealer.license_url`) prima di ogni preventivo.
   - sospeso→blocca (exit 10); rete-giù→grace; anti-furbata (cache: sospeso+offline resta bloccato).
   - Cablato: run.py + app.py (`_CODE_MSG` 8/9/10) + spec (hiddenimport) + `.env.example` + `.gitignore`.
   - Bug corretto: id dealer da `dealer_id` (non `id`/`display_name`) → chiave JSON = `novacar`.
4. **`--remote-allow-origins=*`** già presente in `cdp.launch` (pending #2 = già chiuso).
5. **EXE ricostruita + ri-testata FROZEN** (`PreventivoForge.exe --selftest`): 6/6 gate + 14/14 REGOLE verdi, PDF 2.2MB via cdp-chrome, storico OK.
   - Hardening `app.py`: guard `sys.stdout is None` nel ramo `--selftest` (l'exe windowed crashava sulle print).
6. **Guida consegna** `CONSEGNA-NOVACAR.md` (requisiti PC, uso, SmartScreen, attivazione/sospensione kill-switch via Gist).

## Verificato
- Pipeline sorgente end-to-end: Mercedes GLA 47.490 → 51.915 €, 6/6 gate, 14/14 REGOLE.
- Kill-switch: 6 scenari (attivo/sospeso/annidato/non-elencato/rete-giù/no-url) + anti-furbata OK.
- EXE frozen: pipeline completa verde, PDF prodotto dall'eseguibile.

## File toccati
- **Half A (mio):** `run.py`, `implementation/licenza.py` (nuovo), `.env.example`, `.gitignore`, `preventivo-forge.spec`, `CONSEGNA-NOVACAR.md` (nuovo).
- **Half B (2 righe difensive, avvisato Gael in STATO):** `app.py` (`_CODE_MSG` 8/9/10 + guard stdout selftest).

## Residuo (non bloccante)
- Test su PC realmente pulito **senza Chrome** (verificare che l'errore guidi l'utente).
- Eventuale firma codice per rimuovere SmartScreen.
- Commit + push (in attesa di GO Max).

## Ripresa
- **Gael:** GUI approvata → riprende Empire (V2-2/V2-3). Non tocca Half A.
- **Max:** su GO → commit+push; poi attivazione reale kill-switch (creare Gist + `license_url` nel config Novacar).
