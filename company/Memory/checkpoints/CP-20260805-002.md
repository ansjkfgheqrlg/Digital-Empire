# CP-20260805-002 — PIANO KDP 67: CP0/CP3/CP6/CP8/CP11 chiusi e verificati, CP1 bloccato su Gael

- **Data:** 2026-08-05
- **Ecosistema/Reparto:** 02-INFO-BUSINESS / Workflow (libri-performanti-multiagente/engine)
- **Task:** esecuzione di [PIANO-KDP-67.md](../Ecosistemi/02-INFO-BUSINESS/Workflow/libri-performanti-multiagente/PIANO-KDP-67.md)
  dopo via libera esplicito di Gael (segue [CP-20260805-001](CP-20260805-001.md))
- **Esito:** ⚠️ parziale (5/13 checkpoint chiusi, in corso — non blocca, si riprende con
  "continua con il piano KDP 67")

## Checkpoint chiusi in questa sessione (dettaglio verifica reale, non dichiarazione)

- **CP0** ✅ — struttura `engine/`/`sessions/`/`LIBRI/` creata, `config.py` con TUTTI i path
  relativi a `__file__` (mai hardcoded `/home/user/...`, il bug ricorrente di ogni zip
  consegnato finora), `requirements.txt`. Verificato: `pip install` ok, playwright+chromium
  già presenti sul PC, import reale di `config` conferma cartelle esistenti.
- **CP3** ✅ — `engine/story_validator.py`, classificatore deterministico GO/NO-GO
  niche-story-vs-diario, nessuna chiamata LLM. Self-test 5/5 titoli Amazon reali (presi dal
  report dello zip analizzato) corretti. **Bug reale trovato e corretto durante il test**:
  "journal" da solo non era in `FORBIDDEN_NICHE_KEYWORDS` (solo frasi composte), un titolo
  tipo "Guided Anxiety Journal" passava dal percorso "ambiguo" invece che da un vero match —
  corretto, ritestato.
- **CP6** ✅* — `engine/kdp_formatter.py`, python-docx reale: trim 6x9, margini specchio
  (`mirrorMargins` via XML diretto, verificato presente nel file salvato), font/heading/
  section-break per capitolo, campo numero pagina reale (XML field `PAGE`, verificato).
  Self-test con 2 manoscritti sintetici: uno sotto target (13.3 pagine, correttamente
  rilevato FUORI target) e uno dentro target (120.1 pagine, correttamente rilevato OK) —
  **replica diretta ed esplicita del bug che si è ripetuto 2 volte nello zip originale**
  (dichiarare 120 pagine con in realtà 21). Qui il conteggio è sempre fatto rileggendo il
  file da disco dopo il salvataggio, mai fidandosi della costruzione in memoria. `*` = TOC
  automatico (campo Word) non implementato in questo passaggio, rimandato esplicitamente.
- **CP8** ✅* — `engine/book_output_manager.py` riscritto da zero (sostituisce
  `genera_nuovo_libro.py`, il bug più grave trovato nell'audit: copiava sempre lo stesso
  file template). Self-test: 2 libri con manoscritto/copertina di contenuto diverso
  producono cartelle con file di **dimensione byte diversa** — stesso test forense usato
  nell'audit per smascherare il bug originale, qui verificato che NON si riproduce.
  `FileNotFoundError` esplicito se i file sorgente non esistono, mai un fallback silenzioso.
  `*` = testato con file finti, non ancora con output reale di CP5/CP7 (non esistono finché
  CP1 non sblocca).
- **CP11** ✅ — le 4 varianti finte (`workflow_architecture/`, `official_claude_architecture/`,
  `architettura_sincrona/`, `architettura_completa_7_livelli/`) + i loro generatori
  (`gen_*.py`, `append_detailed.py`) + i 2 documenti narrativi con il riferimento alla API
  Anthropic inventata, spostati (`git mv`, storia preservata) in
  `_archivio_blueprint_narrativo/` con un README che spiega perché. Spostata anche la bozza
  manuale abbandonata del libro "AI Career Pivot" (scritta da Claude prima della correzione
  di Gael del 2026-08-04/05). **Effetto collaterale reale trovato e corretto**: il modulo
  Aureus `EmpireDesk/modules/libri.py` (da CP-20260803-006) referenziava il vecchio path di
  `architettura_completa_7_livelli/` — rotto dallo spostamento (selftest EmpireDesk sceso a
  17/21), corretto aggiornando il path, riverificato con `python app.py --selftest` reale:
  tornato a includere `libri`/`module:libri` = OK. I 2 fallimenti residui nel selftest
  (`preventivi`, `licenze` — `Clienti/Prof Autocad/preventivo-forge` mancante) sono
  pre-esistenti, non causati da questa sessione, fuori perimetro — non toccati.

## CP1 — bloccato, azione richiesta a Gael

`engine/session_manager.py` scritto e verificato in modalità `--check` (rileva
correttamente 0/2 sessioni). La parte di login vera richiede Gael fisicamente al PC
(Amazon e LM Arena hanno 2FA/captcha, non automatizzabile alla cieca senza rischio ban).
**Azione per Gael**: dalla cartella `libri-performanti-multiagente/`, lanciare
`python -m engine.session_manager` — si aprono 2 finestre browser (Amazon poi LM Arena),
fare login in ognuna, premere INVIO nel terminale dopo ogni login.

CP2 (Amazon Research), CP4 (LM Arena Client), CP5 (Book Writer), CP7 (Cover Generator)
restano bloccati a cascata su CP1 — deliberatamente non abbozzati alla cieca senza sessione
reale da ispezionare (lo stesso errore fatto da chi ha consegnato i 2 zip precedenti:
scrivere selettori Playwright plausibili mai testati contro il sito vero).

## Lezioni/errori

- Ogni checkpoint chiuso in questa sessione ha trovato ed esposto almeno un bug reale
  tramite il self-test stesso (keyword mancante in CP3, calibrazione sbagliata nel primo
  giro di CP6, path rotto in CP11) — prova che il metodo "mai dichiarare fatto senza
  esecuzione reale" funziona, non è solo teoria.
- Muoversi sui checkpoint sbloccati (CP3, CP6, CP8, CP11) mentre CP1 aspetta Gael ha
  permesso di chiudere 5/13 checkpoint in una sessione invece di restare fermi — utile
  pattern da ripetere quando un piano ha dipendenze parziali.

## Costi
Nessuna chiamata a pagamento, nessun agente spawnato, nessuna sessione browser reale aperta
verso Amazon/LM Arena (solo il test locale `--check`, che non apre nessun browser).

## RIPRESA DA
CP1 (azione di Gael richiesta sopra), poi a cascata CP2→CP4→CP5→CP7→CP9→CP10→CP12.
Aggiornare [PIANO-KDP-67.md](../Ecosistemi/02-INFO-BUSINESS/Workflow/libri-performanti-multiagente/PIANO-KDP-67.md)
dopo ogni chiusura, come richiesto esplicitamente da Gael.
