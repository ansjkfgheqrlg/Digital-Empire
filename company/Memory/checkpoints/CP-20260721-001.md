# CP-20260721-001 — EmpireDesk B2/B3/B4: fix trovati testando a runtime (toolchain ora disponibile)

- **Data:** 2026-07-21
- **Ecosistema/Reparto:** Empire Desk — Half B «Core & Runtime» / dossier `PIANO-MAESTRO/17-EMPIRE-DESK-APP.md` §5.1 (B2-B4), continuazione G3
- **Task:** chiudere B2 (scheduler, già scritto), B3 (`modules/notify.py`) e B4 (`modules/taskboard.py`), poi verificarli DAVVERO (non solo staticamente) ora che questa sessione ha accesso reale a Python 3.12/Node 24 (PATH override documentato in STATO-EMPIRE dalla sessione G2).
- **Esito:** ✅ B3+B4 scritti, tutti e 4 i moduli backend (scheduler/notify/taskboard + i 3 di Max) verificati **a runtime reale**: selftest 15/15 sia in `python app.py` sia dall'`.exe` frozen già esistente (senza ricostruirlo: `MODULES_DIR` legge dal repo live, non dai `datas` bundlati — conferma che il design del seam B1 regge). **2 bug reali trovati e corretti testando le routes**, non solo rileggendo il codice.
- **Cosa ho fatto:**
  1. **B3 `modules/notify.py`**: notifica Windows nativa (PowerShell + WinRT toast API, zero dipendenze pip) quando una tile transita da `running` a finita, con l'exit code. Osserva via `host.tiles()` — metodo nuovo su `_Host` (app.py), read-only, non consuma il cursore di `poll()`: non ruba mai righe di log a chi guarda il pannello nella UI. `selftest()` verifica solo che `powershell.exe` sia raggiungibile, zero notifiche reali in selftest (Mandato Art.4.3).
  2. **B4 `modules/taskboard.py`**: task board Max/Gael, `state/taskboard.json`. Seed = 18 task REALI copiati testualmente da `PIANO-MAESTRO/16-PIANO-ESTATE-REVENUE.md` (9 Max + 9 Gael), stato neutro `"da_fare"` per tutti (il dossier descrive un piano, non un avanzamento verificato riga per riga — Mandato Art.2, mai inventare stati). Routes `elenco`/`aggiorna`/`aggiungi`.
  3. **Sbloccato l'ambiente anche per questa sessione**: `winget` aveva già installato Python 3.12.10 + Node 24.18.0 (sessione G2, CP-20260720-006), ma gli stub Microsoft Store da 0 byte restano davanti nel PATH ereditato — replicato l'`export PATH=...` documentato in STATO-EMPIRE e confermato funzionante.
  4. **Verifica reale eseguita** (non più solo statica):
     - `python app.py --selftest` → **15/15 PASS** (8 tile + 6 moduli + platform).
     - `dist/EmpireDesk/EmpireDesk.exe --selftest` (build precedente, MAI ricostruita) → **15/15 PASS identico** — conferma che i nuovi moduli si "accendono da soli" su un exe già esistente, esattamente come promesso dal design B1/seam.
     - Test funzionale diretto delle routes (`app._call_module_route`, non solo il selftest): `taskboard/elenco`, `/aggiungi`, `/aggiorna` — tutti corretti al primo giro.
     - Stesso test su `scheduler/aggiungi` ha rivelato **2 bug reali**:
       a. Con `_HOST is None` (scenario plausibile: routes chiamate prima che `run_background` sia partito) la validazione tile veniva **saltata per intero** → accettava tile inesistenti E tile readonly (`stato`) come programmabili. Corretto: rifiuto esplicito se host non pronto, poi verifica sempre `tile_id in _HOST.tile_ids()`.
       b. Nessuna validazione del formato `ora` (accettava `"25:99"`) — aggiunta `_valid_hhmm()`.
       c. `id = f"sch-{int(time.time())}"` collide se due entry si creano nello stesso secondo (verificato: due chiamate consecutive nello stesso test producevano lo STESSO id). Corretto con `_new_id()` (contatore in-process) — stesso fix applicato a `taskboard.py` per lo stesso pattern.
     - Ri-testato dopo il fix: tutti i casi limite (tile inesistente, tile readonly, ora invalida, giorno invalido) ora rifiutati con messaggio chiaro; id verificati univoci su 2 aggiunte consecutive.
  5. **Pulizia**: i test hanno scritto temporaneamente in `state/taskboard.json`/`state/scheduler.json` reali — ripristinato `taskboard.json` da git, rimosso `scheduler.json` di test. Verificato a posteriori: `git status` pulito, 18 task originali intatti.
- **Decisioni prese:** nessun nuovo ADR. Aggiunto `_Host.tiles()` in `app.py` (pura plumbing, stessa giustificazione di `launch/poll/tile_ids` già presenti — non business logic, necessaria per B3).
- **Lezioni/errori:** conferma diretta della lezione già scritta nel CP precedente (G2, rinumerato 006): "nessuna revisione statica sostituisce una build/esecuzione". Qui il salto di qualità è stato testare le ROUTES, non solo il selftest — il selftest 15/15 non avrebbe MAI rivelato i 2 bug di `scheduler.aggiungi` (che riguardano l'input dell'utente via API, non lo stato del sistema).
- **Costi:** lavoro inline, nessun sub-agente.
- **Prossimo passo:** REGISTRO-ERRORI.md di EmpireDesk da aggiornare con questi 2 bug (EDE-10/11) — pendente. G2 già chiuso da sessione precedente; B2-B4 ora chiusi e verificati. Prossimo lato Gael: nessun task nuovo esplicito oltre G3 (già soddisfatto) — in attesa di direttive Max su fase 2 (U1 slice 2, pannelli metrics/revenue/licenze in stile Aureus).
