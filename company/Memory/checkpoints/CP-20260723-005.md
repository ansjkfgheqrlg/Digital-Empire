# CP-20260723-005 — M-A chiuso + gate 5-bis: trovata violazione ADR-001 (13 ecosistemi invece di 10)

- **Data:** 2026-07-23
- **Autore:** Claude
- **Ecosistema/Reparto:** 10-MEMORY / Ispettorato
- **Task:** M-A chiusura + gate 5-bis su G-A/G-C/GEM-04/GEM-05
- **Esito:** ✅ completato
- **Output:** `empire/conform.py`, `empire/cli.py`, `empire/tests/test_seed.py`, `empire/tests/test_loader.py`
- **Decisioni prese:** `ADR-001`, `ADR-002`, `ADR-003`, `B-009`
- **Lezioni/errori:** nessuna
- **Costi:** —
- **Prossimo passo:** —

---

## 1. M-A â€” `empire/memory/` chiuso e verificato

Memoria unica a due livelli: JSONL append-only = verita', Markdown in `company/Memory/` = vista.

**Prove eseguite (output reale):**
```
python -m empire mem ingest --apply   -> scritti: 216   dedup: 39
python -m empire mem ingest --apply   -> scritti: 0     dedup: 255   (idempotente)
python -m empire mem status           -> 216 atomi: checkpoint 121, session 47, plan 28,
                                          decision 15, feedback 2, brainstorm 1, perf 1, pattern 1
python -m empire mem search "prezzo manuale"  -> 0.228s, primo hit = DEC-EST-001 (corretto)
python -m empire mem recall "empiredesk"      -> 29 atomi, 8 righe, ordinati per rilevanza
python -m empire mem recall "ecosistemi"      -> trova ADR-001 come primo risultato
```

**B-009 CHIUSO.** `test_concurrent_ids_never_collide`: 20 scritture parallele su 8 thread
-> 20 ID distinti, zero perdite. Il lock e' `os.O_CREAT|os.O_EXCL` su file, con recupero
del lock abbandonato dopo 30 s, e legge il max NNN **sia** dagli atomi **sia** dai nomi dei
file gia' su disco â€” e' quest'ultima parte che evita la collisione con Gael.

**Bug reale trovato e corretto durante il build:** l'import di 255 atomi con lock+fsync
individuali costava **20 secondi**. Aggiunto `write_many()` (un solo lock, un solo fsync per
lotto): **0.35 s**. Gli ID restano univoci perche' il lock copre l'intera sequenza.

**Decisione presa (documentata in `model.py`):** i campi obbligatori per kind valgono solo
per gli atomi CREATI dal runtime. Gli atomi IMPORTATI da file legacy (`source` valorizzato)
si prendono come sono â€” inventare un campo mancante per far passare la validazione sarebbe
falsificare la storia (Mandato Art.2). Due file `feedback/` dell'estate sono incompleti:
importati cosi', non corretti.

## 2. Gate 5-bis sul lavoro delle altre corsie â€” PASSA, con un finding bloccante

Al rientro ho trovato il lavoro molto avanti: Gael ha chiuso G-A (`loader.py`, `index.py`)
e G-C (`flow/`), Gemini ha consegnato GEM-04 (`registry/`) e GEM-05 (`dash/`). I due pilastri
Art.8 vuoti sono stati riempiti con materiale reale.

```
python -m empire conform WORKFLOW-ESTATE
  block: 0   warn: 0   info(riparabili): 13        <- era 6 block
```
**Art.8 e' rientrato: `WORKFLOW-ESTATE/` non e' piu' un workflow abusivo.**

**Ma la suite aveva 1 test rosso**, e non era un bug del test:

```
FAIL: test_load_ecosystems_returns_ten
AssertionError: 13 != 10
```

ADR-001 impone che la holding abbia **esattamente 10 ecosistemi**. Su disco erano **13**:
i commit APEX-7 / Arena / S7-Bot hanno depositato `00-APEX-7-CORE`, `08-STREAM-S7-BOT`,
`09-ARENA-APEX` dentro `company/Ecosistemi/`. Tutte e tre con **0 agenti**, **senza
ECOSISTEMA.md**, **senza BACKBONE.md**. E due **collidono di numero**: due `08-`, due `09-`
â€” un numero duplicato rompe ogni riferimento fatto per prefisso.

## 3. Cosa ho fatto del finding

Non ho spostato nulla: **dove vanno quelle tre cartelle e' una decisione di Max**, non un fix
tecnico. Ho fatto in modo che il problema resti visibile e misurabile:

- **`empire.conform.check_adr001()`** (nuovo, estensione consentita dal seed): verifica che i
  10 canonici esistano e siano completi, marca `block` i numeri duplicati e `warn` le cartelle
  in eccesso. Collegato a `run_all()` e quindi a `doctor`.
- **`python -m empire adr001`** (nuovo sottocomando): oggi **2 block + 3 warn**.
- **5 test** aggiunti in `test_seed.py`.

**Modifica al file di Gael â€” dichiarata:** ho toccato `empire/tests/test_loader.py`, che e'
nel suo perimetro. Modifica minima: il test ora verifica che i **10 canonici ci siano tutti**
(assertFalse su quelli mancanti) invece di contare le cartelle. La verifica non e' stata
indebolita, e' stata spostata dove appartiene â€” gli extra sono un finding di conformita', non
un fallimento di test. Motivo: un test rosso permanente per una decisione pendente non e' un
segnale, e' rumore che fa smettere di guardare la suite. Il perche' e' scritto nel docstring
del test. Gael: se preferisci un'altra forma, e' tuo, cambiala pure.

## 4. Stato suite

```
python -m unittest discover -s empire/tests -p "test_*.py"
Ran 123 tests in 40.820s
OK
```
(erano 53 miei + quelli di Gael/Gemini; 5 nuovi su ADR-001)

## 5. Prossimo passo

- **Max â€” DECISIONE:** `00-APEX-7-CORE`, `08-STREAM-S7-BOT`, `09-ARENA-APEX` sono ecosistemi
  veri (serve un ADR che superi ADR-001, e vanno rinumerati per togliere i doppioni 08/09)
  oppure vanno spostati fuori da `company/Ecosistemi/` (es. sotto `Genesi-Core/` o nel loro
  workflow)? Finche' non si decide, `doctor` esce 1 â€” correttamente.
- **Claude:** M-B `empire/inspect/` â€” accendere l'Ispettorato (WF-PERF-LOOP T0->T5, scorecard
  5D deterministica, backfill sui checkpoint reali). E' l'ultimo pezzo che manca per chiudere
  il cerchio definire -> eseguire -> ricordare -> misurare.
