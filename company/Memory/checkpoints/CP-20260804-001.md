# CP-20260804-001 — Audit YOUTUBE-AUTOMATION-FACTORY (33 agenti/6 claim di Max) + mappa file-per-file + rilevata collisione live con Gael — PAUSA su richiesta esplicita (crediti)

- **Data:** 2026-08-04
- **Area:** YOUTUBE-AUTOMATION-FACTORY
- **Owner decisione:** Max · **Esecuzione:** Claude (sessione Sonnet 5, diversa da quella di Gael)
- **Esito:** ✅ audit + documentazione completati. ⏸️ Implementazione dei gap **non iniziata**,
  fermata su richiesta esplicita di Max ("fermiamoci... i crediti stanno finendo") dopo aver
  segnalato un rischio di collisione con una sessione Gael attiva in tempo reale sugli stessi file.

## Cosa è stato fatto in questa sessione

### 1. Report stato fabbrica + test reale
Ricostruito lo stato da checkpoint `CP-20260728-*`→`CP-20260803-*` + verifica diretta sul
codice. Eseguiti e confermati verdi:
- `python -m pytest test_youtube_apex7.py` → **11/11 PASS**.
- Run reale `apex7_orchestrator.py run --phase 6 --resume` → **F1→F6 tutte PASS**, SEO 100/100,
  nessun costo (F4 genera solo la spec, non chiama Fliki: il render vero è un passo separato,
  manuale, mai lanciato su richiesta di Max — fermato prima di consumare credito Fliki/Arena).
- Trovato e corretto: `FLIKI_API_KEY` mancava in `YOUTUBE-AUTOMATION-FACTORY/.env` (solo nella
  root del monorepo). Copiata la sola riga (valore mai stampato in chat). File gitignorato.

### 2. Mappa architettura completa, file per file
Scritto [ARCHITETTURA-COMPLETA-FILE-PER-FILE.md](../../../YOUTUBE-AUTOMATION-FACTORY/ARCHITETTURA-COMPLETA-FILE-PER-FILE.md)
(~150 file inventariati). Scoperta principale: **due motori paralleli** nello stesso repo —
`02-AUTOMAZIONI-E-SCRIPTS/` (reale, in uso, 11/11 test) e `youtube_automation_factory/` (package
`src/`-layout nuovo, mai installato: `pytest` fallisce per `pydantic_settings` mancante). Nato dal
commit `f4f50f22` "salvataggio lavoro in corso", senza un checkpoint dedicato che ne spieghi lo
scopo — da chiarire con chi l'ha iniziato.

### 3. Audit delle 6 richieste che Max ricordava di aver dato a Gael
Max ha chiesto conferma che 6 capacità fossero "implementate in modo perfetto, strutturato e
collegato a tutto l'ecosistema". Verificato **riga per riga sul codice**, non sui checkpoint
(che tendono a raccontare l'intento più che il wiring effettivo):

| # | Richiesta | Verdetto reale | Evidenza |
|---|---|---|---|
| 1 | Team copy con workflow | ⚠️ Parziale | `copy_study_dosementale.py` reale ma one-shot, **mai chiamato** da `apex7_orchestrator.py` (grep = 0 match). `capo-copy.md` + operatori copy = solo spec `.md` |
| 2 | Agenti regolatori | ✅ Reali (trovato 21% plagio vero) ma isolati | `grep -rln "regolatori" *.py` → solo se stesso. **Zero riferimenti** in `apex7_orchestrator.py`: si lanciano a mano, non girano nella run automatica |
| 3 | Copertine originali riadattate | ⚠️ Debole | `arena_thumbnail.py` prende la copertina vera del competitor e chiede all'AI di cambiarla (reale) — ma **nessun controllo** verifica il risultato: 0 righe su "copertina/thumbnail" in `regolatori.py` |
| 4 | Team competitor + performance canale | ⚠️ Parziale, ma meglio del previsto | `cashcow_check.py` **è wired** in F1 (subprocess reale, riga ~694) — analizza però un solo canale (@dosementale), non competitor plurali |
| 5 | Team altri canali nicchia | ❌ Rimosso, non mancante | `apex7_orchestrator.py` riga 74-76: *"Questo progetto non fa più scouting di nicchia... rimosso del tutto"*. Codice reale esiste **solo** nello scaffold non installato (`niche_channel_scout_agent.py`) |
| 6 | Team altre nicchie profittevoli | ❌ Stesso motivo di #5 | Canale fisso by design (`RULES-VIDEO-FACTORY-DOSEMENTALE.md`). Codice reale solo nello scaffold isolato (`profitable_niche_agent.py`) |

**Chiarito a Max**: i commit della riorganizzazione (`fde2f60d`, `1570786b`, `56bfd309`) sono
`Co-Authored-By: Claude Opus 5` — una sessione diversa da questa (Sonnet 5). Non esiste nel repo
un file col prompt verbatim di Gael (cercato con grep su frasi chiave in tutto
`YOUTUBE-AUTOMATION-FACTORY/` + `company/Memory/`): resta solo il riassunto già nei checkpoint.
Non è stato "eseguito male" da questa sessione — non è mai arrivato a questa sessione.

## ⚠️ Rilevata collisione live con Gael — motivo della pausa

Prima di iniziare a implementare i 6 gap, sincronizzato il repo (`git fetch` + pull): **erano
arrivati 3 commit nuovi da Gael, timestamp 09:46 e 10:08 di OGGI STESSO**, che toccano esattamente
`apex7_orchestrator.py` (F2, F3) e — riletto dopo il pull — `regolatori.py` risultava già diverso
da quanto letto un'ora prima nella stessa sessione (nuova `aggiorna_qc_registro`, nuovo commento
datato 2026-08-04 su un falso negativo appena corretto). Gael sta producendo video reali in questo
momento: nuovo registro `memory/video_prodotti.json`, F2 ora scorre il catalogo invece di
ripetere sempre lo stesso video, ritmo di lettura ricalibrato su misure reali (173-185 parole/min
osservate sulla voce Calimero, non più 140 da manuale).

Segnalato a Max con `AskUserQuestion` prima di editare i file caldi. Risposta: **fermarsi ora**,
checkpoint, salvare e pushare tutto, pausa (crediti in esaurimento) — non una delle opzioni
"procedi comunque" offerte.

## Local housekeeping fatto durante la sync
- Stashati e poi scartati (`git stash drop`) due diff locali che erano solo residuo del mio run
  di test F1→F6 di oggi (dashboard + `decision_log.json`/`.db`), superati dai dati reali di Gael
  arrivati col pull. Nessuna perdita: erano rigenerabili, non lavoro originale.
- Notato (non toccato, fuori perimetro): `git stash list` mostra anche uno stash `WIP on main`
  di un'altra sessione (Max, 2026-08-03) — lasciato intatto, non è mio.
- Notato (non toccato, fuori perimetro): `CP-20260803-008.md` (Preventa carousel, altra sessione)
  era untracked sul disco — non riguarda YouTube, segnalato solo per completezza del `git status`.

## RIPRESA DA
**Non iniziare editando `apex7_orchestrator.py` o `regolatori.py` senza prima verificare lo stato
di Gael** (`git fetch` + `git log --oneline HEAD..origin/main`): erano i file più caldi del
monorepo in questa sessione.

Ordine di lavoro proposto per la prossima sessione (post-audit, non ancora costruito):
1. **Regolatori → wiring automatico**: agganciare `verifica_nicchia`/`verifica_copy` a fine
   `run_phase_3`, `verifica_configurazione` in F4/F5, con BLOCCO reale (return `False`) come le
   altre fasi. Oggi girano solo a mano via CLI.
2. **Copertine → regolatore-copertina nuovo**: hash percettivo (Pillow è disponibile, verificato
   `12.1.1`) fra `source_thumbnail` e la copertina generata da Arena, soglia di distanza minima.
   Nessun controllo simile esiste oggi.
3. **Copy team → bridge dati**: `copy_study_dosementale.py` scrive solo su wiki (Markdown). Il
   punto di consumo reale è `apex7_orchestrator.py:1237` (`learned_rules.json`,
   `high_performing_tags`) — ma quel file viene **riscritto per intero** da `self_improve.py` a
   ogni F6 (anche a vuoto, se non ci sono log), quindi va usato un file separato
   (es. `memory/copy_intelligence.json`) letto in AND con `learned_rules.json`, non lo stesso
   file, per non farsi cancellare al giro dopo.
4. **Scouting canali/nicchie → tool advisory nuovi**, non riattivazione dello scouting rimosso:
   `channel_discovery.py`/`niche_discovery.py` che scrivono PROPOSTE (`memory/proposte_*.json`)
   senza mai toccare `CANALE_TARGET` — stesso principio già scritto (ma mai collegato) in
   `profitable_niche_agent.py` dello scaffold: "non cambia mai `PRIMARY_NICHE`, propone soltanto".
5. Prima di tutto quanto sopra: valutare con Max/Gael se ha senso costruire nello scaffold
   `youtube_automation_factory/` (già ha `niche_channel_scout_agent.py` e
   `profitable_niche_agent.py` scritti, solo da installare/testare/collegare) invece di
   duplicare la logica nel motore reale — decisione architetturale, non presa qui.

Nessun file di produzione modificato in questa sessione (solo lettura + 1 file nuovo di
documentazione + 1 riga di `.env` locale gitignorata). Sicuro riprendere da qui senza rollback.
