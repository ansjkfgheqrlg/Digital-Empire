# CP-20260702-002 — PreventivoForge: App Desktop (GUI) + motore PDF .exe-ready

- **Data:** 2026-07-02
- **Ecosistema/Reparto:** 01-AGENCY / Cliente Prof Autocad — PreventivoForge (Half B, Gael)
- **Task:** Costruire l'**app per PC** con interfaccia grafica (assegnata da Max, guida §6: "App .exe, la fa Gael;
  minimal, argento, elegante"). Rendere il render PDF eseguibile senza Playwright (per l'.exe).
- **Esito:** ✅ completato — app funzionante e testata end-to-end.

## Output (path reali)
- `app.py` — GUI Tkinter (argento/minimal): campo link + scelta concessionaria + bottone; avanzamento
  live; apre il PDF a fine lavoro. Orchestra `run.py` in un thread (NON riscrive la pipeline). Frozen-aware
  (runs/ e logs/ accanto all'.exe). Modo `--selftest` per test headless.
- `avvia-app.bat` — avvio 1-click della GUI (modo A, usabile subito con Python).
- `preventivo-forge.spec` + `build_exe.bat` — packaging PyInstaller onedir → `dist/PreventivoForge/PreventivoForge.exe` (modo B).
- `implementation/render_pdf.py` — **nuovo motore PDF via `cdp`** (Chrome del PC, `Page.printToPDF`),
  primario e .exe-ready; Playwright/WeasyPrint restano fallback. Lancio Chrome con `--remote-allow-origins=*`
  (Chrome ≥111 rifiuta la connessione WS CDP senza il flag).
- `APP-DESKTOP.md` — guida app + note per Max.
- `.gitignore` — esclusi build artifacts (build/, dist/, *.exe).

## Verifica (TEST)
- **Selftest app→run** (`python app.py --selftest`) su fixture con `window.__INITIAL_STATE__`: pipeline
  completa, **Gate A/B/C/D tutti PASS**, PDF `preventivo_bmw-320d.pdf` reso via **motore=cdp-chrome** (81 KB), rc=0.
- **Motore cdp validato** anche sul test isolato Half B (test_halfb.py): 4 gate verdi, render cdp-chrome.
- **GUI**: costruita senza errori (widget chiave presenti, dropdown concessionarie popolato).
- py_compile OK su render_pdf/app.

## Decisioni prese
- App = wrapper GUI su `run.py`, non riscrittura (ADR-003). Tkinter (zero deps extra, .exe-friendly, leggero).
- Motore PDF primario = cdp/Chrome (allinea l'obiettivo Playwright-free dell'.exe dichiarato da Max in cdp.py).
- Nessun ADR nuovo.

## Confine rispettato
NON modificati i file Half A (run.py, scraper, parser, pricer, common, dealers, **cdp.py**, schema). Il `cdp`
è stato USATO, non toccato. `render_pdf.py`, `glossary_de_it.py`, `app.py` = Half B (Gael).

## Lezioni/errori
- Chrome ≥111 richiede `--remote-allow-origins=*` per la connessione WebSocket CDP grezza (handshake 403) →
  gestito nel render lanciando Chrome col flag. **Segnalato a Max:** utile aggiungerlo anche in `cdp.launch`
  (Half A) per robustezza scraping su Chrome recente.
- Il nuovo parser di Max legge `window.__INITIAL_STATE__`; la modalità `--manual` estrae le dotazioni solo
  se l'HTML salvato contiene quel blob (il fallback `<ul><li>` non è più popolato). Segnalato a Max.

## Costi
API: €0. Install locali: websocket-client (cdp), + PyInstaller a build-time (non eseguito qui).

## Pending / da rifinire con Max (non bloccante)
- Build+test finale dell'.exe sull'ambiente reale (`build_exe.bat`).
- Eventuale `--remote-allow-origins=*` in `cdp.launch` + migrazione scraper a cdp puro (per .exe 100% playwright-free).

## Prossimo passo
App consegnabile. Attendere validazione .exe. Poi (GO Max): ripresa scelta ecosistema Empire (05 / split 06).
