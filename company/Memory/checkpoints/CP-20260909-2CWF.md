# CP-20260909-2CWF — max18 quasi chiuso: v09 100%, atomi 6/6, mancano 5 wiki page

- **Data:** 2026-09-09
- **Ecosistema/Reparto:** 10-memory / Empire Studio — studio del lotto `max18`
- **Codice di ripresa:** `EMP-W4K7` (APERTA) — **basta dirlo in chat nuova**.
- **Task:** chiudere la Fase 1 di `EMP-W4K7` (visione + atomi + wiki + archivio dei 9
  video max18), poi la Fase 2 (implementazione dei consigli).
- **Esito:** ⚠️ parziale — molto avanti da `CP-20260907-MUPD`, ma non chiuso.
- **Motivo del checkpoint:** chat corrente satura di contesto, Max ha ordinato di
  proseguire in chat nuova.

---

## 1. FOTOGRAFIA VERA — ricontata sul disco in questa sessione, non ereditata

| Video | Visione | Atomi | Grafo (componenti/orfani) | Wiki + archivio |
|---|---|---|---|---|
| `v04` (140FuW7b9pk, seminario Jim Rohn — **non** "Second Brain", vedi §3) | ✅ 120/120 | ✅ 78 | non ricalcolato, atomi da un solo agente (probabile gia' 1 componente) | ⚠️ solo archivio meccanico |
| `v05` (RnNSRF4s9nk) | ✅ 98/98 | ✅ 72 | ✅ gia' 1 componente da sessione precedente | ⚠️ solo archivio meccanico |
| `v06` (JTn5pqm9ecM) | ✅ 376/376 | ✅ 228 | ✅ gia' 1 componente da sessione precedente | ⚠️ solo archivio meccanico |
| `v07` (O2IDhISyy8Y) | ✅ 126/126 | ✅ 57 | ✅ 1 componente, 116 archi | ✅ **CHIUSO** (2026-09-06) |
| `v08` (DI5aWJiFAt8) | ✅ 393/393 (sorgente ancora `video.mp4.part`, da riverificare) | ✅ 229 | ✅ **18 componenti, 1 orfano** (era 87/56 — saldatura CONFERMATA finita, 80 relazioni nuove, verificate: zero campi diversi da `relazioni` toccati, zero riferimenti rotti) | ⚠️ solo archivio meccanico |
| `v09` (NmoOZVTrTXA) | ✅ **494/494** (chiuso in questa sessione) | ✅ 323 | ✅ **5 componenti, 0 orfani** (era 83/37) | ⚠️ solo archivio meccanico |

**Atomi totali nel lotto: 987.**

**⭐ Correzione di un mio errore nei checkpoint precedenti**: `CP-20260907-96DY` e
`CP-20260907-MUPD` dicevano "wiki + archivio: zero su sei". **Falso** — `v07` era gia'
chiuso dal 2026-09-06. Il vero conteggio e' **1/6 fatto, 5/6 mancanti** (v04, v05, v06,
v08, v09). Lezione: anche i propri checkpoint precedenti vanno riverificati sul disco,
mai ereditati (vale anche per i miei).

---

## 2. COSA E' STATO FATTO IN QUESTA SESSIONE (dalla ripresa di `CP-20260907-MUPD`)

1. **`v09` portato da 189/494 (38%) a 494/494 (100%)** — sentinelle a ondate sui blocchi
   mancanti, con due interruzioni gestite (rate-limit di sessione alle 17:40, e uno stop
   volontario di Max per lasciare crediti a Gael) e ripresa idempotente da ogni buco
   esatto senza mai perdere lavoro gia' scritto.
2. **`video-analysis.md` di `v09` unito e verificato**: 494 scene, 1→494 senza buchi ne'
   duplicati, header nel formato canonico (`# Video Analysis`, metadati durata/titolo).
3. **Atomizzati `v04` (intero), `v08` e `v09` (in 3 parti ciascuno)** — schema KA-NNN
   gia' in uso nell'Impero (non lo schema generico di content-forge). Un crash di rete
   a meta' di `v09-p2` e' stato recuperato in completamento senza perdere i 20 atomi
   gia' scritti.
4. **Merge e rinumerazione** con script scritto ad-hoc (`unisci_atomi_video.py`, nello
   scratchpad di sessione, non ancora nel repository) con mappa **per-file** — necessario
   perche' lo script canonico `SKILL & Agenti/Empire Studio Suite/empire-studio/scripts/
   unisci_atomi.py` ha un bug reale di collisione id (vedi **B-061** in BACKLOG.md,
   registrato in questa sessione).
5. **Saldatura del grafo** (isole → rete, il difetto "mappe di livello 1" di Justin Sung):
   `v09` da 83/37 a 5/0; `v08` da 87/56 a 18/1 (in corso, vedi tabella).
6. **Archivio meccanico costruito** per v04/v05/v06/v08/v09 (`contenuto-integrale.md` +
   `ingest-manifest.json`, in `SKILL & Agenti/Empire Studio Suite/empire-studio/
   memory-empire/knowledge/<video-id>/`) con script ad-hoc (`assembla_archivio.py`,
   scratchpad di sessione). **`ingest-manifest.json` di v08/v09 ha i numeri di grafo
   DEL MOMENTO IN CUI E' STATO GENERATO — vanno rigenerati dopo che la saldatura di v08
   e' davvero finita.**
7. **Igiene repository**: un conflitto di sync REALE (2 dietro/5 avanti, non un falso
   allarme come le altre volte) risolto con rebase pulito — nessuna sovrapposizione di
   file fra i miei 5 commit e i 2 di Gael. Una cartella `.git/rebase-merge` corrotta
   (lasciata da un tentativo automatico fallito) rimossa con verifica preventiva che il
   suo autostash fosse gia' salvo in un commit vero. **Lavoro in corso di un'ALTRA
   sessione viva** su `scripts/verifica_recap.py` e `scripts/gate_battito_hook.py`
   (fix del formato battito a riquadri) protetto con stash mirato e restituito intatto,
   MAI committato da me (non e' mio da firmare).

---

## 3. ERRORE MIO, CORRETTO NELLO STESSO TURNO

Nel dispatch dell'atomizzazione di `v04` ho scritto nel prompt "argomento: Second Brain/
Obsidian" — sbagliato, confuso con `v01` (che ha quel nome nella cartella). Il vero
contenuto di `v04` e' il seminario motivazionale di Jim Rohn "4 emozioni che possono
cambiare la vita in un giorno", canale "Imprenditor Tips" — **coerente con l'etichetta
"mindset" gia' presente in `EMP-W4K7.md` riga 78**, quindi **nessun file persistente e'
stato corrotto**, l'errore era solo nel mio prompt effimero. L'agente ha ignorato
correttamente la mia etichetta sbagliata e ha estratto dal contenuto vero.

---

## 4. QUELLO CHE MANCA — in ordine, per la ripresa

1. ~~Verificare se la saldatura di v08 e' finita~~ — **confermato finita**: 18
   componenti/1 orfano (KA-164 "Guest pass", isola vera, lasciata cosi' apposta), 80
   relazioni nuove, zero campi diversi da `relazioni` toccati. Nessuna azione qui.
2. **Rigenerare `ingest-manifest.json` di v08 e v09** con i numeri di grafo finali (lo
   script `assembla_archivio.py` e' nello scratchpad di sessione, va riscritto o
   recuperato — non e' nel repository).
3. **Scrivere le 5 pagine wiki mancanti** (v04, v05, v06, v08, v09) in
   `second-brain-vault/wiki/sources/`, stesso livello di profondita' e rigore di
   `Source_Giovanni_Beggiato_LinkedIn_Generare_Clienti.md` (v07, 309 righe, citazioni per
   atomo, verdetto, sezione Connessioni con cross-link reali). E' il pezzo di lavoro piu'
   grosso e piu' creativo rimasto — non meccanico, richiede lettura e sintesi vera per
   ogni video. Poi entry in `wiki/index.md` (stile ricco, non il template generico — vedi
   le entry esistenti per lo stile reale) e in `wiki/log.md`.
4. **Poi `max17` (`EMP-QQ2R`)**: fermo a **9/17** da giorni, non toccato in questa
   sessione — resta debito della stessa Fase 1 complessiva.
5. **Solo dopo, Fase 2** (ordine esplicito di Max, ripetuto due volte: non parte prima
   che la Fase 1 sia chiusa del tutto). Cinque interventi gia' trovati e verificati sul
   codice, pronti a essere implementati quando Max da' il via:
   - `atoms.json` senza archi/isole — **gia' in gran parte risolto per v09, quasi per
     v08**, ma il fix strutturale su `atomizer.py`/`cf-knowledge-graph-agent` (perche'
     non succeda piu' da solo) resta da fare.
   - `frame_extractor.py:133` scarica a 360p per un flag nostro sbagliato (non un limite
     YouTube).
   - `cf-knowledge-graph-agent` e `book-to-skill` dichiarano capacita' (archi, niente
     riassunti) che il codice non ha.
   - `peso_skill.py` non pesa mai `description`, il costo pagato a ogni sessione.
   - **B-061** (nuovo, questa sessione): `unisci_atomi.py` ha collisione id per mappa
     globale invece che per-file.

---

## 5. FILE DI APPOGGIO CREATI SOLO NELLO SCRATCHPAD DI SESSIONE (persi alla chiusura chat)

Questi script hanno fatto lavoro vero ma **non sono nel repository** — se servono ancora
vanno riscritti (la logica e' descritta per intero sopra e nei messaggi di questa
sessione, non serve reinventarla da zero):
- `unisci_atomi_video.py` — merge+rinumerazione atomi con mappa per-file (fix del bug
  B-061 applicato ad-hoc).
- `assembla_archivio.py` — genera `contenuto-integrale.md` + `ingest-manifest.json` da
  `video-analysis.md` + `transcript.md` + `ingest.json` + `atoms.json`.
- `genera_battito.py` — NON legato a questo lavoro, era un tentativo di debug del
  formato battito (vedi nota sotto), scartato perche' Max ha detto di lasciar perdere.

---

## 6. NOTA FUORI PERIMETRO — formato del battito e' cambiato

Durante questa sessione **un'altra sessione viva** ha cambiato la forma del battito
(§6.11 di `emperator.md`): non piu' elenco piatto a sei righe, ma **cinque riquadri
chiusi e centrati con NBSP**, generati da `scripts/verifica_recap.py::costruisci()`.
**Regola nuova sul QUANDO**: il battito si da' SOLO ogni ~10 minuti, su una svolta grossa
di percentuale, o quando Max lo chiede esplicitamente — **non piu' ad ogni turno**.
Ho trovato un bug reale nello script (`_RIENTRO_CHARS`/`FRECCIA` isolati funzionano,
ma il file vero generato falliva la validazione — causa non isolata, il debug e' stato
abbandonato su ordine di Max prima di trovarla). **Alla ripresa in chat nuova: rileggere
`emperator.md` §6.11 per intero prima del primo battito**, non fidarsi di questo
riassunto ne' della vecchia forma piatta.

---

- **Decisioni prese:** nessun ADR nuovo. Backlog: **B-061** (bug `unisci_atomi.py`).
- **Lezioni/errori:** (a) i propri checkpoint precedenti vanno riverificati sul disco,
  mai ereditati — trovato un errore mio proprio in questo modo (b) un errore in un
  prompt effimero (mai scritto su file persistente) non e' un puntatore stale da
  correggere, ma va comunque dichiarato; (c) la saldatura del grafo su atomi prodotti in
  parallelo va sempre fatta, mai data per scontata — isole sono un difetto reale, non
  un dettaglio.
- **Costi:** ~2 giorni di sessione (2026-09-07 sera → 2026-09-09), piu' ondate di
  sentinelle Sonnet in parallelo per visione, atomizzazione e saldatura.
- **Prossimo passo:** ripresa `EMP-W4K7` — verificare v08, rigenerare i due manifest,
  scrivere le 5 pagine wiki, poi `max17`, poi Fase 2 su ordine di Max.
