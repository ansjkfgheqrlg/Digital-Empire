# CP-20260719-005 — Skill ufficiale `master-app-builder` installata in `.claude/skills/`

- **Data:** 2026-07-19
- **Ecosistema/Reparto:** `06b-FORGE/L2.1 SKILL-WORKS` (forgiatura della skill) — deliverable usato da `06a-PLATFORM/L2.2 PRODUCT-ENGINEERING` (`plt-app-builder`, `plt-saas-builder`) per costruire app/tool interni ed esterni
- **Task:** Max ha chiesto di trasformare il prompt "MASTER APP BUILDER v2.0" (fornito in chat) in una skill Claude Code ufficiale, installata correttamente, che studi le app già costruite nel repo prima di progettarne di nuove.
- **Esito:** ✅ completo (skill installata, riconosciuta e presente nell'elenco skill disponibili; nessun test di build reale eseguito perché nessuna nuova app è stata richiesta in questa sessione)
- **Output:**
  - `.claude/skills/master-app-builder/SKILL.md` — kernel operativo (~550 righe): frontmatter YAML (`name: master-app-builder`), 9 checkpoint (0→8), squadra di ~25 agenti logici, memoria a due livelli, gate del Supervisore. Basato su una bozza già presente e più ricca (v2.1) trovata in `master-app-builder-skill/SKILL.md` (non era installata come skill, solo file sciolto nella root), non sul v2.0 incollato da Max in chat.
  - **Aggiunta nuova (non presente nella bozza):** Fase 0.0 "Pattern mining: studio delle app precedenti", obbligatoria prima di ogni design — tabella con 4 precedenti reali del repo e il pattern da riusare: `Clienti/Prof Autocad/preventivo-forge/` (scraping Chrome+CDP, GUI Tkinter argento, PDF cdp-chrome, PyInstaller, gate+selftest, REGISTRO-ERRORI), `EmpireDesk/` (cascata GUI Chrome-app→pywebview→Tkinter, TileManager subprocess, palette slate+argento+`#fb4604`, 3 bug noti da non ripetere), `company/Memory/`+ciclo 9 passi ADR-006, convenzioni SKILL.md locali (frontmatter kebab-case + riga Reparto/Team/Tier).
  - **Aggiunta nuova:** tie-in di governance esplicito con i reparti EMPIRE OS reali (letti da `PIANO-MAESTRO/06a-ECOSISTEMA-PLATFORM-V2.md` §L2.2 e `06b-ECOSISTEMA-FORGE-V2.md` §L2.1), non inventati.
  - **Aggiunta nuova:** sezione 3.0 di precedenza memoria — chiarisce che dentro Digital Empire `company/Memory/` (ADR-002) resta la fonte canonica e `docs/memory/` della skill è solo il livello locale del progetto in costruzione; obbliga comunque il checkpoint in `company/Memory/checkpoints/` a fine sessione.
  - `.claude/skills/master-app-builder/README.md` + `docs/agents/REGISTRY.md`, `docs/rules/*.md`, `docs/references/README.md`, `docs/workflows/README.md`, `docs/memory/*.md` (template), `scripts/session_bootstrap.py` — copiati as-is dalla bozza `master-app-builder-skill/` (contenuto verificato prima della copia, non stub).
- **Decisioni prese:** nessun nuovo ADR — installazione di una skill segue la convenzione già in uso nel repo (frontmatter `name` kebab-case + riga `Reparto/Team/Tier`, vedi `.claude/skills/delivery-playbook/SKILL.md`), non introduce un pattern nuovo.
- **NON toccato:** `master-app-builder-skill/` (bozza originale in root) e `master-build-architecture/` (cartella untracked trovata nella root, contenuto in inglese con affermazioni non verificabili — repo GitHub esterni, PAT, path Linux `/home/user/`, "40 agenti" — riconducibile a una sessione precedente in un ambiente diverso da questo repo Windows; **non usata come fonte** per la skill ufficiale, solo ispezionata). Entrambe le cartelle restano nella root, invariate: l'utente può cancellarle se conferma che sono superate dalla skill ora installata in `.claude/skills/`.
- **Costi:** lavoro inline (io/conductor), 0 swarm/agenti spawnati (task singolo, non ≥2 aree disgiunte).
- **Verifica eseguita:** dopo la creazione, il system-reminder degli skill disponibili elenca `master-app-builder` con la description corretta → la skill è effettivamente installata e individuabile da Claude Code, non solo scritta su disco. Non è stato eseguito nessun build applicativo reale (fuori scope di questo task: nessuna app è stata richiesta).
- **Blocchi:** nessuno.
- **Prossimo passo:** nessuno obbligatorio; alla prossima richiesta di una nuova app/tool, invocare `/master-app-builder` (o lasciare che Claude Code la selezioni in autonomia) per verificarne l'uso end-to-end su un caso reale.
