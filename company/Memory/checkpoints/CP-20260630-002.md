# CP-20260630-002 — PreventivoForge (cliente Prof Autocad) — Half A FATTA

**Data:** 2026-06-30 · **Autore:** Max · **Tipo:** cliente / build workflow

## Contesto
Primo cliente ufficiale: **Prof Autocad** (concessionario auto, import dalla Germania).
Richiesta: workflow che trasforma un **annuncio mobile.de (DE)** in un **PREVENTIVO italiano (PDF)**,
copy migliorato, **prezzo finale nel titolo** (`esposto×1.03+1500+1500`), **multi-concessionaria**.
Metodo richiesto: `architect-agent` (RBI) + `content-forge` (agenti 7-file) + repo `master-build-architecture`.
Decisione esecuzione (Max): **Half A (Max) + handoff a Gael** per Half B.

## Fatto
- **Cartella cliente** `Clienti/Prof Autocad/` popolata (era vuota). README cliente.
- **Architettura/SPEC** `preventivo-forge/00-ARCHITETTURA-WORKFLOW.md` (pipeline 5 step + 4 gate, rischi, RBI, team, split 50/50).
- **Data contract CONGELATO** `schema/listing.schema.json` + `schema/listing_it.schema.json` (cucitura 50/50).
- **Half A runnable + TESTATO** (Python 3.11):
  - `implementation/common.py` (RunContext, config .env, state/trace/log, validazione schema)
  - `implementation/scraper.py` (S1 Playwright persistent+stealth+consenso+anti-bot detect; fallback `--manual`; download foto)
  - `implementation/parser.py` (S2 raw→listing.json: JSON-LD Car primario + DOM fallback + mappe enum DE→IT + parser numeri DE)
  - `implementation/pricer.py` (S4 deterministico; **test: 18.000→21.540 ✅**, formato IT `21.540`, titolo)
  - `implementation/dealers.py` (multi-tenant; prezzo dealer>env) + `concessionarie/prof-autocad/config.json`
  - `run.py` (regia: S1→S2→GateA→[S3]→S4→[S5], import difensivo Half B, `--dealer`/`--manual`/`--list-dealers`)
  - support: `requirements.txt`, `.gitignore`, `.env.example`, `CLAUDE.md` (BRAIN/RBI)
  - **skill** `/preventivo-auto` (`.claude/skills/preventivo-auto/SKILL.md`) — regia che avvia il workflow.
- **Handoff** `HANDOFF-GAEL.md` (contratti funzione S3/S5/gate, formato preventivo, glossario DE→IT, criteri gate B/C/D).
- **Coordinamento:** STATO-EMPIRE → blocco 🛑 per Gael (pausa impero, fai Half B) + blocco cliente aggiornato.

## Test
`py_compile` tutto OK · pricer 18.000→21.540 · format_eur 21.540 · dealer load · parser numeri DE.
(Scraping live mobile.de NON testato qui: anti-bot + ambiente; da provare nell'ambiente cliente.)

## RIPRESA DA (Max, prossimo turno)
1. Agenti **CF-grade 7-file** Half A: `conductor`, `op-scraper`, `op-parser`, `op-pricer` + `agents/CATALOG.md`.
2. Regole RBI `rules/R1-scraping.md`, `R2-parsing.md`, `R4-pricing.md`.
3. `orchestration/` (supervisor/routing/registry/policies).
4. (opz.) test end-to-end con HTML reale dell'annuncio Mercedes GLA via `--manual`.
**GAEL:** Half B (S3+S5+4 QA) — `HANDOFF-GAEL.md`. **Riferimento formato preventivo da Max** (PDF "Preventivo BMW Z4").
