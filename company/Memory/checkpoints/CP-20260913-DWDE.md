# CP-20260913-DWDE — EMP-XR4F F1 — D1 texture + D2 funnel-hero + A02 rail + A01 scheletro in ANTEPRIMA (lf5uapfvv), gate PASS, contrasto misurato

- **Data:** 2026-09-13 (pomeriggio)
- **Ecosistema/Reparto:** 01-agency / Fabbrica Siti
- **Task:** «vai procedi» di Max (dopo lo shock sulla tavola D1: chiarito che era uno schizzo, non il sito) → F1 del V4 v2 (Dossier 38 v2 §G/§I):
  D2 + D1 + A01 + A02, **solo in anteprima**.
- **Esito:** ✅ F1 costruita, gate PASS, anteprima deployata e aperta a Max. ⏸️ `--prod` è suo. ⏸️ texture vere ancora sue.
  1. **File nuovi** (`agency-empire-landing/`): `src/lib/media.ts` (registro texture/ritratti, `null` = posto vuoto), `src/sezioni-aggiunte/hero-texture.tsx`
     (D1: layer assoluto primo figlio della section, `<img>` 2880×1620 `fetchPriority=high`, `cover` = eccezione §14 dichiarata, velo radiale+lineare,
     grana locale sul velo), `funnel-hero.tsx` (D2: 4 blocchi da FATTI/LISTINO, 3 frecce «a salto» SVG bezier a puntini che scorrono, marker, ferme in
     reduced-motion; mobile in colonna con tratteggio verticale), `rail-fatti.tsx` (A02, carta, nastro 70 s, fermo in reduced-motion), `firma.tsx` (A01,
     rende `null` finché `RITRATTI.maxFirma` è null), `src/app/aggiunte-hero.css`.
  2. **Inserimenti puri** (0 righe rimosse, verificato `git diff --numstat`): `layout.tsx` +1 (import css), `page.tsx` +5 (`<Firma />`, `<RailFatti />` dopo `<Hero />`),
     `hero.tsx` +10 (`<HeroTexture />` primo figlio; `<FunnelHero />` in un div NUOVO **fuori** dal blocco del titolo — dentro, le chip d'argento
     posizionate in % finivano sopra i blocchi).
  3. **D2 «titolo più in alto»**: fatto in CSS puro, l'unica regola che tocca un elemento esistente, dichiarata nel file:
     `main > section:first-of-type > div.max-w-5xl { padding-top: 3.25/4.25rem; padding-bottom: 2.25/2.75rem }` (era py-24/py-32).
  4. **Contrasto misurato, non a occhio** (`scratchpad/misura_hero.py`: nasconde i testi, campiona il fondo sotto h1/sottotitolo/funnel, luminanza vs #f4f1ee):
     desktop h1 **min 9,6:1** (p99 16:1), sottotitolo 16,8:1, funnel 17,9:1; mobile h1 15,9:1. Prima taratura del velo dava 14,5:1 ma la texture non si vedeva:
     alleggerito (centro .70/.55, bordi .12).
  5. **Gate** `gate_solo_aggiunte.py` **PASS** (A testo ⊂ build con deroga `--consenti "65includono"` = refuso nostro di X3A4 non ancora in prod; B 0 righe rimosse).
     `npm run build` ok, console 0 errori, grep prezzi a mano = 0. Hero: 1.307 px desktop (era ~1.000), 1.911 mobile.
  6. **Anteprima:** https://agency-empire-landing-lf5uapfvv-maximilians-projects-f0964962.vercel.app/ (protetta da Vercel: si vede da loggati — curl dà 302).
     Aperta nel browser di Max. Commit `a1dc3453`.
- **Output:** file sopra · `public/texture/hero-onde.jpg` + `grana-fuoco.jpg` = **APPROSSIMAZIONI** (`scratchpad/texture_site.py`, 2880×1620 e 2400×1000),
  `LEGGIMI.txt` lo dice: Max li sovrascrive con i suoi, stesso nome · screenshot `scratchpad/f1-hero-{desk,mob}.png`, `f1-rail.png`.
- **Decisioni prese:** nessuna nuova. D2 in CSS (non in hero.tsx) per tenere il gate a 0 righe rimosse pur eseguendo l'ordine.
- **Lezioni/errori:** (1) Tavola D1 senza grana e senza timbro «schizzo» → shock di Max: **ogni mock in tavola porta il timbro «schema, non rendering»**
  e, se il sito ha una firma (grana), la tavola o la porta o lo dichiara. (2) Le chip in `bottom:%` di un contenitore che cresce si spostano: un'aggiunta
  dentro un blocco esistente può muovere cose esistenti — inserire **accanto**, non dentro. (3) Un contrasto «troppo alto» (14:1) è un velo che uccide la
  texture: si misura il minimo E si guarda lo screenshot. (4) Lenis: `window.scrollTo` non riallinea i bounding box — misurare senza scrollare, viewport alto.
- **Segnalato, non toccato (§13):** su mobile «operatività» (82 px minimi) esce dal bordo destro — è così anche nel live. Decisione di Max.
- **Costi:** ~12% sessione, nessuna forza schierata.
- **Prossimo passo (`EMP-XR4F` aperta):** sguardo di Max sull'anteprima → suo `--prod` (`cd agency-empire-landing && npx vercel --prod --yes`) o correzioni;
  poi **F1b = D3 fascia-manifesto + D4 competitor + D5 specchio con immagine** (still 586430f8 e 9152a07f disponibili in AURA, da rigenerare a 2×),
  sempre in anteprima; le texture vere in `public/texture/` quando Max le mette (sovrascrivono le approssimazioni).
