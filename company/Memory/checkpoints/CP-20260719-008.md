# CP-20260719-008 — EmpireDesk: risolta collisione UI Gael/Max su ui/index.html + preso atto ownership

- **Data:** 2026-07-19 (sera)
- **Ecosistema/Reparto:** Empire Desk — coordinamento Half A/Half B, dossier `PIANO-MAESTRO/17-EMPIRE-DESK-APP.md` §5
- **Task:** durante il `git pull --rebase` per pushare CP-20260719-007 (B1), scoperta una collisione reale: Max ha costruito in PARALLELO (stessa sera) un redesign completo di `ui/index.html` ("Empire Premium": nav a tab, palette ink+arancio) con lo STESSO obiettivo del mio switcher pannelli, ma un contratto di rete diverso. `git pull` ha riportato anche l'aggiornamento di `STATO-EMPIRE.md` in cui Max formalizza: **"LA UI/UX È DI MAX, NON DI GAEL"** — cambio di ownership deciso questa sera, dopo che avevo già iniziato B1 sulla base del dossier precedente (che assegnava `ui/index.html` a Gael).
- **Esito:** ✅ risolto senza perdita di lavoro da nessuna delle due parti; ✅ preso atto del cambio ownership, applicato da ora in poi.
- **Cosa ho fatto:**
  1. **Merge manuale, 8 blocchi in conflitto** in `ui/index.html`: tenuto INTERAMENTE il design visivo di Max (più ricco e già strutturato per i pannelli moduli: nav-tab unificata con "Plancia" come prima tab, `.view`/`.view.active`, palette ink-900/ink-950/accent) — il mio bottone-toggle "Pannelli" + vista separata sono stati scartati (superati dal suo design).
  2. **Adattato `app.py` al contratto di Max**, non il contrario: lui è il nuovo owner UI e ha specificato in `STATO-EMPIRE.md` l'endpoint esatto atteso (`POST /api/modules` → `{"modules":[{id,tile,panel_html}]}`). Rinominato `modules_public()`/route `api/modules`/`_WebApi.modules()` (prima erano `panels()`/`api/panels`/chiave `"html"` — mia scelta precedente, ora allineata alla sua).
  3. Verificato che il flusso end-to-end sia coerente: `loadModules()` in `ui/index.html` legge `m.panel_html` (non più `m.html`) — corretto per matchare `app.py`.
  4. `README.md`/`REGISTRO-ERRORI.md` aggiornati con l'ownership e il contratto di rete finale (EDE-8, nuova voce).
- **Decisioni prese:** nessun nuovo ADR. Applicata la regola generale "chi possiede la UI decide il contratto verso la UI, il backend si adegua" — coerente con §5.4 (anti-collisione) e con l'aggiornamento ownership di Max in STATO-EMPIRE.
- **Lezioni/errori:** **EDE-8** in `EmpireDesk/REGISTRO-ERRORI.md` — 2 sessioni Claude (Gael+Max) hanno costruito la STESSA feature in parallelo sullo stesso file prima che l'ownership fosse formalizzata; scoperto solo al momento del pull/push, non prima. Lezione: su un task "focus totale" con 2 sessioni attive, leggere `STATO-EMPIRE.md` di nuovo PRIMA di ogni blocco di lavoro (non solo a inizio sessione) e fare `git pull` appena prima di toccare un file potenzialmente condiviso, non solo all'avvio.
- **Da ora in poi:** **Gael NON tocca più `ui/index.html`** (confermato, applico l'ordine di Max). Il mio perimetro resta: `app.py` (core Python, ora in FREEZE salvo estensioni via `modules/`), `build_exe.bat`, `empiredesk.spec`, e i miei moduli futuri (`modules/scheduler.py` B2, `modules/notify.py` B3, `modules/taskboard.py` B4).
- **Costi:** lavoro inline (io/conductor), 0 swarm/agenti spawnati.
- **Blocchi (invariati):** ambiente sessione senza Python/Node/PyInstaller → nessun test a runtime possibile qui, né per B0 né per B1. Serve macchina reale.
- **Prossimo passo:** comunicare a Max che il merge è pushato e allineato al suo contratto (`/api/modules`). Se Max verifica su macchina reale che l'app gira: chiudere B0+B1 definitivamente (selftest 8/8+ moduli, build exe, test doppio click). Poi B2 (`modules/scheduler.py`), solo lato Python.
