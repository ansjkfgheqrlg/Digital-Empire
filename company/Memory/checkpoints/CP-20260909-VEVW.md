# CP-20260909-VEVW — battito 7º giro: tabella markdown, niente più fence

- **Data:** 2026-09-09
- **Ecosistema/Reparto:** 10-memory / battito Emperator (§6.11) — continuazione di
  CP-20260909-RWR2 / CP-20260909-6T6J (stessa task, "migliora il recap").
- **Task:** Max ha bocciato il 6º giro (battito dentro un blocco ``` ```): screenshot
  mostrava un widget blu con pulsante "copia", allineato a sinistra dentro il riquadro —
  esattamente quello che Max NON voleva ("mai quel formato da copiare e tutto di colori
  azzurri... dev'essere tutto centrato", ripetuto 3 volte). 7º giro: tolto il blocco di
  codice, il battito ora è una TABELLA markdown (GFM) a una colonna — intestazione vuota
  `| |`, separatore centrato `|:---:|`, una riga `| ... |` per etichetta/contenuto/freccia.
  Il centraggio lo fa il renderer via CSS sulla colonna, non più un conteggio di spazi:
  immune al difetto di TUTTI i giri 2-6 (motori che collassano gli spazi ripetuti). Il
  titolo resta testo semplice fuori dalla tabella (l'unica riga a sinistra, invariato).
- **Esito:** ⚠️ parziale — CODICE chiuso e verde, DOTTRINA aggiornata; manca la CONFERMA
  VISIVA di Max sul nuovo formato (lo screenshot che chiude il filone, come per ogni giro
  precedente — nessun giro si è mai dichiarato chiuso senza quella conferma).
- **Output:**
  - `scripts/verifica_recap.py` — riscritto: `costruisci()` genera la tabella invece del
    testo a rientro; `valida()` parsa righe `| ... |` invece di indentazione a spazi/`·`
    (tutti i controlli di "rientro coerente" sono spariti: non c'è più spazio da contare a
    mano). Storia dei 7 giri tenuta per intero nel docstring del modulo.
  - `scripts/gate_battito_hook.py` — la regola è di nuovo invertita: blocca un battito
    trovato DENTRO un blocco ``` (era l'opposto dal 6º giro), valida come tabella quello
    fuori da un fence. `SEGNALE_VOCE`/`SEGNALE_POTERE` aggiornati per riconoscere sia la
    cella `| 🟠 ... |` sia la forma nuda (compatibilità con testo scritto a mano).
  - `scripts/test_gate_battito.py` — riscritto: 11 casi invertiti dove serviva (fence ora
    blocca, tabella pulita ora passa) — **11/11 verdi**.
  - `.claude/agents/emperator.md` §6.11 — due esempi (piatto e ad albero) convertiti in
    tabella, regola 8 invertita (mai dentro ```), regole 4/5/7/10 riscritte per il nuovo
    meccanismo, aggiunto blockquote "❌ Settimo giro" con la lezione, aggiornati i
    riferimenti sparsi (11/11 invece di 6/6). Entrambi gli esempi ri-verificati con
    `valida()` dopo la scrittura — trovato e corretto un errore mio in corsa (avevo
    dimenticato l'etichetta `🟠 Assetto:` in entrambi gli esempi trascritti a mano).
- **Decisioni prese:** nessun ADR — modifica di forma su una regola già esistente (§6.11),
  stesso principio dei giri precedenti.
- **Lezioni/errori:**
  - Trascrivere a mano un esempio che `costruisci()` genera automaticamente è rischioso
    anche quando lo si fa "solo per un file di documentazione": ho dimenticato una riga
    (l'etichetta Assetto) in entrambi gli esempi di emperator.md al primo giro di editing.
    Trovato SOLO perché ho fatto girare `valida()` su quel testo esatto prima di fidarmi —
    esattamente la disciplina che la dottrina stessa chiede prima di mandare un battito a
    Max, e che vale altrettanto quando scrivo un ESEMPIO nella dottrina. Non fidarsi mai di
    una trascrizione a mano, nemmeno breve, nemmeno per la documentazione.
  - Un bug di formato (`%d` su un valore che poteva essere la stringa `"<fine tabella>"`)
    in `valida()` lanciava un'eccezione che il gate inghiottiva in silenzio (PROTEZIONE 3:
    "qualunque errore → esce 0") — il caso di test 3 (battito rotto) falliva SENZA bloccare,
    proprio come nell'incidente del 2026-09-05 con un placeholder troppo lungo. Trovato
    dalla suite di test (`3/11` a `10/11`), non da un'ispezione a occhio del codice — motivo
    in più per far girare sempre la suite dopo ogni modifica al gate, mai fidarsi della
    lettura.
- **Costi:** nessuna chiamata API oltre alla sessione corrente.
- **Prossimo passo (in ordine):**
  1. Mandare a Max un battito di prova nella forma nuova (tabella, no fence) — è la
     conferma visiva che manca per chiudere davvero il filone dei 7 giri.
  2. Se Max conferma: chiudere insieme CP-20260909-RWR2, CP-20260909-6T6J e questo
     CP-20260909-VEVW, un'unica voce di chiusura in STATO-EMPIRE.md.
  3. Se Max boccia ANCORA: non provare un'altra variante tecnica a caso — chiedere a Max
     uno screenshot del rendering reale del nuovo tentativo prima di ipotizzare la causa
     (lezione ripetuta di quasi ogni giro precedente).
