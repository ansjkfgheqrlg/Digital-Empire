# CP-20260720-003 — EmpireDesk G1: verifica statica del commit 85548a30 (Aureus come root)

- **Data:** 2026-07-20
- **Ecosistema/Reparto:** Empire Desk — Half B «Core & Runtime» / dossier `PIANO-MAESTRO/17-EMPIRE-DESK-APP.md` §0-bis
- **Task:** dopo aver chiuso Empire Studio video 10/29 (CP-20260720-002), ho controllato `STATO-EMPIRE.md` come richiesto dalla Regola Zero e scoperto che la priorità attuale assegnata a Gael è EmpireDesk G1 (pivot Aureus), non Empire Studio. G1 risultava già scritto e pushato da una sessione Gael parallela (commit `85548a30`, in background mentre lavoravo su Empire Studio) ma non verificato né segnato nello stato. Ho fatto revisione statica del diff (ambiente ancora senza Python/Node/Chrome, come dichiarato nel commit stesso).
- **Esito:** ✅ revisione statica completata, nessun difetto trovato — G1 resta "scritto, non verificato a runtime" (non posso chiuderlo davvero: serve build/esecuzione su macchina reale).
- **Cosa ho fatto:**
  1. Letto `git show 85548a30` per intero (`app.py`, `build_exe.bat`, `empiredesk.spec`).
  2. Verificato `do_GET`: path-traversal guard (`Path.is_relative_to`) corretto, fallback SPA su `index.html` per route react-router, `/api/*` resta 404 su GET (comportamento preesistente, non regressione), pagina di aiuto onesta se `platform/dist/` manca.
  3. Verificato che `main_chrome_app` e `main_webview` condividano lo stesso server locale (`_start_server()` su porta libera) — `main_webview` prima usava `html=` inline, ora `url=http://127.0.0.1:<porta>/`: coerente col fatto che Aureus è una SPA multi-asset, non embeddabile inline.
  4. Verificato `empiredesk.spec`: `datas` include `platform/dist` (non esiste ancora — costruito da `build_exe.bat` PRIMA di invocare PyInstaller, ordine corretto) + `modules/` + `state/` (controllato su disco: entrambe le cartelle esistono e sono tracciate nel repo, zero rischio di build rotta per path assente).
  5. Aggiornato `company/Memory/STATO-EMPIRE.md`: blocco G1 marcato ✅ scritto+verificato staticamente, con nota esplicita che manca ancora la verifica a runtime (npm install/build + selftest + avvio reale) prima di poter dichiarare G1 davvero chiuso. G2 lasciato come "prossimo, blocca su G1 verificato a runtime".
- **Decisioni prese:** nessun nuovo ADR. Non ho toccato il codice (nessun difetto da correggere).
- **Lezioni/errori:** conferma pattern già visto oggi (CP-20260720-002): quando un task del piano richiede toolchain non presente in questa sessione, la revisione statica del codice è comunque un contributo valido e va registrata in Memory come tale — non va scambiata per "task chiuso". Utile distinguere sempre "scritto" da "verificato a runtime" nello STATO-EMPIRE per non far perdere questa distinzione a chi riprende su macchina reale.
- **Costi:** lavoro inline, nessun sub-agente.
- **Prossimo passo:** su una macchina con Python+Node+Chrome: `cd EmpireDesk/platform && npm install && npm run build`, poi `cd .. && python app.py --selftest` (deve mostrare `platform` OK), poi `python app.py` per verifica visiva reale. Se verde → G2 (build exe + test doppio click).
