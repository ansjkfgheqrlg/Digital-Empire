# CP-20260825-001 — SYNC monorepo: assorbito lavoro CCM/Landing + skill empire-premium-style, trovata chiave Brevo pubblica

- **Data:** 2026-08-25
- **Ecosistema/Reparto:** 10-MEMORY (sync GitHub, ADR-004) / trasversale — 02-INFO-BUSINESS (landing CCM), 03-CONTENT-FACTORY (skill di stile)
- **Task:** richiesta diretta di Max — "aggiorna tutto, git pull, git push". Portare su GitHub
  tutto il lavoro non ancora tracciato nel working tree e riallineare il monorepo.
- **Esito:** ✅ completato — 103 file committati e pushati su `origin/main`.
- **Output:**
  - **`Skill empire-premium-style/`** (10 file) — sorgente della skill di trasformazione stile
    Empire (design system ccm-premium: palette ink/paper/grey + orange `#fb4604`, Onest,
    grana doppio layer, card/bubble/silver-chip, stack Next.js 16 + Tailwind v4 + Lenis +
    Framer Motion + GSAP). Include `SKILL.md`, slash command, 8 file `references/`
    (design-tokens.css, build-playbook, section-patterns, layout/page di riferimento).
    → pagina wiki nuova `tools/Tool_Empire_Premium_Style.md`.
  - **`Crea siti/Siti CCM/`** — 3 build Next.js della sale page CCM
    (`ccm-sale-page-empire` completo con hero/problems/roadmap/faq/audience,
    `ccm-elite-ultimate` con ecosystem/mastery-map/method/objections-grid,
    `ccm-full-empire` parziale: solo globals.css + reveal + smooth-scroll + utils)
    + pipeline statica `builder.py` (Jinja2: `data.json` + `template.html` → `index.html`)
    + `index.html` rigenerato (991 righe nuove, 1723 rimosse) e 2 backup HTML.
  - **`Landing Page/`** — `ccm-empire/` (Next.js: home + masterclass + thank-you, netlify.toml),
    `ccm-dist/` (export statico 3 pagine), `porco-dio-empire/` + relativi HTML,
    5 varianti thank-you, scratch js/ps1, `.claude/settings.json` locale.
  - `second-brain-vault/wiki/tools/Tool_Empire_Premium_Style.md` (nuova) + entry in `index.md`
    + entry in `log.md`.
- **Decisioni prese:** nessuna nuova ADR.
  **Repo annidato assorbito**: `Landing Page/ccm-empire/` era un repository Git a sé
  (1 commit "Initial commit", **nessun remote**). Committarlo così avrebbe messo su GitHub un
  gitlink vuoto — puntatore a un commit che nessuno può clonare. Scelta: rimosso il `.git`
  annidato e assorbiti i 16 file nel monorepo. Storia salvata prima in doppia copia
  (bundle `--all` + copia integrale di `.git`) nello scratchpad di sessione:
  `…/scratchpad/ccm-empire-history.bundle` e `…/ccm-empire-dot-git-backup`.
- **Lezioni/errori:**
  - **⚠️ SICUREZZA — chiave API Brevo in chiaro su repo PUBBLICO.** La chiave
    `xkeysib-1b440a32…a1a46c5f-4J8p0TDOcRTChJz9` (form opt-in newsletter) compare in chiaro
    nei file nuovi **e** era già in `HEAD` dal commit iniziale `57a0ba0b` in 3 file tracciati
    (`Crea siti/Siti CCM/index.html`, `Lancio corso skill beast/Leanding Page CCM/index.html`,
    `Lancio corso skill beast/Sale pag/Siti CCM/icro-empire/src/components/optin-form.tsx`).
    Repo `ansjkfgheqrlg/Digital-Empire` verificato **PUBLIC** (`gh repo view`). Il push di oggi
    non crea l'esposizione — la propaga: era già pubblica da settimane. **Va ruotata su Brevo,
    non solo cancellata dal codice** (la storia Git pubblica è già stata indicizzabile).
    → backlog **B-020**.
  - Un `git pull --rebase` su working tree sporco fallisce prima di iniziare
    (`cannot pull with rebase: You have unstaged changes`): con 103 file da assorbire l'ordine
    corretto è **commit → pull → push**, non pull per primo. Local era comunque già allineato
    (`rev-list --left-right --count` = 0/0), quindi nessun rischio di divergenza.
  - `.gitignore` del monorepo ha retto: `node_modules/`, `.next/`, `dist/`, `out/`, `*.zip`
    hanno tenuto fuori i build — 103 file, ~2,8 MB di soli sorgenti, nessun blob.
- **Costi:** nessuna chiamata a pagamento (sola operatività Git + scrittura memoria/wiki).
- **Prossimo passo:** **ruotare la chiave Brevo** (B-020) e decidere se il form opt-in deve
  passare da un endpoint server invece che da JS client-side. Lato build: `ccm-full-empire`
  è incompleto (mancano `layout.tsx`/`page.tsx`/sezioni) — capire se è un ramo abbandonato
  a favore di `ccm-sale-page-empire` o se va finito.
