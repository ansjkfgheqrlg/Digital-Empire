# CP-20260813-002 — APEX-7: i 3 stream di produzione passano dai 7 gate, main.py riparte su Windows, censimento chiuso con ADR-011

- **Data:** 2026-08-13
- **Autore:** Claude
- **Ecosistema/Reparto:** 11-APEX-7-CORE
- **Task:** Chiusura B-013/B-014, aggancio consumatore reale, ADR-011
- **Esito:** completato
- **Output:** `company/Ecosistemi/11-APEX-7-CORE/arena_generator.py`, `company/Ecosistemi/11-APEX-7-CORE/orchestrator/ruflo_core.py`, `company/Memory/decisions/ADR-011-quinta-implementazione-apex7.md`
- **Decisioni prese:** `CP-20260813-001`, `ADR-011`, `ADR-010`, `ADR-003`, `B-013`, `B-014`, `B-015`
- **Lezioni/errori:** nessuna
- **Costi:** —
- **Prossimo passo:** —

---

Secondo giro su APEX-7 dopo CP-20260813-001. Max: "fai tutto" â€” chiudere i punti
lasciati aperti dal primo ciclo.

## 1. B-013 chiuso â€” ed era piu' grave di come l'avevo scritto

Avevo tracciato "una freccia unicode in `ruflo_core.py`". Provando a far girare il
motore per davvero: **`main.py`, l'entry point del motore canonico, non partiva
affatto su Windows**. Moriva alla riga 21 sul PROPRIO banner box-drawing, prima
ancora di arrivare al workflow. Il motore ufficiale della Coordination Fabric non
era eseguibile sulla macchina su cui si lavora.

Fix con lo split che conta:
- **libreria** (`ruflo_core.py`) -> output solo ASCII (`->` invece di `â†’`). Una
  libreria non puo' imporre un encoding ai suoi chiamanti.
- **entry point** (`main.py`, `run_demo.py`) -> forzano UTF-8 su stdout/stderr e si
  tengono i loro banner. Un entry point puo' dichiarare l'encoding che gli serve.

Verificato: `python main.py "prova breve"` gira end-to-end e produce output reale
dai 6 agenti.

## 2. B-014 chiuso

Il `task_id` ora sopravvive ai restart (`_apex7_task_id` nel context), quindi
`DynamicWorkflowRouter.loop_count` accumula e il guard-rail dei 3 giri scatta.
Verificato: un workflow con critic score 2.0 termina ("Final Score: 2.0") invece
di andare in `RecursionError`.

## 3. Il consumatore reale e' agganciato â€” tutti e tre gli stream

`arena_generator.py` e' il consumatore di produzione del motore condiviso: e'
esattamente skill-forge + carousel-machine + cold-outreach, i tre che ADR-010
elenca come gia' in uso su `domain="default"`. Tutti e tre passavano dal workflow
NUDO. Ora passano dai 7 gate:

    [GATE] skill-forge: CERTIFICATO L1->L7 in 315ms

Ogni run scrive un `<nome>.gate.json` accanto all'output. Il punto: prima quei file
venivano scritti sempre, e dal file non si capiva se valessero qualcosa.

`strict=False` di default (salva ma con scorecard e avviso), `strict=True`
disponibile e documentato: non spengo da un giorno all'altro una pipeline che oggi
produce: ADR-003 dice che il sostituto si valida in parallelo prima di diventare
vincolante. Test `test_i_tre_stream_passano_dalla_pipeline_certificata` verifica per
introspezione che nessuno stream chiami piu' `execute_workflow` direttamente.

## 4. Il test YouTube rosso: il difetto era nel test, non nel codice

`test_apex7_orchestrator_e2e` falliva da giorni ed era stato archiviato come
"preesistente". Diagnosticato: la **Fase 3 viene bloccata dal gate dei 12 minuti**
su uno script reale da 11.0 min â€” con critic score 8.09, quindi sopra soglia: e' la
durata a fermarlo. E' lo stesso gate che CP-20260805-005 aveva reso bloccante
apposta.

Il test pretendeva di arrivare sempre alla fase 6, il che rendeva **un gate che
funziona indistinguibile da un crash**. Riscritto: cattura `SystemExit(1)`, verifica
che una fase risulti davvero marcata fallita, controlla gli artefatti delle SOLE
fasi effettivamente eseguite, e pretende che il blocco sia registrato onestamente
(stato salvato + dashboard aggiornata). Ora 11/11 verde, e un domani un crash vero
non passera' piu' travestito da gate.

## 5. ADR-011 â€” il censimento era incompleto

ADR-010 nasce per curare la frammentazione APEX-7, ma contava 4 linee. Ne esistono
6: la quinta e' `empire/intelligence/apex7/` (~650 righe), la sesta era lo zip
stesso. Una linea non censita non e' governata da nessuno â€” e' la condizione che ha
prodotto il problema che ADR-010 cerca di curare.

`empire/intelligence/apex7/` e' **la piu' onesta del repo**: separa esplicitamente
mock e `LLMBackend` reale, e il suo `ruflo_adapter.py` alza `NotImplementedError`
dichiarando cosa servirebbe per collegare RuFLO davvero. Scritto ADR-011: entra nel
censimento come deprecata-non-cancellata, ma i suoi due pezzi che al motore canonico
MANCANO (il seam per il backend LLM, il contratto RuFLO) vanno promossi in un ciclo
dedicato. Vincolo posto: la Fase 2 di ADR-010 non parte prima, perche' scalare su 13
ecosistemi un motore che non sa ancora parlare a un LLM reale propaga il limite
tredici volte invece di una.

## Verifica

49 orchestration (3 nuovi sul consumatore) + 4 multi-tenant + **11 YouTube, ora
tutti verdi**. Prima di questo ciclo la suite YouTube era 10/11.

## Non fatto, dichiarato

- La promozione di `backends.py`/`ruflo_adapter.py` (B-015) e' scritta in ADR-011 ma
  non eseguita: e' un ciclo suo.
- `carousel-machine` e `cold-outreach` sono agganciati e coperti da test, ma
  verificati end-to-end su dati veri solo per skill-forge: gli altri due chiamano
  Arena.ai e non li ho fatti partire per non consumare crediti senza chiedere.
- `strict=True` non e' mai stato acceso su una run di produzione: e' il passo che
  rende i gate davvero vincolanti, e va deciso da Max dopo aver letto qualche
  scorecard vera.
