# CP-20260721-005 — EmpireDesk: divisione Max/Gael riconfermata, Gael richiamato da V2-2 Lotto 4

- **Data:** 2026-07-21 sera
- **Autore:** Max (via Claude)
- **Contesto:** Max mi scrive "io Max penso solo alla grafica, Gael pensa a tutto il resto" —
  in contrasto con l'ordine delle 15:48 di oggi ("EmpireDesk intera app passa a Max, Gael stop
  immediato", con Gael già redirect a V2-2 Lotto 4). Segnalato il contrasto a Max con
  AskUserQuestion invece di assumere quale versione fosse corrente: **confermato che vale la
  divisione** (il blocco delle 15:48 intendeva "faccio io la grafica", non un monopolio totale).

- **Esito:** ✅ STATO-EMPIRE aggiornato con nuovo blocco in cima; Gael richiamato.

- **Cosa NON ho fatto (per evitare duplicati):** nel frattempo avevo diagnosticato e fixato in
  autonomia un bug reale nella build .exe (PyInstaller ≥6 sposta i `datas` in `_internal/`, ma
  `app.py` li cercava accanto all'exe — Aureus "non trovata" e i moduli sparivano in silenzio da
  frozen). Prima di registrarlo ho verificato REGISTRO-ERRORI.md + STATO: **la stessa classe di
  bug era già stata trovata e risolta da una sessione parallela** (G2, doppia difesa: `DATA_DIR`/
  `MODULES_DIR` puntato al repo live + `contents_directory='.'` nello spec — 13/13 poi 15/15 PASS
  documentati in STATO). La mia modifica locale a `empiredesk.spec` risultava byte-identica a
  quella già pushata (`git diff --stat` vuoto dopo il pull) — nessun commit necessario. Ho comunque
  ri-verificato dal vivo: selftest exe frozen **16/16 PASS** (7 moduli, incl. youtube/notify/
  taskboard aggiunti da allora), doppio click reale → finestra si apre, Aureus servita.

- **Lezione:** quando Max riferisce un problema di seconda mano ("Gael ha dei problemi") senza
  dettagli, investigare da soli (leggere codice, tentare la build) prima di chiedere è giusto —
  ma va SEMPRE incrociato con `git pull` + STATO-EMPIRE fresco, perché un'altra sessione può aver
  già risolto lo stesso problema nel frattempo (qui è successo). Scritto in STATO: la prossima
  volta chiedere a Gael di loggare l'errore ESATTO (comando + messaggio), non solo "problemi".

- **Prossimo passo:** Gael riprende EmpireDesk (logica/collegamenti/funzionamento, `app.py` +
  `modules/` + build) da dove l'aveva lasciato G1/G2/G3. Max resta su grafica/UI/UX (`platform/`).
