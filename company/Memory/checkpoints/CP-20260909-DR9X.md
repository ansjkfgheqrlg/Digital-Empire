# CP-20260909-DR9X — /frantuma schema finale: viola con frecce in markdown, semplificato a split+ID puro

- **Data:** 2026-09-09
- **Ecosistema/Reparto:** 10-MEMORY / auto-implementazione di Emperator
- **Task:** chiusura di `/frantuma` (iniziata CP-20260908-9GQM), dopo tre giri di correzione
  di Max nello stesso filone: (1) rimossa una demo reale non richiesta su TASK-LANCI-BUILD-W3,
  (2) bocciato lo schema "Onde ad albero" (ASCII boxato, concetto onda "cringe"), (3) bocciato
  un Artifact (vietato esplicitamente, vuole tutto in chat), (4) corretto lo scopo stesso della
  funzione: solo split in micro-task con ID, **non** pianificazione di parallelismo/onde.
- **Esito:** ✅ chiuso e approvato ("perfetto, salva questo schema").
  - `scripts/frantuma.py` **riscritto piu' semplice**: via onda/dipendenze/verifica-scope, resta
    solo `conia` (ID atomico per-padre, stesso schema anti-collisione di `adr.py`/`checkpoint.py`)
    e `report` (legge i titoli veri dai file e genera lo schema fisso).
  - **Schema finale, verificato dal codice** (non scritto a mano in dottrina): viola (🟣)
    dominante, frecce in markdown puro (mai dentro un blocco di codice — quello leggeva
    "evidenziato"), un ramo `├──🟣→` per micro-task e `└──🟣→` sull'ultima.
  - **Scoperta tecnica reale**: qualunque riga che comincia con `🟠` viene intercettata da
    `gate_battito_hook.py` e giudicata come un tentativo di recap — un mio mockup con quel
    bullet e' stato bloccato in produzione. Documentato in `emperator.md` §6.24 come vincolo
    non negoziabile per qualunque futuro schema.
  - **8 test** (erano 14, semplificati insieme al codice), suite totale 22/22 verde.
  - `emperator.md` §6.24 riscritta per intero con lo schema finale, tolto ogni riferimento a
    onde/Artifact/scope-check.
- **Output:** `scripts/frantuma.py`, `tests/test_frantuma.py`, `.claude/agents/emperator.md`
  (§6.24). Nessun file reale coniato sotto `company/Memory/tasks/micro/` questa volta — solo
  dimostrazione su cartella temporanea, per non ripetere l'errore del giro precedente.
- **Decisioni prese:** nessuna ADR (capacita' operativa di Emperator, stesso trattamento del
  battito — vive solo in `emperator.md`).
- **Lezioni/errori:**
  - **Non eseguire mai su un caso reale finche' lo schema di risposta non e' approvato.** La
    demo dal vivo su TASK-LANCI-BUILD-W3 nel giro precedente e' stata l'origine del rimprovero
    piu' duro di Max in questo filone ("non dovevi farlo veramente"): un'anteprima con dati
    d'esempio dichiarati come tali bastava, ed e' quello che ho fatto stavolta.
  - **"Fammi un esempio bello" non significa "stesso stile di un'altra funzione".** Avevo
    riusato la pelle del recap (bullet 🟠, tono identico) pensando fosse un pregio di coerenza;
    Max ha chiarito che sono funzioni diverse e vuole un'identita' visiva distinta per colore —
    lezione generale: coerenza di sistema (stesso meccanismo di conio) e coerenza estetica
    (stesso schema visivo) sono due cose diverse, non vanno confuse.
  - **Il mezzo vince sulla richiesta estetica.** "Angoli stondati, contorno perfetto, riempimento
    pieno" non esistono nel markdown puro della chat: l'unica correzione onesta era dirlo chiaro
    invece di tentare ancora un'imitazione ASCII che avrebbe deluso una terza volta.
  - **Uno stesso carattere puo' collidere con un gate esistente.** Il bullet 🟠 non e' "libero":
    e' riservato al riconoscimento del battito. Prima di fissare un simbolo per una nuova
    funzione, controllare cosa gia' lo intercetta.
- **Costi:** nessuna chiamata a modello.
- **Prossimo passo:** usare `/frantuma` la prossima volta che una task grande lo giustifica.
  Nessun esempio reale coniato ora: il primo uso vero parte quando Max lo chiede su una task
  specifica.
