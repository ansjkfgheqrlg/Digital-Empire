# CP-20260702-003 — PreventivoForge: PDF modello NOVACAR + Gate IMG/R + App .exe COSTRUITA

- **Data:** 2026-07-02
- **Ecosistema/Reparto:** 01-AGENCY / Cliente Novacar srl — PreventivoForge (Half B, Gael)
- **Task:** HANDOFF-GAEL-2 (Max) — rifare il PDF sul modello Novacar rispettando le REGOLE-SACRE
  (R-01…R-14), 2 nuovi agenti QA, App .exe. Concessionaria reale = **Novacar srl**.
- **Esito:** ✅ completato — PDF conforme al modello, 6 gate verdi, .exe costruito e validato.

## Output (path reali)
- `templates/preventivo.html` — RIFATTO sul modello Novacar: pag.1 SOLO logo (R-01), logo header ogni
  pagina (R-02), pag.2 dati azienda+titolo+scheda tecnica barra scura/righe alternate (R-03/04/05),
  pag.3 Equipaggiamento+Garanzia+"Totale in strada (Iva inclusa)" con dettaglio (R-06/07/08),
  pagine foto 2/pagina **`object-fit: contain` mai `cover`** (R-09), ultima pagina SOLO logo (R-10). A4 multi-pagina.
- `implementation/render_pdf.py` — nuovo context Novacar (`_company_block`, `_specs_novacar` con i 12 campi
  del modello, `_price_novacar` "Totale in strada", `photo_pages` tutte le foto). Fix trasparenza logo →
  composita su BIANCO (era nero). Foto max 1500px q85. Motore cdp/Chrome (no Playwright).
- `implementation/qa_gate.py` — **`gate_img`** (Gate IMG, R-09: tutte le foto/su disco/risoluzione/no crop) +
  **`gate_regole`** (Gate R, R-01…R-14, scrive `runs/<id>/regole-check.json`). `run_all` esteso.
- Agenti CF-grade 7-file: `agents/verifica/qa-immagini/` + `agents/verifica/qa-regole-checker/` (+ CATALOG aggiornato).
- `app.py` — dealer default = **novacar**.
- **App .exe COSTRUITA:** `dist/PreventivoForge/PreventivoForge.exe` (PyInstaller onedir, gitignorato).

## Verifica (TEST reale)
- Selftest `app.py --selftest` (fixture `__INITIAL_STATE__` BMW + dealer novacar): **6/6 gate PASS**
  (A,B,C,D,IMG,R), PDF via cdp-chrome. **regole-check.json: R-01…R-14 tutte OK.**
- **Ispezione visiva del PDF:** struttura = modello Novacar (cover logo, dati azienda P.IVA/PEC, scheda 12 campi,
  equip+garanzia, "Totale in strada € 30.707" con dettaglio 26.900/807/1.500/1.500, foto contain, chiusura logo). Prezzo 26.900→30.707 ok.
- Logo verificato su sfondo BIANCO (255,255,255).
- **`PreventivoForge.exe --selftest` ESEGUITO:** dealer=Novacar srl, 4 gate verdi, PDF 156 KB via cdp — l'.exe funziona senza Python.
- py_compile OK; struct gate 2 nuovi agenti (7 file, 0 stub) PASS.

## Confine rispettato
NON toccati Half A: `run.py`, `scraper`, `parser`, `pricer`, `common`, `dealers`, **`cdp.py`**, `schema/`.
`cdp` USATO non modificato. Modificati/creati solo file Half B (template, render_pdf, qa_gate, app, agenti).

## Lezioni/errori
- Il logo PNG traspardente veniva compositato su NERO dalla conversione JPEG → fix `alpha_composite` su bianco.
- Modalità `--manual` col nuovo parser di Max estrae le dotazioni solo se l'HTML ha `window.__INITIAL_STATE__`.

## Segnalato a Max (Half A, non bloccante)
- Aggiungere `--remote-allow-origins=*` in `cdp.launch` (Chrome ≥111 rifiuta la connessione WS CDP grezza).
- **Wiring Gate IMG + Gate R in `run.py`** dopo S5 (le 2 chiamate: `gate_img`, `gate_regole` con `dealer`).
- Storico preventivi in `Memory/storico-preventivi/` da popolare a ogni run reale (DoD handoff).

## Costi
API €0. Build .exe locale (PyInstaller, ~163 MB cartella, gitignorata).

## Prossimo passo
Consegna: build+test .exe fatto. Attendere Max per il wiring Gate IMG/R in run.py + primo run su URL reale.
Poi (GO Max): ripresa scelta ecosistema Empire.
