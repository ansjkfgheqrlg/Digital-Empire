# CP-20260724-001 — Completamento Workflow Estate: piano a 3 livelli + 6 lotti + verdetto unico

- **Data:** 2026-07-24
- **Ecosistema/Reparto:** 06-CORE / Platform + 01-AGENCY / Vetrina + 03-CONTENT-FACTORY / YouTube
- **Task:** ordine di Max "finiamo il Workflow Estate, completamente". Pianificazione a 3 livelli
  (ognuno che corregge i limiti dichiarati del precedente) → architettura → swarm di 6 lotti a
  perimetro disgiunto. Rif: `WORKFLOW-ESTATE/01-FLUSSI-E-PIANI/PIANO-COMPLETAMENTO-L1/L2/L3.md` +
  `ARCHITETTURA-COMPLETAMENTO.md`.
- **Esito:** ✅ completato per quanto dipende dalla costruzione — `python -m empire estate` → **exit 0**,
  11 controlli su 13. I 2 rimanenti (Gate-CONTATTI, Gate-REV) dipendono solo da Max e sono
  classificati come tali dal comando, non nascosti.

## Output (path reali)
**Piani e architettura**
- `WORKFLOW-ESTATE/01-FLUSSI-E-PIANI/PIANO-COMPLETAMENTO-L1.md` (inventario) → `-L2.md` (corsie
  AUTO/GATED, ordine per €/h, ladder, DoD con comando) → `-L3.md` (6 lotti disgiunti, comando
  unico, pre-mortem a 7 voci, protocollo di collisione) + `ARCHITETTURA-COMPLETAMENTO.md`

**Motore (LOTTO 1/2/6)**
- `empire/flow/decisions.py` — ADR-EST-006 default-più-veto + `flow veto` per il dissenso tracciato
- `empire/flow/evidence.py` — evidenza calcolata per i gate umani + **guardia di provenienza**
- `empire/estate.py` — verdetto unico, con distinzione proprietario `noi` / `max`
- `empire/inspect/metrics.py` + `__init__.py` — le 6 metriche di telemetria come API
- `empire/tools/video_pack.py` — scaffolding/validazione/render onesto del pacchetto S5
- `empire/tests/test_flow_decisions.py` (25 test) + `empire/tests/test_estate.py` (27 test)

**Prodotti (LOTTI 3/4/5, agenti + completamento mio)**
- `Crea siti/Siti CCM/checkout.config.json` + `pagamento.html` + `empire/tools/checkout.py` — tier 2 attivo
- `Clienti/Prof Autocad/preventa-launch-kit/07_CASE_STUDY_NOVACAR.{md,html,pdf}`
- `Crea siti/Preventa/index.html` — landing standalone
- `WORKFLOW-ESTATE/07-VIDEO-RUN/run-2026-07-23-001/` — pacchetto-render completo, nessun video dichiarato
- `WORKFLOW-ESTATE/06-DASHBOARD-E-METRICHE/AZIONI-MAX.md`

**Verifiche eseguite (non assunte)**
- `python -m empire estate` → exit 0 · `conform WORKFLOW-ESTATE` → **block 0, warn 0** (erano 4 block)
- `python -m pytest empire/tests/ -q` → **207 passed** (erano 150) · `checkout.py --apply` idempotente (md5 identico)

## Decisioni prese
Nessun ADR nuovo. Applicati ADR-EST-006 (default-più-veto), ADR-003 (wrap), ADR-008 (9 artefatti
registrati in `company/skills-map.yaml`, chiave `nuovi_2026_07`).

## 🔴 Difetti reali trovati (tutti della stessa famiglia: controlli che rassicurano invece di misurare)
1. **I 7 lead di `lead.csv` non sono tracciabili a nessuna sorgente.** La guardia di provenienza
   dà `0/7 voci riscontrate in Outreach/**/*.csv`. Su disco esistono solo dati di prova dichiarati
   (`test_lead_finti.csv` con "Autosalone Test Uno"/"Via Finta 1", `esempio_lead_5_righe.csv`).
   **I 61 lead reali dichiarati in STATO-EMPIRE il 23/07 non esistono come file.** Gate-CONTATTI
   lasciato rosso di proposito: confermarlo avrebbe fatto sembrare fatto un lavoro commerciale mai avvenuto.
2. **`company/skills-map.yaml` era YAML non valido** (verificato su `git show HEAD`, quindi
   pre-esistente al mio lavoro): `empire/registry/render.py` emetteva `note:` e `- id:` allo stesso
   livello. L'anagrafe che per ADR-008 garantisce "nessun artefatto orfano" non era caricabile da
   nessun parser — sopravvissuto perché il file veniva letto a occhio, mai da una macchina. Corretto
   il generatore (`artefatti:`) e rigenerato.
3. **`empire/dash/kpi.py`: un valore illeggibile diventava verde** (`return "green"` nel ramo di
   errore). Bastava un errore di lettura perché un KPI si accendesse in salute. Ora → `gray`.
4. **`empire/dash/render_md.py`: la sezione telemetria aveva l'emoji cablata a mano** ("⚪ se il
   testo contiene n/d, altrimenti 🟢"), ignorando le soglie: un first-pass rate dello 0% risultava
   verde. Ora usa `evaluate_status` come ogni altro KPI.
5. **`video_pack.py --check` approvava il proprio scheletro**: il template di `04-SEO-PACK.md`
   conteneva la parola "Manuale", che era il criterio di verifica. Trovato da un test che avevo
   scritto aspettandomi il contrario. Ora un file identico allo scheletro viene respinto.
6. Due `LINK-DEAD` introdotti da me (stringhe con `/` lette come percorsi) — corretti alla fonte.

## Lezioni (per ReasoningBank)
- **Un rosso per fatto mancante e un rosso per lavoro mancante sono indistinguibili dall'esterno.**
  Gate-DEC è stato rosso per due giorni mentre la decisione era attiva per regola: mancava solo
  chi scrivesse il fatto. Da qui `flow decisions`.
- **Un numero giusto su dati falsi è più pericoloso di nessun numero**, perché ha l'aria di essere
  verificato. La guardia di provenienza nasce da qui, e la regex sui nomi non basta: serviva il
  riscontro con una sorgente a monte (i nomi di `lead.csv` sono plausibili e nessuna regex li smaschera).
- **Dieci DoD sparse non vengono mai rieseguite tutte.** Fonderle in `empire estate` è ciò che ha
  reso verificabile il lavoro; ma il comando poteva funzionare solo distinguendo `noi` da `max`,
  altrimenti non avrebbe mai potuto dare esito 0 e sarebbe stato abbandonato.
- **Scrivere il test prima di fidarsi del proprio codice paga:** il difetto n.5 l'ha trovato un test.

## Costi
4 agenti di swarm (LOTTI 1/3/4/5) terminati anzitempo: `You've hit your monthly spend limit`.
Recuperato il loro lavoro parziale già a terra (checkout, case study, pacchetto video) e completato
a mano il resto (landing Preventa, `video_pack.py`, API inspect, link rotti). Nessun lavoro perso.

## Prossimo passo
**Max** — le 4 azioni in `WORKFLOW-ESTATE/06-DASHBOARD-E-METRICHE/AZIONI-MAX.md`, in ordine di soldi:
1. 2 Payment Link Stripe → `checkout.config.json` → tier 1 (10 minuti, il ritorno più alto).
2. Prezzo Preventa (DEC-EST-005, veto M-EST-4) → la landing va online.
3. Decidere su Gate-CONTATTI: recuperare la sorgente dei lead **oppure** rilanciare lo scraper
   con le province vere (M-EST-9 aperta).
4. Canale YouTube + credenziali (M-EST-8) + voce TTS → S5 pubblica.
