# CP-20260729-004 — TASK-YT-003 CHIUSA: F5 Pubblicazione, metadati/titolo/tag reali

- **Data:** 2026-07-29
- **Ecosistema/Reparto:** YOUTUBE-AUTOMATION-FACTORY (modello operativo governato dall'Estate)
- **Task:** TASK-YT-003 (P1) — F5 Pubblicazione: generare metadati/titolo/tag reali dal video+script
  scelti, non hardcoded. Secondo lotto costruito dall'Estate nella stessa sessione, dopo TASK-YT-002.
- **Esito:** ✅ chiusa. Gate VERDE.

## Il difetto
`run_phase_5` scriveva sempre gli stessi `metadati.json` e `brief-miniatura.json`:
titolo "Come Installare CLAUDE CODE in Locale...", descrizione placeholder "Ecco come installare
Claude Code nel terminale...", 3 tag fissi, brief fisso — indipendenti dallo script reale di F3.

## Fix (additivo, stesso unico file)
`apex7_orchestrator.py`, 3 nuovi helper module-level + `run_phase_5` riscritto:
- **`_sezioni_script(script_text)`**: `script.md` reale → `{HOOK/INTRO/CORPO/CTA: testo pulito}`
  (stessa pulizia di `_scene_da_script`: niente regia `➕`, metadati `- **`, timecode).
- **`_overlay_da_hook(hook, title)`**: testo copertina breve (≤5 parole, maiuscolo) dall'HOOK reale.
- **`_costruisci_metadati(title, hook_type, sezioni, high_tags, keyword)`**: titolo reale;
  descrizione costruita da HOOK+INTRO+CORPO+CTA reali + link/CTA + keyword (garantita ≥200 char per
  la soglia SEO senza inventare contenuto); tag = `learned_rules["high_performing_tags"]` + token
  reali del titolo + hook_type, dedup, keyword sempre presente.
- `run_phase_5`: legge `working_memory[script_idea_title/hook_type/script_path]` + `learned_rules`,
  brief con `concept`/`text_overlay` derivati dall'HOOK reale.

## Gate (comandi + output reali)
`scratchpad/gate_yt003.py` esercita `run_phase_5` su 2 script F3 diversi:
```
RUN 1  title="Come installare e configurare Claude Code in 5 minuti"
       tags(12)=[claude code, antigravity, digital empire, come, installare, configurare, claude, code, minuti, tutorial, guida, question]
       seo_total=100.0  pass_soglia_70=True
RUN 2  title="3 errori che rovinano i tuoi prompt con Claude Code"
       tags(12)=[claude code, antigravity, digital empire, errori, rovinano, tuoi, prompt, claude, code, tutorial, guida, statement]
       seo_total=100.0  pass_soglia_70=True
[OK] titolo DIVERSO   [OK] tag DIVERSI   [OK] run1/run2 pass_soglia_70
[OK] run1/run2 validate metadati PASS   [OK] run1/run2 validate brief-miniatura PASS
GATE: VERDE
```
Regressione: `python -m unittest test_youtube_apex7` → **Ran 11 tests — OK (11/11)**.

## Perimetro / taskboard
Toccato solo `apex7_orchestrator.py`. Motore condiviso e file trading non toccati.
`EmpireDesk/state/taskboard.json`: **TASK-YT-003 → fatto**.

## Stato fabbrica YouTube dopo questa sessione
F1-F3 erano già reali. **F4 (TASK-YT-002) e F5 (TASK-YT-003) ora reali.** Restano finti:
- **TASK-YT-004** (F6 Audit): `run_phase_6` appende sempre `views_per_hour: 35.5` finto.
- **TASK-YT-005** (Dashboard): scritta da un percorso fantasma `run_youtube_apex7.py`, sempre PASS.

## Prossimo passo (RIPRESA DA)
**TASK-YT-004** (F6 Audit onesto): manifest `memory/published_videos.json`; senza voce reale per la
run → F6 non appende nulla a `performance_logs.json` (conta righe prima/dopo = 0); con video reale
pubblicato → `views_per_hour` calcolato col fetch pubblico (pattern F2), non inventato. Comando di
regia: `empire cantiere`. Stesso metodo (COORD → build → gate → CP → taskboard → push).
