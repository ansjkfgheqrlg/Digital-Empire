# Tools — workflow-router

## Bash
- `python scripts/yt_ingest.py --input <URL> --run <run-id>`
- `python scripts/frame_extractor.py --run <run-id> --interval 2`

## Read
- `memory/handoffs/intent-<latest>.json`
- `routing-map.md`

## Write
- `memory/handoffs/routing-result-<timestamp>.json`
