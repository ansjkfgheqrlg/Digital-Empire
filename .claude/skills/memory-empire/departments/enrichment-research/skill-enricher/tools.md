# Tools — skill-enricher

## Bash
- `python scripts/enrich_skill.py --target <f> --content <c> --source <s> [--dry-run]`
- `cp <source> <dest>` (backup)

## Read
- `memory/handoffs/proposals-<latest>.json`
- File target prima della modifica

## Edit
- Modifica diretta se enrich_skill.py non disponibile (solo dopo backup)

## Write
- `memory/enrichments/applied-<timestamp>.json`
- `memory/backups/<skill>-<timestamp>.md`
