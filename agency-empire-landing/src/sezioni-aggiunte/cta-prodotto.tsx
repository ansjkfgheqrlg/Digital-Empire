/* F5 (ordine di Max 14/09) — pagina CTA per prodotto, stile all. 08 (funneloperator). Riempita dallo scagnozzo δ. */
export function CtaProdotto({ prodotto }: { prodotto: "outreach" | "content" | "brain" }) {
  void prodotto;
  return null;
}
